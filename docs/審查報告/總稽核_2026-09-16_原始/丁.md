# 丁・實機驗收報告（2026-09-16）

- 對象：`F:\ClaudeWork\qiuqiu-coop`，分支 `coop`，提交 `a5fa358`（工作目錄乾淨，驗收前後 `git status` 都是空的）
- 打包：`npx vite build --outDir <本資料夾>/dist`；預覽 `npx vite preview --outDir <同上> --port 4174 --strictPort`，網址 `http://localhost:4174/qiuqiu-tower/?debug`
- 瀏覽器：Google Chrome 152（無頭、`--user-data-dir=<本資料夾>\chrome`、`--autoplay-policy=no-user-gesture-required`、除錯埠 9334）。連線那段 B 分頁用 `Target.createTarget({newWindow:true})` 開在獨立視窗，兩頁 `visibilityState` 都是 `visible`
- 操作方式：常駐小程式 `driver.mjs` 用一條 CDP 連線接住所有分頁、持續記網路回應（含 MIME）與主控台；點擊一律用 `Input.dispatchMouseEvent`（真的滑鼠事件，音效解鎖、影片自動播放都照真人流程走）。各項腳本 `s1_*.mjs`～`s12_*.mjs` 都在本資料夾
- 中繼：打包版預設的線上中繼 `qiuqiu-relay.qiuqiu-tower.workers.dev`（房號 608781、798490）。實測一則訊息「瀏覽器→中繼→另一台」約 156 毫秒
- 截圖都在 `shots\`，下文只寫檔名
- **注意一個查 404 的坑**：`vite preview` 對不存在的檔案回 200＋`index.html`，只看狀態碼抓不到缺檔。所以驅動程式另外記 MIME，圖片再用 `naturalWidth` 確認真的解得開。整段只抓到一筆「圖回成網頁」，是我自己為了確認貓頭鷹沒有倒地圖而故意去要的 `owl_sentry_down.webp`，不是遊戲發的請求

---

## 壞掉的

### 中-1　換忍具視窗還開著的時候同伴先挑牌 → 疊出第二個換忍具視窗（兩個座位都會）

- **重現**：連線、自己忍具帶滿、戰利品開出忍具 → 350 毫秒後跳「忍具帶滿了。要用「X」換掉哪一支？」→ 還在想的時候，同伴在他那台點一張牌。
- **看到的**：
  - 客戶端（B，菲菲）：自然開出「分身油」，A 挑牌 1.5 秒後，B 的 `#overlay` 裡有 **2 個** `.swap-modal` 疊在一起（外觀只看得出一個，底色變更暗）。截圖 `08d_natural_two_modals.png`
  - 另一次（這場沒開出忍具，由我兩邊同步補一支「貓草茶」給 B，欄位跟引擎自然開出來的一模一樣）：按掉上面那個「不換」，底下那個還在；底下那個改選「換掉這支」→ 背包真的換成 `catgrass_tea+shuriken+onigiri`，**畫面那一行卻還寫「沒有換，「貓草茶」放棄了」**。截圖 `08d_client_two_modals.png`、`08d_client_after_first_close.png`、`08d_client_after_second_choice.png`
  - 主機（A，球球）同理：主機帶滿、視窗開著時客戶端挑牌 → A 也是 2 個視窗（同樣是補的貓草茶）。截圖 `08e_host_two_modals.png`
- **原因（讀程式對照）**：`src/ui/screens/reward.ts` 310～327 行，每次重畫都排一個 350 毫秒的 `showPotionSwap`，只擋 `potionDeclined`／`potionSwapped` 與 `line.isConnected`；同伴挑牌會 `app.show('reward', r, { quiet: true })` 整頁重畫，而「視窗正開著」沒有記在 `r` 上（升級挑牌那邊有 `upsAsking` 擋同一型，忍具這邊沒有）。舊視窗的回呼拿的是已經被換掉的那個 `line`，所以選完寫不回畫面上。
- 連線時只要有人忍具滿，同伴在你想的那幾秒內挑牌幾乎一定發生。

### 中-2　客戶端按「換掉這支」後、動作繞回來之前主機剛好挑牌 → 又彈一次換忍具，再換一次就多出一支忍具

- **重現**（兩種都做了，都中）：
  1. 用 BroadcastChannel 連動：B 送出換忍具的那一刻 A 立刻點牌。時間線（毫秒）：+0 B 送出換忍具／A 點牌 → +156 B 收到牌票、整頁重畫 → +310 換忍具繞回、`potionSwapped` 變 true → **+511 第二個換忍具視窗跳出**，列出的背包已經是換過的「黏鳥膠、手裡劍、飯糰」，下面那行又變回「忍具帶滿了，「黏鳥膠」收不下」。截圖 `08b_client_after_race.png`
  2. 照一般玩家以為剛剛沒換成、再點一次「換掉這支」（第二支）→ B 背包變成 `bird_glue+bird_glue+onigiri`，A 的引擎也一樣（兩台一致、不會斷線，但**一次掉落拿到兩支黏鳥膠**）。截圖 `08b_client_after_second_swap.png`
  3. 不靠連動，兩個真的滑鼠點擊同時送（B 點「換掉這支」、A 點牌）：+162 牌票到 → +321 換忍具繞回 → **+516 視窗又跳出**，B 狀態 `swapped:true` 但畫面寫「忍具帶滿了，「煙霧彈」收不下」。截圖 `08b_parallel_after.png`
- **原因**：同上，350 毫秒的計時器排下去之後不再看 `r.potionSwapped`；而「換忍具已送出、還沒繞回」這段也沒有記號。窗口大約是主機挑牌的時間落在客戶端點擊前後各一個中繼來回（這次約 ±156 毫秒，網路越慢窗口越大）。只影響客戶端（主機的換忍具是同步套用）。

### 低-1　菲菲每次開遊戲後「第一次被打」沒有叫聲

- **重現**：重新整理 → 選菲菲 → 打一場讓魔物打到她兩次。
- **看到的**：音效掛鉤記錄實際出聲的檔案。單機貓頭鷹那場：第 2 回合被打**沒有出任何受傷音**，這時才去要 `hurt_feifei.mp3`（200）；第 4 回合被打才播 `hurt_feifei.mp3`。連線 B 分頁一樣：第 2 回合沒聲、第 3 回合才播。球球第一次被打就播 `hurt.mp3`。
- **原因**：`src/ui/audio.ts` 112 行第一次點擊時的預載清單寫死 `hurt`，沒經過 `sfxFor`，菲菲那顆不在預載裡，第一次用到才下載、那一下就靜音；而且對菲菲白載了一顆 `hurt.mp3`。
- 紀錄：`s5d_ff.json`、`s5d_qq.json`

### 低-2　菲菲的開頭影片字幕還是改寫前的句子，緊接著的幻燈片是新句子

- 影片（烤在 mp4 裡）：「力氣小，就別跟人比力氣。」「……三天了。師父沒回來，師兄也沒回來。」「我真的很怕痛。可是，只剩我能去找他們了。」
- 下一秒的序章幻燈片：「……師父沒有笑她，只把靶子搬近些：「先練準。力氣不夠的地方，就想別的辦法。」」「我還是很怕受傷。可是他們要是也在等人幫忙呢？……我得去。」
- 同一段劇情前後兩種講法。截圖 `02_ff_video_frames.png`（影片六格）對照 `02_ff_slide_1.png`、`02_ff_slide_4.png`。球球那支是使用者自製的，字幕本來就跟幻燈片不同，沒列進來。

### 低-3　地圖擲骰的小氣泡只寫格子型別，兩條路同型別時看不出骰到誰選的

- 兩人選的都是戰鬥格（A 選 `f1-l1`、B 選 `f1-l3`），兩台都只寫「兩人選的路不一樣，擲骰選了「戰鬥」」，接著直接進戰鬥，看不到是哪一格。
- 另外這則系統訊息在戰鬥畫面上畫成對白泡泡、尾巴指著球球的頭，看起來像球球講的（兩台都是）。截圖 `10_host_toast.png`、`10_client_toast.png`

### 低-4（範圍外，順手看到的文案）事件敘述裡菲菲被寫成「牠」

- 例：神龕扶正的結果「菲菲扶起神龕……暖意從爪尖傳到身上，**牠**深吸一口氣」、拿走供品「鑽進**牠**的胸口。**牠**急忙停下」。序章與介面都用「她」（「她卻被木樁打得縮回了手」「扶起同伴 她回 22 點生命」）。球球版原文也是「牠」、序章用「他」，要不要統一由使用者決定。截圖 `05_ev_ff_broken_shrine_c0.png`、`05_ev_ff_broken_shrine_c1.png`

---

## 通過的（逐項做法與看到的）

### 1. 首頁
- 做法：`s1_title.mjs` 載入標題，量兩張圖的大小、動畫，隔 0.3 秒取樣八次位移。
- 看到：`hero/cover.webp`（球球參上）與 `hero/feifei_cover.webp`（菲菲參上）並排，原圖都是 560×560、畫面上都是 283×283；動畫都是 `idle-bob 2.4s`，延遲 0 與 −1.2 秒，取樣位移正好一上一下（反相）；畫面沒有「球球參上」字樣、沒有 `.title-sub`。截圖 `01_title.png`、`01_title_cats.png`

### 2. 開局影片與序章
- 做法：`s2_opening.mjs`，真的點「新的一局」→ 角色卡 → 出發，監聽影片事件、每 0.5 秒讀 `currentTime`，影片結束後逐句讀幻燈片文字並跟 `storyFor(hero).prologue`（用暫存區的 vitest 從原始碼直接匯出，`expected.json`）逐句比對。
- 菲菲：`opening_feifei.mp4`（206）`loadedmetadata` 長 16.55 秒、從 0 開始 `playing`、17.88 秒時 `ended`（t=16.55）→ 四張幻燈片 4/4 句相符，第一句正是「菲菲比球球晚三年入門。球球第一次練功就搶著出招……」，圖是 `feifei_still_*` → 進地圖、舞台標 `feifei`。之後關掉再開一次（主控台監聽版）結果相同。截圖 `02_ff_heroselect.png`、`02_ff_video_mid.png`、`02_ff_slide_1~4.png`、`02_ff_map.png`
- 球球：`opening.mp4` 長 14.50 秒、播到 `ended` → 五句 5/5 相符，第一句「球球是大俠貓的徒弟。每次練習蜷縮……」→ 進地圖。截圖 `02_qq_*`

### 3. 鏡子走廊（菲菲）
- 做法：菲菲開局後 `__app.startFight('mirror_duel', false, 0, 2)`；用觀察器記下魔物 `img.sprite` 換過的每個網址；出飛針、按結束回合看各姿勢；再把牌組換成 10 張「見血封喉」重開一場（這樣第一招一定學到它），塞 5 層中毒後重畫看牌子，結束回合逐拍記血量；最後把魔物血設 1 打倒看倒地圖。
- 看到：名字「鏡中菲菲」；立繪依序出現 `shadow_feifei_idle`→`hurt`（挨飛針）→`block`（學「退開」防禦）→`attack`（學「飛針」出手）→`down`（打倒），每張原圖 460 寬、沒有破圖；意圖牌子「退開｜守 5」「飛針｜攻 3」都是她的牌名。
- 見血封喉：中毒 0 時「見血封喉｜攻 0（照你的中毒）＋守 3」；塞 5 層後「**見血封喉｜攻 5（照你的中毒）＋守 3**」；結束回合：血 76→71（魔物打 5，吻合）→ 第二回合開始中毒結算 71→66、層數 5→4，牌子跟著變「攻 4」。截圖 `03_mirror_start.png`、`03_mirror_poses_sheet.png`、`03_jianxue_intent.png`、`03_mirror_down.png`、原圖對照 `03_shadow_feifei_files.png`

### 4. 狀態列「針上有毒」
- 單機：`poisonNextAttack = { amount: 2 }` 重畫後自己那排是「中毒5、針上有毒2」；結束回合、魔物出手期間還在，第二回合開始消失（只剩「中毒4」）。截圖 `04_needle_chip.png`、`04_needle_chip_after.png`
- 連線補測：B（菲菲）真的打出「別碰針尖喔」→ A 畫面「球球（你）→ 蜷縮4 懶洋洋1 **針上有毒2**」、B 畫面「球球（同伴）→ …**針上有毒2**」；兩邊結束回合後第二回合兩台都消失。截圖 `04_coop_needle_host.png`、`04_coop_needle_client.png`

### 5. 新圖
- 秘寶圖鑑：銅錢劍、師父的斗笠、鐵砂掌套、塔主的茶碗、傳功玉佩、塔主的酒葫蘆、招財貓、虎爪、鐵布衫、月光鏡十件都是真的圖（96 寬，不是名字牌子）。截圖 `05_relics_sheet.png`（單件 `05_relic_0~9.png`）
- 卡牌圖鑑切菲菲：我什麼都沒看到、偷吃術、甩鍋術、吐毛球都用 `card/feifei_*.webp`（299×240）；頭頂與蝴蝶結都完整（吐毛球的馬尾尖貼齊上緣，頭與蝴蝶結沒切到）。截圖 `05_cards_sheet.png`、原圖 `05_cards_raw_files.png`、`05_maoqiu_on_blue.png`
- 地圖事件格：`icon/node_event.webp`（毛線球，96 寬）。截圖 `05_map_event_node_qq.png`、`05_map_qq.png`
- 貓頭鷹夜哨（`owl_sentry`）：兩個角色各打一場，立繪換過 `idle`／`hurt`／`attack`／`block`，四張都 315×318、角與頭盔一致。截圖 `05_owl_qq_sheet.png`、原圖 `05_owl_files.png`
- 事件插圖：`__app.show('event', …)` 後真的點每個選項（升級、移除兩張、三選一都點完）。球球：`event_broken_shrine`、`_r0`、`_r1`、`event_old_master_ghost`、`_r0`、`_r1`、`event_daxia_teach`、`_r0`；菲菲：同一組的 `event_feifei_*` 版本。全部 200、寬 560、沒有破圖。截圖 `05_ev_ff_sheet.png`、`05_ev_qq_sheet.png`
- 整段網路紀錄除了瀏覽器自動要的 `/favicon.ico` 之外沒有 4xx、沒有失敗、沒有「圖回成網頁」。

### 6. 受傷貓叫（單機）
- 菲菲被打：`hurt_feifei.mp3` 200（`audio/mpeg`），之後真的播的是它（第一次沒聲見低-1）。球球被打：播 `hurt.mp3`，整頁沒有要過 `hurt_feifei`。紀錄 `s5d_ff.json`、`s5d_qq.json`

### 7. 貓窩台詞（單機）
- 兩個角色各「打盹」4 次、「磨爪／磨針」3 次，吐槽全部在目前的 `restNapLines`／`restSharpenLines` 裡（例：菲菲「被子好暖，捨不得掀開。」「針尖朝外，收好了。」；球球「哈啊……起床喵。」「這下夠利了喵。」），跟 `6465c18` 的舊句子都不同。按鈕文字菲菲是「磨針」。截圖 `07_rest_ff_nap.png`、`07_rest_qq_sharpen.png` 等

### 8. 連線戰利品
- 開房／加入：A 開房、大廳把「加入的人」設菲菲，B 輸房號加入，兩邊都進地圖、種子相同、座位 0／1、角色 球球／菲菲、沒有紅色橫幅。截圖 `08_lobby_host.png`、`08_map_host.png`、`08_map_client.png`
- 佈置方法（兩台同步做同一件事，所以不會分岔）：下一格的遭遇換成木樁人、魔物血設 1、B 的戰鬥忍具塞滿三支；A 真的出攻擊牌打倒。第一場自然開出「破功散」。
- **「不換」→ 主機挑牌**：B 按「不換，放棄新的」→ 那行變「沒有換，「破功散」放棄了」→ A 真的點牌 → B 整頁重畫、**沒有再彈換忍具**。通過。截圖 `08a_client_swap_modal.png`、`08a_client_after_host_pick.png`
- **淡入**：B 先包 `Element.prototype.animate` 計數，A 挑牌後 B 重畫、計數 0。另外補測地圖投票、秘寶投票、事件投票造成的重畫，計數也都是 0。
- 「換掉第 1 支」那條、以及視窗開著時同伴挑牌，見上面中-1、中-2。

### 9. 秘寶撞件
- 下一格改成大魔物（大飯糰）。兩件「算盤珠、毛線球」，A、B 都挑算盤珠。
- A：「兩人都想要「算盤珠」，擲骰決定：**你拿到「算盤珠」、同伴拿到「毛線球」**」；B：「兩人都想要「算盤珠」，擲骰決定：**同伴拿到「算盤珠」、你拿到「毛線球」**」；兩台引擎的秘寶一致（座位 0 多 `counting_beads`、座位 1 多 `yarn_ball`）。沒撞件那次也對：「你拿到「黑貓面具」、同伴拿到「血契短刀」」／「同伴拿到「黑貓面具」、你拿到「血契短刀」」。截圖 `09_host_toast.png`、`09_client_toast.png`

### 10. 地圖兩人選不同路
- A 選 `f1-l1`、B 選 `f1-l3`：B 在 A 投完時看得到「同伴」記號；兩台都跳「兩人選的路不一樣，擲骰選了「戰鬥」」，兩台 `currentNode` 都是 `f1-l3`、都進戰鬥、沒有橫幅（文字的觀感問題見低-3）。截圖 `10_client_sees_vote.png`、`10_host_toast.png`、`10_client_toast.png`
- 順手補測事件投票：兩人選不同項，兩台都跳「兩人選的不一樣，擲骰選了「扶正神龕…」」、跑同一個選項，各看各自角色的結果圖與文字。截圖 `10b_event_host.png`、`10b_event_client.png`

### 11. 貓窩救人
- B 倒下（兩台同步設 `down`）、下一格改貓窩：A 的按鈕「扶起同伴 她回 22 點生命站起來……」，按下去 A 的吐槽「球球：站穩，等一下換你保護我喵。」＝球球的 `reviveLines`；B 原本「菲菲躺在貓窩旁邊動不了……」，被扶起後出現打盹／磨針，打盹完兩邊一起回地圖、血量兩台一致。
- 反過來 A 倒下、B 扶：「菲菲：嚇到我了。別再倒下去了。」＝菲菲的 `reviveLines`，按鈕寫「他回 22 點生命」。截圖 `11_rest_after_revive_seat1down.png`、`11_rest_after_revive_seat0down.png`

### 12. 連線時菲菲被打
- 兩邊都不出牌按結束回合三次讓木樁人打兩個人：B 要了 `hurt_feifei.mp3`（200、`audio/mpeg`），第二次被打真的播 `hurt_feifei.mp3`；A 每次播 `hurt.mp3`。截圖 `12_client_after_hits.png`

### 主控台
- 驅動程式重開之後（菲菲的貓頭鷹那場起、第六七項、全部連線、以及重跑一次的第二項菲菲與第三四項）兩個分頁除了 `favicon.ico` 404 以外沒有任何錯誤或例外。

---

## 沒驗到的與限制
- 真正跨網路（手機網路、兩台機器）沒測，兩個分頁在同一台機器、走線上中繼。
- 驅動程式重開前（第一項、第二項球球、第三四項第一輪、第五項的圖鑑、事件插圖、球球那場貓頭鷹與地圖）的主控台紀錄沒留；第二項菲菲、第三四項之後重跑一次有記，乾淨。圖鑑、事件插圖、球球那幾段沒有重跑主控台（圖本身都用 `naturalWidth` 確認過解得開）。
- 中-1 的主機那一側、以及客戶端「按掉一個再換」那一次，那場沒開出忍具，是我兩邊同步補一支上去測的；客戶端疊兩個視窗另外用自然開出的「分身油」重現過一次。
- 佈置情境用的都是 `__app` 直接改資料（魔物血、忍具格、下一格的型別與遭遇、倒下旗標、見血封喉牌組），兩台同步改；中-2 其中一次用 BroadcastChannel 讓 A 用程式點牌，另一次是兩個真的滑鼠點擊同時送出。

## 收尾
- 關掉的行程：我的 Chrome（瀏覽器主程序＋子程序共 10 個）、`vite preview` 那一串（bash／cmd／node 共 7 個）、驅動程式 1 個（先收到結束指令自行退出）。之後再用命令列比對一次（含本資料夾路徑、`--port 4174`、`remote-debugging-port=9334`），剩 0 個；4174、9334、9400 三個埠都沒有在聽。
- 倉庫沒有任何變動（`git status` 空白）。

---
---

# 複驗：修正版（`F:\ClaudeWork\qiuqiu-audit`，分支 `audit-0916`，提交 `8273454`）

- 打包 `npx vite build --outDir <本資料夾>/dist-fix`；預覽 `--port 4175`；網址 `http://localhost:4175/qiuqiu-tower/?debug`
- 全新的 Chrome 資料夾 `chrome-fix`（除錯埠 9334）、驅動程式改用 9401；截圖都在 `shots-fix\`
- 預期資料從修正版原始碼重新匯出（`expected-fix.json`、`expected-fix2.json`）
- 中繼照舊走線上（房號 078699）

## 還壞的

### 低-1　狸大人換階段三句連播，第三句蓋在第二句上面（這次拉長停留時間後才出現）
- 重現：單機打狸大人（`__app.startFight('tanuki_lord', true)`）→ 血壓到 131 → 出一張攻擊牌打到換階段。
- 看到的（位置是畫面座標）：
  - 第一句「旁白：狸大人把酒葫蘆拋向半空……」33 字，留 3.2 秒。
  - 第二句「狸大人：看仔細了——變！」第 1.4 秒出現，排在 y=123。
  - 第三句「菲菲：變出這麼多隻……哪一隻才是真的啊？」第 2.8 秒出現，也排在 y=123。
  - 第 3.28～3.50 秒兩句都完全不透明、交疊 264×44，之後第二句淡出到第 4.0 秒才拿掉；這段時間第二句被第三句蓋住。
  - 截圖 `f5_tanuki_phase_t3s.png`（上面還掛著淡出中的第一句，第三句蓋在第二句的位置）
- 原因：`combat.css` 的 `.toast ~ .toast { top: 116px }` 只處理「同時兩句」。修正前每句只留 1.8 秒，第三句出現時第一句已經拿掉，不會三句同時在；現在第一句長到 3.2 秒，三句同時存在，第二、三句擠在同一格。
- 其他同時冒三句的時機（例如餓扁＋血剩很少＋開場白）也會踩到，這次只實測了狸大人。

### 低-2　系統公告的橫條最寬只有舞台一半，超過約 33 字就提早換行、出現孤字，兩行時蓋到更多頂端狀態列
- 看到的：
  - 紙箱撞件「兩人都想要「小魚乾罐」，擲骰決定：你拿到「忍具袋」、同伴拿到「小魚」／「乾罐」」拆兩行，第二行只有「乾罐」」。截圖 `f2_N4_chest_host.png`
  - 事件的長選項名也是兩行。截圖 `f2_N1_event_client.png`
  - 放一條 41 字的測試橫條量寬度：內容寬固定 596px、`max-width` 設 920px 卻用不到。
- 原因：`.notice` 用 `left: 50%` 加 `translateX(-50%)`，自動寬度被限制在右半邊（640px）。
- 觀感：兩行高 68px，2.6 秒內蓋住頂端的秘寶列、牌組、圖鑑鈕（點得穿，只是看不到）。

### 低-3（範圍外，原本就有）　戰利品畫面連著幾則「打贏了，拿到…」疊在同一個位置
- 重現：戰鬥前設 `run.pendingAfterFight = [常見秘寶, 忍具×2]` → 打贏。
- 三則間隔 1.4 秒，每則留 1.8 秒以上、位置都在右上同一點，每次有約 0.4 秒兩則完全重疊。
- 字數在 16 字以內，修正前後停留時間一樣，屬於原本就有的。截圖 `f5_after_toasts_2s.png`

### 低-4（範圍外，原本就有）　進戰利品畫面後 0.35 秒內先挑牌，換忍具視窗會跟著帶到地圖上
- 重現：
  1. 客戶端忍具帶滿。
  2. 一進戰利品畫面立刻點一張牌（點下去 37 毫秒）。
  3. 355 毫秒時換忍具視窗照樣跳出。
  4. 主機接著挑牌，兩人挑完回地圖，視窗還蓋在地圖上。
- 在地圖上按「換掉這支」：兩台的背包都換好了、沒斷線，但地圖頂端狀態列還是舊的三支。
- 截圖 `f1e_client_map_with_modal.png`；舊的計時器是同一套寫法。

## 通過的
1. **換忍具（中-1、中-2）**
   - 客戶端視窗開著、主機先挑牌：1.5 秒後仍只有 1 個視窗（自然掉的「殘破卷軸」）。換第一支當下那列就寫「換成了」，1.2 秒後動作傳回來，背包與狀態列都換好。`f1c_T1_*`
   - 主機視窗開著、客戶端先挑牌：1 個視窗，按不換寫「沒有換」。`f1c_T2_*`
   - 客戶端送出換忍具的同一毫秒主機挑牌，用程式連動一次（`f1c_T3_*`）、兩個真的滑鼠點擊同時送一次（`f1c_T4_*`）：牌票 +171 毫秒到、換忍具 +338 毫秒傳回，**都沒有再彈**；背包、狀態列、那一列一致，兩台引擎一致。
   - 不換 → 主機挑牌：重畫、不彈、淡入 0 次。`f1c_T5_*`
   - 單機：換第一支後 80 毫秒內那列、狀態列、背包都換好，狀態列只有一條。`f1_single_*`
   - 關主信物頁：先收信物才問換忍具，只問一次；答「不換」或「換第二支」之後，用同一份戰利品物件再重畫一次，都不重問，那一列維持答案。`f1_boss_*`
2. **系統公告（低-3）**
   - 地圖投不同路：主機「兩人選的路不一樣，擲骰選了你選的那一格（戰鬥）」，客戶端「……同伴選的那一格（戰鬥）」，兩台進同一格。
   - 進戰鬥時是畫面正上方的 `.notice`（層級 60），不是主角頭上的泡泡；主角開場白與魔物泡泡在它下面、互不遮擋。`f2_map_*_0_2s.png`
   - 事件、秘寶撞件、紙箱撞件各一張，你／同伴視角都對：`f2_N1_event_*`、`f2_N2_relic_*`、`f2_N4_chest_*`
   - 只剩一件又撞件：輸的那台結尾「……同伴拿到「逗貓棒」，你沒分到」，贏的那台沒有這句。`f2_N3_single_*`
   - 另測進 5F 秘笈（一進去就播對白）時，橫條在對白層上面看得到。`f2_dialog_*`
3. **叫聲（低-1）**：重新整理→選菲菲，第一次點擊就連同 `hurt_feifei.mp3`（200）一起下載；第一次被打（木樁人攻 5，76→71）就播 `hurt_feifei.mp3`。
4. **影片字幕（低-2）**：新的 `opening_feifei.mp4` 字幕是「先練準。力氣不夠的地方，就想別的辦法。」「……紫光照上了師父的臉。」「球球抓起頭巾追了出去，連菲菲喊他都沒聽見。」「我還是很怕受傷。可是他們要是也在等人幫忙呢？……我得去。」，跟序章一致；實機播到結束（16.55 秒）接四張幻燈片，4/4 句相符。`02_ff_video_frames.png`
5. **其他四路**
   - ① 鏡子走廊，四種情況牌子的數字都等於實際扣血：
     - 設成「先上 2 層毒再照層數打＋守 3」、身上 3 層：牌子「攻 5（照你的中毒）＋守 3」，提示框「出手時 5 層＝造成 5 點傷害」，實扣 5。
     - 身上 0 層：牌子「攻 0」，提示框「你身上沒有中毒，這一下會撲空」，實扣 0；魔物帶爪力 3 也是 0。
     - 第二關真的一招學「見血封喉、淬毒·改」（爪力 3、身上 2 層）：牌子「攻 5」，實扣 5。
     - 截圖 `f5m_*`
   - ② 打倒狸大人：之後 4 秒內沒有任何一般的打贏吐槽，接的是收場對白。`f5_boss_outro.png`
   - ③ 連三回合把飯糰用光，「飯糰用完」吐槽只跳一次。
   - ④ 長句確實留比較久（33 字留 3.2 秒、12 字留 1.8 秒），但三句同時冒時會疊，見低-1。
   - ⑤ 首頁兩張封面都是 283×283、錯開彈跳，照舊。
   - ⑥ 秘寶圖鑑是「護心短褂」，說明「回合結束時最多保留 10 點蜷縮到下一回合（跟守護符、鐵砂衣的保留量相加）。」，圖 `relic_iron_shirt.webp` 寬 96，清單裡沒有叫「鐵布衫」的秘寶。`f5_book_iron_shirt.png`
- 兩個分頁的主控台除了 `favicon.ico` 之外沒有錯誤；沒有 4xx、沒有圖回成網頁。

## 沒驗到的與限制
- 一般流程打贏關主走的是「過關畫面」，而且關主戰的戰利品不開忍具，玩家實際走不到「獎勵頁的信物＋收不下的忍具」。這一項是我直接用 `__app.show('reward', { kind:'塔主', relic:'tower_token', potionMissed:… })` 開那一頁測的。
- T2～T5 與關主那頁的忍具是補上去的（欄位跟引擎自然開出來的一樣），T1 是自然掉落。
- 連線的菲菲叫聲這一輪沒重測（上一輪已經過，這次改的是預載）。
- 跨網路仍沒測。

## 倉庫狀態
- `qiuqiu-audit` 在 02:27:48～50 多出兩個檔案變動：`docs/怪物工作檯.json`、`docs/牌池總表.json`，只有換行符號不同（`git diff --ignore-cr-at-eol` 空白）。
- 那個時間點我只在等預覽和 Chrome 起來，沒有在那個目錄寫任何東西。我的打包 02:27:23 就結束，匯出測試 02:28:14 才跑，而且只載入內容檔。推測是別的程序跑測試重生了文件；我沒有動也沒有還原。
- `qiuqiu-coop` 仍然乾淨。

## 收尾
- 驅動程式收到結束指令自行退出。
- 殺掉我開的 Chrome（`chrome-fix` 資料夾）10 個、`vite preview --port 4175` 那一串 7 個，共 17 個。
- 再用命令列比對一次剩 0 個；4174、4175、9334、9400、9401 都沒有在聽。
