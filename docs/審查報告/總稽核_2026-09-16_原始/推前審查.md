# 推送前獨立審查：`audit-0916`（基準＝線上版 `a5fa358`，審到 `8273454`）

審查方式：唯讀。臨時測試全部放在本資料夾；舊版程式碼用 `git archive a5fa358 src` 解到 `old_a5fa358/`，同一支腳本新舊各跑一次，才分得出「這批造成的」跟「本來就有的」。工作副本 `git status` 審完仍是乾淨的、HEAD 沒動。

## 總結論

- **沒有「高」**。
- **中 1 條**：倒下的人會被問換忍具、按了換不成、同伴一動作又彈。**本來就有（基準版同樣重現）**，但剛好落在這批宣稱修好的「不再再問」範圍裡。
- **低 9 條**，其中**這批造成的小退步 3 條**：主機在地圖上換完忍具狀態列沒更新、狸大人換階段三句吐槽疊在同一格、系統公告寬度上限沒作用。
- 型別檢查 0 錯；全套測試 183 檔通過／3 檔略過、1314 條通過／4 條略過。

---

## ① 問題清單

### 中-1　倒下的人被問換忍具；按換送不出去後，同伴一動作又彈（既有，非本批退步；新舊兩版都實際重現）

- **位置**：`src/ui/screens/reward.ts:319`——`r.potionMissed ?? …` 前半邊沒看座位、也沒看倒下。
- **成因鏈**：`src/engine/run.ts:490` 只要「站著的人全都收不下」就設 `potionMissed`（倒下的人不在 `standing` 裡）→ 倒下那台 `reward.ts:319` 照樣算成收不下 → `reward.ts:333` 按換 → `src/net/runaction.ts:57` 換忍具要求 `!p.down` → `src/net/session.ts:269` `submitRun` 回 false → `potionAsk` 回到 `undefined` → 下一次整頁重畫（同伴挑秘寶、挑升級）350 毫秒後又彈。
- **情境→結果**：連線，一人打到倒下，站著那位忍具滿，這場掉忍具。倒下那台跳出「忍具帶滿了。要用「煙霧彈」換掉哪一支？」（他背包其實只有一支、根本沒拿到這支），按哪一支都沒換成，同伴一動作又彈；只能按「不換」脫身。不會分岔。
- **重現**：`ui/potion_review.test.ts`「倒下的人」：第一次問 1 個、按換後 `potionAsk=undefined`、那列回到「收不下」、同伴挑秘寶後又彈 1 個。`ui_old/baseline.test.ts` 同腳本跑舊版：同樣問 1、又彈 1。
- **修法**：`reward.ts:319` 對倒下的人直接不算收不下（`iDown` 在第 40 行已經有了）。順手：同一個畫面 `:342` 的「獲得忍具」列、`:204` 的「獲得 N 條小魚乾」列，對倒下的人也是假的（`run.ts:477、:488` 只發給站著的人）——既有、本批沒碰。

### 低-1　主機在視窗開著時被帶回地圖，在地圖上按換：地圖的狀態列停在舊的（本批退步；實際重現）

- **位置**：`reward.ts:338` 只有單機才重畫狀態列；連線改靠 `reward.ts:134-151` 的 `onRunApplied`，但離開頁面後第 135 行直接 return，而 `map.ts` 沒掛 `onRunApplied`。
- **情境→結果**：主機在 350 毫秒內先點了牌 → 重畫後視窗才彈 → 同伴挑完牌 → `backToMap()`，視窗還蓋在地圖上 → 按換。忍具實際換好了（`smoke_bomb,onigiri,firecracker`），地圖狀態列卻顯示「手裡劍,飯糰,鞭炮」，要等下一次地圖重畫才對。舊版同腳本：狀態列是新的（舊碼換完無條件重畫共用畫面層的狀態列）。客戶端新舊兩版都是舊的（既有）。
- **有沒有拋錯／改錯元素**：沒有。`#screen` 是各畫面共用的節點，地圖上 `[data-missed-potion]` 找不到就略過（實測）。
- **重現**：`ui/potion_review.test.ts`「視窗開著時被帶回地圖」＋`ui_old/baseline.test.ts` 對照。
- **修法**：`reward.ts:338` 條件改成「單機，或現在畫面上已經沒有那一列（代表離開了）」也重畫狀態列；或地圖掛一支只重畫狀態列的 `onRunApplied`。

### 低-2　擊倒者倒下時，秘寶跳過了，「打倒時」的能力照發：躺著的人血從 0 變 3（修一半；實際重現）

- **位置**：`src/engine/actions.ts:457`——`if (!reviving) for (const pw of killer.powers)` 沒有 `!killer.down`；同一支函式 `:478` 秘寶那條有加。
- **情境→結果**：連線，身上掛著「忍術·回復卷軸」（打倒回 3／6）或「影子分身＋」（抽牌＋爪力＋回 3）的那位倒下之後，他下的毒毒死魔物 → 對倒下的人回血、抽牌、加爪力。跟乙 低-5（虎爪）同一個病根。戰鬥結算會把倒下的人寫回 0 血，所以只錯在這場戰鬥的畫面上，不分岔、不影響單機錨（單機倒下＝整場輸，走不到這裡）。
- **重現**：`down_killer.test.ts`：倒下＋打倒回 3 的能力 → 血量 3；對照組沒倒下時正常回 3。
- **修法**：`actions.ts:457` 同樣加 `&& !killer.down`。
- **附帶確認（專查 4 的「錢不見」）**：被偷的小魚乾拿回（`:455` `e.stolen`）排在秘寶那條之前，**沒有被跳過**；銅錢劍每殺 +8 對倒下的擊倒者不再給（實測：倒下 +20、沒倒下 +28），跟「倒下的人不分戰利品」一致，不算錢不見。

### 低-3　吐槽留得久之後，狸大人換階段三句有兩句疊在同一格（本批退步；DOM 實際重現）

- **位置**：`src/ui/dialogue.ts:169`（照字數留 1.8～3.2 秒）× `src/ui/screens/combat.ts:2412`（換階段每 1.4 秒一句）× `src/ui/styles/combat.css:576`（`.toast ~ .toast` 只有第二格 116px）；台詞在 `src/content/dialogue.ts:464`。
- **情境→結果**：第一句旁白 30 字 → 留 3.2 秒、3.7 秒才拿掉；第三句在 2.8 秒冒出來時三則都在場，第二、三句都命中 `.toast ~ .toast`，疊在同一個位置（第三句蓋住第二句，約 0.4 秒不透明＋0.5 秒淡出），第一句拿掉後第三句再跳回第一格。舊版第一句 2.3 秒就拿掉，只剩兩則，不會疊。
- **重現**：`ui/toast_overlap.test.ts`：新版 2900 毫秒時在場 3 則、落在第二格 2 則；舊版在場 2 則、第二格 1 則。
- **另**：獎勵頁 `afterToasts`（`src/ui/app.ts:485`，一樣每 1.4 秒一則）在劇場畫面本來就全疊在同一格（`screens.css:1093` 沒有第二格），現在疊得更久（既有、變嚴重一點；只有「要打一場」附帶兩項以上獎勵才會出現）。
- **修法**：輪播間隔改成跟前一句實際留存時間走，或補第三格 `.toast ~ .toast ~ .toast`。

### 低-4　系統公告的 `max-width: 920px` 沒作用，38 字就折兩行、壓到狀態列下面（本批新增；無頭 Edge 瀏覽器實測）

- **位置**：`src/ui/styles/base.css:357-359`。`left: 50%` 加 `transform` 置中、寬度自動時，可用寬度只剩舞台右半邊 640px，920 那個上限永遠碰不到。
- **實測**（載入全部四份樣式表）：24 字（地圖擲骰）476×44、底邊 58；38 字（撞件結果）、39 字（事件擲骰）都是 **640×70、底邊 84**，超過狀態列 56px 的底，蓋到畫面內容上緣約 28px、持續 3 秒。
- **修法**：加 `width: max-content;`（保留 `max-width: 920px`）。

### 低-5　公告固定 2.6 秒，比同一批放寬後的吐槽泡泡還短（推演，未實跑）

- **位置**：`src/ui/dialogue.ts:157-158`。撞件結果 38 字、事件擲骰 39 字只留 2.6 秒；同一批才因為「20 字以上 1.8 秒讀不完」把吐槽放寬到最多 3.2 秒。
- **修法**：公告共用同一條照字數算的公式（或至少 3.2 秒）。

### 低-6　（措辭，疑似）連線時提示框「你身上沒有中毒，這一下會撲空」講得太滿

- **位置**：`src/ui/screens/combat.ts:933`。一招打兩個人時層數各算各的：我 0 層、同伴 3 層，魔物照樣打同伴。預演是從我的角度算，數字沒錯，但「這一下會撲空」讀起來像整招落空。
- **修法**：改成「打你這一下會撲空」。

### 低-7　回填工具：檔頭說明還是舊規則；掃描器遇到正規表達式常值會失準

- `tools/apply_story_table.py:10-12` 檔頭仍寫「球球的句子在 dialogue.ts 全部出現處一起換」，實際已改成「當成句子只准剛好一處」。
- `table_close`（`:46-75`）不認得正規表達式常值：`/["']/` 裡的引號會被當成字串開頭。合成測試回 -1（安全失敗）；換個排法有機會停在錯的 `}`。現有五張對照表都沒有正規表達式，實測五張都找到正確的結尾（`FEIFEI_BOSS_LINES` 第 1053 行、`FEIFEI_EVENT_LINES` 874、`FEIFEI_EVENT_TEXT` 904、`FEIFEI_CAST_LINES` 1082、`firstMeetFeifei` 165）。
- **修法**：檔頭照新規則改寫；掃描器註明只支援「只有字串常值與註解」的表。

### 低-8　`TITLE_ART` 的說明插在 `heroOfKey` 的說明與函式中間

- **位置**：`src/ui/assets.ts:46-57`。「這個鍵是哪一位角色專屬的」那段說明現在掛在 `TITLE_ART` 上，編輯器滑上去會對錯。跟同一批乙 低-11（`settleRelicPicks` 說明被隔開）同一型。
- **修法**：`TITLE_ART` 整段搬到 `heroOfKey` 函式後面。

### 低-9　（純文字後備才會遇到）菲菲第一關過關，線頭與爪痕只剩圖在交代

- **位置**：`src/content/dialogue.ts:575-577`。第一句改成撿針之後，「是師兄的頭巾」「把線頭收進口袋」「循著爪痕往上走」的前文只剩第二張圖（打開看過：她手上拿著藍線、牆上有爪痕）。幻燈片正常播時沒問題；`app.ts:472` 圖不在清單時退回純文字對白、或除錯頁的台詞列表，讀起來會突兀。`slidesReady` 只看清單有沒有鍵，實際很少走到。
- **修法**：可以不動；要動就在第二句前補半句「扶手上勾著一縷深藍色的線」。

### 附註（不算這批的問題）

- `.gitattributes` 的 `*.sh text eol=lf` 只影響之後取出的檔：這個工作目錄 `tools/video/feifei_opening/render.sh` 與 9 支生圖排程仍是 CRLF（`git ls-files --eol` 顯示 `i/lf w/crlf`），但 `git status` 乾淨、**沒有假改動**。現有工作目錄要生效得刪檔重新取出。
- 意圖預演的既有限制（舊碼一樣，不是退步）：多隻魔物同一回合依序出手，前一隻給你的翻肚不算進後一隻；魔物回合開始才加的爪力（`src/engine/combat.ts:818` 階段每回合加、`:836` 每 N 回合加）也不算。

---

## ② 通過

1. **換忍具三態**：單機當場換好、狀態列一條、重畫不再問；主機 `submitRun` 同步套用時 `onRunApplied` 在賦值之前觸發（當下 `potionAsk` 還是 `asking`），但那條只重畫狀態列、不讀記號，狀態列只多掛一條、掛在戰利品頁；客戶端送出當下狀態列是舊的、重畫後那列寫「換成了」且不再問、繞回來後狀態列換新且只有一條；回 false 會回到 `undefined`、下一次重畫再問（連線停掉時不會有重畫，所以不會一直彈）。
2. **信物早退**：問話段在早退之後，`potionAsk` 一定記在「收下」後的新物件；舊回呼在換畫面時被清掉，重畫都用新物件；實跑收下→問→不換→同伴動作→不再問。另外 `afterCombat` 塔主戰不進戰利品頁（走過關畫面／結局），這條今天其實走不到。
3. **公告四處視角**：地圖 `now[app.seat]`、事件 `now[seat]`、兩處秘寶 `relicOutcomeText(…, seat)` 都對；倒下的人那票被洗掉，不會出現擲骰公告，也不會誤寫「你沒分到」（有測試）。
4. **公告定位**：跟舞台一起縮放、不受 `#overlay` 影響；`pointer-events: none` 有效（無頭 Edge `elementFromPoint` 點到底下的按鈕）；層級 60 在對白層 50、視窗 30 之上，其他樣式表沒有蓋到它。
5. **意圖預演對引擎**：`damage` 連打、`damageRandom`、`selfDestruct`（之後不跑）、`damageByPlayerStatus`（撲空也用掉蓄力、`consume` 清層數）、`statusPlayer`、`statusSelf` 都跟 `runEnemyEffects` 一致。沒預演的 `statusAllies`（含自己）、`copyPlayerStatus`、`escape`、`purgePlayer`、`stripPlayer`：掃過全部魔物招式表（含階段、換階段第一招、被打醒），沒有任何一招把它們排在傷害前面（`statusAllies` 只出現在傷害之後；`purgePlayer` 拍的是玩家的爪力，不影響挨打的數字）；鏡貓學牌只產生傷害、防禦、回血、給自己狀態、給你狀態、照層數打六種，影響數字的四種都有預演。同一招裡不會有同一個效果物件出現兩次（`find` 用物件相等比對不會抓錯）。`computeAttack` 從 `combat.ts` 移除後沒有別處需要（只剩機器人估算在用，刻意不動）。
6. **單機錨**：`tests/engine/bot.test.ts`（bal-369、bal-453、燈籠蛾 anchor-fly）、`determinism.test.ts`、`monsters2.test.ts` 共 22 條全過。茶碗規則只命中「塔主的茶碗」；毒針袋（中毒）、鐵砂袋與師父的斗笠（蜷縮）、靈貓鈴（抽牌）不受影響；回血效果沒有目標欄位、只回自己；跳過只少一行紀錄與閃光，不動亂數。
7. **文字**：`FEIFEI_BOSS_LINES` 新鍵「明天就要見到師父了喵。」實跑 `lineFor` 取到她的版本；44F 貓窩球球四句全部有她的版本；`eventTextFor` 取曬太陽第二選項＝重生的 `_feifei_result_text.json`；山賊帶朋友來那句的引號句鍵還在、她版沒有喵也沒有球球；改過的球球句句尾都有喵、她的新句都沒喵；犰狳王收場她版鍵沒變、值已改。「護心短褂」只在秘寶表，圖鑑、罐頭鋪、提示框都讀秘寶表；名詞表的「鐵布衫」是狀態那條，正確。菲菲四張幻燈片（撿彎掉的針／拿著藍線／台階上抹藥／師父攬進懷裡還握著針／落在後頭撿針）與第二關煙塵、球球相擁圖都打開看過，新句跟圖對得上。
8. **秘寶說明照實際時機**：鐵砂掌套「打出後」＝`src/engine/combat.ts:537` 牌效果算完才觸發；月光鏡「這回合沒打過攻擊牌」＝`attackedThisTurn` 在打出攻擊牌時設；護心短褂「保留量相加」＝`relicSum`。
9. **回填工具**：`as_value != 1` 對 `events.ts` 的球球長句不會誤擋（`FEIFEI_EVENT_TEXT` 的鍵在 `dialogue.ts`，不同檔）；兩個內容檔現在沒有任何劇情句重複當值（只有說話者名「黑貓忍者頭目」重複），新規則不會擋到現行句子；型別斷言表頭找得到；註解裡的 `}`、區塊註解、跳脫引號、樣板字串的 `${}`、字串裡的 `//`、除號都處理對（合成測試）。
10. **`TITLE_ART` 三條路**：首頁預載有它；選菲菲後補載會再列一次（`decodeAll` 跳過已解碼的，無害）；戰鬥暖圖排掉它；`docs/分關載入.json` 同步少一行。
11. 其餘：`hungryTold` 一場一次、打贏關主不抽一般吐槽、`hurt_feifei` 預載（檔案存在、名字在型別裡）、撞件輸了補「你沒分到」、除錯頁 `enemyNameFor`、過關預載看座位 0、舊回歸測試清掉「開過的不再開」記號，都對。
12. **型別檢查** `npx tsc --noEmit -p .`：536 檔、0 錯。**全套測試** `npx vitest run`：183 檔通過／3 略過，1314 條通過／4 略過。這批新增與相關的 9 檔單獨再跑一次：66 條全過。

---

## ③ 實際核對過的地方

- 七筆提交的完整差異逐檔讀過；`reward.ts`、`potionswap.ts`、`session.ts`（`submitRun`、`onRunApplied`、`ingestRun`）、`app.ts`（`show`、`afterCombat`）、`actions.ts`（`runEnemyEffects`、`damagePlayer`、`killEnemy`）、`combat.ts`（引擎：`startSeatTurn`、`finishEnemyTurn`、魔物回合；畫面：`intentChip`、`describeMove`、吐槽）、`run.ts`（`finishCombat`）、`runaction.ts` 讀過相關段落。
- 兩台會話＋真畫面（`happy-dom`＋`LoopbackPair`）：`ui/potion_review.test.ts` 6 條（主機、客戶端在途、單機、倒下的人、帶回地圖、信物早退）；`ui_old/baseline.test.ts` 用舊版跑同樣的倒下的人與帶回地圖；`ui/toast_overlap.test.ts` 新舊版吐槽計時。
- 引擎：`down_killer.test.ts`（倒下擊倒者的能力回血、銅錢劍與被偷的錢，含對照組）；`scan_moves.test.ts`（全部魔物招式的效果順序）；`dup_fx.test.ts`（同招重複效果物件）；`text_map.test.ts`（`lineFor`／`eventTextFor`／`castLineFor`、喵規則、秘寶名）。
- 排版：無頭 Edge 瀏覽器（獨立使用者資料夾在本資料夾 `edge-profile/`，跑完確認沒有殘留程序）量公告寬高與點擊穿透：`notice_measure.html`、`notice_pointer.html`。
- 工具：Python 直接載入 `tools/apply_story_table.py` 的 `table_close` 跑五張真表與十個合成邊角（沒有執行 `main`、沒有寫檔）；掃兩個內容檔的重複字串常值。
- 換行：`git ls-files --eol` 看 `.sh` 的索引與工作目錄換行；審前審後 `git status` 都乾淨。
- 圖：`public/assets/bg/` 的菲菲第一關三張、第二關煙塵、菲菲相擁、菲菲回家路、球球相擁。

## ④ 沒查的

- 開頭影片 `public/video/opening_feifei.mp4` 與 `tools/video/feifei_opening/FeifeiOpening.tsx` 的新字幕：沒看影片、沒核對字幕時間。
- `hurt_feifei` 預載：只確認檔案與型別，沒在瀏覽器實際聽。
- 手機實機、兩個真的瀏覽器分頁連線：沒做（用上面的兩台會話架子與無頭 Edge 代替）。
- 回填工具沒對真的改寫表跑（表裡原文都已寫回，跑了只會全部報「出現 0 次」，看不出東西）。
- 事件頁自己的換忍具流程（`event.ts:237` 那條，本批沒改）沒重審。
- 塔主戰掉忍具又帶滿時有沒有被問：塔主戰不進戰利品頁，沒查，不在這批範圍。
- `docs/事件文案.md`、`docs/分關載入.json` 只看差異，沒逐字核對全文。
