# 稽核報告（戊・工具素材打包）— 2026-09-16

倉庫：`F:\ClaudeWork\qiuqiu-coop`（分支 `coop`，HEAD `a5fa358`）。今天差異基準：`40c8cbe`。
方法：`git archive HEAD` 匯出兩份副本到 scratchpad（`repo` 給第 1 項互動實測用、`repo_clean` 給整套建置／測試用），
`node_modules` 用 `mklink /J` 連到原倉庫；另外用真正的 `git clone`（唯讀來源）驗證跨機結帳行為。全程沒有寫回原倉庫任何檔案，沒有任何 git 寫入操作。

---

## 一、問題清單

### 高

**H1. `apply_story_table.py` 對球球（hero='ninja'）的 literal 換字是整檔案盲目 `str.replace()`，沒有出現次數防呆——會靜默改壞剛好文字相同的無關值**
- 檔案：`tools/apply_story_table.py:97-104`（`if kind == "literal": ... src.replace(old, ts_lit(new))`）
- 情境：菲菲那條路徑（`hero=='feifei'`）規定原文必須「剛好出現 1 次」，出現 0 次或 ≥2 次都算問題、不會動手；但球球那條路徑只檢查「出現 0 次」，只要 ≥1 次就直接對整個檔案做 `str.replace`（Python 內建是全部取代）。這是刻意設計（讓球球原句同時當菲菲對照表的鍵一起換），但代價是：**同一個檔案裡任何跟球球那句文字剛好相同的其他值，也會被一起改掉，而且完全不會被回報成問題**。
- 錯誤結果：實際執行證實，一個跟球球台詞文字相同、但語意上完全無關的字典值（模擬「菲菲或別的角色剛好用了同一句」）被靜默改寫，工具還印出「問題 0 條」、離開碼 0（成功）。
- 修法一句：對 hero='ninja' 也印出「出現 N 次」讓人看得到，或改成先用「這是不是在 FEIFEI_*_LINES 的鍵位置」這種結構化比對，不要用整檔字串取代。
- 是否實際重現：**是**，在 scratchpad 副本裡用最小重製檔案跑了兩輪，附完整輸出（見下方「三、實際核對過的地方」）。

### 中

**M1. `apply_story_table.py` 的 `map-add` 對 `firstMeetFeifei` 永遠失敗（找不到對照表），因為它是用 TypeScript 型別斷言語法宣告的**
- 檔案：`tools/apply_story_table.py:116`（`head = re.search(rf"(export const {m}\b[^\n]*=\s*\{{|\n\s*{m}:\s*\{{)", src)`）對照 `src/content/dialogue.ts:51`（`firstMeetFeifei: <Record<string, string>>{`）
- 情境：Python 的 head 偵測只認「`欄位名:` 後面直接接 `{`」，但實際寫法是「`欄位名: <型別斷言>{`」，中間多了 `<Record<string, string>>`，regex 對不上。
- 錯誤結果：**用真正的檔案內容跑這段 regex，`head match: None`**——任何指定 `map: 'firstMeetFeifei'` 的回填列都會被列為問題（「找不到對照表 firstMeetFeifei」），永遠寫不進去，無法用重跑解決，要改程式才行。
- 目前現況：今天兩張表（球球 311 句／菲菲 364 句）裡剛好 0 筆 map-add 落在這裡（因為目前每隻魔物菲菲都已經有自己的初遇台詞），所以還沒真的爆炸過；但下次新增魔物、球球先寫初遇台詞、菲菲還沒寫時，回填腳本會在這裡卡死。`story.firstMeet`（球球自己那份，`dialogue.ts:166`）也是同樣的型別斷言寫法，只是目前程式邏輯不會把它當 map-add 目標，沒有實際影響。
- 修法一句：regex 加一段可選的 `(?:<[^>]*>)?` 吃掉型別斷言，或乾脆比對到 `{` 出現為止而不管中間是什麼。
- 是否實際重現：**是**，直接對現在版本庫裡的 `dialogue.ts` 內容跑同一段 Python regex，結果為 `None`。

**M2. `map-add` 的收尾大括號偵測（`\n(\s*)\}`）找的是「第一個」單獨成行的 `}`，遇到範例程式碼寫在註解裡會抓錯位置，把新增內容悄悄塞進註解裡（沒有任何錯誤訊息）**
- 檔案：`tools/apply_story_table.py:120`
- 情境：用最小重製檔案模擬「對照表結尾前有一段 `/* 範例：{ ... } */` 註解、註解裡剛好有一行只有 `}`」——回填腳本把新的 `'new_key': '新加的值',` 插進了註解內部，不是插進真正的物件字面量。
- 錯誤結果：**實際執行**顯示工具回報「改了 1 句；問題 0 條」（看起來成功），但檢查輸出檔案，新增的那行字躺在 `/* ... */` 裡面，對應的 TypeScript 物件實際上完全沒有新增這個鍵——執行期讀不到，`tsc` 也不會報錯（合法註解），只有真的去讀那個鍵的地方才會發現沒生效。
- 目前現況：**目前這四張真正在用的對照表**（`FEIFEI_EVENT_LINES`、`FEIFEI_BOSS_LINES`、`FEIFEI_CAST_LINES`、`FEIFEI_EVENT_TEXT`）**逐一驗證過收尾位置，都正確**（本身雖然也夾了說明性註解，但沒有「單獨成行的 `}`」這種格式，沒有踩到）；這條是對「以後格式一變會怎樣」的精確推演＋合成重現，不是目前現存的活 bug。
- 修法一句：改用簡單的括號配對計數（而不是找第一個裸 `}`），或至少在插入後重新跑 `tsc --noEmit` 當驗收。
- 是否實際重現：**是**（合成測試檔），**目前正式對照表沒有踩到**。

**M3. `check_size.py` 的首載預算算法漏算了 `feifei_cover.webp`（約 55.6 KB），因為它被籠統歸類成「菲菲專屬、選角後才補」，但首頁其實現在就會秀出它**
- 檔案：`tools/dump_monster_acts.test.ts:57-63`（`heroOfKey(key)` 命中就整批歸 `act=0`／分關載入）對照 `src/ui/screens/title.ts:89-93`
- 情境：`title.ts` 在首頁**無條件**畫出 `hasSprite('hero/feifei_cover')` 為真時的 `<img src=".../feifei_cover.webp">`（跟球球的封面並排顯示，兩隻都在），這是每個玩家一開頁就會抓的圖；但產生 `docs/分關載入.json` 的腳本只憑鍵名裡有沒有 `feifei`／`samurai` 字樣，就把它跟「選好角色才補載」的三百多張菲菲戰鬥用圖歸在同一類（act=0，排除在首載預算外）。
- 錯誤結果：`check_size.py` 報的「首載總計」比玩家實際下載的量少算約 55.6 KB（球球自己的 `hero/cover.webp` 因為鍵名沒有 hero 字樣，正確算進首載；菲菲這張因為鍵名巧合含有 `feifei_` 反而被排除，兩張功能對等卻算法不對等）。
- 目前現況：即使照原本錯誤的算法，`check_size.py` 本次執行仍然全數超標（見中-4），所以這 55.6 KB 只是讓超標幅度看起來比實際小一點點，不影響「有沒有超標」這個結論。
- 修法一句：`title.ts` 這種「不分角色、首頁必畫」的例外鍵，應該在分關載入清單產生腳本裡明確排除、或改成正面表列首載清單而非用鍵名黑名單猜。
- 是否實際重現：**是**（讀原始碼＋比對剛跑出的 `docs/分關載入.json` 內容，兩者對照確認）。

**M4. `check_size.py`（首載體積預算檢查）目前是全面超標，但不是今天造成的，而是延續好幾天的既有落差；`render.sh` 沒有比照 `deploy.sh`／`prepush_gate.sh` 釘 `eol=lf`，換機器 clone 會變 CRLF**
- 詳細數字見「四、跨機器可重現」與「三、實際核對過的地方」。
- 修法一句（體積）：目前是單一 506 KB 的 JS 主 bundle 沒有任何程式碔分割，`vite build` 自己也印出建議用 `dynamic import()` 分割；圖片預算部分維持既有的縮圖流程即可。
- 修法一句（換行）：把 `.gitattributes` 那兩行擴充成 `*.sh text eol=lf`（或至少把 `render.sh` 加進去），一次保護所有現有與未來的 shell 腳本。

### 低

**L1. `apply_story_table.py` 的錯誤訊息「（要剛好 1 次）」對球球那條路徑不準確**——球球只要求「≥1 次」，訊息卻統一寫「要剛好 1 次」，會誤導除錯的人（`tools/apply_story_table.py:101`）。

**L2. `apply_story_table.py` 沒有自帶 `sys.stdout.reconfigure(encoding='utf-8')` 之類的防呆**，純粹靠這台機器系統層級已經是 UTF-8（見下方核對），不是腳本自己穩固。換一台沒開「Windows 全域 UTF-8」、Python 版本更舊的機器，理論上仍有風險（工作機 `DESKTOP-OQGOQK0` 未實測，無法確認）。

**L3. `public/assets/rigs/` 資料夾目前是空的、`manifest.json` 的 `review` 鍵是空陣列**——不是錯誤，只是目前沒在用的殘留分類，供參考。

---

## 二、通過（一行帶過）

- `apply_story_table.py` 對已套用過的表**重跑不會亂改**：第二次執行正確回報「原文出現 0 次」、離開碼 1、檔案內容跟第一次執行後完全一致（有 diff 佐證）。
- `DUMP_STORY=1` 重新匯出：球球第二輪回填表 304/304 句、菲菲改寫完成表 346/346 句的「改寫」內容，跟剛剛重新匯出的「原文」100% 一致，0 筆殘留舊句——下一輪要改寫可以直接從重新匯出開始。
- `tools/ref/monster_refs/`（gitignore）**不是跨機器風險**：`make_fix_0915_jobs.py` 裡三處用到它的地方都經過 `white_ref()`，每次都用已進版控的 webp 現場重繪，沒有任何地方要求這個資料夾要預先存在。
- `.git/hooks/pre-push` 確認不受版控（本來就不可能受版控），實際掛在共用倉庫 `F:\ClaudeWork\qiuqiu-tower\.git\hooks\pre-push`（`qiuqiu-coop` 是它的 worktree），內容正確委派給有版控的 `tools/prepush_gate.sh`；進度紀錄的說明跟現況一致。
- `tools/deploy.sh`、`tools/prepush_gate.sh`：`.gitattributes` 的 `eol=lf` **確實有效**——兩次獨立的真實 `git clone` 都驗證這兩支結帳出來是純 LF，沒有 CRLF。
- 素材清單完整性全過：manifest.json 引用的 1281 個路徑，0 個缺檔、0 個 0 位元組、0 個 webp 檔頭錯誤；`public/assets` 底下沒有清單沒引用的孤兒圖檔（唯一沒被 manifest 收錄的是 28 個 `sfx/*.mp3`，這是設計上本來就不進 manifest，逐一 grep 確認都有程式碼在用）。
- 今天新進倉／改動的圖尺寸全部符合同類基準：影菲菲五個姿勢一致 460×460、菲菲封面 560×560（跟球球封面同尺寸）、九個新塔主秘寶圖示都是 96×96、四張重生的菲菲牌面都是 299×240。
- `opening_feifei.mp4`、`sfx/hurt_feifei.mp3`、`sfx/victory_feifei.mp3` 確認**沒有**被算進首載：影片只在 `playVideo()` 被呼叫（選好角色開新局）那一刻才設定 `<video>.src`；音效走 Web Audio 隨用隨載，這兩個角色專屬音效名字也不在開場那八個常用音效的預載清單裡。
- `npx tsc --noEmit -p .`：0 錯誤。
- `npx vite build`：成功（有預期中的「主 chunk 超過 500 KB」建置警告，跟中-4 的體積問題是同一件事）。
- 全套 `npx vitest run`：180 個檔案通過、3 個檔案跳過（共 183）；1290 條測試通過、4 條跳過（共 1294）；0 條失敗。
- `tools/haze.test.ts` 的真正像素檢查（掃 574 張圖找半透明灰膜）在這台機器上確實有跑（找得到 python 跟 Pillow），結果乾淨。

---

## 三、實際核對過的地方（含關鍵指令與數字）

### 1. 劇情回填工具（scratchpad 副本內操作，沒有動到原倉庫）

- **①②合併測試**：在副本建立最小重製檔（`_audit_fixture/dialogue_fixture.ts`，內容故意讓球球一句台詞同時是 `FEIFEI_BOSS_LINES` 的鍵、又跟一個無關的 `UNRELATED_MAP.greeting` 值文字相同），跑 `python tools/apply_story_table.py _audit_fixture/table.md`：
  - 第一次執行：印出「改了 1 句；問題 0 條」，離開碼 0；檢查檔案，三個出現位置（球球本句、菲菲對照表鍵、無關值）**全部被換掉**——證實 H1。
  - 第二次執行（同一張表、已套用過的內容）：印出「原文在 ... 出現 0 次（要剛好 1 次），沒改」、離開碼 1；跟第一次執行後的檔案內容做 `diff`，**完全沒有差異**——證實①「重跑不會亂改」成立。
- **③**：對正式的 `firstMeetFeifei`（型別斷言語法）直接用 Python 重新跑一次 `map-add` 的 head 偵測 regex，結果 `None`（M1）；另外用合成檔案（一般 `Record<string,string> = {...}` 加一段內含裸 `}` 的區塊註解）重現「新增內容被插進註解裡」（M2）；`blurb` regex 對照現在 `heroselect.ts` 實際內容（單一字串、無串接）人工逐字元核對過，比對邏輯正確，沒有另外用真的 `hasSprite` 值刻意造壞例子去測「串接寫法」這個分支，屬於精確推演而非合成重現（見「四、沒查的」）。
- **④**：用 `cmd /c` 明確 `set PYTHONUTF8=` 清空、分別在「這台機器目前預設 chcp（65001）」與「強制 `chcp 950`」兩種狀況下跑 `python tools/apply_story_table.py`（不帶參數，印出整份中文說明文件）：**兩種情況都正常印出中文、離開碼 0，沒有 `UnicodeEncodeError`**。追查原因：這台機器系統層級 ANSI code page 已經是 65001（Windows「使用 Unicode UTF-8 支援全球語言」設定已開啟，不受單一主控台 `chcp` 影響），另外目前用的 Python 3.12.10 對 Windows 主控台輸出本身也有一定的容錯。**這條在這台機器沒有重現到倉庫記憶所描述的崩潰**，但腳本本身確實沒有 `sys.stdout.reconfigure` 這種主動防呆（L2）。
- **⑤**：在副本跑 `DUMP_STORY=1 npx vitest run tools/dump_story_table.test.ts`，重新產生 `docs/球球_劇情改寫表_2026-09-15.keys.json`、`docs/菲菲_劇情改寫表_2026-09-15.keys.json`；寫小腳本比對這兩份新匯出檔案的「原文」集合，對照球球「回填第二版」表與菲菲「改寫完成」表裡本來就有填「改寫」的列：**球球 304/304、菲菲 346/346 的新句子都已經出現在剛匯出的原文欄，0 筆舊句子殘留**。

### 2. 素材清單完整性

- 跑過的既有測試：`tests/ui/assets.test.ts`（2 條過）、`tools/manifest_hygiene.test.ts`（3 條過）、`tools/assets_nonempty.test.ts`（1 條過）、`tools/down_art.test.ts`（4 條過，含「倒地圖畫布高度跟待機圖一樣」）、`tools/sprites_exist.test.ts`（5 條過）、`tools/haze.test.ts`（真正的像素掃描那條過，備援跳過條件正確被跳過）——全部包含在下面第 6 項的全套執行裡。
- 自己另外寫小腳本核對：manifest 1281 個引用路徑逐一檢查存在性／位元組數／webp 檔頭前 4+4 bytes（`RIFF`/`WEBP`），0 個問題；`public/assets` 底下全部 1310 個實體檔案跟 manifest 引用集合比對，孤兒只有 28 個 `sfx/*.mp3`，逐一 grep `src/`、`worker/` 確認都有引用。
- 用 Pillow 讀取像素尺寸核對今天變動的圖：影菲菲 5 張（460×460 一致）、貓頭鷹夜哨對照組 3 張既有姿勢（315×318 一致，佐證方法論正確）、菲菲封面（560×560，跟球球封面一致）、九個新秘寶圖示（全部 96×96）、四張重生菲菲牌面（全部 299×240，跟既有牌面一致）。
- `git diff --stat 40c8cbe..HEAD -- public/assets` 完整核對過 43 筆變動，逐一歸類（新增／重生／manifest 更新）。

### 3. 首載體積與打包

- `npx tsc --noEmit -p .`：0 錯誤。
- `npx vite build --outDir dist`（在 scratchpad 副本內，未寫到原倉庫的 `dist/`）：成功，`main-*.js` 506.12 KB、`main-*.css` 103.16 KB，建置工具自己印出「主 chunk 超過 500 KB，考慮用 dynamic import()」的警告。
- `python tools/check_size.py`（同一份副本 dist）：**全類別超標**——程式 513.2 KB／200.0 KB（256.6%）、樣式 103.2 KB／55.0 KB（187.6%）、圖片 9.91 MB／9.30 MB（106.5%）、首載總計 10.52 MB／9.70 MB（108.5%），離開碼 1。
- 另外拿 `40c8cbe`（今天基準點）單獨匯出、單獨建置、單獨跑 `check_size.py` 做對照：程式 510.7 KB、樣式 102.9 KB、圖片 9.85 MB、首載總計 **10.47 MB**——跟任務描述的「前幾天記錄的 10.47 MB」完全吻合，證實方法論正確。**今天一天總共只多了約 50 KB 首載（+9 個檔案，全部是新塔主秘寶圖示），JS 只多 2.5 KB、CSS 只多 0.3 KB**——目前的全面超標是既有欠帳、不是今天造成的迴歸。
- `docs/分關載入.json`：直接讀取確認 `feifei_cover.webp` 標記 `act=0`（分關載入／不算首載）、`hero/cover.webp`（球球）完全不在清單裡（＝視為必定首載）；`shadow_feifei_*` 五張全部標記 `act=9`（目前沒有任何遭遇池會用到，改用變裝機制在戰鬥中臨時換皮），對照 `enemies.ts` 的變裝邏輯，這個歸類本身合理（不算 bug，只是首次遇到影菲菲時要重新抓圖，可能有一瞬間延遲）。
- 用原始碼追查 `title.ts`、`video.ts`、`sfxhero.ts`/`audio.ts` 三處，逐行確認 M3 與「opening_feifei.mp4／新增音效不算首載」兩個結論。

### 4. 跨機器可重現

- `tools/ref/monster_refs/`：grep 全部 3 處用法，逐一確認都經過 `white_ref()`（現場用已進版控的來源圖重繪，不要求資料夾預先存在）。
- CRLF／`sh`：用 `git cat-file -p HEAD:<path>` 直接讀 blob 原始位元組（不透過任何 shell pipe，避免踩到 Git-Bash 環境下 `grep` 對 `\r` 判讀不穩定的雷——這點debug 花了一些來回，最後改用 Python 直接讀檔案位元組跟 `subprocess` 抓 `git cat-file` 的原始 stdout 才拿到可信結果），確認：
  - 倉庫裡全部 12 支 `.sh`（`deploy.sh`、`prepush_gate.sh`、`render.sh`、9 支「生圖排程*.sh」）**blob 本身都是純 LF**，沒有商業歷史包袱。
  - `.gitattributes`（2026-09-14 建立）目前**只**對 `tools/deploy.sh`、`tools/prepush_gate.sh` 兩支釘 `eol=lf`；其餘 10 支（含 `render.sh` 與全部 9 支排程腳本）沒有任何保護。
  - 做了兩次獨立的**真正 `git clone`**（`git clone --branch coop --single-branch F:/ClaudeWork/qiuqiu-coop <臨時目錄>`，來源是唯讀操作）：`deploy.sh`／`prepush_gate.sh` 兩次都正確結帳成純 LF（`.gitattributes` 生效）；`render.sh` 兩次都結帳成 100% CRLF（21/21 行）。
  - 對 CRLF 版的 `render.sh`／`deploy.sh`／`prepush_gate.sh` 跑 `sh -n`：**三支都語法檢查通過（離開碼 0），沒有直接爆掉**——另外拿一段簡化的 `case`／`if` 字串比對測試 CRLF 是否會讓比對失效，這台的 `sh` 沒有踩到（`\r` 落在行尾、不在被比對的字串內容裡）。**沒有進一步真的執行 `render.sh`／`deploy.sh` 本體**（前者會對外寫到 `F:\ClaudeWork\remotion-video`、後者會碰真正的 git remote，超出唯讀範圍），所以「語法過但邏輯會不會被 CRLF 咬到」沒有做到窮盡驗證，只能說目前找不到會咬人的具體路徑。
- `.git/hooks/pre-push`：確認是 `qiuqiu-tower` 那份共用倉庫的 hook（`qiuqiu-coop` 的 `.git` 只是指向 worktree 的指標檔），內容跟進度紀錄描述的完全一致。

### 5. 倉庫膨脹

- `git diff --stat 40c8cbe..HEAD` 篩出今天變動的 png/mp4/mp3/webp，依實際檔案大小排序（見下）；最大幾筆：`opening_feifei.mp4` 1.81 MB（合理，正式素材）、`tools/ref/feifei_cover_ref.png` 979 KB、`tools/ref/event_refs*/…png` 四張各 176–237 KB。
- `docs/審查報告/圖_2026-09-15/` 十張聯絡表合計 7.4 MB（最大 `a.png` 2.72 MB）。
- `tools/ref/` 整體現存 57 MB；用 `git rev-list --objects` 抓這個資料夾在 `coop` 分支歷史上所有版本的 blob，累積 61.4 MB、220 個版本（未去重壓縮的粗估）。
- 共用倉庫 `F:\ClaudeWork\qiuqiu-tower\.git` 實際物件庫大小 269 MB。

### 6. 全套測試

`npx vitest run`（scratchpad 乾淨副本，未動到原倉庫的 `docs/事件文案.md`）：

```
Test Files  180 passed | 3 skipped (183)
     Tests  1290 passed | 4 skipped (1294)
  Duration  7.20s（transform 17.04s / import 38.25s / tests 29.39s，四項為多執行緒平行加總，非序列時間）
```

最慢的五個測試檔（用 `--reporter=json` 取精確的 startTime/endTime 差）：

| 名次 | 檔案 | 耗時 |
|---|---|---|
| 1 | `tools/_bal2.test.ts`（600 局平衡模擬） | 6.04 s |
| 2 | `tools/haze.test.ts`（574 張圖像素掃描） | 4.32 s |
| 3 | `tests/engine/map_choice.test.ts` | 2.93 s |
| 4 | `tests/balance.report.test.ts` | 1.29 s |
| 5 | `tests/engine/shop_upgrade_coin.test.ts` | 1.25 s |

（備註：**這裡刻意選擇在 scratchpad 乾淨副本跑，不是在原倉庫跑**，所以原倉庫的 `docs/事件文案.md` 沒有被這次稽核觸碰到——這比題目原本預告的「預期副作用」更保守一點，特此說明選擇。）

---

## 四、沒查的

- 沒有真的執行 `render.sh`、`deploy.sh`、`prepush_gate.sh` 本體（只做 `sh -n` 語法檢查與片段邏輯的合成測試），因為三支都有倉庫以外的真實副作用（算繪影片寫到別的專案目錄、部署碰真正的 git remote／推送閘門）。CRLF 是否會在這三支「真的跑起來」時造成邏輯錯誤（而非語法錯誤），沒有窮盡驗證。
- 沒有機會存取或實測工作機 `DESKTOP-OQGOQK0` 的實際環境（PYTHONUTF8、系統代碼頁、`.git/hooks/pre-push` 是否已安裝），中-4／低-2 提到的「換機器」都只能就目前這台機器的設定＋原始碼邏輯做精確推演。
- 沒有對美術內容做「畫得對不對、角色像不像」這類主觀／視覺審查，只查了技術面（檔案存在、位元組數、格式檔頭、像素尺寸）。
- `blurb` regex 沒有另外合成一個「字串用 `+` 串接」的例子去實測那個分支（現在唯一在用的兩筆資料剛好都是單一字串），這部分結論屬於讀程式碼＋讀正式資料的精確推演，不是實跑重現。
- 沒有掃描 `tools/ref/` 以外的其他資料夾或 `_incoming/`、`tools/codex_raw*/` 等是否也有類似的「可重建卻進了版控」問題，倉庫膨脹的建議只針對題目點名的兩個目標（今天新增大檔＋審查報告聯絡表）與順手發現的 `tools/ref/` 全體規模。
- 沒有檢查 `tools/生圖排程*.sh`（9 支）在**實際排程情境**下是不是真的會被 CRLF 咬到（只確認它們一樣缺乏 `.gitattributes` 保護、blob 本身是純 LF），因為這些腳本不在今天的變動清單裡，屬於順手擴大稽核範圍時發現的旁證，沒有像 `render.sh` 那樣做真正的 `git clone` 個別驗證（判斷跟 `render.sh` 同機制，未逐一重複驗證）。
