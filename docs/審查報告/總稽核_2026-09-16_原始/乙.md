# 稽核乙：引擎、秘寶、存檔（`coop` 分支 `a5fa358`）

稽核時間：2026-09-16 凌晨。倉庫全程唯讀，稽核開始與結束時 `git status` 都是乾淨的、`HEAD` 都是 `a5fa358`。
所有實跑都在複本裡做：`git archive a5fa358` 解到 `乙\repo\`（`node_modules` 另外複製一份），
臨時測試放在 `乙\repo\audit\`；比對今天之前的學牌翻譯時另外解了 `40c8cbe` 的 `src` 到 `乙\repo\old40c8cbe\`。
不在倉庫裡跑全套測試的原因見低-11 第三條。

---

## 一、問題清單

沒有「高」。

### 中-1　鏡貓學到的「照你身上的毒打」：意圖牌子與提示框的數字跟實際打出來的對不上（兩個方向都會錯）

- **位置**：牌子 `src/ui/screens/combat.ts:838`、`:861`，提示框 `:921`，都拿「現在的層數」算；
  引擎 `src/engine/actions.ts:787-798` 是出招當下一條一條效果結算、讀的是**即時**層數；
  機器人估算 `src/engine/smartbot.ts:124` 跟牌子同一套算法。
- **情境甲（多算）**：你身上 0 層毒、鏡貓有爪力（二關遭遇魔氣 3、三關 6，或難度、魔氣暴走加上去的）。
  牌子寫「攻 3（照你的中毒）」、提示寫「現在 0 層＝造成 3 點傷害」；實際 `n <= 0` 直接「撲了個空」，打 0。
- **情境乙（少算，會出人命）**：二三關 `learnCards: 2`，一動抽兩張、效果照抽到的順序串起來。
  排在前面那張先給你上毒（淬毒·改 7、強力塗毒 10／升級 13、飛針 1…），後面的見血封喉讀的是**上完之後**的層數，
  牌子卻是用上毒之前的層數算。實測：
  - 淬毒·改 → 見血封喉，你身上 2 層、二關：牌子 5，實際 12
  - 強力塗毒＋ → 見血封喉＋，你身上 3 層、三關：牌子 12，實際 38
  - 飛針 → 見血封喉（走「攻 X＋Y」那條分支），你身上 4 層：牌子 3＋4＝7，實際 8
  - 牌組只有淬毒·改＋見血封喉時，200 個種子的第一動有 80 次是「先上毒再引爆」的順序
- **同型舊洞**（今天之前、一動兩張那天就有）：盯上你了（翻肚 2）→ 貓抓，牌子 9、實際 13。今天的毒只是把落差放大。
- **修法**：牌子、提示框、機器人估算都照「同一招裡排在前面的 `statusPlayer`」先把層數（和翻肚）加上去再算，
  層數是 0 就寫 0／撲空、不加爪力；或反過來讓引擎出招前先抓一份層數快照——兩個擇一，看設計要哪個意思。
- **重現**：是（引擎實跑＋照抄畫面算式比對；沒開瀏覽器看畫面上的字）。測試檔 `audit\mimic_0916.test.ts`（A、B、B2）、`audit\extra_0916.test.ts`（G）、`audit\weak_0916.test.ts`。

### 低-1　「同一局開過的不再開」把一條舊的回歸測試架空了

- **位置**：`tests/engine/coop_seat_bugs_0914.test.ts:154-163`（高-10「過關秘寶不會開出座位 1 已經有的」），對應 `src/engine/run.ts:565-573`。
- **情境**：這條測試先擲一次三選一、讓座位 1 把三件拿走，再擲第二次。現在第二次本來就會因為「開過」把那三件排掉，
  不管排除名單有沒有算到座位 1。
  **突變驗證**：在複本把 `run.ts:565` 改回只看座位 0 → 測試照綠；再把 `seen` 拿掉 → 測試變紅（跑完已還原）。
- **修掉的那個錯還碰得到的情境**：座位 0 在第一關關主戰倒下，塔主令牌只發給站著的座位 1；
  排除名單若退回只看座位 0，過關三選一會開出令牌，座位 1 挑了拿不到、畫面照演「到手了」。這條測試已經抓不到。
- **修法**：測試在第二次擲之前清掉 `relic_seen:` 旗標，或直接用 `takeRelic` 把塔主池的東西塞給座位 1（不先擲一次）。
- **重現**：是（突變測試）。

### 低-2　混搭連線的鏡子走廊：本機角色講的話，跟實際出現的那隻對不上

- **位置**：初見吐槽照本機角色 `src/ui/app.ts:380`；事件結果文字照本機角色 `src/ui/screens/event.ts:361`；
  變裝看座位 0：`src/engine/actions.ts:677`、`src/engine/combat.ts:82`、`src/ui/screens/combat.ts:1001`。
- **情境**：座位 0 球球、座位 1 菲菲 → 菲菲那台先看到事件結果「鏡中的菲菲踏出鏡面，握針的手勢與她完全相同…」，
  初見吐槽「那張臉明明是我，怎麼會自己笑起來？」，場上卻是「鏡中球球」（影球球立繪＋開場泡泡「（是球球的影子，站起來了）」）。
  反過來（座位 0 菲菲、座位 1 球球）球球那台吐槽「鏡子裡的我，怎麼自己跑出來了喵？」，出來的是「鏡中菲菲」。
  （交辦單引的「那是……我？不對，我才不會那樣笑。」已被劇情改寫稿換掉，現在是上面那句，矛盾一樣。）
- **嚴重度**：只在混搭連線、第一次遇到那隻時；純敘事矛盾，不動數值、不影響鎖步。
- **修法**：本機角色跟座位 0 不同時，初見吐槽改走一句「鏡子照到的是同伴」的版本（或退回開戰口頭禪），事件結果文字同理加一份混搭版。
  **不建議**改成「照本機角色變裝」：學的牌是座位 0 的，長相跟牌又會變成另一種對不上，而且兩台的紀錄名字會不同。
- **重現**：是（引擎名字、開場白與 `storyFor` 實跑）。

### 低-3　座位 0 倒下時，鏡貓照樣抄座位 0 的牌、穿座位 0 的皮，實際只打座位 1

- **位置**：`src/engine/mimic.ts:110`（`learnPool` 用 `cs.player`）、`src/engine/actions.ts:677`。
- **實測**：座位 0 球球上一場倒下、座位 1 菲菲帶見血封喉進鏡子走廊 → 牠叫「鏡中球球」，學牌池是躺著那位的 10 張（貓抓、替身術…），菲菲的牌一張都不學。
- **判斷**：跟「看座位 0」的設計一致，不算錯；若要改，學牌與變裝要一起改成「第一位還站著的」（兩台倒下狀態相同，鎖步安全）。
- **重現**：是。

### 低-4　`consume` 被隱身（或整回合免傷）閃掉時，層數照樣清掉

- **位置**：`src/engine/actions.ts:794-795`。
- **實測**：你 5 層毒＋1 層隱身、效果帶 `consume: true` → 沒掉血、隱身用掉、毒也歸 0。
- 今天沒有任何牌帶 `consume`（見血封喉基礎版 9/14 起不清毒），是埋著的。
- **修法**：真的打中（沒被閃、沒被免傷擋）才清。
- **重現**：是（直接餵效果）。

### 低-5　倒下的人身上的秘寶照樣「發動」

- **位置**：擊倒獎勵發給 `poisonedBy` 那位、沒看 `down`：`src/engine/actions.ts:447`、`:477-486`；
  留蜷縮的秘寶只看 `cs.phase`：`src/engine/combat.ts:912-916`。
- **實測**：座位 1 下的毒、他先倒下，毒死魔物 → 虎爪讓躺著的人血 0→3、爪力 +1，紀錄印「球球（2 號）的秘寶發動：虎爪」；
  座位 1 倒下時身上還有 5 點蜷縮 → 每輪都印「…的秘寶發動：鐵布衫」。
- 戰鬥結束時 `finishCombat` 會把倒下的人血歸 0，所以只是戰鬥中的畫面與紀錄怪。沙丁魚罐、黑曜爪、守護符、鐵砂衣是同一條舊路。
- **修法**：擊倒秘寶那段與留蜷縮那段各加一句「倒下的跳過」（同檔 `onDeathHealPlayer` 那行已經這樣做）。
- **重現**：是（`audit\relics_0916.test.ts`、`audit\down_0916.test.ts`）。

### 低-6　塔主的茶碗滿血時照樣「發動」

- **位置**：`src/engine/combat.ts:346`（每回合開始的秘寶一律先 `fireRelic`）。
- **實測**：80/80 開戰，紀錄照印「秘寶發動：塔主的茶碗」，之後每回合一行＋金光。
  沙丁魚罐 2026-09-10 低-9 訂過「回 0 點不閃、不佔紀錄」，這件沒跟上（舊毛巾、木魚是同型舊路）。
- **修法**：回血型的每回合秘寶在滿血時不發動。
- **重現**：是。

### 低-7　兩件的說明文字跟實際時機有落差

- **鐵砂掌套**（`src/content/relics.ts:108`）「每回合第一次打出攻擊牌時獲得 1 點爪力」：實際在牌效果算完才給（`combat.ts:531-537`），第一張吃不到。
  實測貓抓 6 點 → 打出 6、同回合第二張 7。建議改「打出後」，跟貓抓板同一個講法。
- **月光鏡**（`relics.ts:122`）「回合結束時沒有攻擊的話」：實際判準是「這回合沒打過攻擊牌」（`combat.ts:647`）。
  實測打技能牌「交出來」＋忍具「鐵爪套」共 21 點傷害，回合結束照樣 +2 爪力、+8 蜷縮。建議照止水碗寫「這回合沒打過攻擊牌」。
- **重現**：是。

### 低-8　鐵布衫：疊加方式沒寫、名字撞三個

- **疊加**：`combat.ts:913` 是加總。實測鐵布衫單件留 10、＋守護符 18、＋守護符＋鐵砂衣 24、＋斗笠（留 10 再給 8）18。
  跟既有「守護符＋鐵砂衣」行為一致，但三件說明都只寫「最多保留 N 點」，沒講會疊。要不要改文字由使用者決定，數值不動。
- **撞名**：牌「絕學·鐵布衫」與它給的狀態「鐵布衫」（名詞表 `src/content/glossary.ts:40`：下回合開始獲得蜷縮）、忍具「鐵布衫膏」（本回合免傷），加上這件秘寶，四樣東西三種效果。
  名詞提示框目前不會把秘寶名字配到狀態說明（`markupKeywords` 只用在牌面文字），所以只是認知上的混淆。
- **重現**：疊加是；撞名是讀程式。

### 低-9　機器人不認得這九件

- `src/engine/smartbot.ts:78-84` 的秘寶評分表沒有九個新 `id`，一律預設 5（金飯碗、掌門腰帶是 8），過關三選一較少挑到；
  戰鬥中也沒有「帶月光鏡刻意不出攻擊牌」「帶鐵布衫多留蜷縮」「帶酒葫蘆挑時機喝忍具」的判斷。
  效果本身是引擎在發，報表裡有算到，只是會低估。`smartbot.ts:124` 對 0 層毒也估成魔物爪力（同中-1 甲）。
- 只影響平衡報表。**重現**：讀程式，沒跑對照。

### 低-10　過關畫面預載下一關時沒帶座位 0 的角色

- **位置**：`src/ui/screens/actclear.ts:37` 的 `preloadAct(run.act + 1)` 走預設 `localHero()`。
  混搭連線時暖到的是本機角色那組變裝，不是真的會出現的那組；開戰前 `app.ts:385` 的 `warmEncounter` 有帶對、最多等 1.5 秒，所以最多是晚一點出圖。
- **修法**：補上 `run.players[0]?.hero`。**重現**：讀程式。

### 低-11　雜項

- `src/engine/rewards.ts:133-158`：`settleRelicPicks` 的說明註解被新插入的 `relicOutcomeText` 夾在中間，文件掛到錯的函式上。
- `src/ui/screens/debug.ts:141`：除錯頁「第一次看到每種魔物」照魔物表寫名字，菲菲那頁顯示「鏡中球球：那張臉明明是我…」。
- **全套測試的副作用比交辦單寫的多**：除了 `docs/事件文案.md`，`npx vitest run` 還會無條件重寫
  `docs/牌池總表.json`、`docs/怪物工作檯.json`（寫的是國際標準時間的日期，台灣時間早上 8 點以後就會跟倉庫裡的 `2026-09-15` 不同）、`docs/分關載入.json`。
  這次在複本裡跑，三個檔內容剛好跟倉庫一致；換日之後在倉庫裡跑就會弄髒工作目錄。交代其他代理「唯讀」時例外清單要補這三個。

---

## 二、通過（一行帶過）

- 九件效果、數字、第一回合有沒有、兩人時給對座位：全部對（每件都跑了單人＋座位 1）。
- 斗笠的蜷縮不會在魔物出手前被清：第一回合魔物直接打 7，被 8 點擋住剩 1；魔物打完才修剪；第二回合重給 8（被炸毛時照規則變 6）。
- 「開過的不再開」旗標：寫入只在 `rollActRelics`，兩台各跑一次不會分岔；整局指紋不收它，但也找不到分岔的路；存讀檔、局面碼都帶得回來；舊檔沒旗標＝什麼都沒開過；沒有程式整批數 `run.flags`。
- 照層數打：0 層不走 `damagePlayer`（不反彈、不觸發毛線手套）；算完 0 點也不反彈、不觸發；兩人各算各的層數；蓄力只用一次，而鏡貓本來就拿不到蓄力。
- 學牌翻譯的副作用：`40c8cbe` 與 `HEAD` 逐張（含升級版）比對，結果改變的剛好是註解與測試列出的 22 個，沒有列外的。
- 影菲菲：戰鬥紀錄名字、立繪、開場白都看座位 0，兩台一致；五張立繪已進倉，替身退路走不到；機器人報表用遭遇 `id`，不受名字影響。
- 存檔：今天之前的檔（沒 `flags`、沒開過旗標、身上有斗笠）驗得過，斗笠第一回合照新規則給 8；`RunGain.asked` 只活在事件畫面的陣列裡，不在 `RunState`、不進存檔。
- 今天改過的測試：沒有改到永遠綠的（逐檔判斷見第三節）；被架空的是沒改過的高-10（低-1）。
- `npx tsc --noEmit -p .`：0 錯（TypeScript 7.0.2；註：`tsconfig` 的 `include` 沒有 `tools/`，那邊的測試檔不做型別檢查）。
- `npx vitest run`：183 個檔 180 過、3 略過；1294 條 1290 過、4 略過（三支要環境變數才跑的倒檔，加上「找不到 python」的提示條——python 找得到，灰膜檢查本身有跑、綠）。
- 錨值：`tests/engine/bot.test.ts` 的 `bal-369`／`bal-453`、`lantern_moth`、`rats3` 三條都有跑、都綠、沒有略過標記；全倉庫沒有 `it.skip`／`.only`。
  （提醒：`bal-369`／`bal-453` 都死在第一關關主，**沒有任何錨會走到過關三選一**，今天「錨值全綠」對開過不再開這件事沒有資訊量。）

---

## 三、實際核對過的地方

### 1. 九件塔主秘寶（`audit\relics_0916.test.ts`、`audit\extra_0916.test.ts`）

| 秘寶 | 單人實測 | 座位 1 實測 | 疊加／時機 |
|---|---|---|---|
| 師父的斗笠 | 第一回合 8、第二回合 8（炸毛時 6） | 座位 1 兩回合各 8、座位 0 是 0，紀錄寫「球球（2 號）的」 | 魔物打 7 被擋、剩 1；打完才修剪 |
| 鐵砂掌套 | 第一張攻擊後爪力 1，第一張 6、第二張 7 | 座位 1 打，爪力給座位 1 | 牌效果之後才給（低-7） |
| 塔主的茶碗 | 50→52 | 座位 1 52、座位 0 不變 | 滿血也發動（低-6） |
| 傳功玉佩 | 第一回合 7 張、4 飯糰；第二回合 5／3 | 座位 1 7／4、座位 0 5／3 | — |
| 塔主的酒葫蘆 | 爪力 +2、蜷縮 +6 | 座位 1 喝的給座位 1 | 忍具打完最後一隻不發動（跟舊毛巾同規則） |
| 招財貓 | 52 vs 沒帶 22，多 30 | 座位 1 54、座位 0 24（`fishPerSeat`） | — |
| 虎爪 | 爪力 +1、50→53 | 座位 1 打倒給座位 1；座位 0 打倒不給；座位 1 下的毒毒死給座位 1 | 擊倒者倒下仍回血（低-5） |
| 鐵布衫 | 30 點留 10 | 座位 1 留 10、座位 0 歸 0 | 加總（低-8） |
| 月光鏡 | 爪力 +2、蜷縮 +8，撐過魔物回合；打了攻擊牌就不發 | 兩人各算各的（打了的座位 1 沒有） | 技能牌＋忍具傷害仍算沒攻擊（低-7） |

### 2. 「開過的不再開」旗標

- 呼叫點：`src/ui/screens/actclear.ts:38`（整個畫面只由 `app.ts:471-472` 開一次；同伴投票走內部 `render()` 不重擲；除錯頁跳轉只有貓窩、紙箱、罐頭鋪、結算）、`src/engine/bot.ts:95`、`src/engine/smartbot.ts:820`、測試（`act_relics_pool`、`audit`、`run`、`coop_seat_bugs_0914`、`lockstep_mixed`）。
- 讀 `run.flags` 的地方：`net/hash.ts:106` 只收 `event:`／`sequel:`；`map.ts:309、315`、`run.ts:123、129、135、517、1147`、`app.ts:187-188、221、291-292、351、379` 都是精確鍵。
- 連線：沒有中途加入一局的路（兩台各自 `newCoopRun`），斷線接回是補收訊息、分頁不重建，不會重跑過關畫面。
- 存檔：`save.ts` 整份 `JSON.stringify`，`checkRun` 只補空的 `flags`；實跑存讀檔與局面碼來回都帶得回來（`audit\save_0916.test.ts`）。
- 突變：見低-1。

### 3. 鏡貓學牌與照層數打（`audit\mimic_0916.test.ts`、`audit\extra_0916.test.ts`、`audit\learn_diff.test.ts`）

- 隱身閃掉＋`consume`：層數照清（低-4）。
- 爪力、蓄力、翻肚跟牌子：見中-1；蓄力的來源只有 `chargeNext`，學牌翻不出來，鏡貓拿不到。
- 一招打兩個人：各算各的（引擎 `enemyTargets`；原有測試也釘著）。
- 0 層與算完 0 點：不反彈、不觸發毛線手套、不掉血。
- 座位 0 倒下：抄座位 0（低-3）。
- `learnCard` 新舊逐張比對結果：鐵頭功、亡命、幫你墊一下、先幫你留著、鐵心＋、封印解除＋，以及她的手滑、淬毒·改、毒霧、見血封喉、一針斃命、不要過來！（各含升級版，共 22 個結果）。

### 4. 影菲菲變裝

- 名字：`makeEnemy` 用座位 0 的角色（戰鬥紀錄、開場白那行、學牌那行都是同一個名字）；開場白從變裝那份挑（`combat.ts:82-86`）。
- 立繪：`combat.ts:1001` `artOfEnemy`、開戰暖圖 `combat.ts:568`、`app.ts:385` 帶座位 0；`manifest.json` 有 `codex/monster_shadow_feifei` 五張、`public/assets/monsters/` 檔在；`docs/分關載入.json` 標 9（不算首載）。
- 其他地方：地圖節點標題 `app.ts:497` 用魔物表名字（鏡子走廊是事件叫出來的，碰不到）；機器人報表用遭遇 `id`；除錯頁見低-11。
- 混搭連線吐槽：見低-2。

### 5. 存檔相容

- 舊檔（刪掉 `flags`、身上有 `master_hat`）：`checkRun` 通過、補空旗標、過關三選一正常、斗笠第一回合 8。
- 今天沒有改名或移除任何秘寶、牌、遭遇、事件 `id`（`relics.ts` 只有新增；`cards.ts` 沒動）。
- `RunGain` 只出現在 `run.ts:1005` 型別、`applyRunEffects` 的輸出陣列與 `event.ts` 畫面變數；`RunState` 沒有這一欄。獎勵頁的 `potionDeclined` 掛在畫面參數上，也不進存檔。

### 6. 今天改過的測試（`git diff 40c8cbe..HEAD --stat -- tests tools/*.test.ts`，15 檔）

- `tests/content/relics.test.ts`：件數 66→75、塔主池 10→19，跟內容同步，變嚴。
- `tests/engine/mimic.test.ts`：鐵頭功 `null` → `[{damage 16}]`，刻意行為變更、有註解；結界仍 `null`。合理。
- `tests/engine/mimic_feifei.test.ts`、`act_relics_pool.test.ts`、`relic_outcome_text.test.ts`、`tests/ui/sfxhero.test.ts`、`potion_ask_once.test.ts`：新增。後者是讀原始碼的正規式，脆但不會永遠綠。
- `tests/ui/assets.test.ts`：新增替身退路那條，合理。
- `tools/down_art.test.ts`：加變裝對照表（只對影菲菲），合理。
- `tests/ui/hero_video.test.ts`：規則改成「查對照表才播」＋對照表內容＋檔案存在，合理。
- `tests/engine/feifei_0914b.test.ts`：四句整句改成「有『菲菲：「…」』且沒有喵」，深藏不露那句「師父」改成「她有開口」。放寬，理由是劇情整批改寫；共用事件「她有自己的版本」由 `feifei_event_lines.test.ts` 的「每一句都有她自己的版本」「沒有只拿掉喵」兩條守著。合理放寬。
- `tests/content/feifei_event_text.test.ts`：假師兄改回「鏡中的自己」，現在這幾條就算完全沒有她的專屬文字、只靠換名字也會過，但設計本身改了，合理放寬。
- `tests/content/feifei_event_lines.test.ts` 賣藥那條：`not.toContain(his)` 的 `his` 帶句尾「喵」，單看會漏掉「只拿掉喵」的退路；同檔另兩條擋得住，合理。
- `tools/dump_story_table.test.ts`：要 `DUMP_STORY=1` 才跑，不影響。
- `tests/ui/node-fs.d.ts`：多宣告一個 `existsSync`。

### 7. 全套

- 型別檢查、全套測試數字、錨值：見第二節。完整輸出在 `乙\tsc.out`、`乙\vitest_full.out`。

---

## 四、沒查的

- 沒開瀏覽器：影菲菲立繪在畫面上的大小與位置（`combat.ts:649` 的尺寸覆寫與 `combat.css:147` 是照影球球的圖調的）、牌子實際顯示的字（中-1 是照抄算式比對）、秘寶金光、音效。
- 沒做連線實機（兩個分頁）；座位 1 的驗證都是引擎層直接組兩人戰鬥。
- 沒碰平衡：照規矩沒調數值、沒跑 `smartRun` 前後對照；九件新秘寶與「開過不再開」會讓過關三選一的分布整個變掉，9/15 之前的機器人報表不能直接拿來比第三關。
- 今天的畫面層其他改動（獎勵頁與事件頁的「不換只問一次」、同伴投票安靜重畫、貓叫換檔、首頁、地圖）只看了跟本題有交集的片段。
- 中繼伺服器（`worker/`）沒看。
