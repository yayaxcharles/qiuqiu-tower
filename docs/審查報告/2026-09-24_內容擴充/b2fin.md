# b2fin 報告：第二批事件收尾（2026-09-23～24）

工作副本 `F:\ClaudeWork\qq-b2fin`，分支 `c0923-b2fin`（從 `79d8b034` 開出，沒有合併任何分支）。本機伺服器只開了 5391（`vite preview`），跑完看過命令列才關，現在沒有東西在聽。

## 結論先講

| 項目 | 結果 |
|---|---|
| 1 三個限定秘寶選項 | 在 `79d8b034` 上**已經真的給秘寶**（不是 60 條小魚乾）；補了代號對照測試，改成不存在的代號會紅 |
| 2 「拿到了什麼」寫師門集到幾件 | 兩處都改走 `relicLongText`，實機看到「【師門 2／3】…」 |
| 3 劇本數字 | 遊戲裡四隻的事件文字都沒寫到那幾個數字；只在劇本檔頂端加「以程式為準」註記 |
| 4① 球球第二批文字延後載入 | **首載程式 631.2 → 608.1 KB（92.8% → 89.4%，少 23.1 KB）**；`docs/事件文案.md` 重生一字不差 |
| 4② 同伴讓條件選項出現 | 結果文字**和結果圖**都換成同伴那一版（實機抓到第一版只換字、圖還是自己的，已修） |
| 5 平衡 | 跟 `79d8b034` **逐位元相同**（引擎沒動）；三件限定秘寶進場的影響 ≤0.03 層；**機器人 2400 局一次都沒拿欠條** |
| 驗收 | `tsc` 零錯誤；`vitest` 339 檔全過、3134 條過、**沒有 Errors 那一行**；反向驗證 27 條全紅 |

## 一、逐項

### 1. 三個給限定秘寶的選項（`cell_bandit` ①、`miasma_crystal` ①、`shadow_truth` ②）
- 確認：`79d8b034` 已合進 b2mech，三件秘寶定義都在（`src/content/relics.ts:290-299`），`relicId` 走 `run.ts:1301` 直接給；實機四隻各拿一次，秘寶進了身上、小魚乾沒變、沒有「改拿 60 條」那行字。
- 新測試 `tests/engine/event_relic_ids_0923.test.ts`（6 條）：
  - 所有事件（含座位不對稱、賭局兩邊、條件選項的 `requires`）裡的 `relicId`／`loseRelicId`／`requires: relic` 代號，全部查得到定義（走法本身先驗：六個已知代號都有掃到）。
  - 事件限定池每一件都有事件給（不然整局拿不到）。
  - 三個選項實際套效果：拿到那一件、`gains` 有它、小魚乾不變、紀錄沒有小魚乾；標籤寫的名字就是那一件。
  - 身上有斗笠時拿舊木劍，說明寫【師門 2／3】。

### 2. 事件畫面「拿到了什麼」改用 `relicLongText`
- `src/ui/screens/event.ts:93` 新增 `gainText(g, owned)`（秘寶走 `relicLongText`、忍具照舊）；`gainsNode`（放大圖示上方那行）、`gainRows`（對白框那一列）都改用它，兩處呼叫傳 `me(run, seat).relics`（效果已套完，剛拿到的也算）。
- 測試 `tests/ui/b2fin_event_0923.test.ts` 前三條（照原始碼守接線；行為在上一支的最後一條）。
- 實機：四隻拿舊木劍的結果頁兩處都寫「每場戰鬥開始時獲得 1 點爪力與 4 點蜷縮。【師門 2／3】集到任兩件：每場戰鬥第一回合多 1 顆飯糰。」，上方跳「師門套組湊成了」。

### 3. 劇本數字
- 查法：四隻的事件標題、敘述、選項、結果、條件提示句（`src/content/*.ts`，秘寶定義除外）裡，欠條／會員卡／批發箱／算盤／帳本前後 30 字有沒有數字 → **一處都沒有**，事件文字不用改。
- 劇本檔 `scratchpad\content\design2_事件劇本.md` 頂端加了「以程式為準」註記＋對照表：欠條 15→25 條、會員卡「停在拿到時」→「放生一律 40 條」、批發箱多帶 1→2 支、店主的算盤 `shop_abacus`→店主的帳本 `shop_ledger`；並註明 bal 代理還在調數字、一樣以 `relics.ts` 為準。

### 4① 球球的第二批事件文字改成延後載入
- 做法（照第〇批 0-1）：十八篇的開頭與五十三個結果、八條條件選項的結果，用腳本整段剪到延後載入的 `src/content/event-text-b2.ts`（`NINJA_EVENT_TEXT_B2`:19、`NINJA_COND_RESULT_B2`:130），原處（`events-batch2.ts`、`events.ts` 那八條）的 `text`／`result` 留空；那支一載入就填回事件資料（:146-151）。代號或選項對不上會當場丟錯，不會靜靜留白。腳本自己驗算過：搬走的每一段都原字出現在新表、舊檔一段不剩（`scratchpad\content\b2fin\move_ninja.py`）。
- **選項標籤與標題留在主程式**：標籤寫著效果，跟 `outcome` 一起看才對得上，兩樣加起來不到 6 KB（見「需要主控決定」1）。
- 讀文字的地方全部找過（腳本 `find_readers.py` 照靜態匯入走每一支測試與工具）：
  - 執行時：只有事件畫面與除錯總覽讀，兩個都靜態匯入 `event-text.ts`（→ `event-text-b2.ts`），讀到時一定已經填好；引擎、三支機器人、預載只讀效果與圖鍵，不讀文字。
  - 測試／工具會靜靜少驗的三支補上匯入：`tests/content/events.test.ts`（每篇都有字）、`tests/content/quote_position.test.ts`（引號位置，不補會跳過那十八篇）、`tools/dump_events.test.ts`（產 `docs/事件文案.md`）。其餘都經由 `event-text` 間接載入。
  - 證據：全套跑完 `docs/事件文案.md` 只差換行（內容一字不差），代表四隻、七十三篇的文字一段都沒掉。
- 首載（`SITE_NAME=qiuqiu-tower-coop npm run build`＋`python tools/check_size.py`）：

| 類別 | 修前 `79d8b034` | 修後 `60e1be15` |
|---|---|---|
| 首載程式 | 631.2 KB（92.8%） | **608.1 KB（89.4%）** |
| 按需程式 | 558.0 KB | 582.6 KB（事件那一塊 277.5→301.9 KB） |
| 首載總計 | 9.78 MB（86.6%） | 9.76 MB（86.4%） |

  共用的那一塊壓縮後 47.2→37.2 KB。**b2event 報告說的「41 KB」是整批首載增量**（含引擎、機器人、其他內容），球球的敘述與結果實際是 23 KB 左右，這一刀拿回的就是這些。原始輸出 `scratchpad\content\b2fin_size_before.txt`、`b2fin\size_after.txt`。
- 測試：`tests/ui/event_text_split.test.ts` 補兩條：主程式靜態匯入走得到的每一支都找不到任何一段球球第二批文字、而且碰不到 `event-text-b2.ts`；載入後十八篇的開頭與每個結果、八條條件結果都填回，另外三隻對照表的鍵全部對得到。

### 4② 連線時同伴讓條件選項出現：結果寫同伴做的事
- 判準 `src/engine/eventcond.ts:119 resultSeat`：養成型條件（流派牌、秘寶、忍具滿了）是同伴讓它出現的（`choiceGate` 的 `by`）就照同伴；兩人都符合、單人、付錢型、旗標照本機。跟事件開頭那句條件提示句同一套（提示句本來就照 `by` 挑）。**「既有連線改口機制」我的理解是這一套**：拿同伴那一位自己的版本（四隻都有、主角都是他自己），沒有新寫句子。
- 畫面 `src/ui/screens/event.ts`：`take()` 在套效果**之前**定案（:658-659；鈴鐺那條套完就交出去了，問不出是誰的），三條結果路（一般結算 :538、連線學招、連線學招都不要）都照它寫。
- **實機抓到並修掉的問題**（`60e1be15`）：第一版只換了字，開房的球球看到「菲菲看著自己的針……」，結果圖卻是球球在喝解藥（圖文對不上，違反第四道門檻）。修法：`resultArtHeroFor`（:209）用同一個判準挑結果圖，畫、等圖、進畫面預載三處都傳（`preload.ts` 的 `eventResultUrl`／`eventResultUrls`／`preloadEventResults`／`warmResultArt` 多一個選填的角色參數）；還沒解好時頂著的主圖仍是畫面上剛畫過那張（`fallback.hero`）。
- 同一輪還抓到一個潛在錯：球球那一位的 `hero` 欄可能不填，直接傳 undefined 會被預設值當成「本機這一位」→ 本機菲菲、同伴球球讓它出現時會寫錯人。改走 `heroOf`，實機第 C 段專門驗這個。
- 測試：`tests/engine/cond_result_seat_0923.test.ts`（6 條：只有同伴、兩人都有、同伴帶鈴鐺且證明套完才問會退回本機、我倒下、單人／付錢型／旗標／一般選項、七條養成型另外三隻都有自己的版本且主角是他）；`tests/ui/b2fin_event_0923.test.ts` 後段（接線、預載照給的角色挑的兩條行為測試）。
- 做不到的：選項**標籤**沒跟著改（例：【菲菲的毒】讓她拿**你的**毒試新解藥），見「需要主控決定」3。

### 5. 平衡快量（難度 1；四隻各 600 局、雙人兩組各 300 局；種子照 b2mech 的 `bal.ts`）

| 組 | `79d8b034` | `46f96048`（本分支，引擎相同） | 三件退回 60 條小魚乾（變體） |
|---|---|---|---|
| 球球 | 24.26 層、通關 10.0% | 24.26、10.0% | 24.25、9.7% |
| 菲菲 | 26.45、8.8% | 26.45、8.8% | 26.42、8.7% |
| 噹噹 | 24.75、8.3% | 24.75、8.3% | 24.72、8.7% |
| 封封 | 22.02、3.5% | 22.02、3.5% | 22.00、3.5% |
| 球球＋菲菲 | 20.67、6.0% | 20.67、6.0% | 20.67、6.3% |
| 噹噹＋封封 | 18.91、1.7% | 18.91、1.7% | 18.91、1.7% |

- 前兩欄整份輸出**逐位元相同**：這批只動文字搬家與畫面，引擎、機器人一行沒動（`resultSeat` 只有畫面在叫）。第三個提交也只動畫面與預載。
- 三件限定秘寶真的進場的影響：跟「退回小魚乾」比最多差 0.03 層，因為機器人很少拿到——每 600 局裡拿到魔氣殘片 2～7 局、舊木劍 3～18 局，**山賊的欠條 0 局**（四隻都是）。原因：量尺給欠條的分數（球球 7.7、菲菲 15.6、噹噹 20.1、封封 3）扣掉 5 點血，比不過另一個選項「挖出 50 條小魚乾」的 17.5 分，機器人一律挖魚乾。見「需要主控決定」2。
- 腳本與原始輸出：`scratchpad\content\b2fin\bal.ts`、`bal_var.ts`，`before.log`／`after.log`／`var_on.log`／`var_off.log`。

## 二、實機（Playwright，`channel:'chrome'`、每個瀏覽器環境全新暫存設定檔、只開 `localhost:5391/?debug`）

- 單人四隻 × 三篇（每篇主圖＋每個選項各開一次）：每隻 72 項檢查，**0 項沒過、主控台錯誤與 404 為 0**。檢查含：標題／本文／按鈕是這一隻的版本、主圖與結果圖是這一隻那張且載好、秘寶真的拿到、小魚乾不變、兩處說明＝`relicLongText`。修完第三個提交後用新打包重跑一次，四隻一樣全過（另存 `b2fin_shots_regress\`，跟我看過那批逐張比對，只有「敲碎」那張的丟牌特效時間點不同）。
- 連線（開房球球＋加入菲菲，貼碼直連）× 賣藥的三花貓條件選項三段：A 只有菲菲養出毒、B 兩人都有、C 只有球球有。41 項，0 項沒過、沒有斷線橫幅、兩台整局帳一致。A 開房那台是【菲菲的毒】＋「因為：菲菲後來學的牌裡有 3 張會上毒」＋菲菲的提示句，結果是菲菲的字＋菲菲的圖；C 菲菲那台是【球球的毒】，結果是球球的字＋球球的圖；B 各看各的。
- **每一張主圖、結果圖都用 Read 看過**：單人四隻 × 三篇共 61 張（每隻 15～16 張：主圖 3 張＋9 個選項的結果頁，疊著秘寶或牌的那幾頁另看藏起那一層的原圖；三選一挑牌的畫面沒看）；連線最後一版 12 張看了 10 張，沒看的兩張（A 段菲菲那台主圖、B 段球球那台主圖）是前一版打包看過的同一個畫面。圖文對得上、是這隻貓自己的版本、沒有空白或錯圖。截圖在 `scratchpad\content\b2fin_shots\<角色>\` 與 `coop_cond_ninja_feifei\`；腳本 `scratchpad\content\b2fin_pw\`（`solo.js`、`coop_cond.js`、`expected.ts`）。

## 三、反向驗證（27 條全紅，還原後 `git diff` 雜湊前後一致）

- `scratchpad\content\b2fin\redcheck.py`（18 條，輸出 `redcheck_out.txt`）：三個代號改成不存在的（各一條）；放大那一顆／對白框那一列改回 `d.text`、說明出口改回只寫原文、沒傳身上秘寶；十八篇文字搬回 `events-batch2.ts`、八條結果搬回 `events.ts`、首載模組靜態匯入 `event-text-b2`、沒填回開頭、沒填回條件結果、事件資料測試拿掉匯入；`resultSeat` 不看 `by`、畫面套完效果才問、三條結果路各自沒傳、`evText` 的角色參數沒用上。
- `redcheck2.py`（9 條，輸出 `redcheck2_out.txt`）：結果圖照舊用 `artHero`、`resultArtHeroFor` 永遠回原本、預載不看給的角色、名單沒把角色傳下去、沒走 `heroOf`、頂替的主圖跟著換成同伴的、等圖沒傳角色、進畫面預載沒照選項挑、`take()` 沒定結果圖。

## 四、改動清單（既有測試改了期待的都寫理由）
- 程式：`src/content/event-text-b2.ts`、`events-batch2.ts`、`events.ts`（只清空八條結果＋註解）、`src/engine/eventcond.ts`（新增 `resultSeat`）、`src/ui/screens/event.ts`、`src/ui/preload.ts`。
- 新測試 3 支：`tests/engine/event_relic_ids_0923.test.ts`、`tests/engine/cond_result_seat_0923.test.ts`、`tests/ui/b2fin_event_0923.test.ts`；`tests/ui/event_text_split.test.ts` 補兩條。
- 既有測試改期待（行為本來就要變）：`tools/coop_leak.test.ts`（`evText(raw)`→多帶 `resultHero`）、`tests/ui/event_result_art.test.ts`（閉包替身多宣告 `resultArtHero`、兩處原始碼比對、`warmResultArt` 多一個參數）、`tests/ui/mirror_hall_coop_art.test.ts`（結果圖改走 `resultArtHero`，並補一條「其餘仍是 artHero」）；三支測試／工具補匯入 `event-text-b2`（見 4①）。
- 全套跑完照雷 5 還原了只差日期換行的四份 docs（這批沒有真的重生的 docs）。

## 五、沒做的、觀察到的
- 全套第一次跑時「兩個人的機器人走得完連線限定事件」那條紅過一次（8.3 秒；單獨跑 1.5 秒過，之後三次全套都過）。應該是整套平行跑時同步的 80 局連線機器人超過預設 5 秒時限，我沒改它，列給主控留意（是舊測試、不是這批造成的推測，沒有另外證明）。
- `tools/story_table_lib.ts` 的劇情改寫表把共用事件的球球原句都標成 `events.ts`；第二批的那幾段本來就在 `events-batch2.ts`、現在在 `event-text-b2.ts`，回填腳本找不到它們。b2event 之前就是這樣，這次沒動（不在範圍）。

## 六、需要主控決定
1. **選項標籤也要不要延後載入**：十八篇的標籤＋八條條件標籤約 6 KB 還在主程式。我留下的理由是標籤寫著效果、跟 `outcome` 一起看才對得上；要搬的話做法同一套，另外三隻的標籤本來就走 `eventTextFor`。
2. **機器人永遠不拿山賊的欠條**（2400 局 0 次）：牢裡的山賊一律選挖 50 條，欠條在平衡量測裡等於不存在。要嘛接受（真人可能會拿），要嘛調機器人估值或選項②的 50 條——這牽涉秘寶數值與量尺，bal 代理在動，我沒碰。
3. **條件選項的標籤在連線時仍寫「你」**：例如同伴讓它出現時是【菲菲的毒】讓她拿**你的**毒試新解藥、【球球的毒】同理。結果字與圖已經照同伴，標籤要不要也改口（要改就是八條標籤各寫一個同伴版，或改成不帶人稱的寫法）。

## 七、主控裁決之後（提交 `03ff8e55`）

裁決：①標籤 6 KB 留首載不搬；②欠條估值交給平衡代理；③**要改口**；另外把偶爾超時的那條測試放寬等待上限。
- ③ `src/content/event-text-b2.ts` 的 `COND_LABEL_PARTNER`：七條養成型條件選項各一條同伴版標籤，`{同伴}` 走 `coopFill`，括號裡的效果一字不動（例：「讓她拿菲菲的毒試新解藥」）。付錢型（整包買下）、旗標型（回信、看過它練）沒有「誰達成」，照原本。事件畫面新增 `labelText`（`choiceGate` 的 `by` 是同伴才換），按鈕與擲骰結果那一句都走它。
- 測試：`tests/ui/b2fin_event_0923.test.ts` 補三條（每條養成型都有、效果括號跟原標籤相同、四隻兩兩配對稱呼換好沒留記號、畫面接線）。反向驗證加 7 條（`redcheck.py` 6a～6g），全紅；上面 27 條重跑也全紅，共 34 條。
- 實機（重新打包，開房球球＋加入菲菲，A／B／C 三段）：47 項 0 沒過。A 段開房那台寫【菲菲的毒】讓她拿菲菲的毒試新解藥，C 段菲菲那台寫【球球的毒】讓她拿球球的毒…，B 段兩台照原本「你的毒」。截圖逐張看過。
- 測試放寬：「兩個人的機器人走得完連線限定事件」照 `shop_upgrade_coin` 那支的做法，等待上限改成 30 秒、斷言不動、註明原因。這種看機器忙不忙才會紅的，沒辦法用改回去的方式證明會紅，只能列出來。
- 首載程式仍是 608.1 KB。全套 339 檔全過、3137 條過、沒有 Errors。
- 誠實說明：這一輪我有一次用 Bash 的 heredoc 改暫存區的驗證腳本，反斜線被折掉、寫出一行斷掉的字串（倉庫的鐵則明講不准這樣做）。發現之後用 Edit 修好，也確認腳本語法正確。倉庫檔案沒被波及，倉庫的改動都是用 Write／Edit 做的。

## git log --oneline 79d8b034..HEAD
```
03ff8e55 主控裁決（b2fin）：連線時同伴讓條件選項出現，按鈕標籤照實際達成的人寫；連線機器人那條測試放寬等待上限
60e1be15 實機驗收修正（b2fin）：連線時同伴讓條件選項出現，結果圖也跟著換成同伴那一版（字寫菲菲、圖卻畫球球）
46f96048 內容擴充第二批收尾（b2fin）：事件裡的秘寶代號都對得到、拿到秘寶的說明寫師門集到幾件、連線時同伴讓條件選項出現就寫同伴做的事
acee00ce 內容擴充第二批收尾（b2fin）：球球的第二批事件文字改成跟另外三隻一樣延後載入，首載程式 631.2→608.1 KB
```
（`coop..HEAD` 共 42 筆，其餘 39 筆是開分支前就在整合分支上的第〇～二批與合併，不是我的。）
