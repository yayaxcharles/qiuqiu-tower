# 第三輪實機驗收：內容擴充第〇批＋第一批（qq-cint）

驗收代理，2026-09-23 21:27–22:40。報告只寫親眼看到、親手量到的東西；沒測到的另列一節。

## 一句話結論

`46e077ef` 四道門檻都沒有比線上差，新秘寶、新忍具、5F 三版、球球三篇事件、圖鑑、分關載入、連線都照規格動；但它有一個連線會整局停掉的高風險問題（加入方網路很慢、主機在事件裡換掉剛拿到的忍具；是協調者推前審查抓到的，我第一輪用 1.6 Mbps 沒撞到）。這個問題在 `95a2ab63` 已經修好，我用同一支腳本在修前重現、在修後確認不再發生。**建議推 `95a2ab63`，不要推 `46e077ef`。** 平衡：只算內容擴充，六組都在誤差內。

聯絡表（親眼看過）：`accept_c1\_總表.jpg`、`accept_c1\_四項門檻.jpg`；細表在 `accept_c1\gate12\`、`gate4\`、`sheets\`。

## 〇、驗的是哪一版

| 用途 | 版本 | 怎麼打包 | 連接埠 |
|---|---|---|---|
| 主驗收 | `46e077ef`（`c0923-int`） | 在 qq-cint 裡 `SITE_NAME=qiuqiu-tower-coop` 打包（21:27），`vite preview` | 5301 |
| 線上對照 | `6362a6b3` | `git archive 6362a6b3` 到暫存區、正式打包 | 5303 |
| 補驗 | `95a2ab63` | `git archive 95a2ab63` 到暫存區 `accept_c1\int2_src`、正式打包 | 5304 |

- 驗到一半（21:49、22:00）整合分支多了兩筆合併，HEAD 從 `46e077ef` 變 `95a2ab63`。主驗收全部用 21:27 那次打包（當時 HEAD 還是 `46e077ef`，git 記錄可查），不受影響。
- 補驗沒在 qq-cint 裡重新打包：那時事件圖分身還在用 5301 那份，重打會把它正在測的東西換掉。改用 `git archive` 出同一個提交、在暫存區打包，內容一樣。
- qiuqiu-coop 現在 HEAD 是 `2b8c3c7f`，它是 `6362a6b3` 之後的一筆純文件提交，程式碼跟線上一樣。

## 一、四道門檻（跟線上 6362a6b3 比）

### 1. 大小：正常，跟線上一模一樣
- 四隻貓 345 個角色畫面的檔名一致，頭的大小量出來的數字新舊逐格完全一樣（少數躺姿吻合度不到 0.90、量不準的格，例如菲菲翻肚 0.88，新舊也是同一個數字，人眼看正常）。
- 限速 1.6 Mbps 第一場：逐格動作還沒到時，用的是**對的那隻貓**的待機靜態圖；逐格到了換過去那一下，腳底差 1～3 像素、身高差 2～10 像素（球球 242→252 最多，約 4%），**新舊兩版這幾個數字完全一樣**，是靜態圖與逐格本來就有的差，不是這次改出來的。
- 證據：`accept_c1\gate12\_大小_1..4.jpg`、`_慢網路.jpg`、`sheets\_慢網路換手.jpg`（看過）。

### 2. 貓窩：正常，跟線上差 0 像素
- 三關 × 三張底圖 × 五種姿勢，四隻貓 180 組新舊對照，貓窩框位置差 0 像素；另外真的走進貓窩打盹／磨爪 24 次，也是 0 像素。
- 新秘寶「不眠香爐」：打盹按鈕改寫成「打盹（不眠香爐：睡了也不回血）」，四隻貓按鈕字都放得下；真的打盹，血不變。
- 證據：`gate12\_貓窩_1..3.jpg`、`_不眠香爐.jpg`（看過）。

### 3. 流暢度：正常，跟線上一樣
- 四隻貓 × 近戰、近戰接防禦、丟東西、挨打、擋一部分、最後一擊＋勝利，每 50 毫秒一格新舊並排：位置、時間點一樣，沒有跳位；逐格亮度掃描沒有閃白、閃黑。
- 單人整局跑一次（四隻：標題→選角→序章→地圖→戰鬥→罐頭鋪→貓窩→第一關關主，封封另外打到第二關關主）：全部走完，主控台 0 錯誤、0 個 404。
- 證據：`sheets\fluid_{ninja,feifei,dangdang,fengfeng}.jpg`（看過）、`shots\solo_*_int.log`。

### 4. 事件圖片：正常；舊事件一格都沒變，新事件都是本角色那一版
- 除錯頁四隻 × 全部事件逐格比對：572 格畫出 552 格（沒畫的 20 格是挑牌畫面，本來就沒插圖）。**線上原有的圖逐位元組比，不同 0 格**；線上沒有的格全部屬於新事件（球球 17、其他三隻各 6）；出現別隻貓的圖 0；畫面跟除錯頁對不上 0；文字改動 0。
- 從地圖真的點進去（四隻、新舊各 22 格）：主圖、結果圖 22 格全對；不限速進事件畫面新 13～33 毫秒、舊 11～34 毫秒；新版結果圖 0 次空白。
- 「用到才載」之後有沒有變空白、錯圖、錯貓：限速時新版一進事件畫面主圖就是完整的，結果圖點下去就在；線上版主圖空白 1.4～8.4 秒、結果圖 2.2～8.0 秒才出來（見第六節表）。
- 新事件：第二關 5F 舊木箱、第三關 5F 最後一頁，四隻 × 三選項全部點過；球球三篇（藍頭巾、木靶、屋頂上的影子）都點過。細節見第六節。
- 我自己的補驗（95a2ab63 單人 16 個事件、連線 5F）也都是本角色的圖，見第十三節。
- 證據：`gate4\_事件圖.jpg`、`_新事件.jpg`、`_新事件_球球.jpg`、`_載入取樣.jpg`、`_連線5F.jpg`、`_事件圖_{貓}_{1..7}.jpg`（總覽與新事件、載入、連線我親眼看過；28 張分表是分身逐張看的）。

## 二、新內容（除錯模式塞新秘寶、新忍具，實際開打）

每隻塞 6 件新秘寶（含花費型、鎖角色、給同伴的）＋3 支新忍具（都有會丟出去的火雷珠），第 1 層那場魔物血 ×4，看數字、說明、秘寶列。四隻主控台 0 錯誤。

| 貓 | 看到的 |
|---|---|
| 球球 | 開打 5 飯糰（不眠香爐、銅臭錢袋各 +1，這兩件是花費型）；分食便當一個人時給自己 2 蜷縮；同心結一個人時爪力 2；火雷珠每隻魔物 −14，飛行物約 0.49 秒落地；鐵布衫油 +10 蜷縮 +3 反彈；替身人偶 +1 隱身、抽 2 張；蛇牙墜打一下多 1 中毒；罐頭鋪因銅臭錢袋貴五成（160→240、55→83、45→68）；帶不眠香爐打盹血不變 |
| 菲菲 | 中毒 10（起始秘寶 3＋藥王鼎 5＋毒霧香囊 2）、狂刀鞘爪力 4、翻肚 2；竹筒潛水第二回合變隱身；斷念珠手牌 6、戰利品只剩 2 張可選；散毒粉 10→其他魔物各 +5；千針膏之後打一下 +2 中毒 |
| 噹噹 | 反彈 4、4 飯糰、蜷縮 6；第二回合蜷縮 10（秤砣腰帶留 8＋便當 2）；對半包子 +8；魔物打過來被反彈 −9（以牙還牙粉 +4、鐵砧 +1 對得上） |
| 封封 | 開打蓄氣 8；提神茶 8→12（到上限）；劍意符喝了還是 12；影忍頭帶潛水→隱身；4 飯糰；帶不眠香爐打盹血不變 |

- 秘寶列：新圖示全部有圖；說明框文字跟 `relics.ts`、`potions.ts` 逐字對得上。
- 罐頭鋪：忍具有「常見／罕見／稀有」牌子，跟資料一致。
- **沒驗到**：劍穗結（封封那場手上沒有攻擊牌可以觸發）。
- 證據：`sheets\_新內容.jpg`、`_秘寶列與貨架.jpg`（看過）；逐項數字在 `shots\new_*_int\result.json`。

## 三、載入

### 圖鑑（標題畫面直接開「秘寶與忍具圖鑑」）
| | 新（140 格） | 線上（112 格） |
|---|---|---|
| 不限速：全部圖到齊 | 0.14 秒 | 0.08 秒 |
| 限速 1.6 Mbps：全部圖到齊 | 6.3 秒 | 5.2 秒 |

兩版都 0 張破圖、0 格退回純文字。新版慢的 1 秒是多了 28 件的圖。

### 開局到第一場、事件、逐格動作（各跑兩次，新／線上）
| 量什麼 | 不限速 新 | 不限速 線上 | 限速 新 | 限速 線上 |
|---|---|---|---|---|
| 開局→第一場手牌出來 | 8.45／8.44 秒 | 8.42／8.41 秒 | **11.5／11.4 秒** | 31.5／31.6 秒 |
| 第一場玩家那格空著的取樣數 | 0 | 0 | **0** | 150 |
| 點事件格→事件畫面 | 0.06 秒 | 0.01 秒 | 0.72 秒 | 0.01～0.03 秒 |
| 點事件格→結果圖 | 2.07／2.08 秒 | 2.03／2.02 秒 | **2.74／2.76 秒** | 7.0／7.2 秒 |
| 點選項→結果畫面 | 0.01 秒 | 0.01 秒 | **0.01 秒** | 5.0／5.2 秒 |
| 菲菲逐格動作到（玩家） | 0.23／0.25 秒 | 0.29／0.25 秒 | 170 秒 | 96 秒 |
| 逐格動作全部到 | 0.35／0.38 秒 | 0.41／0.35 秒 | 170 秒 | 110 秒 |

- 不限速：每一項都在 0.5 秒門檻內（最大差 0.05 秒，是事件畫面改成用到才載）。`95a2ab63` 把事件文案併進事件畫面那一塊之後，同一支腳本量進事件畫面是 0.013 秒（`46e077ef` 是 0.062 秒），這 0.05 秒也回來了。
- 限速：開局快 20 秒、事件結果快 4 秒；代價是逐格動作晚到（玩家 96→170 秒），這段時間用靜態立繪頂著，換手不跳（見門檻 1）。
- 證據：`accept_c1\load\log_*.txt`、`shots\slowswitch_*`、`sheets\_慢網路換手.jpg`。

## 四、連線（中繼、兩組、一台限速）

| 組 | 限速的那台 | 事件投票 | 對帳 | 紅條 |
|---|---|---|---|---|
| 球球（主機）＋封封（加入） | 封封 1.6 Mbps | 兩台 1.2～1.7 秒進畫面；各看各的貓（球球那台 `event_rescue`、封封那台 `event_fengfeng_rescue`），主機投了之後加入方按鈕標「← 同伴」 | 11 次 0 次對不上 | 沒有 |
| 菲菲（主機）＋噹噹（加入） | 菲菲 1.6 Mbps | 同上，各看各的 | 9 次 0 次對不上 | 沒有 |

- 走完：事件→第一場（擋下、塞壞毛病、同伴出牌讓位）→貓窩→第一關關主→雙貓過關幻燈片→第二關地圖。
- 連線牌圖第一次抽到就是完整的兩貓混搭圖。
- 「同伴才用得到」：球球那台的「養氣葫蘆」掛牌子、封封那台同一件不掛；菲菲那台的「秤砣腰帶」掛、噹噹那台不掛。都照鎖角色的規則。
- 證據：`shots\coop3_*_int_full\`、`shots\coop3_*_int\`（過關三選一截圖）。

## 五、平衡（新種子，難度 1）

| 組 | 局數 | 新：平均樓層／第一關主／第二關主／破關 | 線上 | 樓差 | 只算內容（比 `d819b149`） |
|---|---|---|---|---|---|
| 球球 | 600 | 24.4／49%／56%／7.7% | 22.0／48%／44%／2.3% | +2.4±0.6 | +0.9±0.7 |
| 菲菲 | 600 | 27.9／70%／55%／9.2% | 24.8／69%／40%／3.0% | +3.1±0.6 | +0.9±0.7 |
| 噹噹 | 600 | 24.6／56%／44%／8.3% | 23.2／55%／39%／3.0% | +1.4±0.6 | −0.1±0.6 |
| 封封 | 600 | 22.7／47%／33%／3.0% | 20.8／43%／21%／0.3% | +1.9±0.5 | +0.2±0.6 |
| 球球＋菲菲 | 300 | 23.2／60%／54%／8.3% | 20.9／54%／43%／2.7% | +2.3±0.8 | +0.6±0.9 |
| 噹噹＋封封 | 300 | 19.4／38%／41%／3.0% | 18.2／30%／34%／1.0% | +1.2±0.7 | +0.0±0.7 |

- 跟線上比全部往上 1.2～3.1 層，但拆開看幾乎都是 `d819b149` 那次機器人改進（尺規＋忍具使用規則）造成的；只算內容擴充，六組都在誤差內。
- `95a2ab63` 用同一批種子重跑 3,000 局，**逐局結果跟 `46e077ef` 一模一樣**（兩筆修正沒動到機器人會走的戰鬥與分配）。
- 證據：`accept_c1\bal\cmp_*.json`、`int2_*.json`。

## 六、事件圖與新事件細節（46e077ef，分身實測）

### 限速 1.6 Mbps 載入取樣（菲菲，3F 調藥與 5F 秘笈，每 25 毫秒一格）
| 情境 | 新版 | 線上 |
|---|---|---|
| 剛開局馬上點格子→進事件畫面 | 0.85～0.90 秒（地圖下方寫「正在準備事件……」）；5F 0.02 秒 | 0.02 秒 |
| 同上→主圖完整 | 一進畫面就完整 | 1.4～1.6 秒，中間 0.25～0.9 秒整片淺色 |
| 在地圖停 30 秒再點→主圖完整 | 0.02 秒 | 3.4～8.4 秒，淺色到 8.35 秒 |
| 點選項（或挑完牌）→結果圖完整 | 馬上 | 2.2～8.0 秒 |

### 新事件實際玩（全部從地圖點進去）
- 第二關 5F「舊木箱」四隻：翻秘笈＝三選一、牌組 +1；帶走舊忍具＝忍具 +2；蓋回去＝沒效果。
- 第三關 5F「最後一頁」四隻：照著練＝牌組 +1；摺進衣襟＝升級兩張；收好不看＝回 10 血。
- 這兩篇沒有開場對白（只有第一關 5F 秘笈有）。噹噹、封封標題是「大俠貓的舊木箱」，球球、菲菲是「師父的舊木箱」。
- 球球藍頭巾：生命上限與目前血各 +6／回 18 血／沒效果。木靶：升級一張、扣 6 血／牌組多一張罕見牌／沒效果。
- 球球屋頂上的影子：「躲著看」刪一張牌並記標記；「追上去」記標記後開打，第二關鏡中球球 104 血、第三關 131 血；打贏獎勵畫面多「事件獎金 30 條小魚乾」，接著升級一張，實測有升級。
- 60 局單人第一關地圖抽樣：藍頭巾、木靶只出現在球球的地圖（9 次、7 次），其他三隻 0 次、各自只出現自己的專屬事件。

### 連線混搭（菲菲開房＋球球加入，貼碼直連，新舊各一次，走第一關 5F）
兩台各照自己的貓顯示圖與對白（菲菲「是師父親手寫的。」、球球「這是師父寫的喵。」），結果圖也各自一版；兩台帳本一致、沒有紅條；新舊兩版這一段完全相同。連線時角色專屬事件不排進地圖（`map.ts:309`），這局 8 個事件格也確實沒有。

## 七、問題清單

### 高
1. **（46e077ef 有，95a2ab63 已修、已驗）連線事件：加入方網路很慢時，主機換掉剛拿到的忍具，整局停掉。**
   重現：連線兩台，加入方限速 5 KB/s；主機身上忍具差一格滿；兩人在「師父的舊木箱」都選「帶走箱裡的舊忍具」；主機在跳出的換忍具視窗換掉最後一格。→ 加入方紅條「第 3 號整局動作在這邊做不出來」、主機紅條「對方離開了」。
   說明：我第一輪連線是 1.6 Mbps，結果圖來得及，所以沒撞到；這條是協調者的推前審查抓到的，我用同一支腳本在修前重現、修後確認沒了。

### 中
沒有。

### 低
1. **（46e077ef 有，95a2ab63 已修、已驗）地圖預抓事件畫面失敗後，走進事件格停在「畫面載入失敗」。** 重現見第十三節 4。
2. **帶不眠香爐打盹，起床台詞還是「睡飽了，出發喵」。** 重現：帶不眠香爐進貓窩按打盹。畫面下方寫「球球躺了一下，但沒有回血」，右上泡泡卻是「球球：睡飽了，出發喵。」（封封那張是「……沒那麼痠了」，也是有休息到的口氣）。見 `gate12\_不眠香爐.jpg`。
3. **限速時逐格動作比線上晚到**（四隻第一場玩家逐格：新 143～221 秒，線上 67～150 秒）。這是「先讓開局快、大圖晚點來」的取捨，期間用靜態立繪頂著、不空白、換手不跳；列出來讓使用者知道。
4. **限速時圖鑑全部圖到齊慢 1 秒**（5.2→6.3 秒），因為多了 28 件的圖；不限速差 0.06 秒。
5. **限速剛開局馬上走 5F 點「研讀秘笈」，三張牌的圖最慢 1.4 秒才出來**（線上 3.8 秒，已經比線上好；這是牌圖不是事件圖）。見 `gate4\_載入取樣_挑牌.jpg`。
6. **限速剛開局馬上點事件格，要在地圖上多等 0.85～0.90 秒才換過去**（線上 0.02 秒就換，但換過去主圖空白 1.4～1.6 秒）。等的時候地圖下方有「正在準備事件……」。取捨，列出來。

### 資訊（不是問題，記下來）
- 限速第一場最前面約 2 秒整個場景（背景加角色）是暗的，新舊兩版一樣；之後新版比線上早很多有東西看（噹噹 0.4 秒內、封封 2.2 秒內，線上 4～17 秒）。
- 加入方 5 KB/s 時，事件結果圖 6 秒到不了，用主圖頂著，結果畫面在投票後 8.7 秒才出來。照設計，但這麼慢的網路下等 8～9 秒。

## 八、主控台錯誤與 404
- 46e077ef 主驗收：單人四隻整局、新內容四隻、流暢度八段、連線兩組完整跑＋兩組過關三選一、載入 16 次、圖鑑 4 次：**主控台 0 錯誤、0 個 404、0 個請求失敗**。
- 事件圖分身（新舊兩版全部測試）：主控台錯誤與警告 0、頁面錯誤 0、400 以上 0、請求失敗 0。
- 95a2ab63 補驗：單人 8 次、連線 1 組：0 錯誤。
- 只有兩處有錯誤，都是故意製造的：修前連線重現（`[連線] 停下來了…（swap）`、`連線已經停了，「map」的選擇沒送出去`），以及斷網測試我自己攔掉的事件畫面檔。

## 九、跟線上比：變好／變差
| 變好 | 變差 |
|---|---|
| 限速開局→第一場手牌 31.5→11.5 秒 | 限速逐格動作晚到（67～150→143～221 秒） |
| 限速點選項→結果畫面 5.0→0.01 秒、點事件格→結果圖 7.0→2.7 秒 | 限速點事件格→事件畫面 0.01～0.03→0.72～0.90 秒（但一進去主圖就完整，整段到結果圖快 4 秒） |
| 限速事件主圖空白 1.4～8.4 秒→0 | |
| | 限速圖鑑慢 1 秒（多 28 件） |
| 限速第一場玩家那格空白取樣 150→0 | 46e077ef 不限速進事件畫面慢 0.05 秒（95a2ab63 已回到 0.013 秒） |
| 新秘寶 18、新忍具 10、5F 三版、球球三篇都能用 | 不眠香爐起床台詞沒跟著改（低） |
| 95a2ab63 事件畫面抓失敗會自己換網址重抓 | |
| 大小、貓窩、流暢度、舊事件圖：跟線上一模一樣 | |

## 十、沒測到的
- 劍穗結（封封那場手上沒有攻擊牌）。
- 連線走第三關 5F；連線大魔物戰利品二選一的「同伴才用得到」（這輪只驗了過關三選一與紙箱）。
- 「再試一次」那顆按鈕本身：95a2ab63 的自動重抓先成功了，按鈕沒出現過。Safari 那條「錯誤訊息沒網址」的退路沒測。
- 這輪連線只用中繼；貼碼直連只有事件圖分身跑過第一關 5F。手機、`?motion=0` 這輪沒重跑。
- 網速是 Chrome 模擬的，不是真的慢網路；模擬對 WebSocket 生效到什麼程度沒有另外確認。
- 第二、三關 5F 與球球三篇沒有在限速下走過（限速只走了第一關 5F、3F 調藥、江湖救急；連線第二關 5F 是加入方 5 KB/s）。
- 續集事件（例如「山賊再現」）只在除錯頁比對了圖，沒從地圖走到；屋頂影子第三關只測了「追上去」；它記下的「遇過影子」標記目前沒有任何事件會讀。

## 十一、用過的作弊（都在除錯模式 `?debug`，連線時兩台做一模一樣的改動）
- 開新局 `__app.newRun`；直接塞秘寶、忍具、小魚乾 700；第 1 層那場魔物血 ×4。
- 地圖跳格（改目前所在格與足跡）；把事件格換成「江湖救急」。
- 關主血 ×0.25、打兩輪後魔物血改 1；過關三選一前把塔主池大部分標成「看過」，逼出鎖角色那件。
- 補驗：主機忍具放 2 支、加入方清空；常見池塞給主機只留兩件；Chrome 網速模擬（1.6 Mbps、5 KB/s）；攔截事件畫面檔模擬斷網。
- 平衡：機器人直接跑引擎，沒有作弊。

## 十二、收尾
- 我開的四個伺服器（5301、5302、5303、5304）先對過命令列再關掉，連同殘留的子行程共 8 個，四個埠都已經沒人在聽。另外兩個 qiuqiu-coop 的伺服器（5192、5193，綁 127.0.0.1）不是我開的，沒動。
- 帶我暫存設定檔的 Chrome 0 個；`%TEMP%\qq-accept-profiles` 已刪。暫存區兩個指向倉庫 `node_modules` 的連結已拆（只拆連結，兩邊的 `node_modules` 都還在）。
- 兩個倉庫 0 個已追蹤檔被改、沒有提交。qiuqiu-coop 裡的未追蹤檔是 09-20～09-23 00:05 就在的，不是我的。
- 寫報告時 qq-cint 的 HEAD 又變成 `1ab83fc8`（第二批事件圖約 240 張＋生圖工具，`src` 沒動、圖還沒登記）。**這一版我沒驗**，本報告的結論只到 `95a2ab63`。

## 十三、補驗 95a2ab63（協調者追加，最後一節）

打包：`git archive 95a2ab63`→暫存區 `accept_c1\int2_src`，`SITE_NAME=qiuqiu-tower-coop` 正式打包，5304 埠。修前對照用 5301（`46e077ef`）跑同一支腳本。

### 1. 單人四隻 × 兩種網速，各走兩個事件（江湖救急＋第一關 5F 秘笈）
16 個事件全部：主圖、結果圖都是**本角色那一版**；結果畫面出來之後插圖空著的取樣 0；主控台 0 錯誤。5F 標題：球球、菲菲「師父留下的秘笈」，噹噹、封封「大俠貓留下的秘笈」。

| 江湖救急選「收下小魚乾」 | 點下去→小魚乾變多 | 點下去→結果畫面 |
|---|---|---|
| 95a2ab63 不限速（四隻） | 0.007～0.008 秒 | 0.007～0.008 秒 |
| 95a2ab63 限速 1.6 Mbps（四隻） | **0.004～0.006 秒** | 0.93～1.95 秒（等結果圖） |
| 46e077ef 限速（同一支腳本） | 0.86～1.97 秒 | 0.86～1.97 秒 |

修正照說的做：效果當場套上，只有畫面等結果圖。進事件畫面 95a2ab63 是 0.013 秒（46e077ef 0.062 秒）。
- 證據：`sheets\_補驗_單人事件.jpg`（看過；第一欄「主圖」是換場淡入那 0.2 秒拍到的，底下還看得到地圖，腳本記下的圖名是對的）、`shots\ev2_*\result.json`。

### 2. 連線：第二關 5F 帶走忍具，主機換掉剛拿到的那支（加入方限速）
球球開房（不限速）＋封封加入（1.6 Mbps，走到第二關 4F 時再降到 **5 KB/s**），中繼連線。兩台一樣先把主機身上放 2 支忍具（帶得下 3 支）。
- 兩台都投「帶走箱裡的舊忍具」；投票結算那一刻，加入方的結果圖還沒下載（資源清單裡查不到），條件跟審查重現的一樣。
- 主機：結果圖 `daxia_chest_r1`；拿到「順手牽羊爪」放第 3 格，第二支「亂石包」收不下，跳出「忍具帶滿了。要用「亂石包」換掉哪一支？」，投票後 1.25 秒換掉第 3 格（剛拿到的那支）。
- 加入方：投票後 8.7 秒出結果畫面；結果圖 6 秒內沒到，照設計用本角色主圖頂著，文字是封封版。
- **對帳 3 次（結算＋換完、再等 4 秒、回地圖）全部一致**：兩台都記得主機是「鐵布衫油＋替身人偶＋亂石包」。兩台都沒有紅條，主控台 0 錯誤。
- **修前 46e077ef 同一支腳本**：加入方紅條「連線出問題：第 3 號整局動作在這邊做不出來」，主機紅條「對方離開了」；主控台 `[連線] 停下來了： 第 3 號整局動作在這邊做不出來（swap）`；帳本分岔（主機那台記「對半包子」，加入方那台還是「順手牽羊爪」），之後連紙箱都走不進去。
- 證據：`sheets\_補驗_連線.jpg`（看過，末列是修前紅條）、`shots\coop5f_ninja_fengfeng_{int2,int}\`。

### 3. 連線紙箱的「同伴才用得到」
兩台一樣把常見池除了「磨劍石（只給封封）」「斗笠」都塞給主機，紙箱只剩這兩件。
- 球球那台：磨劍石掛紅色小牌子「同伴才用得到」；封封那台：同一件沒牌子。兩件圖都有。
- 主機挑斗笠、加入方挑磨劍石，泡泡寫「你拿到「斗笠」，同伴拿到「磨劍石」」；對帳 2 次一致。

### 4. 地圖預抓時斷網、恢復後走進事件格
用攔截讓事件畫面那支檔案在地圖上那 8 秒抓失敗，之後放行再走進事件格。
- 95a2ab63：0.013 秒進得去（換了 `?retry=3` 的網址重抓成功）。
- 46e077ef：停在「畫面載入失敗，請重新整理再試。」，只有「重新整理」一顆（連線時等於整局結束）。
- 證據：`shots\retry_{int2,int}\`。

### 5. 平衡
同一批 3,000 局逐局一模一樣（見第五節）。
