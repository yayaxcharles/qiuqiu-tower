# b3qmark：問號格變化（伏擊、行腳商、路邊紙箱）

worktree `F:\ClaudeWork\qq-b3qmark`，分支 `c0923-b3qmark`，從 `c0923-b3base`（`25952e4d`）開出，一筆提交 `a7a77800`。
依據：設計稿 `design3_大結構.md` 第三節、主控裁決第 3、5 條、`batch3_art_keys.md`（`q_ambush`／`q_merchant`／`q_roadbox`、`shop/merchant*`）。

驗收：`npx tsc --noEmit -p .` 零錯誤；`npx vitest run` 339 個檔案、3152 條全過（9 條原本就略過），**沒有 Errors 那一行**。
跑完照雷 5 收拾：`docs/怪物工作檯.json`、`牌池總表.json`、`事件文案.md` 只差日期與換行，已還原；`docs/分關載入.json` 是真的重生，一起提交（見第八節）。

---

## 一、擲法（引擎）

- **新模組 `src/engine/qmark.ts`**：常數（5%／+5%／20%、權重 40／35／25、伏擊 20 條與 6 點、`QMARK_ART` 三個圖編號）、`qmarkChance`、`qmarkProtected`、`ambushAllowed`、`rollQmark`、`ambushOutcomes`。
  - `rollQmark`（99 行）用分支亂數 `seed|q|關|格子|累積`，**不推整局 `run.rng`**；伏擊那一組用 `seed|ambush|關|格子`，從這一層的一般戰鬥池挑（不會是大魔物、不抽修飾詞）。
  - 不擲、照樣 +1 的格子：5F（`run.ts:106`）、剛換成待出的後集（`run.ts:154、162` 的 `swapped`）、本來就是後集／鏈（`requiresFlag`）或稀有事件（**看事件定義上的 `rare` 欄位**，不看代號清單，`qmark.ts:48`）。
  - 變了的那一格原本那篇**不記「遇過」**（`run.ts:162` 變了就 `return`，不寫 `event:`）。
  - 第一關 3F 以前、或任一位帶 `qmarkNoAmbush`（平安繩）不出伏擊；`qmarkEvery`（探路杖）每一位各數各的，到點那一格直接指定行腳商或路邊紙箱（各一半）。這兩個是照設計稿的掛鉤名讀（見第十節）。
  - 格子上已經有結果的（除錯工具先標的）不重擲，只補伏擊那一組——實機驗收的連線那一段就是靠這個。
- `types.ts`：`MapNode.variant?: '伏擊'|'行腳商'|'路邊紙箱'`、`RunState.qmark?: number`。
- **行腳商**（`run.ts`）：`makeMerchant`（1075 行）／`makeMerchants`（1108）——牌 1 罕見＋1 稀有（照這一位的職業）、秘寶 1（常見 60%／大魔物 40%，抽不到換另一池）、忍具 2（罕見以上，照忍具權重 27：8）；`ShopStock.merchant`、`shopClosed`（879）；`priceFor` 行腳商一律「定價 × 0.7 × 難度倍率」（1130），**零錢罐、貪吃錢袋、銅臭錢袋、帳本、批發箱都不算，也不收欠條**（主控裁決第 5 條）；買一樣就收攤（`buyCard／buyRelic／buyPotion` 開頭擋）；不能重整（943）。
- **路邊紙箱**（`run.ts:846、850`）：常見 75%／大魔物 25%，開法直接交給 `openChest(run, seat, pools)`／`openChestCoop(run, pools)`（兩支加了 `pools` 參數，預設不變）。
- **連線**：`net/runaction.ts` 收攤了不放行買東西、行腳商不放行放生與重整（不擋的話會發號碼、套用失敗、整場斷線）；`net/hash.ts:135-136` 收了累積數與每一格的變化（伏擊連同那一組）。
- **存檔**（`save.ts:217-224`）：舊檔沒有 `qmark`＝0、沒有 `variant`＝原本那篇；壞掉的只丟那一欄，伏擊沒有遭遇就當原本的事件。
- **機器人**：`smartbot.ts` 的 `smartQmark`（伏擊照 `eventValue` 比兩條路：血少於五成跑、血多打）、`shopAtMerchant`（秘寶 ≥6 買得起就買、不然牌 ≥7、不然忍具少於 2 支買便宜的）、紙箱照開；`coopbot.ts:290` 的 `coopQmark`（伏擊兩人估值加總、行腳商各買各的、紙箱兩件各挑）；`bot.ts:108-110`（亂打的也走得過）。`nodeScore` 的事件格照舊 50。

## 二、畫面

- `app.ts:427` 走格子先分流、`enterQmark`（501）：行腳商的攤子當場抽、先掛上會話；拆地圖的投票處理；舞台點不動、等文字模組＋這一種的揭曉圖（伏擊還等事件畫面那一塊），最多 10 秒；換過去那一拍播音效＋揭曉。原本 `case '事件': this.enterEvent(...)` 那一行一個字沒動（有測試守）。
- **揭曉一拍**（`ui/qmark.ts` 的 `playQmarkReveal`＋`screens.css` 最後一段）：畫面中央一張問號卡 0.28 秒翻面、0.12 秒淡掉（合計 0.4 秒），翻成交叉的爪（伏擊）／行腳商本人／紙箱；上方橫幅照設計稿三句，留 1.9 秒後淡掉；整塊 `pointer-events: none`，翻的時候也點得到底下的選項。掛在疊層，連線投票的安靜重畫不會把它拔掉。
- **伏擊**走事件畫面：`enterQmark` 組一篇 `EventDef`（`ui/qmark.ts` 的 `ambushEvent`）以 `props.qmark` 帶進去，事件畫面只改兩行（`event.ts:173-174`、`511`：這一篇的本文已經是這一位的版本，不再過 `eventTextFor`）；投票、結果、「開打」照一般事件走。
- **行腳商**沿用罐頭鋪畫面（`shop.ts`）：先一段開頭（揭曉圖＋這一位的話、「看看貨架（只能挑一樣）」）→ 貨架；立繪用 `shop/merchant`、`_happy`、`_no`（照對照表那條退路規則）；對白換成行腳商那幾句（進來／成交／錢不夠／沒買就走）；只有「走了」一顆（連線是「逛好了」，兩人都按才上樓）；買了一樣之後那格寫「買下了」、其他寫「收攤了」；劃掉的原價只乘難度。
- **路邊紙箱**沿用紙箱畫面（`chest.ts`）：第一段的圖換成揭曉圖、對白框是這一位的開頭；點開之後跟 8F 紙箱一樣，開箱那句 `chestLines` 改在點開之後講。
- **地圖**（`map.ts`）：每個還沒走過的問號格掛說明（**寫出目前機率**，主控裁決第 3 條），例：「通常是事件，偶爾會是伏擊、行腳商或路邊紙箱。現在走進問號格，約 20% 會變（每遇到一次一般事件多 5%、最多 20%，變過就回到 5%）。」第一關 3F 以前或有人帶平安繩時多一句「這一格不會是伏擊」；5F 寫「固定的事件，不會變」。**不寫哪一格是後集／稀有**（會劇透）。走過、變過的格子畫成實際的圖示（戰鬥／罐頭鋪／紙箱）＋左上角小問號。
- 文字：`src/content/qmark-text.ts`（四隻各一份整段＋行腳商七句，照設計稿 3-6 逐字）；按需載入，打包成獨立的一塊 4.54 KB。系統口吻的字（橫幅、兩個選項標籤、說明）在 `ui/qmark.ts`，不指名角色、不加喵。

## 三、圖接上、改成用到才下載

- 接上：三種揭曉圖四隻各一份（鍵走 `eventArtKey`：`bg/event_q_*`、`bg/event_<角色>_q_*`）、行腳商三張立繪。`BATCH3_PENDING` 拿掉 `q_ambush`、`q_merchant`、`q_roadbox`；孤兒檢查改從引擎的 `QMARK_ART` 認這三個編號（`tools/manifest_hygiene.test.ts`）。
- 延後：`bgacts.ts:96` 的 `qmarkMainKeys()` 併進 `deferredBgKeys()`（開場、選角補載都跳過，他版照現成規則換回共用鍵比對）；`assets.ts:138` 的 `MERCHANT_SPRITES` 開場跳過；`tools/dump_monster_acts.test.ts` 把這 15 張寫成 0，並加一條「一張都不准算首載」。
- 預抓：地圖畫面出來、這張地圖還有會變的問號格才抓文字模組與本機這一位的三張揭曉圖＋行腳商三張（`preload.ts:274` 的 `preloadQmarkArt`，**不插隊**，一局平均遇不到一次，不跟這張地圖的事件主圖搶）；走進去時插隊再要一次、最多等 6 秒（`warmQmarkArt`，伏擊與行腳商連事件畫面底圖一起）。

## 四、首載量測（`SITE_NAME=qiuqiu-tower-coop npm run build` → `python tools/check_size.py`）

| 類別 | 起點（本分支拿掉 src 改動、同一份重生過的分關清單） | 本分支 | 上限 |
|---|---|---|---|
| 首載程式 | 631.2 KB | 639.9 KB（+8.7） | 680 KB |
| 樣式 | 114.3 KB | 115.7 KB（+1.4） | 120 KB |
| 圖片 | 9.65＋0.22＝約 9.87 MB（見下） | 9.65 MB | 10.60 MB |
| 首載總計 | 約 10.62 MB | 10.40 MB | 11.30 MB |

- 我這條線從首載拿掉 **219 KB**：球球版揭曉圖 3 張 109 KB＋行腳商立繪 3 張 110 KB（他版 9 張本來就不算首載，但原本選角時會一起抓，現在也延後）。
- **要提醒主控**：派工單寫的「11.15／10.60 MB」是拿**起點提交裡沒重生的 `docs/分關載入.json`** 量的——那份少了第三批 94 張角色專屬圖的 0，所以多算了約 1.3 MB。跑一次全套測試（或 `npx vitest run tools/dump_monster_acts.test.ts`）重生之後，起點本身就是約 9.87 MB。我提交的那份分關清單含這 94 筆＋我的 15 筆；另外三條線也會重生出同樣那 94 筆（內容相同，合併時不衝突）。
- 樣式離 120 KB 剩 4.3 KB：四條線合起來很可能超過，主控裁決第 8 條准調到 130 KB（我沒動 `check_size.py`）。

## 五、一局平均遇到幾次（照 `scratchpad\content\sim.ts` 的做法）

腳本 `scratchpad\b3qmark\sim_qmark.ts`（esbuild 打包後跑真的 `smartRun`，用 `withSmartProbe` 記帳），四隻各 600 局、難度 1、兩組種子：

| | 會擲的問號格 | 變化 | 伏擊 | 行腳商 | 紙箱 | 0 次／1 次／2 次以上 |
|---|---|---|---|---|---|---|
| 設計稿估（機器人平均一局 4 格） | 4 | 0.45 | 0.18 | — | — | 58%／38%／3% |
| 實測全部 2400 局（A／B 兩組） | 3.63／3.62 | 0.49／0.52 | 0.18／0.20 | 0.18／0.18 | 0.13／0.13 | 60%／32%／8%、59%／32%／9% |
| 設計稿估（完整一局 7 格） | 7 | 0.92 | 0.37 | — | — | 30%／51%／20% |
| 實測打到塔頂關主的 298 局（A／B） | 6.90／6.94 | 0.93／1.06 | 0.33／0.40 | 0.33／0.35 | 0.28／0.32 | 35%／42%／23%、31%／39%／30% |

- 跟設計稿的精算對得上；「2 次以上」比精算多一點，因為 5F 與後集那幾格不擲但累積照樣 +1，後面的格子機率較高。
- 伏擊 444／483 次：機器人迎戰 85%／83%，**死在伏擊 0／4 次**（兩組合計 927 次裡 4 次）。
- 對照同種子的起點程式（沒有問號格變化）：通關 169→160、177→160（2400 局，−0.4～−0.7 個百分點）；打到第三關 444→431、456→419。兩組都略降，但單組差距在一個標準差左右（約 ±11 局），**還不能說有影響**；要定案得等第三批合完的 600 局量尺。原始數字在 `scratchpad\b3qmark\sim_*_600*.json`／`.txt`。

## 六、實機（Playwright，`vite preview --port 5411`，每個 context 全新暫存設定檔，只開本機）

- 除錯手段：先在 Node 端用同一份引擎算出「站在某格前、累積 3（20%）時**自然會擲出**某一種」的種子與格子（`scratchpad\b3qmark\seeds.json`），瀏覽器裡開同一顆種子、`run.qmark = 3`、站到那一格前面、真的點下去——走的是正式的擲法，不是硬塞。
- **單人四隻各三種**（`pw\solo.js`）：球球 46 項、菲菲 44、噹噹 46、封封 44，全過、主控台與網路 0 錯誤。每一種比對：揭曉一拍出現、畫面種類、**圖是這一隻自己的版本而且載好了**、名牌、開頭逐字等於文字表、按鈕；行腳商貨架 2＋3、立繪是行腳商、只有「走了」、買一樣其餘收攤、成交那句、扣錢；路邊紙箱點開後冒自己那句開箱台詞、秘寶 +1；伏擊兩個選項、那一組跟 Node 端算的一樣（兩邊同一套分支亂數）、球球與噹噹走「迎戰」進到那一場並多拿 20 條、菲菲與封封走「甩掉牠們」扣 6 點；回地圖那一格換成實際圖示＋小問號、有存檔；地圖說明寫「約 20% 會變」；行腳商與橘貓老闆的立繪顯示框一樣大（210×266）。
- **揭曉膠卷**：暫停動畫、逐格設時間（0、70、140、210、280、340、400、800、1500 毫秒）拍，自己用 Read 看過。**抓到一個 bug 當場修了**：第一版把淡出寫在翻面那一層，瀏覽器因此把卡片壓成 2D，翻過去只看到**鏡像的問號、背面永遠翻不出來**；改成外層淡出、內層翻面後，四隻三種都是「問號→交叉的爪／行腳商／紙箱」、0.4 秒淡掉、底下直接是那一種的畫面，沒有閃白、沒有露出舊畫面。補了一條測試守「翻面那一層不准動不透明度」。
- **連線一組**（`pw\coop.js`，開房球球＋加入菲菲，貼碼直連）：行腳商、路邊紙箱、伏擊各遇一次，25 項全過、0 錯誤、沒有斷線橫幅。兩台都演揭曉、各看各的圖與開頭；行腳商各買各的一樣、一人按好時不上樓、兩人都按才一起走；紙箱兩台都開出兩件、各挑一件；伏擊兩台那一組一樣、投「甩掉牠們」各看自己的結果；每一段結束兩台整局帳（亂數、累積、各格變化、兩人家當）逐字一樣。上一輪截圖看到開房那台的對白框很淡，查了是既有的對白框進場動畫第 39 毫秒被拍到，1.2 秒後重拍正常。
- 截圖：`scratchpad\b3qmark\shots\`（`solo_<角色>\`、`coop_ninja_feifei\`；聯絡表 `ninja_screens.png`、`feifei_screens.png`、`dangdang_screens.png`、`fengfeng_screens.png`、`coop_screens.png`；膠卷 `ninja_*_crop.png`、`others_reveal.png`；地圖回填放大 `ninja_map_zoom.png`）。
- 四道門檻：大小——行腳商立繪跟橘貓老闆同一個顯示框，頭寬是美術那邊對到 153 的；貓窩——沒動；流暢——見膠卷；事件圖——十二張都是那一隻自己的版本、圖文對得上（球球炸毛跳起配「原來是陷阱喵」、菲菲背貼牆、噹噹架護臂、封封按劍；菲菲躲欄杆探頭、噹噹指鬆掉的繩子；菲菲敲箱子、噹噹看箱底、封封扶箱蓋），沒有改到任何既有的圖。

## 七、測試與「會反向變紅」

新增 `tests/engine/qmark_0923.test.ts`（24 條）、`tests/ui/qmark_ui_0923.test.ts`（12 條）、`tests/content/qmark_text_0923.test.ts`（4 條）；`tools/dump_monster_acts.test.ts` 加一條；`tools/manifest_hygiene.test.ts` 改名單來源。
`tests/engine/event_variety_0914.test.ts` 改一行：那條假設「走過的事件格一定記遇過」，現在變掉的那一格照設計稿不記，所以只收沒變的那幾格。

用 `scratchpad\b3qmark\redcheck.py` 一條一條改回壞寫法、跑對應測試、確認紅、逐位元組還原，**20 條全部紅過**：
稀有事件不擲／鏈與後集不擲／換成待出後集那一格不擲（第一次沒紅——原本的測試挑到的後集都有 `requiresFlag`，被另一條擋掉了；補了「球球的屋頂上的影子」那條才紅）／變了不記遇過／5F +1／第一關 3F 無伏擊／平安繩看任一位／變了歸零／行腳商不吃罐頭鋪秘寶／只做一筆生意（引擎、連線各一）／行腳商沒有放生／壞累積只丟那一欄／指紋收變化／紙箱 75%／揭曉圖開場不載／分關清單兩圈／翻面層不動不透明度。

## 八、沒做的與原因

- **平安繩「每走進一個問號格回 5 點」（`qmarkHeal`）、箱中箱多給一件（`chestExtra`）沒做**：那是新秘寶 9 件的效果，歸 `b3rare`；跟變化本身無關，我做了會跟他們重複。路邊紙箱的開法直接走 `openChest`／`openChestCoop`，他們把箱中箱接在那兩支裡的話路邊紙箱自動吃得到。
- **地圖圖例**：遊戲裡沒有圖例，我改成每個問號格滑上去的說明（主控裁決第 3 條那句寫在這裡）。
- **音效**：沒有「門鈴」「紙箱」專用音，沿用戰鬥開始那聲（伏擊）、買東西那聲（行腳商）、翻紙那聲（紙箱）。
- `docs/事件文案.md` 沒收問號格的文字（那份照事件表產生，這三種不是事件）。
- 沒有推、沒有合併。

## 九、跟設計稿不一樣的地方（都是小處，照實寫）

1. 變過的格子小問號掛**左上角**（設計稿寫右上角，但右上角是打過的勾勾，疊在一起看不清）。
2. 行腳商的攤子底圖用**事件畫面那張走廊**，不是罐頭鋪店內（他是在樓梯轉角擺攤）。
3. 行腳商先一段開頭、按「看看貨架」才攤開（多按一下；揭曉圖與開頭那段話要有地方放，貨架那一頁放不下）。
4. 探路杖到點那一格照設計稿 3-1 的列法「照樣算一次正常事件」，累積 +1、不歸零（3-4 另一句說變了就歸零，兩句互相矛盾，我照清單那句）。

## 十、需要主控決定的事

1. **三個秘寶掛鉤跟 `b3rare` 的分工**：平安繩的「不出伏擊」（`qmarkNoAmbush`）、探路杖的計數與指定（`qmarkEvery`）我在 `engine/qmark.ts` 裡照設計稿的**掛鉤名**讀（用寬鬆型別讀，沒改 `RelicDef.hooks` 的型別，免得跟他們同一段衝突）。合併時請確認 b3rare 那兩件秘寶的 `hooks` 就叫 `qmarkNoAmbush: true`、`qmarkEvery: 3`，而且**他們沒有在別處再數一次探路杖**（不然會數兩次）；合完可以把那個寬鬆讀法換成正式型別。平安繩回血、箱中箱由他們接。
2. **稀有事件的旗標名**：我看的是事件定義上的 `rare`（設計稿新L `EventDef.rare?: { weight }`）。b3rare 若改用別的名字，`qmarkProtected` 那一行要跟著改（有測試用假事件守著，改錯會紅）。
3. **`enterEvent` 會三方衝突**：我在後集那段加了 `swapped` 變數與一行 `rollQmark`；b3rare 也要在同一支加「稀有事件不換後集」。衝突不大但一定會有，合的時候兩邊都要留。`shop.ts` 的立繪那兩行跟 `b3shop` 的店主輪替同一處（我只把鍵名的前綴抽成 `base`），`hash.ts`、`save.ts` 也各會有一兩行相鄰的新增。
4. 樣式 115.7／120 KB，四條合完多半要照裁決第 8 條調到 130。
5. 平衡：通關兩組都略降 0.4～0.7 個百分點（在雜訊邊緣），交給合完之後的 600 局量尺判斷；若真的偏難，最直接的是把伏擊權重 40 降一點，或機器人迎戰的血量門檻調高。

## 提交

```
a7a77800 內容擴充第三批（b3qmark）：問號格變化——伏擊、行腳商、路邊紙箱
```
（`git log --oneline c0923-b3base..HEAD`，只有這一筆）
