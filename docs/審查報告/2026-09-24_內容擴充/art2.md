# 美術代理 art2 報告：內容擴充第二批的秘寶、忍具圖示（2026-09-23）

worktree `F:\ClaudeWork\qq-art2`，分支 `c0923-art2`（從 `c0923-int` `52b847f3` 開出）。
事件圖這一輪照指示沒做；第三批「秘寶補到約 120」沒做。秘寶、忍具的資料（`relics.ts`、`potions.ts`）一行都沒動。

## 一、成果

| 類別 | 張數 | 生圖次數 | 重生 | 大小 |
|---|---|---|---|---|
| 秘寶圖示（計數型 5、角色新掛鉤 5、罐頭鋪限定 3、事件限定 3） | 16 | 18 | 1（線香） | 22 張合計 140.5 KB，平均 6.4 KB（3.9～8.9 KB） |
| 忍具圖示 | 6 | 6 | 0 | （含在上面） |
| 飛行物（迷魂香 `daze_incense`，112×89） | 1 | 2 | 0（兩張挑一） | 4.6 KB，按需動作，不算首載 |

- 師門套組三件裡只有「師父的舊木劍」沒圖（斗笠、酒葫蘆早就有），它本來就在事件限定 3 件裡，所以總數是 16 不是 17。
- **代號是我取的**（提案第二批表沒給），對照表：`scratchpad\content\batch2_icon_keys.md`（代號、名稱、鍵名、檔名、畫了什麼、接線注意事項）。
- 做法同第一批：gpt-image-1.5 真透明（codex-oauth），每張附一張「三張同類既有圖示拼成的畫風參考」（除了師父的舊木劍參考師門套組另兩件，每張都放一張第一批的新圖示）；128×128、長邊貼滿、WebP quality 80。生圖原檔在 `tools/motion-art-source/c2/`（`.gitignore` 擋 png），提示詞與選定紀錄 json 進版控。工具 `tools/gen_content_batch2_art.py`，裁切、閘門、清單寫法、聯絡表都直接借第一批那支。
- **計數型 5 件**（木人樁、沙漏、線香、暗器匣、撲滿）：右下角 44×44（128 像素座標）完全透明，圖上沒有字，數字由程式疊。挑圖時用程式找「不碰那一角的最大縮放、最靠中間的位置」，存成 webp 後再驗一次那一角 alpha ≤ 8，沒過就不收。
- **重生 1 張**：線香第一次畫成「細香插在小蓮花座」，32 像素時只剩一條紅線、認不出來 → 改成「青瓷香碗占下半、一根明顯比線粗的香」，第二次採用。
- **看圖**（自己用 Read 逐張看過）：畫風跟新舊圖示一致、單一物件、沒有字（會員卡、欠條、替換符、五毒譜封面、八卦鏡的卦象都放大確認過是圖或橫槓，不是字）、沒有多餘的手或角色、透明邊乾淨；跟相似的舊圖示分得開（木人樁 vs 木樁、撲滿 vs 零錢罐、算盤 vs 算盤珠、便當 vs 分食便當、照妖鏡 vs 銅鏡／鏡片、回魂香／迷魂香／線香 vs 先手香）。

聯絡表（都在 `scratchpad\content\art2\sheets\`）：
- `c2_relic_500.png`、`c2_potion_500.png`：500 像素、透明處鋪格紋。
- `c2_relic_mix_1x.png`、`c2_potion_mix_1x.png`：新舊一張隔一張、**實際遊戲大小**（上半狀態列 32 像素、下半戰鬥忍具欄 42 像素，嵌板照 `components.css`；黃條＝新圖，舊的有第一批也有更早的）；`*_3x.png` 是放大三倍看細節用。
- `c2_counter_1x.png`、`_3x.png`：計數型五件疊上假數字（白字黑邊）看角落夠不夠。
- `c2_projectile_500.png`：迷魂香跟貓薄荷球、煙霧彈、火雷珠並排。

## 二、首載：選哪一條、為什麼

**選「整組秘寶忍具圖示改成進入一局後才載」**（`codex/relic_*`、`codex/potion_*`，新舊全部 162 張）。

- 理由：第二批 22 張裡，計數型、罐頭鋪限定、事件限定這 11 件跟角色無關，照舊劍穗那套在 `heroOfKey` 特例化救不到它們，照樣吃首載；第三批還要再補。整組延後一次讓出約 0.55 MB，以後再加圖示也不必每批處理。
- 時機：進入一局那一刻由 `preloadHeroArt` 補（`adoptRun` 叫它，新的一局、續玩、連線開局三個入口都走那裡），排在角色專屬圖後面。
- 取捨：「續玩」直接進地圖時，狀態列那幾件秘寶是那一刻自己的 `<img>` 去要（每張 4～9 KB），沒有預先解好；標題直接開圖鑑是現抓。兩者一般網路下都看不出來，慢網路實測見第四段（跟改之前一樣快）。

改的地方：
- `src/ui/assets.ts`：新增 `isItemIcon`、`itemIconUrls`；`preloadArt` 跳過秘寶忍具圖示（一行）。
- `src/ui/preload.ts`：`preloadHeroArt` 把 `itemIconUrls()` 接在角色專屬圖後面（用 Set 去重，封封的舊劍穗兩邊都會列到）。
- `tools/dump_monster_acts.test.ts`：分關載入表把秘寶忍具圖示記 0；加一條「一張都不准算首載」；`docs/分關載入.json` 重生（多 161 筆，全部是 0）。
- `tools/check_size.py`：只補檔頭說明一段（預算數字沒動）。

## 三、測試與反向變紅
- `tests/ui/preload_hero.test.ts` 新增「秘寶與忍具圖示開場不載、進入一局才補；狀態、介面那些 `icon/` 圖示照舊開場就載」（假 `Image` 記下要了哪些網址）。
  - 把 `preloadArt` 的跳過那行拿掉 → 紅（開場多要了圖示）；把 `preloadHeroArt` 的 `itemIconUrls()` 拿掉 → 紅（進一局沒補）。改回來綠。
- `tools/dump_monster_acts.test.ts`：把記 0 那一圈拿掉 → 紅，列出 139 筆留在首載；改回來綠（那次跑完的分關載入表已重生回正確內容，差異確認為零）。
- `npx tsc --noEmit -p .` 0 錯誤；`npx vitest run` 311 個檔、2699 條通過、6 條跳過。照雷 5 收拾：`怪物工作檯.json` 只差日期、`事件文案.md`／`牌池總表.json` 只差換行，都還原。

## 四、實測數字

`npm run build && python tools/check_size.py`：

| 項目 | 開分支時（`52b847f3`） | 現在（`4525e97a`） |
|---|---|---|
| 首載圖片 | 9.57 / 10.60 MB（90.3%） | **9.04 / 10.60 MB（85.2%）** |
| 首載總計 | 10.24 / 11.30 MB（90.6%） | **9.71 / 11.30 MB（85.9%）** |
| 首載程式 | 557.4 KB | 557.6 KB（多兩個小函式） |
| 分關載入 | 55.21 MB | 55.89 MB（舊圖示 547 KB＋新 140 KB 移進來） |

所以第二批不但沒增加首載，首載總計還少了 0.53 MB，剩約 1.6 MB 餘裕。（派工單說只剩約 50 KB，那是第一批那個分支的數字；第〇批合進來之後開分支時實測就已經是 10.24 MB。）

實機（Playwright，`channel:'chrome'`、每個情境全新暫存設定檔、只開本機 5361、正式網站沒碰；結果 json 與截圖在 `scratchpad\content\art2\pw\out\`）：

| 情境 | 結果 |
|---|---|
| 打包版，標題等開場預載跑完 | 要了 437 張圖，**秘寶忍具圖示 0 張** |
| 接著開新的一局（`newRun`） | 261 毫秒內把清單上 162 張秘寶忍具圖示全部要了 |
| 打包版，預載跑完後在標題開圖鑑 | 112 列、112 張圖 **378 毫秒全部載好**，破圖 0、退回文字牌 0 |
| 打包版，慢網路（約 1.6 Mbps）標題一出來就開圖鑑（開場幾百張還在排隊） | 112 張 **5.4 秒全部載好**，破圖 0、文字牌 0；前 2 秒是空框。**改之前同一個情境也是 5.4 秒**（另外打包一份拿掉延後的版本對照過），因為原本圖示也排在立繪後面，沒有變差 |
| 開發伺服器，把第一批 28、第二批 22 件塞進圖鑑（資料還沒接線，只在頁面裡塞進同一個模組的陣列、價錢 1～N 排到各區最前面） | 162 列全部載好，**兩批 50 張都有圖、沒有空白、沒有文字牌**；截圖 `dev-inject_sec1.png`（常見區）、`dev-inject_sec4.png`（忍具）等 |

## 五、沒做的、跟派工單不一樣的
- 事件圖、第三批：照指示沒做。
- 迷魂香做了飛行物、照妖鏡沒做：迷魂香是單體丟向魔物的狀態忍具（同貓薄荷球、定身釘），照妖鏡是舉起來照、不是丟的。**要不要真的走「丟」由接線的人定**，建議的 `PROJECTILE_LOOKS` 數字寫在對照表。
- 開發伺服器會重寫 `node_modules/.vite` 的相依快取（那是連到主資料夾的共用快取，vitest 平常也寫那裡），沒動其他東西。
- 服務行程都是我自己開的（5361 的 `vite preview`／`vite`），看過命令列才關，現在 5361 沒有東西在聽。

## 六、需要主控決定
1. **代號**：16 件秘寶、6 支忍具的代號是我取的（對照表第二、三、四段），要改名請在程式接線前講。
2. **圖鑑缺兩個新池的分區**：`itemcompendium.ts` 的 `RELIC_POOLS` 沒有罐頭鋪限定、事件限定，這 6 件接線後不會出現在圖鑑裡，要請程式代理補（對照表最後一段）。
3. 圖示整組延後（第二段）這個做法可不可以；不行的話退回去只要還原 `76c8d222` 一筆，但第二批 140 KB 會回到首載。

## 七、`git log --oneline c0923-int..HEAD`
```
4525e97a 美術 c2（內容擴充第二批）：迷魂香飛行物
76c8d222 內容擴充第二批：秘寶與忍具圖示整組改成進入一局才補（不吃首載）
6407a7ad 美術 c2（內容擴充第二批）：秘寶圖示 16 張、忍具圖示 6 張＋生圖工具
```
（`coop..HEAD` 另外還有開分支前就在 `c0923-int` 上的第〇批、第一批 8 筆，不是我的。）
