# 美術代理 art1 報告：內容擴充第一批的圖（2026-09-23）

worktree `F:\ClaudeWork\qq-art1`，分支 `c0923-art1`（從 coop `2b8c3c7f` 開出）。只放圖、登記素材清單、改了三支測試與兩支生圖工具；遊戲程式與內容資料（`relics.ts`、`potions.ts`、`events.ts`、`src/ui/**`）一行都沒動。

## 一、成果

| 類別 | 張數 | 生圖次數 | 重生 | 聯絡表（自己用 Read 逐張看過） |
|---|---|---|---|---|
| 秘寶圖示 | 18 | 18 | 0 | `scratchpad\content\art1\sheets\c1_relic_500.png` |
| 忍具圖示 | 10 | 10 | 0 | `scratchpad\content\art1\sheets\c1_potion_500.png` |
| 秘寶、忍具新舊混排（實際遊戲大小） | — | — | — | `c1_relic_mix_1x.png`、`c1_potion_mix_1x.png`（原大：上半狀態列 32 像素、下半戰鬥忍具欄 42 像素，淺色嵌板照 `components.css`）；`*_mix_3x.png` 是放大三倍看細節用 |
| 飛行物（火雷珠 `thunder_bead`） | 1 | 1 | 0 | `c1_projectile_500.png`（跟煙霧彈、貓薄荷球、鞭炮並排） |
| 事件圖 | 36 | 37 | 1 | `c1_event_500_1.png`～`_5.png` |
| **合計** | **65** | **66**（成功 65、被擋 1） | 1 | 以上都在 `scratchpad\content\art1\sheets\` |

- 事件圖 36 張＝5F 第二關「師父的舊木箱」四隻×3（主圖＋r0＋r1）、第三關「最後一頁」四隻×4（主圖＋r0＋r1＋r2）、球球三篇 8（藍頭巾 3、木靶 3、屋頂影子 2）。張數照 `daxia_teach` 的結構與 `event_result_art.test.ts` 的規則推：無效果與進戰鬥的選項不畫（`daxia_teach` 現在是 8 張：四隻×主圖＋r0）。
- **重生 1 張**：`event_ninja_blue_headband_r1`（撕成繃帶）第一次被生圖端的安全檢查擋掉（`safety_violations=[self-harm]`，「包紮手臂上的擦傷」被誤判），沒出圖；改成「急救式地纏在膝蓋上、不提傷口」重生一次就過。其餘 64 張都是第一次就採用。
- 看圖檢查的項目：長相是不是新版（球球奶油白底灰褐紋、菲菲藍眼深棕臉斑＋蝴蝶結、噹噹黑白＋銅護臂、封封橘虎斑＋紅圍巾＋直劍）、每張只有那一隻（影子那兩張另有影球球）、沒有字（秘笈與紙頁只有貓的招式圖，放大確認過）、透明邊、圖文對得上（掌印記號、鎖已彈開、兩件舊忍具、壓著石頭的紙頁、封封出劍時劍鞘還在腰上、球球自己的頭巾是深藍而欄杆上那條是淡藍）。灰膜檢查 `check_haze.py` 掃新圖 0 張。
- 做法：gpt-image-1.5 真透明（codex-oauth）。圖示參考＝三張同類既有圖示拼一張；事件參考＝這隻的新版待機第 1 格＋同一隻既有事件圖（5F 用各自的 `daxia_teach`）＋屋頂那篇多附 `shadow_cat_idle`。圖示 128×128、長邊貼滿、quality 80；事件 560×420、quality 80。生圖原檔在 `tools/motion-art-source/c1/`（`.gitignore` 擋 png），提示詞與選定紀錄 json 進版控。

## 二、鍵名對照表
`C:\Users\yayax\AppData\Local\Temp\claude\C--Users-yayax\a6460c69-085f-4478-bf45-7ca4bf6a4a03\scratchpad\content\batch1_art_keys.md`：命名規則（圖示、事件主圖／結果圖、角色專屬、`data-art-cast`、飛行物、延後名單怎麼算）、每一張的最終鍵名與檔名、接線時要做的六件事、實測數字。新事件代號照派工單，沒有改名。

## 三、跟派工單不一樣的地方（請主控看）

1. **5 張球球版事件主圖檔案放好、先不登記清單**（`daxia_chest`、`daxia_lastpage`、`ninja_blue_headband`、`ninja_target`、`ninja_roof_shadow`）。
   原因：事件還沒接線，這 5 張不是結果圖、沒有角色前綴、也不在任何一關的 `bgKeysForAct` 裡，登記後 `preloadArt` 會讓**每個人開場就下載**（210 KB）。實測過一次全部登記：`check_size.py` 圖片 10.67／10.60 MB、首載總計 11.46／11.30 MB，**兩項超標**，推送閘門會擋。
   派工單說「新圖一律在延後下載名單」，但延後名單（`docs/分關載入.json`）是照執行期規則算出來的、不能手加；不改程式就沒辦法讓這 5 張延後，所以選「照最小改動先不進清單」。接線時跑 `python tools/gen_content_batch1_art.py register` 就登記進去（已實測可用、只加 5 行），**登記前要先做對照表第五段第 2、3 條**，不然閘門會紅。
2. **圖示 28 張算首載（174 KB）**：`preloadArt` 開場載整組 `icons`，沒有延後管道；提案本來也是把圖示算首載。現在首載圖片 10.46／10.60 MB（98.7%）、首載總計 11.25／11.30 MB（99.5%），**過關但只剩約 50 KB**。要不要延後、怎麼延後見「需要主控決定」。
3. **改了三支測試**（都在事件圖那一筆提交，註明「第一批程式接線前暫放」）：
   - `tools/manifest_hygiene.test.ts`：加 `PENDING_C1`（五篇代號＋結果圖序號），孤兒檢查先當「有人會要」；另加一條「暫放名單裡的事件都還沒接線」，事件進 `events.ts` 還留在名單上就紅，逼程式代理接線時把名單拿掉。
   - `tools/feifei_stills.test.ts`、`tools/dangdang_stills.test.ts` 的「退回球球的缺口」：把 `bg/event_ninja_` 開頭當成球球專屬（跟她們自己的專屬事件同一個道理），否則球球專屬圖會被算成菲菲、噹噹的缺口。這條是永久規則，不是暫放。
   - 確認會紅：拿掉 `PENDING_C1` 的 `ninja_target` → 孤兒檢查紅、列出那 3 張；名單塞進已接線的 `daxia_teach` → 新那條紅；兩支缺口檢查把前綴改錯 → 兩條都紅。改回來全綠。
4. **清單寫法**：`tools/manifest_io.py` 的 `merge`（`add_icons.py` 等舊工具也一樣）寫的是一格縮排，但進版控的清單是兩格縮排＋結尾換行，用它會把整份三千行改掉。我的工具改成照兩格縮排寫，差異只有新增的行（`make_dialogue_portraits.py` 也是這樣寫）。舊工具的問題沒動，只記在這裡。

## 四、驗收
- `npx tsc --noEmit -p .`：0 錯誤。
- `npx vitest run`：307 個檔、2676 條通過（開分支時 2675 條，多的是上面新加那一條），6 條跳過。跑完照雷 5 收拾：`怪物工作檯.json` 只差日期、`事件文案.md`／`牌池總表.json` 只差換行，都還原；`分關載入.json` 真的多了 31 筆（新結果圖與三隻他版全部是 0），已提交。
- `npm run build && python tools/check_size.py`：大小 OK（數字見上）。
- 沒做的：沒有開本機伺服器實機看（事件、秘寶、忍具都還沒接線，遊戲裡叫不出這些圖）；「實際遊戲大小」是照樣式表的尺寸與嵌板顏色合成的，不是遊戲截圖。接線後請程式代理或主控實機看一次。

## 五、需要主控決定
1. 圖示 174 KB 算首載可不可以？不行的話兩條路：(a) 鎖角色的六件（`whet_stone`、`tassel_knot`、`qi_gourd`、`qi_tea`、`sword_talisman` → 封封；`iron_weight_belt` → 噹噹，共 34.4 KB）比照舊劍穗在 `heroOfKey` 特例化；(b) 圖示整組改成選角後才載（改 `preloadArt`）。首載總計只剩約 50 KB，第二批圖示進來前一定要處理其中一條。
2. 那 5 張主圖「先放檔、不登記」這個做法可不可以？可以的話，工單 C 的程式代理照對照表第五段做即可；如果第〇批 0-2（事件主圖照地圖預載）先進來，直接跑 `register` 就好。

## 六、`git log --oneline coop..HEAD`
```
b9495a85 美術 c1（內容擴充第一批）：5F 兩版與球球三篇專屬事件的插圖 36 張
18465be2 美術 c1（內容擴充第一批）：火雷珠飛行物
c0b122a2 美術 c1（內容擴充第一批）：忍具圖示 10 張
04c6d248 美術 c1（內容擴充第一批）：秘寶圖示 18 張＋生圖工具
```
