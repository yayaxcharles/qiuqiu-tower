# lazy 報告：第〇批 0-1 事件文字分包、0-2 事件主圖照地圖現抓

worktree `F:\ClaudeWork\qq-lazy`，分支 `c0923-lazy`（從 coop `2b8c3c7f` 開出），一筆提交 `09c4a72c`。

## 首載數字（`SITE_NAME=qiuqiu-tower-coop npm run build` → `python tools/check_size.py`）

| 類別 | 修前（coop 原版 2b8c3c7f） | 修後（09c4a72c） | 差 | 目標 |
|---|---|---|---|---|
| 首載程式 | 675.5 KB（99.3%） | **557.4 KB**（82.0%） | **−118.1 KB** | 至少 −60 KB ✔ |
| 首載圖片 | 10.29 MB（579 檔，97.1%） | **9.40 MB**（548 檔，88.7%） | **約 −890 KB** | 至少 −600 KB ✔ |
| 首載總計 | 11.07 MB（98.0%） | **10.07 MB**（89.1%） | −1.00 MB | — |
| 按需程式 | 278.1 KB | 398.6 KB | +120.5 KB（事件畫面 10.2 KB＋事件文字 110.3 KB 兩塊） | — |

原始輸出：`scratchpad\content\lazy_size_before.txt`、`lazy_size_after.txt`。預算數字沒動，`tools/check_size.py` 檔頭補了一段 09-23 的紀錄。

## 改了什麼

**0-1 事件文字分包**
- 新檔 `src/content/event-text.ts`（927 行）：噹噹 `DANGDANG_EVENT_TEXT`、封封 `FENGFENG_EVENT_TEXT`（原在 `fengfeng-dialogue.ts`）、菲菲 `FEIFEI_EVENT_LINES`／`FEIFEI_EVENT_TEXT`／`FEIFEI_TA`、鏡子走廊 `MIRROR_EVENT_TEXT`（連 `HALL`），加上 `eventTextFor`、`mirrorEventText`，用腳本整段剪貼（驗算：拿掉的 908 行除了 4 行刻意改的，全部原字出現在新檔）。
- `src/content/dialogue.ts:927` 新增 `coopMirror()`、`:1383` `mixedLine` 改成匯出（新檔要用）；刪掉搬走的那幾段與 `FENGFENG_EVENT_TEXT` 匯入。
- `src/main.ts:27` 事件畫面改 `registerLazyScreen('event', …)`，拿掉靜態匯入。
- `src/ui/app.ts:430、463` 走進事件格改走新的 `enterEvent`：先 `coop.clearScreenHooks('event')`（把地圖的投票處理拆掉）→ 舞台上鎖（沿用 `fight-pending`）→ 等「事件畫面模組＋這一格主圖＋事件畫面底圖」（圖最多 6 秒，模組不設時限）→ 這一局還在才 `show('event')`；等超過 0.4 秒把地圖下方提示換成「正在準備事件……」。
- `src/ui/screens/map.ts:408-409` 地圖一出來就在背景抓事件畫面模組與這張地圖的事件圖。
- 別的讀者全部找過：地圖事件格只顯示「事件」兩字、結算不讀、連線混搭換句（`mixedLine`）跟著 `eventTextFor` 一起在新檔、除錯頁本來就是按需載入（改從新檔匯入）；主程式靜態匯入鏈上沒有任何模組讀這幾張表（有測試守）。
- 工具與測試改匯入路徑（只動匯入那幾行）：`tests/` 15 支、`tools/*.test.ts` 4 支、`tools/story_table_lib.ts`（EV／ET 列寫回的檔改成 event-text.ts）、`tools/apply_story_table.py`（球球原句改了之後同步換鍵的檔改成 event-text.ts）。

**0-2 事件主圖照地圖現抓**
- `src/ui/bgacts.ts:87` 新增 `eventMainKeys()`；`bgKeysForAct` 不再列事件圖；`deferredBgKeys` 併入全部事件主圖（`:109`）→ 開場 `preloadArt`、選角 `heroArtUrls`、進關 `preloadAct` 都不抓事件主圖（紙箱借用的 `bg/event_chest_*` 不是事件，照舊首載）。
- `src/ui/preload.ts:144-222` 新增 `mapEventIds`（這張地圖的事件格＋這一關待出的後集，判準照抄引擎 `enterEvent`）、`mapEventArtUrls`、`eventScreenBgUrl`、`preloadMapEvents`、`warmEventArt`。鍵走跟事件畫面同一條路（`eventArtKey`＋`eventArtHero`：本機這一位的版本、沒有退回球球那張、連線的鏡子走廊照座位 0）。自己一組、留參照、換地圖整組放掉；插隊（`fetchPriority='high'`）；**地圖自己的底圖排第一**（見「需要主控決定」1）。
- `src/ui/assets.ts:493-503` `decodeAll` 加一個選填的 `priority`（設 `img.fetchPriority`）。
- `tools/dump_monster_acts.test.ts:38` 事件主圖在 `docs/分關載入.json` 一律寫 0（按需），並加了斷言；`docs/分關載入.json` 重生（+38 −7 行）。

## 測試（`npx tsc --noEmit -p .` 零錯誤；`npx vitest run` 311 檔 2697 條全綠，另 5 檔 6 條原本就跳過）

新增 4 支（22 條）：
- `tests/ui/event_text_split.test.ts`：照原始碼走一遍主程式的**靜態**匯入鏈，碰不到 `event-text.ts`／`screens/event.ts`；大表只住在新檔；main.ts 用延後登記。
- `tests/ui/enter_event_0923.test.ts`：把 `enterEvent` 切出來跑（仿 `adopt_run_0923`）：先拆投票處理、兩樣都到才換、模組失敗也換過去交給載入畫面、局丟了不換、0.4 秒提示、單機可走；接線（`enterNode` 不直接 show、地圖先抓）。
- `tests/net/event_late_screen_0923.test.ts`：真的 `CoopSession`＋`LoopbackPair`：拆掉之後同伴的事件票與下一格地圖票都先存著、地圖那支一張都接不到；事件畫面掛上時補跑；反例（不拆會接到地圖票）。
- `tests/ui/map_event_art.test.ts`：名單含每個事件格與 5F；待出後集排第一，並**拿引擎的 `chooseNode` 真的走進去**確認換出來的那篇在名單裡；四隻各抓自己版本、沒有剪影；連線鏡子走廊照座位 0；只抓這張地圖、插隊、留參照、不重送、換關換組、地圖底圖排第一；走進去時 6 秒上限。

改寫 2 支既有測試的期待（行為本來就要變）：`tests/ui/bgacts.test.ts`（事件主圖改成全部延後、不在任何一關清單、紙箱三張不准延後）、`tests/ui/preload_hero.test.ts`（開場／選角／進關都不再抓事件主圖）。

**怎麼確認會紅**：`scratchpad\content\lazy_redcheck.py` 把 18 處修正逐一改回壞寫法、跑對應測試、再還原（還原後 `git diff` 雜湊前後一致），18 處全部變紅：首載畫面靜態匯入事件文字、main 改回靜態匯入、走進事件格直接換畫面、不拆投票處理、不等主圖、不看局還在不在、地圖不先抓、後集沒列、抓的不是本機版本、不插隊、不等事件畫面底圖、地圖預載不抓底圖、地圖底圖不排第一、不留參照、沒有 6 秒上限、事件圖放回每關清單、延後名單沒併、分關載入表沒歸 0。輸出在 `lazy_redcheck_out.txt`。

## 實機（Playwright，`channel:'chrome'`、每個 context 全新暫存設定檔、`vite preview --port 5321`，打包版＋`?debug`）

腳本與原始紀錄在 `scratchpad\content\lazy_pw\`，截圖與逐格膠卷在 `scratchpad\content\lazy_shots\`（每段膠卷有亮度掃描與聯絡表 `*_sheet.jpg`，我逐張看過其中的聯絡表與截圖）。作弊只有一種：地圖跳格（把「目前所在」改成事件格的上一格，再用真的滑鼠點事件格）。文字對照用 `vitest` 匯出的 `eventTextFor` 期望值（`expected*.json`，暫存測試跑完即刪，沒進倉庫）。

| 場次 | 事件 | 從點下去到事件畫面 | 載入畫面 | 主圖空白格 | 圖是自己的版本 | 標題／敘述／選項／結果字 | 閃白格 |
|---|---|---|---|---|---|---|---|
| 球球 | 此路不通、5F 秘笈 | 13／8 毫秒 | 無 | 0 | ✔ | 全對 | 0 |
| 菲菲 | 此路不通、5F 秘笈 | 8／8 | 無 | 0 | ✔ | 全對 | 0 |
| 噹噹 | 曬太陽、5F 秘笈 | 8／8 | 無 | 0 | ✔ | 全對 | 0 |
| 封封 | 鏡子走廊、5F 秘笈 | 13／13 | 無 | 0 | ✔ | 全對 | 0 |
| 球球限速 1.6 Mbps（地圖一出來就點） | 此路不通、5F | 2920（出現提示）／12 | 無 | 0 | ✔ | 全對 | 0 |
| 封封限速 1.6 Mbps（同上） | 鏡子走廊、5F | 1427（出現提示）／8 | 無 | 0 | ✔ | 全對 | 0 |

主控台與網路錯誤：每一場 0 筆。

**連線**（開房噹噹不限速、加入菲菲限速 1.6 Mbps，貼碼直連，不經正式中繼）：
- A：兩台投同一個事件格；開房那台進了事件畫面立刻投票，**那一刻加入那台還停在地圖上等（`fight-pending`、提示「正在準備事件……」）**；約 1.7 秒後加入那台進來，按鈕上已經掛著「← 同伴」，再投同一項 → 兩台結算、結果字各是自己的版本（混搭情境下的期望值）、兩台局面對帳一致、沒有紅色橫幅。圖：開房 `event_dangdang_*`、加入 `event_feifei_*`，進來那一格就是好的。
- B（倒下＋極慢）：兩台做一樣的改動（加入那位倒下、那一格換成這張地圖沒排到的事件），加入那台降到 10 KB/s。開房那台一個人決定 → 按「繼續」→ 投了下一格，**這時加入那台仍在地圖上等，而且沒有被那張地圖票帶走**（畫面序列只有 地圖→事件）；它進事件畫面時補跑同伴那一票、直接出結果，按「繼續」回地圖再補跑地圖票，走進同一格。兩台局面一致、沒有紅色橫幅。
- 截圖：`lazy_shots\coop\A2_guest_waiting.png`（慢的那台在等、同伴的票已經掛在地圖上）、`A3_guest_event_sees_vote.png`、`B1…B3`。

我看過的畫面：沒有空白、沒有閃白（限速膠卷亮度 19～42，第一版 110 的那次見下）、沒有錯圖、沒有錯字。

**實機抓到並修掉的一個問題**：第一版只等主圖，限速時第一次進事件畫面，**事件畫面的底圖（`bg/screen_event`）還沒到**，整片露出舞台米白底 1.2 秒（膠卷亮度 110）。它本來就在開場那批裡，但排在幾百張圖最後，跟我這次的改動無關、原版一樣會發生；既然要求「第一次進事件畫面不能閃白」，就把它一起插隊、一起等。修後亮度最高 41、沒有米白格。

## 沒做的與原因

- ~~結果圖（`_r<n>`）仍是點選項那一刻才抓~~ → 主控補派後已做，見文末「追加」一節。
- **球球的事件原文（`events.ts`，55 KB）仍在首載**：引擎要讀事件的選項與結果，文字跟規則寫在同一份資料裡，要拆得動資料結構，超出這次範圍；需要時是下一刀。
- 限速下第一次看到地圖時，地圖底圖與節點圖示本身要一兩秒才鋪好（開場那批還在下載）——原版就這樣，不是這次造成的；我讓地圖底圖在這次新增的插隊批次裡排第一，避免被事件圖擠到後面。
- 09-15 產生的 `docs/*_劇情改寫表_2026-09-15.keys.json` 裡 EV／ET 那幾列還寫著 `dialogue.ts`；要再用那兩份舊表回填，先重跑 `DUMP_STORY=1` 產新表。
- 沒 push、沒 merge。實機用的 Playwright 是 `scratchpad\w0923\pw` 那份（唯讀借用）。

## 需要主控決定

1. **地圖底圖排第一的取捨**：限速 1.6 Mbps、地圖一出來就立刻點事件格時，要等約 1.4～2.9 秒（有提示、畫面完整）；如果改成事件圖排第一，那一下會快約 1 秒，但地圖會先空著一兩秒才鋪底圖。實際上第一個事件格最早在 2F（要先打一場），平常碰不到這個等待。我選了「地圖先完整」。
2. **主圖等待上限 6 秒**（照開打等連線牌面的 6 秒）：10 KB/s 那種極慢網路會超過，超過就照樣進去、主圖晚一點出現（不會是錯圖或破圖框，是那一塊先空著）。要不要拉長？
3. 首載程式與圖片各空出 118 KB、約 0.9 MB，**預算上限沒動**（680 KB／10.6 MB）。要不要把上限往下調，把這次空出來的鎖住，由主控決定。

主控已裁定（09-23）：1 照舊、2 不動、3 不調。

## 追加：事件結果圖先抓、選了才等（主控 09-23 補派，提交 `3aa0bd8d`）

**改了什麼**
- `src/ui/preload.ts`：新增 `eventResultUrls`／`preloadEventResults`（這個事件所有選項的結果圖，插隊、留著、跟主圖同一組，還在路上的不重送）、`warmResultArt`（選了之後等那一張，上限 6 秒，沒有結果圖的選項立刻好）、`eventArtReady`／`whenEventArtDecoded`（解好了沒、等它解好）。挑圖走跟畫面同一條路（本機這一位的版本、連線的鏡子走廊照座位 0）。
- `src/ui/screens/event.ts`：
  - 進畫面就 `void preloadEventResults(run, ev.id)`。
  - 新的 `whenResultArtReady`：單人點選項、連線票結算，兩條路都先等那張結果圖才跑 `take()`；等的時候舞台點不動、鍵盤再按也不收（`resolving`），超過 0.4 秒在對白框補「正在準備……」，這一局丟了就不跑。
  - `eventArt` 多一個「頂替」參數：**等滿 6 秒結果圖還沒解好，就先放這一篇的主圖，解好再換上，抓不到就一直用主圖**。這是實機逼出來的，見下。
- 首載：程式 558.2 KB（多 0.8 KB）、圖片 9.40 MB 不變，預算內。

**實機逼出來的根因**（限速 1.6 Mbps，`lazy_pw\probe2.js`）：本機預覽是 HTTP/1.1，一個主機最多開 6 條連線。地圖出來約 6 秒後，這 6 條全被大檔佔住：跳過了卻還在下載的開頭影片（1.8 MB）、背景音樂（1.9 MB）、菲菲的逐格動作圖集（每張 0.65～1.3 MB）。每條只分到約 33 KB/s，新的請求再怎麼插隊也得排到有一條空出來。實測「讀完 4 秒再點」那一次，結果圖排了 11.9 秒才開始傳。第一版只等 6 秒，等完照畫，結果插圖那一塊空了約 545 格，所以才加了上面的「主圖頂著」。
- 正式網站（GitHub Pages）應該是 HTTP/2，沒有 6 條的限制，情況應該會好很多。**這一點我沒驗證**（規矩不准開正式網站）。
- 開頭影片跳過後還繼續下載、逐格動作圖集搶頻寬，這兩件不在這次範圍，列在「沒做的」。

**測試**：`tests/ui/event_result_art.test.ts` 15 條。把 `whenResultArtReady` 與 `eventArt` 原封不動切出來跑（仿 `adopt_run_0923`），另有名單、插隊、不重送、6 秒上限、接線。`scratchpad\content\lazy_redcheck2.py` 把 17 處改回壞寫法，**全部變紅**，還原後綠（輸出在 `lazy_redcheck2_out.txt`）。`tests/ui/mirror_hall_coop_art.test.ts` 有一條原始碼比對跟著新參數改了一行。全套：tsc 零錯誤，vitest 312 檔 2712 條綠。

**實機**（菲菲，限速 1.6 Mbps，`lazy_pw\result.js`，截圖與膠卷在 `lazy_shots\result_feifei_slow\`，聯絡表我逐張看過）：
| 點法 | 結果 |
|---|---|
| 一進事件畫面就點（江湖救急） | 等 0.8 秒，出現「正在準備……」，結果圖一插進來就是好的，空白 0 格，閃白 0，字對 |
| 讀 4 秒再點（此路不通） | 結果圖被大檔擋著，等滿 6 秒，先放主圖＋拿到的牌，空白 0 格，閃白 0；約 3.5 秒後結果圖解好、換上（最後截圖是菲菲跌坐那張結果圖），字對 |

回歸：噹噹不限速單人 2 個事件全過；連線 A、B 兩段重跑，兩台對帳一致，沒有紅色橫幅，主控台錯誤 0。

**這一節沒做的**
- 限速下，拿到的牌圖（例如「絆線」）在結果畫面上還是空的。牌面屬於開場那一批，被同樣的連線塞車擋住，**原版就這樣**，不在這次範圍。
- 事件主圖在 10 KB/s 那種極慢網路下，等滿 6 秒仍可能沒到（連線 B 段看到的是正在一行行畫出來的主圖）。主控裁定上限不動，所以照舊。
- 建議之後另開一刀，縮短慢網路下所有等待的根本做法：①開頭影片跳過後清掉 `src`，停止下載；②逐格動作圖集降低優先權，或晚一點再抓。

## 追加二：慢網路讓路（主控 09-23 再派，先 `git merge c0923-int`，提交 `148bb399`）

**改了什麼**
- ①影片（`src/ui/video.ts`）：跳過、播完、這一局被丟掉時，除了拿掉 `src`，再叫一次 `load()`。**只拿掉 src，瀏覽器會繼續把整支影片抓完**：實測跳過之後，開頭影片（1.8 MB）還佔著一條連線好幾十秒。連線局本來就不播影片，不受影響。
- ②新增 `src/ui/heavy-lane.ts`（大檔專用排隊）。逐格動作圖集，包括角色（`frame-motion.ts`）和魔物（`enemy-motion.ts`），不再自己設網址，一律交給它：
  - 同時最多 2 張，優先權設低（正式網站是 HTTP/2，靠優先權讓路）；
  - 開場那一批（開場圖＋第一關魔物）抓完才開始，最多擋 90 秒；
  - 卡住的一張最多佔位 90 秒；
  - 戰鬥裡正要畫的那張插到最前面；魔物動作一律插隊。
  - 這些設定在 `main.ts` 開機時打開（`armHeavyLane(2)`、`holdHeavyLane()`）。**沒打開時等於不存在**，所以測試、動作試玩頁跟原本一模一樣，既有動作測試一條都沒改。
- 選好角色時（`preload.ts` 的 `preloadHeroArt`），逐格動作排在這一位的靜態圖後面。
- ③動作還沒到時，戰鬥照舊交還靜態立繪（`combat.ts` 的 `mountMotion` 看 `ready()`），這一點確認仍有效：實測玩家那一格每 0.1～0.4 秒取樣一次，「什麼都沒有」是 0 格（見下表）。
- **背景音樂沒動**：它正在播就一定要一條連線串流，唯一能做的是延後開始放音樂，那會改到聽覺體驗，所以沒做。列在「需要主控決定」。

**量測**（限速約 1.6 Mbps，`lazy_pw\measure.js`；修前是 `dfc9abf8` 另外打包成 `dist_before`、埠號 5322；修後埠號 5321。每場都是全新設定檔、固定節奏：影片 1 秒後跳過、幻燈片每 1.2 秒點一下、地圖出現 1 秒後點 1F 戰鬥格。「點事件」那場是在地圖上等 30 秒再走、讀 2 秒再點選項，因為真人第一個事件最早在 2F。每種各跑兩次）

| 1.6 Mbps（菲菲） | 修前（兩次） | 修後（兩次） |
|---|---|---|
| 開局→第一場戰鬥手牌圖全到 | 15.0／14.7 秒 | **11.7／11.8 秒** |
| 點戰鬥格→手牌圖全到 | 6.5／6.2 秒 | **3.3／3.3 秒** |
| 進戰鬥後玩家那一格什麼都沒有（靜態圖還沒到、動作也沒到） | 19／17 格（約 1.9 秒） | **0／0** |
| 點事件格→事件畫面出來 | 6.0／6.0 秒 | **0.56／0.69 秒** |
| 點事件格→結果圖到（含讀 2 秒） | 9.0／9.0 秒 | **2.6／2.7 秒** |
| 點選項→結果圖到 | 0.93／1.00 秒 | **0.007 秒**（已經在了） |
| **代價：逐格動作動起來** | 101 秒 | **161 秒**（這段期間是靜態立繪，不空白） |

修後另外量了球球、噹噹，玩家那一格「什麼都沒有」也都是 0 格。動作動起來：球球 199 秒、噹噹 145 秒（球球的圖集最多，約 33 MB）。

一般寬頻（約 20 Mbps，`LAZY_DOWN=2500000`，同樣快速點掉序章）：
- 修前：手牌圖全到 11.4 秒，進戰鬥那一刻動作就在（9.9 秒），但**手牌空了 1.5 秒**。
- 修後：手牌 8.6 秒就全到（點下去 0.24 秒），但**動作要到 17.9 秒才動起來**，進戰鬥後前 9 秒是靜態立繪。
- 實際玩家會看序章、影片不一定跳，第一場戰鬥通常在開局 40 秒以後；照上面的速度，兩種寫法到那時動作都已經到了。這是我的推估，沒實測。

**測試**：
- `tests/ui/heavy_lane.test.ts` 12 條：上限、掛著、90 秒保險、卡住讓位、失敗讓位、插隊、低優先權、預載不把排隊中的圖當壞圖、延後的那張排最後、靜態圖先到、魔物插隊、接線。
- `tests/ui/story_overlay_0923.test.ts` 的假節點補了 `load()`，並加一條「按跳過連下載一起停」。
- `scratchpad\content\lazy_redcheck3.py` 把 15 處改回壞寫法，全部變紅（輸出 `lazy_redcheck3_out.txt`）。
- 全套：tsc 零錯誤，vitest 315 檔 2753 條全綠。
- 首載：程式 559.3 KB、圖片 9.57 MB，預算內。圖片比上一節多，是合進 `c0923-int` 帶進來的其他內容，不是這一刀造成的。

**實機截圖**：`lazy_shots\measure_*\A_combat.png`（修後第一場戰鬥：牌面全到、靜態立繪；動作到了之後換成逐格）、`B_result.png`。

**這一節沒做的**
- 背景音樂照舊佔一條連線，見「需要主控決定」。
- 牌面、魔物立繪那批開場小圖本身的順序沒動（開場那一批仍是六條一起抓）。

**需要主控決定（追加二）**
1. **逐格動作晚到的代價**：慢網路下動作動起來從 101 秒變 161 秒。一般寬頻、又快速點掉序章的話，第一場戰鬥前 9 秒是靜態立繪（使用者的四道門檻有一條「不露舊立繪」）。要改回「動作優先」只要把 `main.ts` 的 `holdHeavyLane()` 拿掉，或把上限調高，代價是手牌、事件圖又會空著等。
2. 背景音樂要不要也讓路，例如開場那一批抓完才開始放。這樣會改到剛進地圖那幾十秒的音樂。

## 追加三：慢網路才讓路（主控不接受「一般寬頻第一場前 9 秒是靜態立繪」，提交 `9777396b`）

**做法**（選了主控建議的②＋③）
- 新增 `src/ui/netspeed.ts`，開場那一批一開抓就計時，量這台網路快不快：
  - **2.5 秒內收到 1 MB 算快**（約 3.2 Mbps 以上），收不到算慢；
  - 用的是瀏覽器自己記的每個檔「收了多少、什麼時候收完」，不另外下載測速檔；快取裡本來就有的也算，所以回鍋玩家一下就判快；
  - 瀏覽器說在省流量、或連線是 2G／3G（`navigator.connection`），直接當慢；
  - 量不了（沒有 `PerformanceObserver`）當快，也就是照原本的行為。
- `main.ts`：量出來之前大圖集先等著，最多 2.5 秒；實測不限速約 0.03 秒量完，20 Mbps 約 0.4 秒。
  - **快**：大圖集不設上限、不等，**跟 `dfc9abf8` 原本的行為一模一樣**；
  - **慢**：同時兩張、開場那一批抓完才開始（上一節那一套）。
- ③背景音樂（`bgm.ts` 的 `deferBgm`）：**慢網路才延後**，等開場那一批抓完才開始放，最多等 90 秒。等的時候照樣記著「該放哪首」，放的時候放當下那一首。快網路一點下去就放，照原本。
- 選好角色時「靜態圖先、動作後」也改成只在慢網路做。
- 沒做①（核心動作排在手牌後面立刻抓）：②做完之後快網路已經跟原本一樣，慢網路的動作到齊時間不在目標內；再細分動作會動到 `frame-motion` 的「全部到齊才算好」，改動面大。

**量測**（每種各跑兩次。修前 `dfc9abf8` 打包在 `dist_before`、埠號 5322；修後 5321。腳本 `lazy_pw\measure.js`（A、B）、`measure_c.js`（C）；結果 json 在 `lazy_shots\measure_*`、`lazy_shots\measure_c\`）

C「第一場戰鬥玩家與魔物的逐格動作到齊」：從開新局算起，馬上開一場有老鼠的戰鬥（老鼠有逐格動作），看玩家與每隻魔物都換上逐格畫布的時間。

| 網速 | 修前（兩次） | 修後（兩次） | 差 |
|---|---|---|---|
| 不限速 | 0.38／0.36 秒 | 0.34／0.33 秒 | 快 0.03 秒 |
| 20 Mbps（另外加量） | 9.68／9.72 秒 | 9.96／10.01 秒 | **慢 0.28～0.29 秒**（量速度那一下；目標 0.5 秒內） |

A、B（同上一節的做法；「修前」是 `dfc9abf8`）：

| 網速 | 項目 | 修前（兩次） | 修後（兩次） |
|---|---|---|---|
| 不限速 | 開局→手牌圖全到 | 8.48／8.43 秒 | 8.45／8.43 秒 |
| 不限速 | 逐格動作在第一場戰鬥開打時已經到 | 是／是 | 是／是 |
| 不限速 | 點事件格→結果圖（含讀 2 秒） | 2.08／2.08 秒 | 2.07／2.02 秒 |
| 20 Mbps | 開局→手牌圖全到 | 11.57／11.53 秒 | 11.44／11.46 秒 |
| 20 Mbps | 逐格動作在第一場戰鬥開打時已經到 | 是／是 | **是／是**（上一版是 17.9 秒才到，這一版改回來了） |
| 1.6 Mbps | 開局→手牌圖全到 | 15.0／14.7 秒 | **11.6／11.7 秒**（保住） |
| 1.6 Mbps | 點事件格→結果圖（含讀 2 秒） | 9.0／9.0 秒 | **2.63／2.64 秒**（保住） |
| 1.6 Mbps | 玩家那一格什麼都沒有 | 19／17 格 | 0／0 |
| 1.6 Mbps | 逐格動作動起來 | 101 秒 | 159 秒（代價照舊，只在慢網路） |

**測試**（tsc 零錯誤；vitest 317 檔 2763 條全綠）：
- `tests/ui/netspeed_0923.test.ts`（7 條）：收滿 1 MB 當場判快、2.5 秒不到判慢、開機前收完的不算、快取裡的算、省流量／3G 直接慢、量不了當快；音樂等的時候不放，等完放當下那一首；沒延後照原本一點就放。
- `tests/ui/slow_boot_0923.test.ts`（2 條）：開機接線，用真的大檔那一條。量的時候先掛著；快＝放行、不設上限、音樂不等；慢＝上限 2、開場那一批抓完才放、音樂也等。
- `tests/ui/heavy_lane.test.ts`：「靜態圖先到」改成快、慢兩種各一條（快網路不能等）。
- `scratchpad\content\lazy_redcheck4.py` 把 16 處改回壞寫法，**全部變紅**（輸出 `lazy_redcheck4_out.txt`）。上一節那 15 處重跑，13 處仍紅；另外 2 處改的是舊的開機寫法，已經換掉，由這一節的開機測試接手。

**需要主控決定（追加三）**
1. 快慢的門檻：2.5 秒收 1 MB（約 3.2 Mbps）。4～5 Mbps 的手機網路會被判成快，照原本全部一起抓，也就是那種網速拿不到上一節的讓路改善。要照顧到那一段，可以把門檻調高，代價是 20 Mbps 量速度那一下也會更久，目前是 0.28 秒。
2. 量速度那一下讓 20 Mbps 的動作晚 0.28 秒到。只有「一開頁就立刻開新局、立刻開戰」才碰得到；平常玩家在標題停留的時間比這長，零延遲。

## 追加四：推前審查 高-1、低-1（先 `git merge c0923-int`（`46e077ef`），提交 `5064c373`）

**高-1 修法**（照審查建議）：**效果在票結算那一拍當場套，只有畫面等結果圖**。
- `event.ts`：單人點選項、連線票結算都直接 `take()`，兩台同一拍套效果。
- 結果圖還沒到時，結果畫面由新的 `paint` 先扣住：等的期間只記最後一筆要畫的，例如同伴挑完牌的那次重畫會蓋掉前一筆；圖到了才畫，最多等 6 秒。舞台鎖、0.4 秒提示、這一局丟了就不畫，這些照舊。
- 單人也改成同樣做法。效果先套、畫面後出，對單人沒有差別，也少一條分支。

**高-1 實機重現**（`lazy_pw\repro_h1.js`，貼碼直連）：
- 設定：開房噹噹不限速，加入菲菲限速 5 KB/s。兩台同樣布置：事件「迷路的小黑貓」選「送牠到集合處」（2 支忍具、有結果圖），主機的忍具袋只剩一格。主機先投、加入方後投，主機跳出換忍具視窗就換掉剛拿到的那支（投票後 0.78 秒）。
- **修前**（`46e077ef` 打包）：加入方紅色橫幅，錯誤是「第 1 號整局動作在這邊做不出來（swap）」，主機接著「連線結束了」。兩台的忍具不一樣：主機 `smoke_bomb+smoke_bomb+catgrass_tea`，加入方 `…+firecracker`。
- **修後**：兩邊都沒有橫幅，局面一致（`smoke_bomb+smoke_bomb+needle_rain`）。加入方的結果畫面是自己的結果圖，截圖 `lazy_shots\repro_h1_after\guest.png`，我看過。

**低-1 修法**：
- 新增 `src/ui/event-loader.ts`，事件畫面那一塊的載入：
  - 地圖預抓、走進事件格、載入畫面三個地方共用同一次；
  - 失敗了從錯誤訊息拿出那一塊的網址，加 `?retry=N` 再抓兩次（隔 0.6、1.2 秒）。錯誤訊息沒寫網址（Safari）就用原方式重試；
  - 還是失敗就往外丟，下一次叫會整個重來。
  - 先實測過：Chrome 同一個網址失敗一次之後，再載一樣失敗，網址換個參數就好（`lazy_pw\import_retry.js`）。
- `app.ts` 的 `enterEvent`：模組最多等 10 秒，到了就照樣換過去，交給載入畫面接著等，不會停在地圖上點不動。
- `lazy-screen.ts`：載入失敗時先給「再試一次」（重畫載入畫面、再叫一次載入），再給「重新整理」，訊息寫明連線中重新整理會中斷這一局。
- 除錯總覽改成經由事件畫面拿文案（`debug.ts` 從 `./event` 匯入，事件畫面轉出）。這樣打包時文案跟事件畫面**併成同一塊**（128 KB），重試只有一個網址要換。原本文案另成一塊：它失敗的話，換最外面那一塊的網址也救不回來。
  - 先試過改打包設定（`manualChunks`）：打包器把共用的東西全拉進去、主程式縮成 50 KB，那條路不行，已還原。
- 連線：走進事件格時兩台的狀態只差「進了這一格」，沒有誰先套了什麼，所以一台卡在載入只會讓另一台等，不會分岔。

**低-1 實機**（`lazy_pw\repro_l1.js`）：開新局後斷網，地圖出現時預抓事件畫面失敗；恢復網路後走進事件格。
- 修前：20 秒內停在「畫面載入失敗」。
- 修後：0.1 秒就進得去。請求紀錄是 `event-….js` 失敗 → `?retry=1`（還在斷網）失敗 → `?retry=2` 成功。

**測試**（tsc 零錯誤；vitest 327 檔 2929 條全綠）：
- `tests/net/event_swap_timing_0923.test.ts` 3 條：真的 `CoopSession`＋`LoopbackPair.hold` 做出一台先一台後。兩台各掛一支跟事件畫面一樣的「票湊齊那一拍套效果」，主機先投、加入方先投兩種都不分岔。反例：加入方等圖才套，主機的 swap 先到 → 加入方停掉、局面不一致。
- `tests/ui/event_result_art.test.ts` 補 4 條：`paint` 扣住只留最後一筆、沒結果圖直接畫、局丟了不畫、接線（兩條路都當場 `take()`，而且 `take()` 裡先套效果再扣畫面）。
- `tests/ui/event_loader_0923.test.ts` 6 條；`enter_event_0923` 補「卡住最多 10 秒就換過去」；`lazy_screen` 補「再試一次」；`event_text_split` 補「事件文案只有事件畫面直接引用」。
- `scratchpad\content\lazy_redcheck5.py` 把 13 處改回壞寫法，**全部變紅**（輸出 `lazy_redcheck5_out.txt`）。
- 回歸：菲菲限速的結果圖兩種點法重跑（`result.js`），空白 0、字對。
- 首載程式 575.1 KB，比合併後的整合分支多約 0.7 KB（`event-loader.ts`），在預算內。

**沒做的**：中-1（紙箱、大魔物戰利品沒掛「同伴才用得到」）、資訊-2 不是派給我的，沒動。

## git log --oneline coop..HEAD

```
5064c373 推前審查修正：事件效果在票結算那一拍套、只有畫面等結果圖；事件畫面載入加時限與換網址重試
（46e077ef 以前是主控整合分支的合併）
9777396b 慢網路才讓路：開場量速度，一般寬頻照原本全部一起抓，量到慢才限大圖集、延後背景音樂
148bb399 慢網路讓路：跳過的影片停止下載、逐格動作大圖集排在開場小圖之後且同時最多兩張
dfc9abf8 合併 c0923-lazy 補修：進事件先抓結果圖，還沒到先用主圖頂著（主控合的）
3aa0bd8d 內容擴充 0-2 補：事件結果圖進畫面先抓、選了才等，等滿 6 秒還沒到先用主圖頂著
09c4a72c 內容擴充第〇批 0-1、0-2：事件文字分包、事件畫面按需載入、事件主圖照地圖現抓
```
（`3aa0bd8d` 的提交訊息寫測試「16 條」，實際是 15 條，以這份報告為準。另外 `c0923-int` 其他分支的提交也經由合併進了本分支，沒逐一列。）
