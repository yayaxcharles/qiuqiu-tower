# 第五輪實機驗收：內容擴充第三批（qq-cint3）

驗收代理，2026-09-24 04:30–06:10。報告只寫我或分身親眼看到、親手量到的。這一輪有四個分身，分別負責門檻一二加店主立繪、門檻四、單人四隻、連線兩組。沒測到的另外列一節。

## 一句話結論

**`6a49ff32` 沒有高風險問題，四道門檻都沒有比線上差。有一條中：長毛掌櫃那間，對白字壓住秘寶的價錢。**
- 協調者後來追加的 `cd20f93b` 修了四條連線高風險，四條我都補驗了：貓窩夢枕被打斷、夢枕連點、淨化過的原件重抽、紫霧②兩種順序。每一條都先在修前 `6a49ff32` 用同一套做法重現出卡住、斷線或分岔，再確認修後沒了。冒煙測試（祝福＋普通戰鬥＋罐頭鋪）也過了。見第十三節。**建議推 `cd20f93b`。**
- 平衡：用 b3int 第九節的量法、新種子複量，跟 b3int 報的數字在誤差內一致。

聯絡表（我親眼看過）：`accept_c3\_總表.jpg`、`accept_c3\_四項門檻.jpg`。細表在 `accept_c3\gate12\`、`gate4\`、`solo\`、`coop\`、`sheets\`、`supp\`。

## 〇、驗的是哪一版

| 用途 | 版本 | 打包 | 埠 |
|---|---|---|---|
| 驗收 | `6a49ff32`（`c0923-int3`） | `git archive` 到暫存區 `accept_c3\int_src`，正式打包；型別檢查 0 錯誤 | 5301 |
| 線上對照 | `83bf458b` | `git archive` 到 `accept_c3\ctl_src` | 5303 |
| 補驗 | `cd20f93b` | `git archive` 到 `accept_c3\int3_src`；型別檢查 0 錯誤、`check_size` 大小 OK（首載總計 9.81／11.30 MB） | 5304 |

- 沒在 qq-cint3 裡打包，跟前兩輪同一個理由：驗到一半分支會動。這次果然也動了：門檻四分身看到 qq-cint3 工作目錄有 7 個檔被改動、還沒提交。取出同一個提交來打包，內容一樣。
- 這一版序章後多了開局祝福畫面。共用工具在祝福畫面預設點第一張（安全類），單人、連線分身則是自己挑，四類都挑過。

## 一、四道門檻（跟線上 83bf458b 比）

### 1. 大小：正常，跟線上一樣（分身）
- **角色圖檔**：345 個角色圖檔、11 份逐格資料，新舊完全相同。
- **畫面上的框**：地圖頭像、對白頭像、過關亮相、選角、戰鬥逐格與 31 張靜態，外框最大差 0.03 像素。
- **頭的量測**：140 項中只有 2 項吻合度差 0.01，是截圖誤差。超過 5% 的那幾格是躺姿、轉頭量不準，新舊同一個數字，跟第四輪同一批。
- **限速 1.6 Mbps 第一場換手**，新舊數字相同：
  - 身高 245～250→252、腳底差 1～3 像素。
  - 逐格到齊（新／舊）：球球 202／201 秒、菲菲 162／161、噹噹 144／143、封封 179／177。
- **店主立繪**：五位（橘貓老闆、玳瑁婆婆、長毛掌櫃、阿福、行腳商）的框全都一樣。
  - 框都是 210×266、位置 (0,294)、腳底線 559，跟線上的橘貓老闆同一個框。
  - 用原圖透明度逐像素算，三個表情對每一格貨架的重疊都是 **0 像素**；桌機、手機橫拿，第一、二關都算過。
  - 第二關七格並排、婆婆那間六格忍具並排、四顆鈕的版面，都在舞台內，字不溢出。
- 證據：`gate12\_大小_1..4.jpg`、`_慢網路.jpg`、`_店主立繪.jpg`。我親眼看過 `_店主立繪`，其餘是分身看的，四項門檻表也收了。

### 2. 貓窩：正常，跟線上差 0 像素（分身）
- 三關 × 三底圖 × 五姿勢 × 四隻，共 180 組：外框差 0、圖檔相同。另外真的按打盹、磨爪 24 組，也是差 0。
- 不眠香爐：四隻「燻得睡不著」，44F 照樣回滿。暖毯：回 26、下一場 20 蜷縮。新舊一樣，按鈕都一行放得下。
- **清心香（第三批新鈕）**：只有帶魔氣秘寶才出現。
  - 第三顆鈕放得下，貓的位置跟只有兩顆鈕時差 0。
  - 帶一件直接淨化；帶兩件先跳挑選窗，可以「先不要」。
  - 點完有旁白＋四隻各一句。
- 證據：`gate12\_貓窩_1..3.jpg`、`_不眠香爐.jpg`（我看過 `_不眠香爐`）。

### 3. 流暢度：正常，跟線上一樣
- **戰鬥**：四隻 × 近戰、近戰接防禦、丟東西、挨打、擋一部分、最後一擊＋勝利，每 50 毫秒新舊並排。節奏與位置一樣，亮度掃描沒有閃白、閃黑。飄字時間新舊差 0～50 毫秒；最大的是菲菲勝利那一擊，新 493、舊 443 毫秒，第四輪也有同樣幅度的晃動。
- **祝福畫面**（新版才有）：
  - 序章最後一下→祝福、標題按續玩→祝福、選一張→地圖，三段都是淡入淡出。
  - 祝福主圖與四張圖示第一格就在，沒有空白、沒有露出舊畫面。
  - 限速時祝福出現那一刻圖示已經到齊（7.51 秒出現、7.51 秒到齊），因為序章播放時就在背景抓了。
- **問號格揭曉**（球球三種，另外三隻各一種）：
  - 約 50～100 毫秒出現問號卡，200 毫秒翻成交叉的爪／行腳商／紙箱，400～450 毫秒淡掉。
  - 底下的畫面約 150 毫秒換好。沒有閃白、沒有露出地圖。
- **店主立繪進出**：進店、買一樣換表情、離店，新版（玳瑁婆婆）與線上（橘貓老闆）節奏一樣。進店那一下的亮度跳（暗的地圖→亮的店）兩版都有，是換場本身；新版店主第一格就在。
- **單人整局**：四隻都走到第一關關主並打贏（分身，見第三節），主控台 0 錯誤、0 個 404。
- 證據（都看過）：
  - `sheets\fluid_{ninja,feifei,dangdang,fengfeng}.jpg`（看過球球、封封）
  - `sheets\fluid3_{貓}.jpg`（看過球球）
  - `sheets\_祝福換場.jpg`
  - `sheets\_qmark_ninja.jpg`、`_qmark_others.jpg`

### 4. 事件圖片：正常；既有事件一格都沒變，第三批四隻都是自己的版本（分身＋我）
- 事件數：球球 61→66 篇、另外三隻 62→67 篇，多出來的 5 篇就是稀有事件。
- 線上原有的圖格：球球 183、菲菲 187、噹噹 188、封封 189 格。**鍵與位元組全部相同，不同 0 格**；倒了的神龕原有的主圖與兩張結果圖也沒變。既有事件的文字改動 0；唯一多的字是神龕【魔氣】達成時的一句提示。
- 第三批新增 88 個圖鍵，全部看過：稀有事件 68、神龕新結果圖 4、祝福主圖 4、問號格揭曉 12。不是本角色的 0、別隻貓的圖 0、空白 0。
- 稀有事件 5 篇：主圖與每個選項的結果圖都是本角色版，都掛「難得一見」金牌，神龕不掛。
  - 籤筒各種籤都抽過。
  - 叫醒大魔物會出現「開打」。
  - 紫霧②沒有魔氣時改成移除一張牌（牌組 10→9）。
  - 貓薄荷田用的是替代版，放大看過，大俠貓本人沒出現。
- 限速 1.6 Mbps（菲菲）：祝福主圖一進畫面就完整；伏擊在點下去 145 毫秒內就是完整畫面；溫泉、神龕的結果圖 150 毫秒內畫好。
- 證據：`gate4\_事件圖.jpg`、`_稀有事件_{貓}.jpg`、`_問號格與祝福.jpg`、`_載入取樣.jpg`。我看過總覽、封封稀有事件、問號格與祝福。

## 二、單人四隻各一局（分身）
自動檢查：球球 49／49、菲菲 51／51、噹噹 56／56、封封 56／56；主控台錯誤與 404 都是 0。

| 段 | 看到的 |
|---|---|
| 祝福 | 四類各挑一次：球球安全（舊忍具袋，+2 支忍具）、菲菲換牌（一疊招式圖，三選一）、噹噹代價（沾了魔氣的舊護腕，上限 76→68）、封封賭運氣（藥酒，失手、上限 −3）。主圖是自己的版本，選完回地圖，提示寫的跟實際拿到的一樣 |
| 地圖 | 婆婆那間疊小頭像、滑上去「今天顧店：玳瑁婆婆…」；問號格說明寫「約 20% 會變」。自然擲出的客座：球球 6F 掌櫃、11F 婆婆；噹噹 13F 婆婆；封封 11F 阿福 |
| 問號格 | 三種都是照 b3qmark 的種子自然擲出：球球路邊紙箱（秘寶 +1）、菲菲與封封行腳商（×0.7，買一樣其他收攤）、噹噹伏擊（甩掉牠們，68→62）。回地圖後那一格換成實際圖示＋小問號 |
| 客座店主 | 四隻都請婆婆淨化：提示、扣 90、魔氣護符→清心護符、婆婆一句＋自己回一句、閃光，鈕變成「這間已經淨化過了」。噹噹身上兩件，先跳「要淨化哪一件？」。噹噹逛阿福：放生半價、換招扣 40、原地換一張。封封逛掌櫃：私藏一定有，帳本 198（加一成） |
| 稀有事件 | 四隻各一篇（除錯指定），「難得一見」金牌，效果對：貓薄荷田 +血、上限 +6；溫泉 +30；籤筒中籤；紫霧①拿到魔氣舊護腕 |
| 第一關關主 | 四隻都打贏、進過關畫面 |

證據：`solo\_單人_{貓}.jpg`（我看過噹噹那張）。

## 三、連線兩組（分身，中繼；對帳 19 次全部一致、沒有紅條、主控台 0 錯誤）
- g1：球球開房（不限速）＋封封加入（限速）；g2：噹噹開房（限速）＋菲菲加入。自動檢查 58／58、54／54。
- **祝福各選各的**：快的那台每 0.3 秒點一下序章，2～3 秒進祝福；慢的那台每 2 秒點一下，12～16 秒才進。快的先選，停在「等封封挑……」；慢的那台看到同伴那排亮起、還有「球球從包袱裡拿了…」那一行。兩人都選好才一起進地圖。兩人選的是不同類，其中一人走三選一。
- **問號格**：兩組都遇到行腳商，另外 g1 路邊紙箱、g2 伏擊。
  - 行腳商：兩台的攤子一樣；一人按好時不上樓，兩人都按才一起走。
  - 路邊紙箱：兩件各挑一件。
  - 伏擊：投票後兩台都 76→70。
- **客座店主**：g1 婆婆、g2 阿福。兩台的名牌、立繪、兩份貨架（含價錢）JSON 一模一樣。
- **淨化**：
  - 婆婆那間只有開房那台有淨化鈕。
  - 神龕【魔氣】：開房的人要從兩件挑一件，加入方自動投空票、沒卡住。加入方那台標籤改口成【球球的魔氣】，結果也用球球的字與圖。
  - 溫泉②同樣沒卡住。
- 證據：`coop\_連線_g1.jpg`、`_連線_g2.jpg`（我看過 g1）。

## 四、回魂香：同一回合被兩隻魔物連打（我）
- 新版：封封喝了回魂香、血壓到 1，兩隻老鼠各啃一口。紀錄寫「回魂香的煙拉住了封封，留下 1 點生命，這一輪魔物再打也打不倒」，接著「回魂香的煙還繞著封封，這一下打不倒」。回到自己的回合時血還是 1，血條下有「回魂香：打不倒」牌子。
- 線上：同樣兩口，第二下就倒地、這一局輸了。
- 上一輪的低（「只救第一下」）照主控裁決修好了。
- 證據：`sheets\_回魂香.jpg`（看過）、`shots\revive_fengfeng_{int,old}\`。

## 五、載入（菲菲；限速＝1.6 Mbps；新／線上各兩次）

| 量什麼 | 不限速 新 | 不限速 線上 | 限速 新 | 限速 線上 |
|---|---|---|---|---|
| 開局→序章結束（新＝祝福出現、線上＝地圖） | 7.36／7.37 秒 | 7.36／7.38 秒 | 7.51／7.48 秒 | 7.47／7.45 秒 |
| 開局→第一場手牌 | 8.94／8.96 秒 | 8.43／8.46 秒 | 12.27／12.37 秒 | 11.77／11.74 秒 |
| 點戰鬥格→手牌 | 0.053／0.055 秒 | 0.054／0.057 秒 | 3.23／3.34 秒 | 3.28／3.27 秒 |
| 第一場玩家那格空著 | 0 | 0 | 0 | 0 |
| 點事件格→事件畫面 | 0.014／0.023 秒 | 0.014／0.015 秒 | 0.61／1.00 秒 | 0.69／0.62 秒 |
| 點事件格→結果圖 | 2.02／2.04 秒 | 2.04／2.03 秒 | 2.62／3.01 秒 | 2.70／2.64 秒 |
| 逐格動作到（玩家） | 0.24～0.27 秒（中位 0.27） | 0.23 秒 | 171.8／171.7 秒 | 170.4／170.4 秒 |
| 逐格全部到 | 0.35～0.40 秒 | 0.35～0.36 秒 | 171.8／171.7 秒 | 170.4／170.4 秒 |
| 圖鑑（標題直接開）全部圖到 | 0.13 秒（177 列） | 0.12 秒（162 列） | 7.7 秒 | 7.2 秒 |

- 「開局→第一場」新版多的 0.5 秒是多了一個祝福畫面：腳本在圖示到齊後等 0.3 秒才點，再加上換場。序章結束的時間、點戰鬥格→手牌的時間，兩版一樣。
- 不限速每一項都在 0.5 秒內。逐格動作前兩次量到 0.55／0.36 秒，那時四個分身同時在跑瀏覽器；分身都停了之後重量三次是 0.24～0.27 秒，表上用重量的數字，前兩次也留在 `load\log_cfast_new_1,2.txt`。
- 限速：逐格慢 1.4 秒、圖鑑慢 0.5 秒（多 15 列）、點事件格→結果圖有一次多 0.4 秒。都在雜訊邊緣，沒有空白。
- 限速第一場換手（四隻，我自己也量了）：靜態→逐格的框新舊一樣；封封兩版前 2.4 秒都有空白（新 78 次取樣、舊 82 次），其他三隻 0。
- 證據：`accept_c3\load\log_*.txt`、`shots\comp_*`、`shots\slowswitch_*`。

## 六、平衡複量（照 b3int 第九節：聰明機器人、難度 1、基準 `d819b149`、機器人照常拿祝福）

新種子兩組：`acc5`、`acc5b`。每組單人每隻 2,400 局、雙人每組 1,200 局，兩組合起來單人每隻 4,800 局、雙人 2,400 局。

| 組 | 基準 `d819b149` 樓層 | `6a49ff32` 樓層 | 差（兩組平均±標準誤） | b3int 報的差 | 關主一差 我／b3int | 關主二差 我／b3int |
|---|---|---|---|---|---|---|
| 球球 | 23.75／23.93 | 25.39／25.07 | **+1.39±0.25** | +1.34 | +5.6／+5.9 | +0.2／−2.2 |
| 菲菲 | 27.30／27.06 | 27.43／27.90 | **+0.48±0.23** | +0.70 | +3.5／+3.4 | −0.1／+1.1 |
| 噹噹 | 24.06／23.81 | 24.43／24.44 | **+0.51±0.22** | +1.07 | +2.2／+4.5 | +2.8／+2.0 |
| 封封 | 23.03／22.59 | 22.37／22.63 | **−0.31±0.20** | −0.16 | −0.4／+1.5 | −2.3／−5.2 |
| 球球＋菲菲 | 22.30／22.47 | 23.07／23.55 | **+0.92±0.31** | +0.75 | +3.5／+4.4 | −3.2／−5.5 |
| 噹噹＋封封 | 20.07／19.77 | 20.50／19.38 | **+0.02±0.26** | +0.23 | +1.2／+3.2 | +0.8／+10.5 |

- 樓層：六組都跟 b3int 一致。差最多的是噹噹（我 +0.51、b3int +1.07），約 1.8 個標準誤，還在兩倍標準誤內；其餘都在 1 個標準誤左右。基準的絕對樓層也跟 b3int 接近（b3int：23.97／27.21／23.91／22.60／22.34／19.76）。
- 關主勝率：方向都對得上。只有噹噹＋封封的第二關關主對不上：b3int +10.5、我 +0.8。這一格每組只有兩百多場（我這邊 260／276、252／221 場），單一格的標準誤約 3 個百分點，兩邊差約兩倍標準誤，列給主控知道。
- 線上 `83bf458b` 對基準（我量的 `acc5` 組）：球球 −0.43、菲菲 −1.63、噹噹 −0.71、封封 −1.09、雙人 −0.96／−0.78。也就是第三批（主要是祝福）把第一、二批掉的補回來了，跟 b3int 第五節的拆解一致。
- 通關率（`acc5`／`acc5b`）：球球 9.8／8.9%、菲菲 9.5／9.9%、噹噹 7.8／8.0%、封封 2.6／3.8%。球球的平均樓層仍比菲菲低（25.2 對 27.7）。
- 腳本與原始數字：`accept_c3\bal\`（`bal3.ts`、`bal3b.ts`、`big\`、`bigb\`）。

## 七、問題清單

### 高
`6a49ff32` 沒有。協調者另外找到四條，修在 `cd20f93b`，補驗見第十三節。

### 中
1. **長毛掌櫃那間，對白那一行字壓住秘寶的價錢**（分身量到、我看過截圖）。
   - 原因：他那間一定有店長私藏，私藏那件（集章卡）的說明很長，把秘寶那一排撐高。
   - 被蓋住的：桌機第二關小坐墊、木人樁、算盤珠三格的價錢（重疊 22～26 像素）；手機橫拿第一、二關兩三格（25～29 像素）。第二次進店只講碎念也會壓到（8～21 像素）。
   - 手機上婆婆、阿福那間也壓到 9～11 像素；線上橘貓老闆在手機第二關本來就有 3 像素。
   - 重現：桌機 1280×720，把第二關一間罐頭鋪指定成長毛掌櫃再走進去。
   - 截圖：`gate12\keepers\new_desktop_act2\curio.png`、`new_phone_act2_revisit\curio_again.png`；`_店主立繪.jpg` 最下面一列。

### 低
1. **兩行頂部提示疊在一起**：第一次見到婆婆或阿福，進店 4 秒內按淨化或換招，結果那句會蓋在初見旁白上。截圖：`solo\shots\solo_ninja\4b_tortoise_purified.png`。
2. **貓窩多了清心香那顆鈕之後，對白框往上移 78 像素**，「貓窩暖暖的…」兩行字疊在貓的下半身上。重現：帶魔氣護符進 9F 貓窩。見 `gate12\_不眠香爐.jpg`。
3. **限速第一場，靜態退路圖比線上晚 0.3～0.8 秒出現**，那段空白多 1 次取樣（分身量到）。
4. **行腳商的頭看起來比橘貓老闆小兩成多**（臉寬約 74 對 99 像素）；掌櫃、阿福小約一成。框是同一個，是美術比例。
5. 限速時逐格動作慢約 1.4 秒、圖鑑慢 0.5 秒（多 15 列）。

### 資訊
- 拿到秘寶的結果頁，放大圖示上方那行說明（含「沾了魔氣：可以淨化…」）壓在結果圖上緣。挑絕學那頁的牌卡蓋住人物，這是既有版面，b3rare 已經報過。
- 限速那台逛阿福時，截圖看到米白底加兩格還沒出圖，約 2 秒內補齊。線上版同一段時間也一樣，不是這次造成的。

## 八、主控台錯誤與 404
- `6a49ff32` 與 `cd20f93b` 的全部場景（四個分身、流暢度、載入、回魂香、補驗）：**主控台錯誤 0、頁面錯誤 0、404 為 0**。
- 只有門檻一二分身的量測腳本讀畫布時，觸發過一條 Chrome 效能警告，是工具自己造成的。

## 九、跟線上比：變好／變差
| 變好 | 變差 |
|---|---|
| 回魂香同一回合連打不會倒 | 長毛掌櫃對白壓價錢（中） |
| 祝福、問號格、客座店主、稀有事件、淨化都照規格動，單人、連線都走過 | 兩行頂部提示重疊、清心香把旁白推上去（低） |
| 平衡：第一、二批掉的樓層補回來了（對基準 −0.3～+1.4） | 限速靜態退路圖晚 0.3～0.8 秒（低） |
| 大小、貓窩、戰鬥流暢度、既有事件圖跟線上一模一樣 | 開局多一個祝福畫面，進第一場多 0.5 秒（設計本來就是這樣） |

## 十、沒測到的
- 伏擊選「迎戰」；兩人投不同選項的擲骰；連線長毛掌櫃那間；貼碼直連。
- 探路杖、平安繩、箱中箱、夢枕（單人）、藥簍、集章卡的實機觸發；稀有事件出現在第二、三關真的地圖上（都是換進第一關的格子）。
- 手機直式；連線時的罐頭鋪、貓窩版面；成交、買不起兩個表情只用原圖算重疊，沒在畫面上拍。
- 限速只測菲菲（門檻四）與四隻第一場；網速是 Chrome 模擬的。

## 十一、用過的作弊（都在除錯模式 `?debug`；連線時兩台做一模一樣的改動）
- 開新局，地圖跳格（有時往回跳）。
- 把事件格換成指定事件、把格子標成某一種問號格變化（引擎照規則不重擲）、指定罐頭鋪的店主。
- 塞秘寶、忍具、小魚乾；把問號格的累積設成 3；血設成六成或壓到 1；關主或魔物血改 1；戰鬥格換成三隻老鼠。
- 補驗：過關前把塔主池全標看過，大魔物池除了三件全塞給自己。
- 平衡：機器人直接跑引擎，沒有作弊。

## 十二、收尾
- 我開的三個伺服器（5301、5303、5304）先對過命令列才關，殘留的子行程一起關了；5301～5304 現在都沒人在聽。清單裡有一個行程編號的命令列對不上（可能已經自己結束了），我沒動它。
- 帶我暫存設定檔的 Chrome 0 個；`%TEMP%\qq-accept-profiles` 已刪。暫存區三個指向倉庫 `node_modules` 的連結已拆，只拆連結，兩邊的 `node_modules` 都還在。四個分身都回報瀏覽器已關、沒動倉庫。
- 兩個倉庫都是 0 個已追蹤檔被改、我沒有提交。qq-cint3 的 HEAD 現在是 `cd20f93b`，門檻四分身當時看到的那 7 個未提交改動已經不在了。
- **長毛掌櫃壓價錢那條（中）**：`cd20f93b` 的罐頭鋪畫面只改了「第一次見到」的判斷與「算不算有」，版面沒動，所以這條在 `cd20f93b` 應該還在。我沒有在 `cd20f93b` 重量。兩行頂部提示重疊那條（低）同理。

## 十三、補驗 cd20f93b（協調者追加，最後一節）

打包：`git archive cd20f93b` 到 `accept_c3\int3_src`，正式打包，5304 埠；修前對照用 5301（`6a49ff32`）。

### 1. 淨化之後刻意讓三選一出現原件（我）
- 做法：球球帶魔氣護符，在婆婆那間真的按「請婆婆淨化」，扣 90、換成清心護符。之後把塔主池全部標成看過，把大魔物池除了「魔氣護符、暗器匣、鐵壁」以外全部塞給自己，再打第一關關主。
- **cd20f93b**：過關三選一只出兩件（鐵壁、暗器匣），**魔氣護符沒有出現**。
- **修前 6a49ff32**：同一套，三件都出，魔氣護符又被抽到。
- 證據：`supp\_補驗_單人.jpg`（看過）、`shots\supp_ninja_{int3,int}\`。

### 2. 冒煙測試：祝福＋普通戰鬥＋罐頭鋪（我，cd20f93b）
- 祝福拿了護身符，回地圖時提示「接下來 5 場戰鬥開始時給全體魔物 1 層翻肚」。
- 1F 普通戰鬥打贏，拿到戰利品。
- 婆婆那間：買一支忍具扣 41，淨化鈕在，淨化成功，提示「「魔氣護符」淨化成「清心護符」了。」。
- 打過第一關關主，進過關畫面。
- 主控台 0 錯誤、0 個 404。

### 3. 連線：貓窩夢枕、紫霧②兩種順序（分身，中繼，每組一台限速）
兩組都跑：g1 球球開房＋封封加入（限速），g2 噹噹開房（限速）＋菲菲加入。修後兩組自動檢查各 39／39，對帳各 7 次全部一致，沒有紅條、主控台 0 錯誤。

- **貓窩夢枕被打斷**：帶夢枕的那位打盹後攤出三張升級牌，同伴先做完。挑牌頁還在，多一行「某某在旁邊睡了一下」，挑得下去，兩台一起離開。
  - 修前 6a49ff32：同一段，三張牌被「打盹／磨劍」選單蓋掉，8 秒後還是選單。分身沒去點那兩顆鈕，按下去會怎樣沒驗。
- **夢枕連點**：挑牌頁兩張交錯點 8 下（約 0.37 秒），1.8 秒後寫「學會了「循息＋」」（g2 是「封印解除＋」）。牌組只多一張、不斷線。
  - 修前：兩台都跳紅條「第 5 號整局動作在這邊做不出來」，主控台寫「停下來了：…（restCard）」，整場停住。
  - 這只證明畫面擋得住連點；引擎那一層（第二張不發號碼）被畫面先擋掉了，沒有單獨驗到。
- **紫霧②兩種順序**：一人帶兩件魔氣秘寶（跳淨化挑選窗），另一人沒有（跳移除一張牌）。
  - 順序 A（淨化的先挑完）：先挑完的那台停在「等同伴挑完」、按不動；另一台只有一個移除視窗。兩人都挑完才一起出「繼續」。
  - 順序 B（移除的先挑完）：同樣兩人都挑完才出「繼續」。
  - 兩種順序帳都一致。
  - 修前順序 A：先挑完的那台直接出「繼續」、上樓，另一台疊了兩個移除視窗。移除只套在自己那台，同一個人的牌組一台 10 張、一台 9 張，分岔了。
  - 修前順序 B：一台已經上樓，另一台淨化血契短刀只套到自己那台，生命上限一台 91、一台 79，分岔了。
  - 修前兩組各有 2 次對帳對不上。
- 沒測到：夢枕按「都不要」、倒下的人在貓窩、紫霧②兩人都帶或都沒帶魔氣、在第二關自然遇到紫霧②、貼碼直連。
- 證據：`supp\_補驗_連線.jpg`（看過，下兩列是修前）、`supp\shots\g{1,2}_5304_ABC1C2\result.json`、修前 `g1_5301_*`、`g2_5301_C2_pre`。
