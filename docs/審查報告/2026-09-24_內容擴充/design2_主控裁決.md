# design2 事件劇本：主控裁決（2026-09-23，蓋過劇本第十一節）

1. 第一批 `ninja_roof_shadow` 兩個選項都記 `chain:shadow_2`；①另記 `chain:shadow_2_fought`、②另記 `chain:shadow_2_watched`。（已通知第一批事件代理。）
2. 單人球球的影子鏈第二集換成屋頂那篇（劇本「新9」）：照劇本做。
3. 圖全做，不砍（使用者：生圖額度幾乎無限）：共約 247 張，第十節的省圖清單不採用。
4. 「店主的算盤」改名「店主的帳本」，代號 `shop_ledger`（跟既有「算盤珠」分開）。
5. 連線結果圖 `_r0`／`_r1` 若測試不允許一張圖登記兩個鍵，就複製成兩個檔各自登記。
