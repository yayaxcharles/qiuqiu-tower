第一筆提交（16 件秘寶＋6 支忍具的定義）：`395df246`（分支 `c0923-b2mech`，worktree `F:\ClaudeWork\qq-b2mech`）

# b2mech 報告：內容擴充第二批的新機制（計數型、角色新時機、限定池、師門套組、忍具新效果）

## 〇、主控裁決之後（`ec957a0e` 合併 c0923-int、`c8f245cd` 調數字）
- 合併 `c0923-int`（`3abe3e7c`）：全套 332 檔全綠、**0 errors**（那 6 個是 `main_motion_boot` 缺 `deferBgm`，整合分支已修）。
- 滿月劍意門檻 12→**10**（掛鉤改名 `qiReachDoubleNext: 10`，「從不到 10 變成 10 以上那一刻」、每回合一次，機器人估值同一個門檻）。重量：封封 +0.37 層（每場 0.16 次，原本 +0.2／0.08）。**仍然偏弱**，機器人不囤氣是主因。
- 四件加數字，對照常見池（下四分位 +1.1、中位 +2.2 層）；機器人對小魚乾量不準，另外用心算（一層約值 135 條，拿小魚乾罐 10 條／場＝+0.9、幸運錢幣 20 條／場＝+1.75 兩件校正）：

| 秘寶 | 改之前 → 改之後 | 量尺四隻平均 前→後 | 心算依據 |
|---|---|---|---|
| 撲滿 | 每 3 格 25 → **40** 條 | +0.4 → +0.7 | 完整一局約倒 8 次＝320 條，落在小魚乾罐（約 250）與幸運錢幣（約 500）之間；40、50 條量尺一樣是 +0.7 |
| 會員卡 | 價錢停住 → **放生一律 40 條、不再漲** | +0.05 → +0.34 | 一局放生四五次：平常 450～625 條、會員卡 160～200 條，省 290～425 條（同上一檔）；機器人一局只放生一次左右 |
| 批發箱 | 多帶 1 支 → **2 支**（＋忍具半價） | +0.65 → +0.83 | 一局五間店每間買兩支省約 225 條，多兩格留得住稀有忍具；機器人身上少於兩支才買，格子多半空著 |
| 山賊的欠條 | 每場 15 → **25** 條（進店付 10 不變） | +0.6 → +1.45 | 跟貪吃錢袋同樣 25 條、代價比較輕；常見池中下。事件池另兩件是純數值（+3.4～3.8），它是拿錢那一路，不硬拉 |

- 說明文字全部跟著改、依據寫在 `relics.ts` 各件的註解。**事件劇本表裡寫的「欠條 15 條」「會員卡停在拿到時」是舊數字**，事件那邊讀的是秘寶定義，不會錯；劇本文件本身沒改。
- 反向驗證補 5 條都紅（`redcheck3.py`／`.log`）；平衡快量兩批都在 ±2 層內（最大菲菲 A 批 −1.65）。

```
6d967b67 內容擴充第二批（b2mech）：第二關起店長私藏讓秘寶四格＋忍具三格七格並排，貨架再縮一級（桌機與手機橫拿實拍過）
f366b41f 內容擴充第二批（b2mech）：補兩組測試（滿月劍意的機器人灌氣、戰鬥與圖鑑的畫面接線）
cb2463c7 內容擴充第二批（b2mech）：計數型、角色五個新時機、兩個限定池、師門套組、四支新忍具效果接好，機器人會用、連線與存檔照顧到
395df246 內容擴充第二批（b2mech 第一筆）：16 件秘寶與 6 支忍具的定義先上，效果接好前標 wip 不進抽取池
```
（`git log --oneline c0923-int..HEAD`；`coop..HEAD` 另外還有開分支前就在 `c0923-int` 上的第〇、一批與美術合併，不是我的。）

驗收：`npx tsc --noEmit -p .` 零錯誤；`npx vitest run` 332 檔全綠（3015 過、9 跳過）。跑完照雷 5 還原了只差日期換行的四份 docs；`docs/秘寶量尺.md`、`docs/忍具使用率.md`、`src/engine/relic-ratings.json` 是真的重生、已提交。
全套跑完畫面會印「6 errors」（`tests/ui/main_motion_boot.test.ts` 的 unhandled rejection）：**開分支前就有**（我在基底 stash 掉改動跑過同一個檔，一樣 6 個），不是這批造成的，測試本身是綠的。
打包 `npm run build && python tools/check_size.py`：**大小 OK**——首載程式 590.2／680 KB（86.8%）、樣式 113.5／120 KB（94.6%）、圖片 9.04／10.6 MB、首載總計 9.74／11.3 MB。

## 給 b2event 的代號（第一筆就在，後面沒改）
- 事件限定（`pool: '事件'`，不進任何抽取池）：`master_wooden_sword` 師父的舊木劍（圖示鍵 `codex/relic_master_wood_sword`，師門套組）、`miasma_shard` 魔氣殘片（`codex/relic_demon_shard`）、`bandit_iou` 山賊的欠條。
- 罐頭鋪限定（`pool: '罐頭鋪'`）：`member_card`、`shop_ledger` 店主的帳本（`codex/relic_shop_abacus`）、`bulk_crate` 批發箱（`codex/relic_wholesale_crate`）。
- 套組：`RelicDef.set === '師門'`；`content/relics.ts` 有 `RELIC_SETS`、`setMembers`、`setCount`、`activeSets`、`relicLongText(def, owned)`（說明＋「【師門 n／3】…」）。**事件畫面的「拿到了什麼」清單（`event.ts` 133 行）我沒改**，那支檔 b2event 在動；要讓舊木劍在事件結果裡也寫出集到幾件，換成 `relicLongText(d, me(run, seat).relics)` 即可。
- 第一筆的 `wip` 旗標在 `cb2463c7` 已經整個拿掉（16 件、6 支都接好了）。

## 一、新機制（引擎／連線指紋／存檔／機器人／畫面）

| 機制 | 引擎（檔案：位置） | 連線指紋 | 存檔 | 機器人 | 畫面 |
|---|---|---|---|---|---|
| 每 N 回合 `everyNTurns`（沙漏、線香） | `combat.ts` `startSeatTurn`，排在每回合秘寶後面，`cs.turn % n === 0` | 回合數本來就在 | 不用存 | 被動，自動吃到 | 圖示右下角疊「這場第幾回合除以 N 的餘數」 |
| 每回合第 N 張打完 `onNthCard`（暗器匣） | `combat.ts` `playCard`，**排在牌效果之後**（不然可能先打死這張牌的目標）；秘寶打人不吃爪力（`effects.ts` `damageScatter`） | 張數本來就在 | — | 被動 | 疊「這回合打了幾張」（到 N 停） |
| 跨場計數 `RunPlayer.counters`（木人樁數攻擊牌、撲滿數非戰鬥格） | 木人樁：`playCard` 第 10 張那一張 `doubleDamage`；戰鬥裡用 `PlayerCombat.relicCounters`（`startCombat`／`beginCombat` 抄入、`finishCombat` 寫回）。撲滿：`run.ts` `tickNodeCounters`，`chooseNode` 走進事件／罐頭鋪／貓窩／紙箱時數 | 戰鬥 `rc[...]`、整局 `ctr[...]`，**有才串**（沒帶的局指紋一個位元不變） | 可選欄位；`checkRun` 只丟壞掉的那幾格（非物件、非非負整數、不是秘寶代號），不整份判壞檔 | 被動 | 地圖與戰鬥都疊數字；撲滿倒錢時狀態列跳「撲滿滿了，倒出 25 條小魚乾」 |
| 蓄氣灌滿（滿月劍意） | `effects.ts` `gainQi` 從不滿變成 12 那一刻 → `doubleNext = 1`，每回合一次（`fullMoonTurn`） | `fmt` | 戰鬥內 | 吐納那類灌到 12 時加 6 分（照分身油） | 秘寶閃光＋一行紀錄 |
| 花掉蓄氣（收鞘墜） | `effects.ts` `spendQi`（唯一的花氣出口）→ 每 6 點給 1 顆飯糰，零頭 `qiSpentAcc` | `qsa` | 戰鬥內 | 被動 | 閃光＋紀錄 |
| 中毒結算（五毒譜） | `combat.ts` `stepEnemyTurn`：**下毒的那一位**（`poisonedBy`，跟擊倒獎勵同判準）帶著才加 | 本來就有 `pby` | — | 被動 | 每跳一次閃光 |
| 回合末蜷縮（鐵壁） | `combat.ts` `endSeatTurn`，排在「沒出手」秘寶後面；超過 15 的部分每 2 點換 1 點反彈，蜷縮不減 | 反彈在狀態裡 | — | 被動 | 閃光＋紀錄 |
| 閃避（影分身卷軸） | `actions.ts` `damagePlayer` 閃過的那一支 → `onDodge` | 走下面的 `energyNextTurn`／`drawNextTurn` | — | 被動 | 閃光＋「下回合飯糰」牌子 |
| 罐頭鋪限定池＋店長私藏 | `run.ts` `makeShop`：一半的店在秘寶最右邊多一格（`rollLimitedRelic`，候選為空時**不擲骰**）；`reshuffleShop` 私藏那格照舊從限定池換；`buyRelic` 記 `shop_bought:<座位>:<代號>` | 旗標收進整局指紋 | 旗標在 `flags` | 一般貨架規則就會買（分數照量尺） | 紫色「店長私藏」小牌子；第二關起七格並排加 `.seven` 縮一級 |
| 價格規則（帳本、批發箱、會員卡、欠條） | `priceFor(run, it, seat, shop)`：帶貨架才算批發箱（只折忍具）與帳本（`shop.anyBought` 之前半價），幾個倍率相乘**最後捨入一次**；每次成交 `afterBuy` 重標整間；`buyRemove` 會員卡不漲；`makeShop` 結尾欠條付 10（不夠付到 0，記在 `shop.entryFee`） | 貨架兩台同一份 | — | 價錢讀 `it.price`（重標過的） | 劃掉原價＋「第一件半價」牌子；放生鈕寫「會員價，不再漲」；進門跳「老闆認得那張欠條，先收走 10 條小魚乾」 |
| 事件限定池 | 抽法本來就只抽前四池，不用擋；事件由 b2event 用 `relicId` 給 | — | — | 量尺照量 | 圖鑑兩個新區「罐頭鋪限定」「事件限定」 |
| 師門套組 | `combat.ts` 第一回合 `+ setFirstTurnEnergy`，套裡那幾件一起閃、紀錄寫「師門套組：第一回合多 1 顆飯糰」 | 秘寶清單本來就在 | — | `setBonusScore`：湊成那一件加「飯糰袋量出來的層差」（同一個效果），`bestRelic` 與罐頭鋪都讀 | 說明多「【師門 n／3】…」；湊成時跳提示；封面圖鑑三件並排、沒拿到的剪影（讀續玩那一局） |
| 延後一回合的飯糰 `energyNextTurn`（便當、影分身卷軸） | 立旗標；`startSeatTurn` 補滿之後發 | `ent` | 戰鬥內 | 便當：飯糰用光、仗還長才開 | 人身上「下回合飯糰 +N」牌子 |
| 戰鬥內保命 `guardLethal`（回魂香） | `damagePlayer` 被打倒那一刻：最後一口氣先用，香第二順位 | `gl` | 戰鬥內 | 這拍會死、或大場面血三成以下 | 「回魂香」牌子 |
| 全體拔狀態（照妖鏡） | `removeStatuses` 加 `target: 'all'` | 狀態 | — | 有虛化、全場隱身兩層以上、或一層隱身擋在我打得出去的攻擊前面 | 一行紀錄 |
| 手牌換隨機升級版（替換符） | 選牌選單 `purpose: 'transform'` → `combat.ts` `transformCard`：這一位抽得到的忍術牌（`pickable`）、戰鬥亂數、新編號、只在這場；手上沒牌不給喝 | 手牌 | — | 有爛牌（2 分以下）才喝，選單換最爛那張 | 選單標題「挑一張要換掉的牌」 |
| 改魔物目標（迷魂香） | `daze` 掛狀態「迷魂」＋`dazedBy`；`runEnemyEffects` 打人的效果改打旁邊的同伴：**照站位不擲骰**（右邊第一隻站著的，沒有就左邊最近的，一隻都沒有就打空）；吃牠的爪力與對方翻肚、同伴防禦擋得住；打倒算下香那一位；給你的減益照樣來；敵方回合收尾清掉 | 狀態＋`dzb` | 戰鬥內 | 打我最痛的那隻 12 點以上、旁邊有同伴（或只剩牠且挨下去剩不到四成）；中了迷魂的不算進挨打 | 牌子「攻 4（迷魂：打同伴）」、提示框寫打誰、狀態列「迷魂」（借忍具圖示）；丟出去有飛行物 `daze_incense` |

另外照主控通知：**帶不眠香爐打盹沒回血時**，四隻各一句「香爐燻得睡不著」（`content/dialogue.ts` 的 `RESTLESS_NAP_LINES`＋`napLinesFor`，貓窩單機與連線兩條路都走 `napQuip`，畫面層沒有喵）。

## 二、16 件秘寶（最終數字＋量尺：四隻各 600 局的平均多爬幾層，括號是每場發動次數）

池子參考（b1data 報告的舊件層差）：常見中位 +1.9／最高 +5.4；大魔物中位 +2.7／最高 +9.2；塔主中位 +4.0／最高 +13.6。

| 秘寶 | 池 | 牌面文字＝實際 | 球球 | 菲菲 | 噹噹 | 封封 |
|---|---|---|---|---|---|---|
| 木人樁 `wooden_dummy` | 常見 140 | 每打出第 10 張攻擊牌，那一張的傷害加倍（跨戰鬥累計） | +1.8（0.76） | +0.8（0.66） | +2.3（0.67） | +1.9（0.62） |
| 沙漏 `hourglass` | 常見 150 | 每 3 回合（第 3、6、9…回合開始時）多 1 顆飯糰 | +2.8（1.51） | +2.4（1.18） | +2.8（1.41） | +2.0（1.14） |
| 撲滿 `piggy_bank` | 常見 110 | 每走進 3 個不是戰鬥的格子得 25 條（跨關） | +0.5 | +0.5 | +0.9 | −0.2 |
| 線香 `incense_stick` | 大魔物 190 | 每 4 回合獲得 1 層隱身 | +5.3（1.04） | +4.4（0.75） | +4.6（0.96） | +4.8（0.78） |
| 暗器匣 `dart_case` | 大魔物 190 | 每回合打出第 3 張牌後，對隨機一隻 5 點 | +2.8（1.97） | +2.2（2.38） | +3.3（2.53） | +2.7（2.15） |
| 滿月劍意 `full_moon_sword` | 塔主 240，只給封封 | 蓄氣灌到 12 的那一刻，這回合下一張攻擊加倍（每回合一次） | — | — | — | **+0.2（0.08）量不準** |
| 收鞘墜 `sheath_pendant` | 大魔物 190，只給封封 | 這場每花掉 6 點蓄氣，1 顆飯糰 | — | — | — | +1.7（0.95） |
| 五毒譜 `five_poison_manual` | 塔主 240 | 你下的毒每回合結算時多扣 **2** 點 | +0.6（1.2） | **+4.1**（4.51） | +0.8（1.21） | +0.8（0.9） |
| 鐵壁 `iron_wall` | 大魔物 190 | 回合結束蜷縮超過 15 的部分每 2 點換 1 點反彈（蜷縮不減） | +2.2（0.62） | +1.6（0.64） | +4.2（1.0） | +3.5（0.7） |
| 影分身卷軸 `clone_scroll` | 塔主 240 | 每次閃過攻擊，下回合多抽 1 張、**多 1 顆飯糰** | **+3.8**（1.45） | +0.8 | +0.6 | +0.8 |
| 會員卡 `member_card` | 罐頭鋪 140 | 放生的價錢不再上漲 | +0.0 | +0.1 | +0.1 | +0.0（量不準） |
| 店主的帳本 `shop_ledger` | 罐頭鋪 180 | 每間店買的第一件半價（放生、重整不算） | +1.7 | +4.0 | +2.7 | +2.4 |
| 批發箱 `bulk_crate` | 罐頭鋪 160 | 忍具多帶 1 支、罐頭鋪忍具半價 | +0.3 | +0.8 | +0.8 | +0.7 |
| 師父的舊木劍 `master_wooden_sword` | 事件 200 | 開場 1 點爪力與 4 點蜷縮（師門） | +4.2 | +4.3 | +3.2 | +3.5 |
| 魔氣殘片 `miasma_shard` | 事件 190 | 最大生命 +15；開場 1 層炸毛 | +2.9 | +3.4 | +4.7 | +2.7 |
| 山賊的欠條 `bandit_iou` | 事件 150 | 打贏多拿 15 條；每間罐頭鋪先付 10 條舊帳 | +0.1 | +0.8 | +0.9 | +0.6 |

**量完調的兩件（理由）**：五毒譜照提案多扣 1 點時菲菲只多 1.7 層（塔主中位 4.0，過關三選一裡等於廢選項），2 點＝4.1、3 點＝5.6，取 2；影分身卷軸只抽牌時球球 +0.9，改「抽 2 張」也只有 +0.6（多的是飯糰不是牌），加「下回合 1 顆飯糰」＝ +3.8，接近塔主中位。變體是在記憶體裡改掛鉤、用量尺同一套算法量的（`scratchpad\content\b2mech\variant.ts`／`variants.log`），沒動分數表。
**其餘沒調**：沒有一件超過自己池子的舊最高（線香 +4.8 在大魔物池偏強但低於九尾墜 +9.2）；撲滿、會員卡、批發箱、欠條偏弱，量尺本來就對「價值在小魚乾與罐頭鋪」的機制量不準（`UNRELIABLE_HOOKS` 補了五條說明）。

### 機器人真的觸發得到嗎（`trigger.ts`，每件四隻各 150 局；量尺那一欄是戰鬥裡的發動次數）
- 計數型與新時機都有觸發：木人樁每場 0.6～0.8 次、沙漏 1.1～1.5、線香 0.8～1.0、暗器匣 2.0～2.5、收鞘墜 0.95、五毒譜 0.9～4.6（菲菲最多）、鐵壁 0.5～1.0、影分身卷軸 0.24～1.5（球球最多）；撲滿每局倒錢 3.6～4.9 次。
- 罐頭鋪三件：機器人一局逛 2.4～3.3 間店；帳本用到半價的店（有買東西）約九成、會員卡有放生的店三四成、批發箱有買忍具的店約三成四；欠條每間都付。
- **量不準的**：滿月劍意（機器人不囤蓄氣，每場只 0.08 次；已教它「這張把蓄氣灌到 12 時加分」，單條測試證明會先打吐納，但整局仍很少囤到 9 以上）；會員卡、批發箱、撲滿、欠條（價值在錢與逛店）。

## 三、6 支忍具（`docs/忍具使用率.md`，四隻各 600 局）

| 忍具 | 稀有度・價 | 效果 | 喝法 | 喝掉率 |
|---|---|---|---|---|
| 回魂香 `revive_incense` | 稀有 70 | 這場接下來第一次會被打倒時留 1 點生命 | 這拍會死／大場面血三成以下 | 98% |
| 便當 `bento` | 常見 40 | 下回合開始多 2 顆飯糰 | 飯糰用光、仗還長 | 100% |
| 照妖鏡 `demon_mirror` | 罕見 55 | 拔掉全體魔物的隱身、潛水與虛化 | 見上表 | 45%（第一版規則只有 9%，放寬後重量） |
| 傳功丹 `transfer_pill` | 常見 45 | 同伴抽 2 張、你多 1 顆飯糰（一個人時自己抽） | 走既有「飯糰」那條 | 99% |
| 替換符 `swap_talisman` | 罕見 50 | 挑一張手牌換成隨機升級牌（只在這場） | 有爛牌才喝 | 100% |
| 迷魂香 `daze_incense` | 稀有 70 | 目標這回合的攻擊改打旁邊的同伴（沒有同伴就打空）；丟出去 | 見上表 | 100% |

稀有度分級後 51 支：常見 24、罕見 18、稀有 9；「越稀有越貴」照樣成立（測試釘著）。忍具動作：便當、傳功丹算「吃」，迷魂香「丟」，其餘施術。

## 四、測試與反向驗證
新增 6 個測試檔：`tests/engine/content_batch2_mechanics.test.ts`（45 條，每個機制與每件內容）、`content_batch2_save.test.ts`（5 條：**舊存檔讀回**、壞計數只丟那幾格、**重整續玩計數沒掉**、戰鬥打到一半重整回到進場前的數、**連線兩台一致**＋計數走岔一格指紋就不同）、`smartbot_potions_batch2.test.ts`（20 條：六支的該喝／不該喝、迷魂不算挨打、滿月劍意灌氣、套組加分）、`tests/ui/relic_counter_0923.test.ts`（10 條：數字算法與畫面接線）、`tests/content/restless_nap_0923.test.ts`（3 條）。改動前提的測試：秘寶 95→111、忍具 45→51、稀有度分布、鎖角色名單、狀態清單、飛行物、`patch_field_light`（狀態列比對多了計數字串）、`run.test` 罐頭鋪秘寶格數、`bot.test` 錨值重錄。
**反向驗證 43 條全部會紅**（`scratchpad\content\b2mech\redcheck.py`、`redcheck2.py`，逐條改回壞寫法跑對應測試、跑完逐位元還原，前後 `git status` 一樣；紀錄 `redcheck.log`、`redcheck2.log`）：每一種機制的發動、計數的抄入寫回、指紋、存檔清洗、機器人每一條喝法、選單換最爛那張、套組加分、狀態列疊數字、欠條提示、香爐台詞都在內。

## 五、平衡快量（難度 1；四隻各 600 局、雙人兩組各 300 局；兩批種子；開分支前＝`c0923-int` `1ab83fc8`）

| 組 | 批 A 前→後 | 批 B 前→後 | 兩批平均變動 |
|---|---|---|---|
| 球球 | 23.08 → 24.59 | 23.74 → 23.20 | +0.49 |
| 菲菲 | 27.79 → 26.11 | 27.79 → 27.18 | −1.15 |
| 噹噹 | 24.88 → 24.33 | 24.72 → 23.86 | −0.71 |
| 封封 | 22.86 → 21.93 | 23.12 → 22.28 | −0.89 |
| 球球＋菲菲 | 22.06 → 21.71 | 21.65 → 21.58 | −0.21 |
| 噹噹＋封封 | 20.31 → 19.69 | 18.64 → 19.01 | −0.13 |

**沒有任何一組超過 ±2 層**（最大是菲菲 A 批 −1.68）。菲菲、噹噹、封封兩批都小降，我查了一下（`dilute.ts`／`dilute.log`，A 批）：拿掉三件新塔主秘寶幾乎沒差（排除「塔主池被稀釋」）；拿掉全部新秘寶菲菲回升 0.8、拿掉新忍具回升 0.75，都只補回一部分。剩下的主要是亂數走向變了——店長私藏那一格多擲一次骰，同一顆種子之後的地圖、戰利品都不一樣，前後兩份已經不是逐局配對的比較（兩份獨立 600 局的差，標準誤約 0.9 層）。沒有觸發「停下來處理」，列給主控留意。量測腳本 `scratchpad\content\b2mech\bal.ts`，修前打包檔 `bal_before.mjs` 留著可重跑。

## 六、實機（Playwright，`vite preview --port 5371`，`channel:'chrome'`、每個 context 全新暫存設定檔，只開本機；截圖在 `scratchpad\content\b2mech_shots\`，我逐張用 Read 看過）
- 四隻各塞新秘寶、打兩場三隻老鼠（`pw_solo.js`）：狀態列右下角數字（撲滿 2、木人樁 8，放大確認讀得到）、師門湊成提示（球球、菲菲）、套組說明「【師門 2／3】」、暗器匣第三張後閃光與紀錄、第 3 回合沙漏多 1 顆（4/3）、第 4 回合線香隱身、木人樁第 10 張「傷害加倍」、撲滿倒錢提示（噹噹 623→648）。
- 每支新忍具各用一次：迷魂香（丟出去的飛行物、牌子「攻 4（迷魂：打同伴）」、下一拍「打中了小老鼠兵」）、便當（「下回合飯糰 +2」→ 下回合 5 顆）、替換符（選單換出「殘影分身＋」）、照妖鏡、回魂香（牌子）、傳功丹。
- 定點（`pw_focus.js`、`pw_phone.js`）：鐵壁（蜷縮 25 → 反彈 +5、名牌「鐵壁」）、五毒譜（6 層毒一跳 8）、滿月劍意（蓄氣 9→12、紀錄「下一張攻擊牌傷害加倍」）、收鞘墜（第三張平斬後換回 1 顆）、影分身卷軸（閃兩下 → 下回合 7 張、5/3 飯糰）、封面圖鑑師門那一區（集到 2／3、酒葫蘆剪影）與兩個限定區。
- 罐頭鋪：第一關店長私藏（帳本、批發箱）、欠條進門提示、噹噹帶帳本＋批發箱的劃價（忍具 40→10）與會員價放生鈕；**第二關七格（珍品架＋私藏）第一版擠出舞台**（左邊被老闆蓋住、右邊剪刺鉗切掉），加了 `.seven` 縮一級後桌機 216～1257 像素、手機橫拿特價不再折兩行（`focus_shop_act2_seven*.png`）。
- 不眠香爐打盹：紀錄「球球躺了一下，但沒有回血。」＋「那個香爐一直冒煙，燻得我整晚睡不著喵。」（台詞抓自畫面；那一張泡泡的截圖晚一拍沒拍到，文字與測試都有）。
- 連線（`pw_coop.js`，兩個分頁貼碼直連、球球開房＋菲菲加入）：球球對中間那隻丟迷魂香、菲菲喝傳功丹（球球手牌 6→8），兩邊按結束回合：迷魂那隻打中右邊那隻（27 血），**兩台的魔物與玩家數字逐項相同、沒有斷線橫幅、主控台零錯誤**。
- 五個腳本的錯誤紀錄（頁面例外、主控台錯誤、HTTP 4xx/5xx）都是空的。本機伺服器 5371 我自己開的那幾個行程看過命令列才關，現在沒有東西在聽。

## 七、我替使用者做的決定
1. **木人樁照殺戮尖塔的鋼筆尖**：第 10 張那一張加倍（提案寫「下一張」；數到 10 就加倍、不用把旗標帶到下一張，也不會卡在回合結束時失效）。圖示顯示 0～9。
2. **照妖鏡多拔隱身**：提案寫「虛化與潛水」，但魔物躲起來靠的是隱身（九隻會自己掛），潛水只有鏡中球球學牌才會有；只拔那兩個的話這支幾乎沒場合用。
3. **迷魂香**：只改「攻擊」（給你的減益、塞牌、偷魚照舊）；同伴照站位挑（右邊優先、不擲骰）；沒有同伴就打空（不打自己——打自己的話關主一招 36 點打回自己身上太強）；打倒算下香的那一位。
4. **替換符換出忍術池**（不含絕學），升級版、戰鬥亂數、只在這場。
5. **滿月劍意用「灌滿那一刻」**（提案表把它列在「時機」），已經滿著再加不算。
6. **鐵壁量的是你按結束回合那一刻**（魔物還沒打），蜷縮不減；影分身卷軸的「抽 1 張」落在下回合（閃避都在魔物回合）。
7. **五毒譜看下毒的那一位**（連線時各看各的）。撲滿倒下的人也照樣數（數的是走了幾格）。
8. **店長私藏照劇本「多一格、五成」**，不是提案的「第三格改擺」（劇本是取得管道的準），買過的記在旗標、被事件拿走也不再擺。
9. 價格：帳本、批發箱、零錢罐、特價、錢袋全部相乘最後捨入一次；帳本買的就是帳本自己時不打折（還沒拿到）。
10. 五毒譜 1→2 點、影分身卷軸加下回合 1 顆飯糰（見第二節）；照妖鏡喝法放寬。
11. 第一筆暫用的 `wip` 旗標接好之後整個拿掉，不留死欄位。

## 八、需要主控決定
1. **店主的帳本的圖示是一把算盤**（美術 art2 照原名「店主的算盤」生的），名字照裁決改成「帳本」之後圖文對不上——要不要重生一張帳本圖（鍵可以沿用 `codex/relic_shop_abacus` 換檔，或另開鍵）？
2. **滿月劍意機器人量不準**（+0.2 層、每場 0.08 次）：要接受「真人才會囤氣」，還是把門檻改成「蓄氣 10 以上」這種機器人也碰得到的？
3. 撲滿、會員卡、批發箱、欠條偏弱（+0～+0.9），量尺對它們本來就量不準；要不要直接加數字（例如撲滿 25→35）？
4. 平衡快量三隻小降（−0.7～−1.2，平均），都在 ±2 內；合併後建議主控用別的種子再看一次。
5. 事件畫面的「拿到了什麼」清單沒顯示師門集到幾件（那支是 b2event 的範圍，見上面「給 b2event」）。

## 九、誠實說明
- 量尺用的基準是這個分支現在的樣子（店長私藏多擲骰之後），舊的 95 件沒重量——它們在分數表裡還是第〇／一批量的數字，跟這 16 件不是同一次跑出來的（種子、局數一樣，引擎差一點點）。
- 師門套組的加成量尺量不到（一件一件量），機器人那份分數是借飯糰袋的層差推的，不是實測。
- 平衡快量是在拿掉 `wip`、調完兩件、放寬照妖鏡之後量的最後一版；中間那一版（照妖鏡改之前）數字差不到 0.1。
