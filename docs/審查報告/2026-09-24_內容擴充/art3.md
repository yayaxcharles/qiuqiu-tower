# 美術代理 art3 報告：內容擴充第二批事件插圖 A 組（2026-09-23）

worktree `F:\ClaudeWork\qq-art3`，分支 `c0923-art3`（從 `c0923-int` `3448f06e` 開出）。
範圍＝劇本 `design2_事件劇本.md` 第三節（郵差鴿鏈）、第四節（影子鏈，含新9）、第六節（連線限定 3 篇）。第五、七節沒碰（art4 的）。
只放圖、不登記素材清單；遊戲程式與內容資料一行都沒動。改了兩支測試（永久規則，見第三段）。

## 一、成果

| 篇 | 張數 | 生圖次數 | 重生（原因） | 聯絡表（500 像素，自己用 Read 看過） |
|---|---|---|---|---|
| 配角設計圖（郵差鴿、老爺爺、師父的舊木劍） | 3（參考用，不進 public） | 4 | 老爺爺 1：第一次毛髮畫得太寫實、跟遊戲的 Q 版不搭 | — |
| A1 `pigeon_lost` 迷路的郵差鴿 | 12 | 13 | 1：封封 r1 地上多畫了一個郵差包（鴿子只有一個包，應該背走） | `c2a_pigeon_lost_500.png` |
| A2 `pigeon_grandpa` 給爺爺的信 | 16 | 18 | 2：噹噹 r1、封封 r2 屋頂與燈籠頂到圖的上緣被切（閘門擋下） | `c2a_pigeon_grandpa_500.png` |
| A3 `pigeon_reply` 風裡的回信 | 16 | 16 | 0 | `c2a_pigeon_reply_500.png` |
| B1 `shadow_loose` 影子不見了 | 12 | 16 | 4：r1 四張「影子回到腳下」看不出地上有影子（那張圖的重點），而且火把還連著牆上的鐵架 | `c2a_shadow_loose_500.png` |
| B2 `shadow_study` 偷練的影子 | 8 | 8 | 0 | `c2a_shadow_study_500.png` |
| B3 `shadow_truth` 影子的真面目 | 12 | 12 | 0 | `c2a_shadow_truth_500.png` |
| 連線 3 篇（橋、翹翹板、流星） | 9（＋3 個 `_r1` 共用鍵） | 14 | 2：橋 r2 整座橋只剩兩塊板（應該只少橋頭兩塊）、翹翹板 r0 架子頂到上緣被切；另外 3 次是連線被伺服器切斷（WinError 10054），原樣重跑就過 | `c2a_coop_rope_bridge_500.png`、`c2a_coop_seesaw_500.png`、`c2a_coop_shooting_star_500.png`、三篇合一 `c2a_coop_all_500.png` |
| **合計** | **85 張進 public**（＋3 張設計圖） | **101 次**（成功 98、斷線 3） | 內容重生 10 次（9 張事件圖＋1 張設計圖） | 都在 `scratchpad\content\art3\sheets\` |

- 張數對得上劇本第十節：12＋16＋16＋12＋8＋12＝76，連線 3×3＝9。連線 ①② 共用一張圖，`_r1` 登記時指到 `_r0` 的檔（主控裁決 5；查過測試沒有「一檔一鍵」的限制，實測登記後全套不紅，所以沒複製）。
- 做法：gpt-image-1.5 真透明（codex-oauth）。參考圖＝這隻的新版待機第 1 格（長相只照它）＋同一隻、同一段塔的既有事件圖（塔下＝`seclusion`、塔中＝第一批 `daxia_chest`、塔頂＝第一批 `daxia_lastpage`）；配角先生設計圖定稿再附給每一張（郵差鴿、老爺爺、木劍三集四隻長一樣）；影子鏈多附這隻自己的影子魔物待機（`shadow_cat`／`shadow_<角色>`）與木樁人 `wood_dummy`。連線三篇只附「既有事件圖裁掉主角的那一塊」當畫風參考。原檔在 `tools/motion-art-source/c2a/`（png 不進版控；`prompts.json`、`picks.json` 進版控）。
- 看圖的項目：新版長相（球球奶油白灰褐紋深藍頭巾、菲菲藍眼深棕臉斑＋紫蝴蝶結馬尾、噹噹黑白＋銅護臂、封封橘虎斑＋紅圍巾＋直劍）、每張只有該出現的主角與劇本寫的配角、沒有字（信封、信紙、蠟筆畫、刻字處都只有爪印或圖）、沒有多的手、透明邊沒切到，逐段對劇本的「圖面」與四份文字（例：A2 ① 四隻各自的反應、A2 ② 信尾各自的記號、A3 ② 蠟筆畫各自被誇大的特徵、B1 ① 各自撞到的地方、B3 木劍四份同一把）。`check_haze.py` 掃新圖 0 張灰膜。
- **閘門強制收 5 張**：`event_pigeon_lost`、`event_dangdang_pigeon_lost`、`event_pigeon_grandpa`、`event_pigeon_grandpa_r1`、`event_feifei_shadow_loose`。只有「透明比例低於 30%」那一項沒過（27～30%，場景帶了比較大片的地板與牆），四角全透明、主體沒碰邊、放大看過不是背景沒去掉。有其他閘門問題的一律重生（上表那 3 張切邊）。

## 二、鍵名對照表
`scratchpad\content\batch2_event_art_keys_A.md`：命名規則、每一張的鍵名／檔名／對應劇本哪一段哪個選項／畫了什麼、接線步驟、實測數字。
登記一行：`python tools/gen_content_batch2_events_a.py register`（88 個鍵，兩格縮排只加行）。

## 三、跟派工單不一樣、或要注意的地方

1. **沒有加「第二批程式接線前暫放」名單**：孤兒檢查只看清單裡的鍵，這批沒登記就不會被當孤兒，現在全綠，不需要。
   但**登記一定要跟事件接線放在同一筆提交**：實測只登記不接線 → 孤兒檢查紅 88 個鍵，而且 9 張底圖（球球版 6 張主圖＋連線 3 張主圖，349 KB）會變成每個人的首載（主圖要靠 `events.ts` 裡有那篇才會被 `eventMainKeys()` 延後）。接線之後首載 +0。
2. **改了兩支測試（永久規則）**：`tools/feifei_stills.test.ts`、`tools/dangdang_stills.test.ts` 的「退回球球的事件圖只准變少」把 `bg/event_coop_` 開頭排除（純場景、四隻看同一張）。確認會紅：登記全部新圖後把這兩行改回舊寫法 → 兩條都紅（缺口 12）；改回來全綠。現在還沒登記，這兩行不影響任何數字。
3. **新9 的處理**：單人球球的影子鏈第二集換成屋頂那篇，所以 `shadow_study` 的球球版兩張實際上看不到；仍照畫（它是 `eventArtKey` 的底鍵，新9 沒做或拿掉時球球也有自己的圖）。屋頂那篇是第一批的圖，沒動。
4. **木劍跟秘寶圖示差一點**：事件圖照劇本畫了銅片＋兩顆釘、劍尖白布；art2 的圖示 `relic_master_wood_sword`（並排看過）只有淺木、藏青布握把、紅繩，劍身較寬。整體看得出是同一把，細節不逐項一致。
5. 沒做的：沒有開本機伺服器實機看（事件還沒接線，遊戲裡叫不出這些圖）。接線後請實機看一次，尤其連線三篇兩邊放立繪之後的構圖。

## 四、驗收
- `npx tsc --noEmit -p .`：0 錯誤。
- `npx vitest run`：314 個檔、2741 條通過、9 條跳過。跑完照雷 5 收拾：`怪物工作檯.json` 只差日期、其他三份只差換行，都還原（這批沒登記，`分關載入.json` 沒有實質變化）。
- `npm run build && python tools/check_size.py`：圖片 9.04／10.60 MB（85.2%）、首載總計 9.71／11.30 MB（85.9%），大小 OK。
- 模擬登記（沒提交）：只登記 → 全套 2750 條只紅孤兒那 1 條；登記＋把 9 篇塞進孤兒檢查名單模擬接線 → `manifest_hygiene`、`feifei_stills`、`dangdang_stills` 18 條全綠。清單與 docs 都已還原。

## 五、需要主控決定
1. 登記要跟事件接線同一筆（第三段第 1 條），請交代給接線的程式代理。
2. 木劍圖示要不要補銅片與白布（第三段第 4 條）；我判斷 32～42 像素看不出來，不必。

## 六、`git log --oneline c0923-int..HEAD`
```
dfc20dd6 美術 c2a：「退回球球的事件圖」缺口檢查不把連線限定事件的純場景算成缺口
f87a6830 美術 c2a（內容擴充第二批事件圖 A）：連線限定三篇 9 張（純場景）
bc701fb8 美術 c2a（內容擴充第二批事件圖 A）：影子的真面目 12 張
dee25f5c 美術 c2a（內容擴充第二批事件圖 A）：偷練的影子 8 張
a1fec5d3 美術 c2a（內容擴充第二批事件圖 A）：影子不見了 12 張
42f7357a 美術 c2a（內容擴充第二批事件圖 A）：給爺爺的信 16 張
a6f6fd17 美術 c2a（內容擴充第二批事件圖 A）：風裡的回信 16 張
161c70f4 美術 c2a（內容擴充第二批事件圖 A）：迷路的郵差鴿 12 張＋生圖工具
```
