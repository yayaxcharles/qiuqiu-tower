# 爪破魔塔 內容擴充（秘寶、忍具、事件）：每個代理的共同規則

## 背景
- 網頁牌組肉鴿「爪破魔塔」（TypeScript＋Vite＋vitest，殺戮尖塔式）。四隻貓：球球 ninja、菲菲 feifei、噹噹 dangdang、封封 fengfeng。單人＋兩人連線（兩台各跑決定性引擎，只傳玩家動作）。
- 使用者 2026-09-23：「角色都不錯了，缺秘寶、忍具跟事件的變化性，這些才該補」。裁定：事件文字改成用到才下載（好）、塔主池加代價型秘寶（要）、第〇～三批全做，依序做。
- **提案全文（你的清單與規格來源）**：`F:\ClaudeWork\qiuqiu-coop\docs\審查報告\2026-09-23\內容擴充提案_秘寶忍具事件.md`。動手前讀你負責的那幾節。名稱、代號（`snake_fang` 這類）照提案，要改名先寫進報告「需要主控決定」。
- 倉庫的雷與規矩另見 `C:\Users\yayax\AppData\Local\Temp\claude\C--Users-yayax\a6460c69-085f-4478-bf45-7ca4bf6a4a03\scratchpad\w0923\共同規則.md`（**也要整份讀**：worktree 範圍、CRLF、不裝 @types/node、不用 happy-dom、畫面層不寫喵、全套測試會重寫 docs、每個修正補會反向變紅的測試、提交格式、回報格式）。本檔是在那份之上**追加**的。

## 追加規矩
1. **單機版不動**：只在連線版倉庫（coop）開的分支工作；不碰 `F:\ClaudeWork\qiuqiu-tower`。
2. **連線決定性**：新效果只能用引擎的種子亂數；新增會影響結果的狀態（計數器、旗標、稀有度抽法）要收進連線指紋 `src/net/hash.ts`；畫面層不可改引擎狀態。
3. **存檔相容**：舊存檔沒有新欄位時要有預設值、讀得進來（寫測試：用一份舊格式存檔讀回來）。
4. **機器人要會用新東西**：新秘寶要有評分（`smartbot.ts` 的 `RELIC_RATING` 或第〇批做好的量尺）、新忍具要在 `maybePotion` 有使用規則、新事件選項要有估值。過去的教訓：機器人不會用的機制會把整份平衡量歪。
5. **首載體積**：推送閘門會跑 `tools/check_size.py`，超標就擋。新圖一律走延後下載（不進首載），新文字放在延後載入的模組。打包後自己跑一次 `python tools/check_size.py` 看數字。
6. **使用者的四道硬門檻**（每批推之前主控會驗，你做的時候就要守）：角色大小（比頭不比外框，跟同一隻待機差 ≤5%）、貓窩位置不變、動作流暢（不閃白、不露舊立繪、不跳位）、事件圖片（圖文對得上、是這隻貓自己的版本、沒打算改的圖一張都不能變）。
7. **角色聲音**：球球句尾常帶「喵」、大俠貓的大徒弟；菲菲是師妹、女生，旁白用「她」，喊球球「師兄」；噹噹跟大俠貓**不是師徒**，喊「大俠貓」；封封**不講喵**、用劍、有蓄氣。先讀 `src/content/dialogue.ts`、`fengfeng-dialogue.ts` 抓語氣。
8. **生圖**（有生圖的代理）：`python "%USERPROFILE%\.codex\skills\codex-ppt\scripts\image_gen.py" generate|edit --backend codex-oauth ...`。`--model` 會被伺服器忽略（實際是新模型）；**回來的尺寸不照要求**，要自己裁縮到同類既有圖的尺寸與格式；要透明傳 `--model gpt-image-1.5 --background transparent`。一張約 20～45 秒，可 3～4 張並行，遇「at capacity」等一下重試。參考圖一律附**新版**畫風的同類圖＋角色新版待機（不用舊立繪當長相參考）。先讀 `tools/art_rules.py` 的七道提示詞規則。每批排 500 像素聯絡表，**自己用 Read 逐張看**（長相、畫風、多餘的手或字、透明邊、圖文對不對），不合格重生。生圖原檔放 `tools/motion-art-source/<批次名>/` 並加進 .gitignore。
9. 開本機伺服器用派工單給的埠號；只關自己開的行程（先看命令列）。實機用 Playwright `channel:'chrome'`、每個 context 全新暫存設定檔，網址加 `?debug` 才有 `window.__app`；**不開正式網站、不寫它的 localStorage**。
