# 美術代理 art4 報告：內容擴充第二批事件圖 B 組（2026-09-23）

worktree `F:\ClaudeWork\qq-art4`，分支 `c0923-art4`（從 `c0923-int` `3448f06e` 開出）。範圍＝劇本第五節（關卡限定 9 篇）＋第七節（條件選項 8 條的新結果圖）。
只放圖、寫生圖工具；**素材清單、遊戲程式、內容資料、測試一行都沒動**（`git diff --name-status 3448f06e..HEAD` 全部是新增：156 張圖＋工具＋`c2eb` 的 .gitignore／prompts.json／picks.json）。

## 一、成果

| 篇 | 張數 | 重生 | 聯絡表（`scratchpad\content\art4\sheets\`，自己用 Read 逐張看過） |
|---|---|---|---|
| 牢裡的山賊 `cell_bandit`（塔下） | 12 | 0 | `c2eb_cell_bandit_500.png` |
| 老鼠開的澡堂 `rat_bathhouse`（塔下） | 12 | 0 | `c2eb_rat_bathhouse_500.png` |
| 地窖裡的冬眠熊 `bear_cellar`（塔下） | 12 | 3 | `c2eb_bear_cellar_500.png` |
| 書庫的梯子 `library_ladder`（塔中） | 16 | 3 | `c2eb_library_ladder_500.png` |
| 送上樓的便當 `tower_kitchen`（塔中） | 16 | 1 | `c2eb_tower_kitchen_500.png` |
| 木人巷 `wooden_men_alley`（塔中） | 12 | 0 | `c2eb_wooden_men_alley_500.png` |
| 掉下來的星星 `fallen_star`（塔頂） | 16 | 4 | `c2eb_fallen_star_500.png` |
| 風鈴長廊 `wind_chimes`（塔頂） | 16 | 5（1 次是連線被斷） | `c2eb_wind_chimes_500.png` |
| 魔氣結晶 `miasma_crystal`（塔頂） | 12 | 0 | `c2eb_miasma_crystal_500.png` |
| 條件選項 8 條（各 4 張） | 32 | 2（噹噹「師父的影子」） | 每篇 `c2eb_<代號>_500.png`、跟既有主圖上下對照 `c2eb_<代號>_vs_main.png`、合併 `c2eb_cond_all_500_1.png`／`_2.png` |
| **合計** | **156** | 生圖 174 次（採用：第 1 次 140 張、第 2 次 15 張、第 3 次 1 張；失敗 1 次） | 27 張聯絡表 |

張數照劇本第十節總表：關卡限定 124 張（9 篇×4 隻×〔主圖＋有東西的結果〕）＋條件選項 32 張。進戰鬥與無效果的選項不畫（`event_result_art.test.ts` 的 `shouldHaveArt`）。

**重生的原因（都是「圖文對不上」或「被切到」，沒有長相走鐘）**
- 冬眠熊①（球球、菲菲、封封）：被熊壓扁時看不到布包 → 補「布包要看得見」。
- 書庫（噹噹主圖、噹噹與封封②）、風鈴長廊（噹噹主圖、封封①、球球②）：書架／屋簷頂到圖的上緣（閘門擋下）→ 補「整個場景留在圖內」。
- 便當②（球球）：背上一個、手上又捧一個 → 補「只有背上那一個」。
- 星星③（四隻）：看不出「暖光從上面灑下來」→ 補光束。光束是半透明的，疊在第三關事件底圖上看過，不是灰膜。
- 風鈴③（菲菲）：她舉手但風鈴還掛在屋簷上 → 補「風鈴拿在她手上」。
- 師父的影子新結果（噹噹）：前兩次影子坐著、腳沒碰到他的腳跟 → 第三次改成影子站在他身後、腳尖點腳跟。

**看圖檢查的項目**：長相是新版（球球奶油白底灰褐紋＋深藍頭巾、菲菲藍眼深棕臉斑＋蝴蝶結馬尾、噹噹黑白＋銅護臂、封封橘虎斑＋紅圍巾＋直劍）、每張只有那一隻主角加劇本寫的配角、沒有字（牌子、書背、旗子、紙條只有圖案或爪印）、透明邊、圖文對得上。特別放大看過的：魔氣結晶①倒影的眼睛是紫的、本體眼睛照原色（球球黑、菲菲藍、噹噹與封封琥珀）；睡著的守衛新結果的暗影裡只露眼睛和一隻爪。`check_haze.py` 全櫃 2206 張 0 灰膜。

**三關畫風**：塔下＝灰石牆＋火把暖光（參考 `seclusion`）、塔中＝暖棕木造＋紙燈籠紙拉門（參考第一批 `daxia_chest`）、塔頂＝夜裡藍灰石台＋木欄杆＋遠處紫霧（參考第一批 `daxia_lastpage`）；提示詞每一關各有一段材質光線描述並互相排除（塔中寫明「不要石牢牆、不要火把、不要夜空」）。聯絡表並排看分得出來。
**條件選項**：參考圖②一律用那篇**既有、同一隻**的主圖，場景與配角（大橘貓、三花貓、白貓與黑衣師弟、灰貓商人、小黑貓、影子）照它畫，所以跟主圖是同一篇。

## 二、做法
- 工具 `tools/gen_content_batch2_events_b.py`（照 `gen_content_batch1_art.py` 延伸，閘門、清碎點、補 4:3、清單寫法、聯絡表直接借它的）：gpt-image-1.5 真透明（codex-oauth），參考圖① 這隻的新版待機第 1 格、② 同關／同篇既有圖、③④ 視需要（冬眠熊、飯糰怪、木人、老鼠的魔物待機圖；風鈴、魔氣殘片、師父的斗笠、鈴鐺的秘寶圖示；師父的長相裁自 `still_teach`）。挑定後 560×420、WebP quality 80。
- **`pick --force` 只放行一種閘門**：「透明比例 20～30%」。這一批的場景（牢房、澡堂、書庫、木人巷）比第一批大，14 張的透明處落在 22～30%；既有事件圖本來就有 20% 的（`event_dangdang_seclusion` 20%、`event_cell_bandit` 系列 19～24%）。四角不透明、主體碰邊一律不放行（工具裡寫死，書庫噹噹主圖就是被這條擋下重生的）。被放行的 14 張名單在 `picks.json` 的 `gate` 欄位。
- 生圖原檔在 `tools/motion-art-source/c2eb/`（.gitignore 擋 png 與 `_ref/`），提示詞與選定紀錄 json 進版控。

## 三、鍵名對照表
`C:\Users\yayax\AppData\Local\Temp\claude\C--Users-yayax\a6460c69-085f-4478-bf45-7ca4bf6a4a03\scratchpad\content\batch2_event_art_keys_B.md`：命名規則、156 張每一張的鍵與檔名、對應劇本哪一段哪個選項、接線時要做的事。表是從 `picks.json` 產生並對過張數（156），不是手抄的。

## 四、驗收
- `npx tsc --noEmit -p .`：0 錯誤。
- `npx vitest run`：314 個檔、2741 條通過、9 條跳過（沒登記所以衛生測試不會紅，沒加暫放名單）。跑完照雷 5 收拾：四份 docs 只差換行與日期，全部還原。
- `npm run build && python tools/check_size.py`：大小 OK。圖片 9.04／10.60 MB、首載總計 9.71／11.30 MB，跟開分支前一樣（156 張沒登記，打包後落在「未引用圖」）。
- **試登記一次（量完已還原）**：`register` 後衛生測試「事件插圖沒有沒人用的孤兒」紅（事件還沒接線，預期中）；147 張被 `dump_monster_acts` 算成延後，9 張球球版主圖（420.8 KB）算首載 → 圖片 9.46 MB、首載總計 10.13 MB，仍在預算內。詳見對照表第三段。
- 沒做的：沒開本機伺服器實機看（事件還沒接線，遊戲裡叫不出這些圖）；「疊在事件底圖上」只合成看了星星③一篇。接線後請程式代理或主控實機看一次。

## 五、需要主控決定
1. **魔氣殘片的代號**：劇本寫 `miasma_shard`，art2 的圖示取名 `codex/relic_demon_shard`。我的魔氣結晶圖照那張圖示畫；秘寶資料接線時兩邊要用同一個代號。
2. **登記時機**：建議跟 `events.ts` 接線同一筆提交跑 `register`（先登記會讓衛生測試紅）。如果要我這邊先登記，就得照第一批加「第二批程式接線前暫放」名單，我沒做，因為派工單說只放檔。
3. 劇本「很貪心的商人」新結果圖寫「眼鏡滑到鼻尖」又寫「布包角落夾著一副眼鏡」，兩句互相矛盾；我照結果文字（眼鏡差點被包進去）畫成眼鏡夾在布包角、商人臉上沒眼鏡。

## 六、`git log --oneline 3448f06e..HEAD`
```
ce107daa 美術 c2eb（內容擴充第二批事件圖 B 組）：條件選項 noisy_kitchen 4 張
b1c81926 美術 c2eb（內容擴充第二批事件圖 B 組）：條件選項 lost_kitten 4 張
c8cec47d 美術 c2eb（內容擴充第二批事件圖 B 組）：條件選項 old_master_ghost 4 張
7b4e63e6 美術 c2eb（內容擴充第二批事件圖 B 組）：條件選項 greedy_merchant 4 張
0e661769 美術 c2eb（內容擴充第二批事件圖 B 組）：條件選項 heavy_door 4 張
1cf6eafd 美術 c2eb（內容擴充第二批事件圖 B 組）：條件選項 sparring_cat 4 張
89c2b55f 美術 c2eb（內容擴充第二批事件圖 B 組）：條件選項 medicine_cat 4 張
0426e123 美術 c2eb（內容擴充第二批事件圖 B 組）：條件選項 sleeping_guard 4 張
164c1385 美術 c2eb（內容擴充第二批事件圖 B 組）：「風鈴長廊」16 張
fec801b1 美術 c2eb（內容擴充第二批事件圖 B 組）：「掉下來的星星」16 張
056fd96c 美術 c2eb（內容擴充第二批事件圖 B 組）：「魔氣結晶」12 張
522217a5 美術 c2eb（內容擴充第二批事件圖 B 組）：「送上樓的便當」16 張
e5838b4c 美術 c2eb（內容擴充第二批事件圖 B 組）：「書庫的梯子」16 張
9aa17ec5 美術 c2eb（內容擴充第二批事件圖 B 組）：「木人巷」12 張
e4eef412 美術 c2eb（內容擴充第二批事件圖 B 組）：「地窖裡的冬眠熊」12 張
5f31af65 美術 c2eb（內容擴充第二批事件圖 B 組）：「老鼠開的澡堂」12 張
97545819 美術 c2eb（內容擴充第二批事件圖 B 組）：生圖工具＋「牢裡的山賊」12 張
```
