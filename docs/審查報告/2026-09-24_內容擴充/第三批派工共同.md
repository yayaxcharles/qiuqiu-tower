# 第三批程式接線：四條線共同說明

- 四條線：`b3bless`（開局祝福）、`b3qmark`（問號格變化＋行腳商＋路邊紙箱＋伏擊）、`b3shop`（罐頭鋪店主輪替）、`b3rare`（稀有事件 5 篇＋魔氣秘寶與淨化＋新秘寶 9＋淨化版 6）。各自的 worktree `F:\ClaudeWork\qq-<代號>`、分支 `c0923-<代號>`，都從 `c0923-b3base`（`25952e4d`）開出：已含第〇～二批全部內容，**第三批 130 張圖已登記進素材清單**（`tools/gen_content_batch3_art.py`），事件圖編號在 `tools/manifest_hygiene.test.ts` 的 `BATCH3_PENDING` 暫放名單——**接好哪一項就把它從名單拿掉**（有測試會提醒）。
- **設計稿（唯一來源）**：`scratchpad\content\design3_大結構.md`；**主控裁決**：`scratchpad\content\design3_主控裁決.md`（蓋過設計稿第十節）；圖的鍵名：`scratchpad\content\batch3_art_keys.md`（美術報告 `art5.md`；貓薄荷田那篇是「大俠貓本人不出場」替代版，圖面描述在對照表第四段，文字要照它對齊）。
- **首載預算**：登記後首載圖片 11.15／10.60 MB、首載總計 11.89／11.30 MB 超標（`python tools/check_size.py`，先 `SITE_NAME=qiuqiu-tower-coop npm run build`）。超出的約 0.8 MB 是祝福物品圖示（不在 `isItemIcon` 判斷裡）、店主立繪、祝福主圖、問號格三種的球球版；稀有事件主圖接進 `events.ts` 後會自動延後。**你那條線負責的圖要改成用到才下載**，完成時自己打包量一次，寫進報告。四條都做完、合在一起後要回到預算內。
- 新的隨機一律用**分支亂數**（不推整局亂數，既有種子不位移），會影響結果的新狀態進連線指紋、存檔有預設值、舊存檔讀得回來。機器人要會用、會估。
- 其他線也在改 `src/engine/run.ts`、`map.ts`、`events.ts`、`relics.ts`、畫面檔：**改動集中、不要搬段落、不要重新縮排**，方便合併。
- 驗收：共同規則全部（**全套測試 0 errors**，看 vitest 的 `Errors` 那一行）；每一項補會反向變紅的測試；實機截圖自己用 Read 看過（使用者四道門檻：大小、貓窩、流暢、事件圖圖文對得上且是這隻貓的版本）。
