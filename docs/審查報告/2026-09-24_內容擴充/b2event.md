# b2event：內容擴充第二批「事件」回報（2026-09-23）

工作副本 `F:\ClaudeWork\qq-b2event`，分支 `c0923-b2event`（從 `c0923-int` @1ab83fc8 開出）。埠 5381（預覽伺服器已關，子行程也確認關掉）。

## 〇、先講卡住的地方（要主控處理）

**合併 `c0923-b2mech` 與 `c0923-int` 被權限系統擋下**（自動模式判讀器理由：修改共用資源）。我沒有繞道（沒有挑檔案複製、沒有摘提交），所以以下三件**還沒做**：

1. 三個事件限定秘寶的選項現在還是「退回給 60 條小魚乾」：`cell_bandit` ①（`bandit_iou`）、`miasma_crystal` ①（`miasma_shard`）、`shadow_truth` ②（`master_wooden_sword`＋回 10 血）。代號已照裁決寫好（`src/content/events-batch2.ts:91、101、205`），合進來就生效，不用改事件資料。`wind_chimes` ③ 的 `wind_chime` 本來就有，已經會給。
2. 事件畫面「拿到了什麼」那一欄改用 `relicLongText`：這支函式只在 b2mech 分支上，要合進來才能改。
3. 全套測試現在是 **331 檔全過、3027 條過、6 個錯誤**；6 個錯誤全在 `tests/ui/main_motion_boot.test.ts`（背景音樂假模組少 `deferBgm`），就是 `c0923-int` 已修的那個，跟我的改動無關（開工前的基準也是這 6 個）。

**我先用不動分支的方式預演過合併**（`git merge-tree`，只算結果、不改任何分支或檔案）：

- `c0923-int`：**沒有衝突**。
- `c0923-b2mech`：4 個檔各 1 處衝突，都很小：
  - `src/engine/combat.ts`、`src/engine/run.ts`：只有 `import` 那一行，兩邊的名字取聯集即可（combat 要 `cards`＋`encounterSkin`；run 要 `relics`＋`applyCarriedEffects`）。
  - `src/net/hash.ts`：兩邊各加一個指紋欄位（我的 `nextFight`、他的 `counters`），兩行都留。
  - `tests/engine/bot.test.ts`：兩個固定種子的錨點數字，合完重跑重填。

**合併後的收尾清單**（放行後我可以接手，約半小時）：
1. `git merge c0923-int` → `git merge c0923-b2mech`，照上面解衝突。
2. `src/ui/screens/event.ts:78`（`gainsNode` 的效果行）與 `:135`（`gainRows` 的說明）：秘寶改用 `relicLongText(d, me(run, seat).relics)`，兩支函式多帶一個「身上秘寶」參數。
3. 補測試：事件裡每個 `relicId` 都對得到 `relicById`；三個選項實際拿到秘寶、不是退回小魚乾；故意把代號改錯要變紅。
4. 全套測試（目標 0 錯誤）、型別檢查、`check_size`、錨點重填；平衡 600 局×4 再跑一次（三件新秘寶會進場）。
5. Playwright 重拍這三篇×4 位的結果頁，逐張看。

## 一、改了什麼（提交 3 筆）

```
b06e0380 測試補一條：條件選項的效果照劇本（交出鈴鐺、交出忍具、整包買下 150 條）
97cb7699 實機驗收修正：連線座位不對稱的選項只有一邊要挑牌時，兩台卡在「等同伴挑完」
c9860e73 內容擴充第二批事件：兩條事件鏈、關卡限定九篇、連線限定三篇、八條條件選項（241 張圖同一筆登記）
```

### 引擎機制（劇本的新1、新2、新4～新9；新3、新10 是 b2mech 的）
- **條件選項（新1）**：`src/engine/eventcond.ts`（新檔）。`cardTags`:35、`deckTagCount`:53（不算起手牌、升級前後算同一張）、`choiceGate`:94、`visibleChoices`:114。型別 `ChoiceCond` 在 `src/engine/types.ts:703`，`EventChoice.requires/requiresLabel/bySeat`:721-729。
  - 連線的算法：「養成型」（牌組標籤、身上秘寶）任一位站著的達成就出現；「付錢型」（小魚乾、忍具滿）每一位都要達成；旗標看整局。條件沒達成的選項不顯示；達成的在按鈕上標【標籤】＋一行「因為：誰身上帶著什麼」。
- **給指定秘寶／交出指定秘寶／交出最便宜的忍具／帶進下一場（新2、新4、新5、新6）**：`src/engine/run.ts:1198、1206、1213、1224`；交出秘寶抽成 `dropRelic`:1244（跟原本的 `loseRelic` 共用還原生命上限、忍具格）。下一場開打時套用：`run.ts:221` → `combat.ts:159 applyCarriedEffects`。`nextFight` 進指紋 `src/net/hash.ts:128`、舊存檔檢查 `src/engine/save.ts:172`。
- **連線限定＋座位不對稱（新7）**：`seatTextIndex`:123、`choiceOrder`:128（座位 1 把「我拿」排第一，投票照舊用絕對索引）、`choiceEffectsFor`:135。有人倒下時不對稱選項不出現。事件畫面放兩位立繪：`src/ui/scene.ts`（`portrait2`）、`src/ui/screens/event.ts:202`。
- **單人限定、球球換成屋頂那篇（新8、新9）**：`src/engine/map.ts:314-316`；換關時標後集 `run.ts:541`。
- **事件權重**：`map.ts:410 weightedOrder`（不放回的加權抽；全部權重 1 時跟原本 `rng.shuffle` 一模一樣，舊種子的地圖不變）。
- **影子鏈自己的一場戰鬥**：`src/content/enemies.ts:1300-1302` 三筆 `shadow_duel*`（數值照鏡子走廊那三筆），`SHADOW_SKINS`:1602、`encounterSkin`:1626；名牌是「球球的影子／菲菲的影子／噹噹的影子／封封的影子」，開場白不講鏡子，用的是鏡像對手的換裝。`combat.ts:84` 套上。
- **學完招接著挑牌升級（`then`）**：單人、連線都接好（師父的影子【師門】那條用得到）。

### 內容
- 18 篇新事件：`src/content/events-batch2.ts`（郵差鴿 3 集、影子 3 集、關卡限定 9 篇、連線限定 3 篇），文字照劇本逐字轉。
- 8 條條件選項接在既有事件上，屋頂上的影子改打 `shadow_duel`：`src/content/events.ts`。
- 菲菲、噹噹、封封三位的版本＋條件提示句＋連線稱呼：`src/content/event-text-b2.ts`（延後載入，接進 `event-text.ts`）。
- 241 張圖用兩支登記腳本在**同一筆提交**登記進 `public/assets/manifest.json`；`docs/事件文案.md`、`docs/分關載入.json` 重新產生並提交。

### 機器人
- `smartbot`、`coopbot`、`bot` 都只看「看得到的選項」，每個新效果都有估值；座位不對稱照自己那一邊的效果估；鏈的第一集旗標估 12 分（`smartbot.ts:1334`），不然機器人永遠不會開鏈。`coopbot` 會投票、會處理不對稱結果。

## 二、做了哪些決定、為什麼

| 決定 | 理由 |
|---|---|
| 鏈的第一集（迷路的郵差鴿、影子不見了）權重 3 | 權重 1 時第三集只有約 3%～5% 的到第三關的局看得到；比過 1／2／3，選 3 讓開鏈又到第三關的局有 86% 走完 |
| 機器人把「開鏈」估 12 分 | 旗標本身沒有數值，不給分的話模擬裡鏈永遠不會開，量不到第三集出現率 |
| 連線不對稱選項：`choices[0]`＝座位 0 拿、`choices[1]`＝座位 1 拿 | 投票用絕對索引才不會分岔；畫面上每人各自把「我拿」排第一 |
| 單人球球影子鏈第二集換屋頂那篇（新9） | 主控裁決第 2 條；球球第三集的提示句改成講「屋脊後面」，不講練功房 |
| 連線三篇的 `_r1` 共用 `_r0` 的檔 | 登記腳本本來就這樣做、測試允許，沒有複製成兩個檔 |
| 幾個舊測試改成釘住遭遇 | 加權抽會改變地圖亂數的走法，兩條舊測試假設的第一場怪物變了；改成直接指定遭遇，測的東西不變 |

## 三、重複感：改前改後（機器人各跑 800 局，同一組種子；另跑 4000 局確認）

| 項目（不含 5F 固定事件） | 改前 800 | 改後 800 | 改前 4000 | 改後 4000 |
|---|---|---|---|---|
| 每局看到幾篇不同的事件 | 4.04 | 3.97 | 3.99 | 3.88 |
| 同一局重複遇到 | 0 | 0 | 0 | 0 |
| 相鄰兩局重疊率 | 12.0% | 9.5% | 11.3% | 9.0% |
| 相鄰兩局至少撞一篇 | 37% | 28% | 36% | 28% |
| 到第三關的局：相鄰重疊率 | 18.3% | 14.8% | 19.1% | 13.6% |
| 到第三關的局：至少撞一篇 | 72% | 68% | 77% | 61% |
| 到第三關的局：每局不同事件 | 7.17 | 7.25 | 7.21 | 7.19 |
| 郵差鴿走到第三集（到第三關的局） | — | 10.5% | — | 11.8% |
| 影子走到第三集（到第三關的局） | — | 11.2% | — | 12.7% |
| 開了鏈又到第三關的局走完第三集 | — | 76.2% | — | 86.1% |

每局事件篇數微降是因為平均爬的樓層少了約 0.6 層（見下節），不是抽法變差；到第三關的局篇數持平。

## 四、平衡（每位 600 局、連線兩組各 300 局）

| 對象 | 平均樓層 改前→改後 | 勝率 改前→改後 |
|---|---|---|
| 球球 | 24.18 → 23.73（−0.45） | 7.5% → 8.3% |
| 菲菲 | 27.19 → 27.44（+0.25） | 10.5% → 11.2% |
| 噹噹 | 24.52 → 23.61（−0.92） | 8.0% → 7.7% |
| 封封 | 23.07 → 22.69（−0.38） | 2.5% → 3.2% |
| 球球＋菲菲 | 22.35 → 22.28（−0.07） | 6.7% → 9.0% |
| 噹噹＋封封 | 19.28 → 19.15（−0.13） | 2.3% → 3.0% |

全部在 ±1 層內，沒有超過 ±2 的，不需另外調查。注意：三件限定秘寶現在是退回小魚乾，合進 b2mech 後要重跑一次。

## 五、測試

- 新測試 4 檔：`tests/engine/content_batch2_events_0923.test.ts`（47 條）、`tests/content/content_batch2_text_0923.test.ts`（29）、`tests/ui/content_batch2_event_ui_0923.test.ts`（13）、`tests/net/coop_event_byseat_0923.test.ts`（5，用 `LoopbackPair.hold` 驗兩台不分岔）。
- 全套：331 檔過、3027 條過、6 個錯誤（上述 `main_motion_boot`，合 `c0923-int` 就會消失）。型別檢查乾淨。
- `check_size`：首載程式 616.0／680 KB（90.6%，比開工前多約 41 KB，是球球版事件文字留在主程式裡）、首載圖片 9.04 MB 沒變、首載總計 9.77／11.30 MB。9 張球球版主圖與連線主圖都不在首載。
- **反向變紅**：41 條全部確認會紅（腳本 `scratchpad\content\b2event_work\redcheck*.py`，輸出 `redcheck*_out.txt`）：機制 31 條、連線卡住的修正 2 條、鏈與條件選項的內容接線 8 條。做法：腳本逐條把原始碼改壞一處、跑對應測試、確認變紅、再還原。
- 舊存檔：沒有 `nextFight` 欄位的舊檔照讀（有測試）；存檔形狀不對會被擋。

## 六、實機驗收（Playwright，`vite preview --port 5381`＋`?debug`，每個瀏覽器環境都是全新暫存設定檔）

- 單人四位 × 18 篇（含鏈三集、九篇關卡限定、八條條件選項、影子那一場戰鬥）：每位 392 項檢查，**0 項沒過、主控台錯誤與 404 為 0**。
- 連線兩組（開房菲菲＋加入球球、開房噹噹＋加入封封）× 連線三篇（兩邊各選一次、擲骰、有人倒下）：每組 118 項，0 項沒過、沒有對帳失敗。
- 連線「學完招接挑牌升級」＋付錢型條件（一人 149 條兩台都看不到）：12 項，0 項沒過。
- **每一張主圖、結果圖都截了、也都用 Read 看過**：52 張聯絡表＋總聯絡表在 `scratchpad\content\b2event_shots\sheets\`（原圖在同層各角色資料夾）。四位各 11 張、連線兩組各 4 張，逐張確認圖文對得上、是那一隻貓自己的版本、沒有空白或錯圖。連線三篇照美術規格是純場景（圖裡不畫主角），兩位立繪放兩邊。影子那一場四位都看過：名牌是「某某的影子」，影子的樣子是那一位的黑色換裝。
- 實機抓到一個真的問題並已修（97cb7699）：連線不對稱選項只有一邊要挑牌時，另一邊一直不投票、兩台卡在「等同伴挑完」；另外舊票有機會被當成「移除這張牌」。修法：沒得挑的一邊自動投空票，沒要挑牌的那位不套票裡的牌號。兩條都補了會反向變紅的測試（已確認會紅）。

## 七、沒做的

- 合併 b2mech、c0923-int 之後的五件事（見第〇節），因為合併被權限擋下。
- 連線版的條件選項，如果是同伴達成的，結果文字仍寫看畫面那一位做的事（劇本只寫了一份單人文字）。

## 八、需要主控決定

1. **合併的權限**：要嘛主控自己合（衝突很小，見第〇節），要嘛請使用者放行後叫我接手收尾。
2. 首載程式多 41 KB（90.6%）：要不要把球球版事件文字也改成延後載入。
3. 噹噹、封封的牌池沒有【隱身】牌、只有封封有【蓄氣】牌：這兩條條件選項對其他角色幾乎不會出現（劇本本來就這樣設計，但出現率很低）。
4. 單人球球的影子第二集只有屋頂那篇（第二、三關），走到第三集的機率比其他三位略低。
5. 小地方：「多張移除」的視窗在小螢幕上確認鈕在畫面下方要捲（舊畫面的事）；「風裡的回信」文字長，插圖會被擠小；第一關「地窖裡的冬眠熊」打起來的那條偏硬。
6. 為了加權抽而改釘遭遇的兩條舊測試（`dangdang.test`、`echofirst.test`），之後地圖亂數再動還會碰到。
