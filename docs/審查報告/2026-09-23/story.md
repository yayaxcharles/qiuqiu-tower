# 劇情、台詞、角色設定一致性稽核（c625d72..1902d32，分支 coop）

唯讀稽核，沒有改任何倉庫檔。有跑的：`npx tsc --noEmit -p .`（通過）、九支內容測試逐一單獨跑（name_unique、fengfeng_story、fengfeng_shared_dialogue、no_meow_leak、mixed_three_heroes、coop_feifei_ninja、story_consistency_0916、victory_slides、defeat_last_word，全部通過）。另外寫了三支暫存檢查腳本（放在 tmp_story\，用 esbuild 打包後用 node 跑），也把封封的劇情圖排成聯絡表逐張看過。

## 高
沒有找到。

## 中

### 中-1　封封跟人一起爬時，貓又婆婆那句還在說「兩個徒弟都沒回村」
- 位置：`src/content/fengfeng-dialogue.ts:515`（`FENGFENG_BOSS_LINES` 的「婆婆，我來找大俠貓和他的兩個徒弟。他們都沒回村。」）；原因在 `src/content/dialogue.ts:2154`，`lineFor` 的封封那一條沒有過 `mixedLine`，而 `MIXED_LINES`（933–988）根本沒有 `fengfeng` 這一層。
- 玩家看到的情境：連線「封封＋球球」或「封封＋菲菲」，打到貓又婆婆那一關。封封對婆婆說兩個徒弟都沒回村，可是其中一個就站在他旁邊。這就是之前「三張改口表只有一層」那類錯，這次犯在封封身上：他的關主台詞、事件台詞都不會依同伴換句。
- 信心：確認。

### 中-2　菲菲跟師兄一起爬，哭牆事件還在說「師父和師兄都還沒回來」
- 位置：`src/content/dialogue.ts:1575`（`FEIFEI_EVENT_LINES` 的「我知道上面危險，可師父和師兄都還沒回來。」）；`MIXED_LINES.feifei.ninja`（935–949）有收月亮窗那句，這句漏了。
- 玩家看到的情境：連線「菲菲＋球球」遇到共用事件「哭牆」（crying_wall，連線局會排到），選「敲開藏物磚」那個選項。師兄就在身邊，她卻說師兄還沒回來。
- 信心：確認（`eventTextFor` 會把引號裡這句過 `mixedLine`，但表裡查不到這個鍵）。

### 中-3　封封單人序章第四張圖配錯台詞：村貓在村裡回話，圖卻是封封已經在塔下撿魚乾
- 位置：`src/ui/storyslides.ts:68-69, 100`；`src/content/fengfeng-dialogue.ts:17-42`
- 玩家看到的情境：玩封封開新局。序章有五張圖、六句台詞，而且沒標任何切點，所以前四張各配一句、最後一張吃掉剩下的。結果第四句「村貓：大俠貓中了魔氣，跑進那座塔……」配到 `fengfeng_still_tower`，那張圖畫的是封封蹲在塔門口撿小魚乾。真正講「走到塔下看見小魚乾」的那句旁白，跟最後一句一起擠到第五張 `fengfeng_story_p05`，而那張又是另一張塔下撿魚乾的圖。應該是第三、四句（封封問、村貓答）都配 `still_meet`。
- 信心：確認（程式怎麼切、圖畫了什麼，兩邊都核過）。

### 中-4　封封第二關過關，最後一張圖只有他一個人，底下卻是噹噹連講三句
- 位置：`src/ui/storyslides.ts:117-120`；`src/content/fengfeng-dialogue.ts:61-86`
- 玩家看到的情境：玩封封打完第二關關主。六句台詞配三張圖，照「一張一句、最後一張吃剩下的」切法，第三到第六句（噹噹「我沒事，是牠走不動」、封封「你把牠送出去」、噹噹「大俠貓現在認不得人……」、旁白「等兩人走過轉角」）全都配在 `fengfeng_still_act2_moonstairs` 上。那張圖是封封獨自站在月光樓梯前，噹噹和受傷的村貓都不在畫面裡。
- 信心：確認（圖看過了）。

## 低

### 低-1　連線「噹噹＋菲菲」落敗，菲菲那台的結算畫面沒有遺言
- 位置：`src/content/dialogue.ts:1170-1176`（`MIXED_SCENES['dangdang+feifei'].defeat` 裡菲菲一句都沒講）；`defeatLastWord` 在 2420 行
- 情境：菲菲那一台落敗結算畫面找不到她講的最後一句，`result.ts` 退回「菲菲倒下了。」。畫面不會空白，但另外五組搭檔都有主角自己的一句，只有這組沒有。
- 信心：確認。

### 低-2　封封的事件文字有一句讀起來歧義：「教封封母親傳的調息法」
- 位置：`src/content/fengfeng-dialogue.ts:773`（山賊回禮事件選「請牠把回禮留給母親」）
- 情境：這句讀起來像是「封封的母親」傳下的方法，原意是山賊母親傳的。建議改成「教封封自己母親傳下的調息法」這一類寫法。
- 信心：確認（純文字問題）。

### 低-3　連線「噹噹＋球球」時，噹噹說「他的兩個徒弟都在找他」
- 位置：`src/content/dialogue.ts:2264-2265`（`DANGDANG_BOSS_LINES` 的「好，我去叫他喵。」→「他的兩個徒弟都在找他。我這就上去。」）
- 情境：打完破戒住持之後的收場。這組搭檔的序章裡，菲菲留在村口替傷貓包紮，並沒有進塔找師父。
- 信心：可能（要看使用者覺得「在找」算不算成立）。

### 低-4　連線「封封＋球球」時，封封在哭牆事件說「朋友還沒回來，我得去找」
- 位置：`src/content/fengfeng-dialogue.ts:744`
- 情境：這組搭檔的序章裡，菲菲、噹噹都留在村裡，唯一還沒找回來的是大俠貓。跟中-1 同一個原因：封封沒有依同伴換句的那一層。
- 信心：可能。

### 低-5　圖沒載到、退回純對白時，劇本裡的「球球：」會被改成封封／噹噹自己在講
- 位置：`src/ui/app.ts:215, 455, 527`（單人時 `literal` 傳的是 `hasCoopScene`，結果是 false）；`src/ui/dialogue.ts:119, 143`。受影響的台詞：`fengfeng-dialogue.ts:93-94`（塔頂「封封，別傷到師父喵！」）、160-161、188-189（結局）；噹噹單人序章與結局裡球球的那幾句（`dialogue.ts:796, 851, 855, 860`）。
- 情境：只有圖清單讀取失敗、或舊快取沒有這些圖，退回 `playDialogue` 時才會發生。那時名牌和頭像都會換成本機這一位，於是會出現「封封：封封，別傷到師父！」，接著封封又回自己一句。平常有圖、走幻燈片時照字面播，沒有這個問題。
- 信心：確認（程式路徑）；觸發機率低。

### 低-6　封封的結算收尾句是用陣列位置抓的，而且跟結局裡已經演過的那句重複
- 位置：`src/content/fengfeng-dialogue.ts:490`（`victoryTeaser: fengfengVictory[9]?.text`）
- 情境：通關結算畫面會把結局第十句「幸好這批藥趕上了。我的手也得包一下。」再印一次；另外三隻都有另外寫的收尾句。而且結局只要多一句或少一句，抓到的就會變成別人講的話，不會報錯（跟 `storyslides.ts:158` 用 `victory[10]` 找院子那段是同一種寫法）。
- 信心：可能（重複那一點可能是刻意的；會不會抓錯是結構上的風險）。

## 逐項檢查過、沒有問題的
1. 對照表的鍵有沒有跟著原句改：FEIFEI_EVENT_TEXT（6）、FEIFEI_EVENT_LINES（88）、DANGDANG_EVENT_TEXT（124）、FENGFENG_EVENT_TEXT（203）、FEIFEI／DANGDANG／FENGFENG_BOSS_LINES（各 50）、三張 CAST_LINES（5／9／54），每個鍵都還對得到原句，沒有一個會靜靜退回球球版。FEIFEI_TA 每個片語在事件裡都剛好出現一次。四隻貓的魔物初見吐槽都涵蓋全部魔物，沒有多也沒有少。共用事件裡所有提到球球的字串，噹噹和封封都有自己的版本；沒涵蓋到的只有各角色的專屬事件（連線局不排、別的角色也遇不到，所以不算）。
2. 球球＋菲菲 09-22 合演的序章、兩次過關、師父開場與兩個階段、結局、落敗：說話者、稱呼、喵的規則都對，時間線跟兩人各自的單人序章接得上。其他五組合演場景也逐組看過說話者。
3. `DialogueLine.speaker` 列舉、`portraitHero`／`portraitOf`、`heroSpeaker`、`defeatLastWord`（照 `heroName` 查）都已經涵蓋封封，說話者都沒有寫死。
4. 封封的台詞、短句、初見吐槽、合演場景、閒聊、專屬事件都沒有「喵」；噹噹沒有喊「師父」；菲菲的「牠→她」替換都對得上。
5. 事件插圖：封封共用事件的主圖和結果圖在圖清單裡都有自己的版本，`data-art-cast` 會標成 fengfeng；5F 秘笈四隻都有自己的圖。
6. 名稱撞名：把四隻貓各自看到的牌名、秘寶、忍具、狀態全部丟進同一個集合比對，沒有撞名。改名的四件秘寶（續勁護甲、軟甲背心、暖身護腕、望月禪坐），玩家看得到的地方都沒有殘留舊名。

補充（屬於已知項，沒有另外列）：`MIXED_FIRST_MEET`、`MIRROR_EVENT_TEXT` 都沒有封封那一層，連線時鏡子走廊的吐槽和事件文字不會依同伴換。這跟已知的「鏡子事件文字是舊版」是同一件事。
