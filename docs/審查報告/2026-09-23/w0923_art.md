# art 代理（美術）工作紀錄：2026-09-23

分支 `w0923-art`（worktree `F:\ClaudeWork\qq-art`），從 `1902d32` 開出。盤點報告在同資料夾 `art_inventory.md`。

## 批次 1：網站小圖示＋連線不播單人結尾影片（`69c0c3d`、`8883b59`）

- **做了什麼**：
  - `public/favicon.png` 換成正式的圖：球球正面大頭＋頭巾，gpt-image-1.5 真透明，長相參考新版對白頭像。一次生 3 張候選，選第 2 張。回來的原圖是 1254×1254，工具自己裁到外框、補成正方形、縮成 64×64，6.7 KB（128 的要 18 KB，首載預算不划算）。
  - 工具 `tools/gen_favicon.py`（refs／gen／pick），提示詞與選定紀錄 `tools/motion-art-source/favicon/{prompts,picks}.json`，生圖原檔不進版控（`.gitignore`）。
  - `src/ui/app.ts` 結尾影片那一行多判斷 `!this.coop`：連線局通關比照開場不播球球的單人結尾影片。
- **沒照派工單做的一件**：`index.html` 的 `<link>` 我**沒有加**。建置代理那筆（`qq-build` 的 `6b116d9`）已經加了 `<link>`、打包外掛不改檔名的排除、還有一條真的跑打包的測試；我再加一份會在合併時跟那筆同一段衝突。合併時 `public/favicon.png` 兩邊都是新增，取我這張即可；那筆 `index.html` 註解裡寫的「暫用」可以順手改掉。
- **張數**：生圖 3 次、採用 1 張。
- **聯絡表**：`art_inv\favicon_try.png`（三張候選）、`art_inv\favicon_preview.png`（16／32／64 像素實際大小）。16 像素看得出頭巾和臉。
- **測試**：
  - `tests/ui/favicon_art.test.ts`：64×64、RGBA、10 KB 以內、選定紀錄對得上。確認會紅：用 128 重選一次，三條都紅；改回 64 轉綠。
  - `tests/ui/hero_video.test.ts` 加一條「連線局通關不播單人結尾影片」。確認會紅：把 `app.ts` 暫時還原，這條紅；改回來轉綠。
  - 全套 `npx vitest run` 277 個檔全綠、`npx tsc --noEmit -p .` 零錯誤（照雷 5 把四個重生的 docs 還原）。
- **另外**：我的分支沒有建置代理那條排除，所以單獨打包我這支時 `favicon.png` 會被加雜湊改名（打包訊息「.png 1」）；跟 `qq-build` 合併之後就正常，單獨看我這支不用管。

## 批次 2：四隻「空手擲出」＋飛爪＋拋爪接線（`87755e0a`）

- **美術**：
  - 四套 8 格空手擲出：球球 `motion/qiuqiu/generated_toss.webp`、菲菲／噹噹／封封 `motion/<角色>/toss.webp`（1536×1024 無損，跟其他出牌動作同一等級）。長相參考只附新版待機 8 格，不附舊立繪、也不附擲手裏劍那套（怕把手裏劍抄進手裡）。四隻都是**第 1 次就合格**。
  - 量頭（不量外框）：第 1、8 格的頭是新版待機的 0.97～1.01 倍，`sizeFix` 都不用修；寬度、重心、第 8 格回待機的檢查都過；畫布外框沒有變大（四隻的外框都落在既有動作之內）。
  - 腳底：球球幾乎不動；菲菲、噹噹、封封出手那一兩格後腳往後踏（前腳縮回 20～30 單位），幅度跟既有的吼、推掌、劍刺同一級，沒重生。
  - 飛爪飛行物 `motion/projectile/grapple.webp`（128×…，5 KB）：金色四爪帶一段麻繩，參考拋爪牌面。生 2 次、用第 2 次。**第 1 次的原檔被我用錯 `review_sheet.py` 參數（它第一個參數是輸出檔）蓋成聯絡表**，移到 `tools/motion-art-source/proj/_preview/` 留著，第 1 次本來就沒用上。
- **接線**（丟的牌與丟出去的忍具四隻都改播 `toss`）：
  - 球球：聚葉成刀、撒手鐧、毛球彈、拋爪＋七支忍具；手裏劍亂舞與忍具「手裡劍」照舊擲手裏劍（手上那枚剛好對）。
  - 菲菲：撒手鐧、毒丸彈、毒砂、聚葉成刀、手裏劍亂舞、拋爪＋六支忍具；針術牌照舊、絆索與麻繩照舊反手甩（手上本來就空）、三連針照舊連針。
  - 噹噹：聚葉成刀、撒手鐧、毛球彈、拋爪＋八支忍具（原本推掌）。封封：聚葉成刀、撒手鐧、毛球彈、手裏劍亂舞、拋爪＋八支忍具（原本劍刺）。
  - 出手時點寫進動作資料（`releaseTimes: [240]`、`impactTimes: [410]`，原速；載入時換 1.5 倍速＝160／273 毫秒），飛多久＝命中－出手；出手點量自第 4 格張開的手掌（`projectile-flight.ts` 的 `TOSS_ORIGIN`）。
  - 延後下載、不進開場解碼預載；圖還沒到先播原本借的那套（球球、菲菲擲手裏劍／彈針，噹噹推掌，封封劍刺）。拋爪飛 `grapple`；`toss` 沒有預設飛行物（沒指定就不飛，不猜一個可能對不上的）。
  - 噹噹、封封打兩輪的牌（聚葉成刀、手裏劍亂舞）沿用既有的「第二輪重播出手那兩格」；球球、菲菲第二輪隔 0.1 秒從手上再飛一批、不重做手勢（沒另外做）。
- **張數**：生圖 6 次（擲出 4、飛爪 2），採用 5 張（4 套動作＋1 張飛行物）。
- **聯絡表**：
  - `docs/審查報告/空手擲出_逐格_2026-09-23.jpg`：四隻照遊戲大小、腳底對齊，第一格是新版待機當基準（盤點用的 500 像素格子）。
  - `docs/審查報告/空手擲出_實機膠卷_2026-09-23.jpg`：實機膠卷（下面那段）。
  - 我逐格看過：四隻出手前手上都是空拳或空手、出手格張開，飛出去的是葉片、苦無、毛球、毒丸、毒砂、飛爪、木桶、鞭炮、麻繩、煙霧彈，沒有多的手、字或透明邊。
- **實機**（`dist`＋`vite preview` 5271 埠，無頭 Chrome、全新暫存設定檔、`?debug`，腳本 `scratchpad\w0923\pw\tossfilm.js`）：四隻各一場，等擲出圖在背景解好，逐一打牌、用三支忍具，CDP 逐格錄影，共 32 段。每一段畫到的都是 `toss`（球球的手裏劍亂舞是 `shuriken`、菲菲的麻繩是 `needle_backhand`，都照設計）；飛出去的種類跟表上一致；**沒有任何一格露出靜態立繪**。主控台只有一筆 404（`favicon.ico`，我這支沒有 `<link>`，見批次 1）。
- **測試**：
  - `tests/ui/projectiles.test.ts` 新增「出手前手上拿的東西跟飛出去的對得上」：四隻 × 拿得到的每張牌（基礎、升級）× 35 支忍具，手上不是空的就必須跟飛出去的同一種東西；另加「拋爪四隻都原地丟、飛出飛爪」。確認會紅：把 `potion-motion.ts` 暫時改回舊版，四隻全紅；把 `qiuqiu-motion.ts` 改回舊版，球球的兩條與拋爪紅。
  - 新檔 `tests/ui/toss_motion.test.ts`：四張圖雜湊跟打包紀錄一致（換圖沒重量出手點會紅）、量頭 ±5%、出手在第 4 格開頭、飛 170 毫秒、原地、延後下載。
  - 既有五支（`card_motion_coverage`、`combat_no_old_pose`、`companion_motion`、`qiuqiu_motion`、`projectiles`）的期待值照新對照表改，並把延後下載的假圖清單加上擲出那四張。
  - 全套 279 個檔全綠、`tsc` 零錯誤；`python tools/check_size.py` 首載 97.2%（新圖都在「按需動作」，首載沒變）。

## 批次 3：鏡中封封 5＋菲菲鏡子走廊主圖 1（`042a72cf`）

- **鏡中封封**：五張各一次就過。黑紫煙霧身體、兩隻發光紫眼、有黑外框的實心煙，留著封封的圍巾、背心、腰帶、一把劍（劍也是黑的、邊緣發紫光）。參考圖＝鏡中噹噹的同一個姿勢＋封封新版待機第 1 格（鏡射朝左）；舊圖不當參考。照 `add_sprite.py` 魔物的規矩貼進 460×460（主體貼底、置中），同檔名覆蓋、清單鍵不動。主體高佔畫布 0.99（舊 0.58～0.68），倒地 0.63（舊 0.32；另外三隻倒地 0.79～0.88，他那把劍平放讓整張偏寬，沒另外處理）。朝向逐張目視（四張朝左、倒地躺著），`docs/enemy-facing-audit.json` 只換那五行雜湊。`enemies.ts` 的 `MIRROR_FENGFENG` 註解補一句來由。
- **菲菲鏡子走廊主圖**：生 2 次、用第 2 次。照原圖的構圖與畫風，大鏡子裡換成紮蝴蝶結、捏著三根針、笑著的她自己的黑影，沒有頭巾。工具自己把 1536×1024 的原檔照主體外框補成 4:3、縮成 560×420。
- **張數**：生圖 7 次、採用 6 張。
- **聯絡表**：`docs/審查報告/鏡中封封與菲菲鏡子圖_2026-09-23.jpg`（上半五個姿勢＋鏡中噹噹對照，下半兩張實機截圖）。逐張看過：沒有灰虎斑的毛色、沒有多的手或字、邊緣乾淨。
- **實機**：`pw\mirrorshot.js`，封封開一場 `mirror_duel`（名牌寫「鏡中封封」、畫的是新圖），菲菲開鏡子走廊事件（圖上是她的黑影、文字對得上）。主控台只有 favicon.ico 那一筆 404。
- **測試**：`tests/ui/mirror_shadow_art.test.ts`（主體高、暗色比例、雜湊、菲菲那張的選定紀錄）。確認會紅：把兩組舊圖放回來，雜湊與紀錄那兩條紅；另外量過舊圖的主體高 0.58～0.68、暗色比例 0.62～0.68，都過不了 0.9 的門檻。全套 280 個檔全綠。

## 批次 4：戰鬥靜態立繪 24 種 × 四隻（`057a49c1`）

- **做了什麼**（0 張生圖）：`tools/pack_static_from_motion.py` 從同一隻的新版逐格圖集裁代表格，照戰鬥比例（560 畫布＝270 遊戲單位）換算、同檔名覆蓋 96 張，程式與清單不動。
  - 取哪一格：出招取出手或打到那格、狀態取停著呼吸的第 8 格（中毒第 4 格）、挨打取挨打那一張、「擲」取空手擲出那套（手上沒東西）。
  - 位置：腳底定位點對齊待機（四隻各算一次、全部姿勢共用），換姿勢不左右跳。
  - 放不下的處理：劍整把伸出去、騰空、翻滾倒立那幾格比畫布寬或高，改取同一招前後收著的那格（球球輕功取蹲下起跳、閃避取翻完蹲低、封封出招取出劍前）；真的放不下才縮，96 張縮了 6 張（0.90～0.99）。
  - 封封：舊圖底邊不等高、靠 `combat.css` 補位移。換掉的九張（翻肚、中毒、懶洋洋、炸毛、隱身、鐵布衫、挨打、掛彩、氣勢）底邊改貼畫布底、那九條位移拿掉；09-22 換的頭暈、倒地、待機、落敗四條留著。
  - 大小：新圖比舊圖大，球球那 24 張沒有角色前綴、算在首載裡。無損透明度時首載 11.34／11.30 MB 超標，透明度改有損 70（暗底放大看不出毛邊），首載 97.9%（原本 97.2%）。
- **聯絡表**：`docs/審查報告/靜態立繪換新畫風_2026-09-23.jpg`（上：四隻 × 24 種新舊對照；下：`?motion=0` 實機截圖，待機、出招、隱身、鐵布衫）。逐張看過，四隻都站在同一條地面線、大小一致；封封隱身與鐵布衫拿掉位移後跟待機同高。
- **實機**：`pw\staticshot.js`（`?debug&motion=0`），主控台沒有錯誤。
- **測試**：新檔 `tests/ui/static_from_motion.test.ts`（96 張雜湊、縮放不超過一成五、封封位移拿掉與保留）。確認會紅：把 `combat.css` 改回舊版，位移那條紅。`rest_art_0922.test.ts` 裡封封去殘渣那三張（出招、閃避、輕功）被整張換掉，改由新測試守雜湊。全套 282 個檔全綠。

## 批次 5：石獅子第二階段 2 張＋噹噹對白頭像（`6aa55b9d`）

- **石獅子**：第二階段補挨打、防禦（各一次就過），參考圖＝第二階段待機的長相＋第一階段的同一個姿勢；貼進第一階段待機的畫布（同一隻共用畫布、換階段不跳）。清單只在這一隻那段補兩行（工具原本想用 `manifest_io.merge`，結果整份重寫成兩千多行差異，改成只補那一段）。朝向逐張目視朝左，補進 `enemy-facing-audit.json`（插在同一群組後面，不整份排序）。
- **噹噹頭像**：原本裁自待機圖集（167×240），改從選角那張新畫風待機靜態圖裁（363×527），另外三隻不動。
- **張數**：生圖 2 次、採用 2 張；頭像 0 次。
- **聯絡表**：`art_inv\stonelion_p2.png`、`art_inv\dd_portrait_cmp.png`（我逐張看過）。
- **測試**：新檔 `tests/ui/minor_art_0923.test.ts`；`dialogue_portrait.test.ts` 噹噹那條照新來源改。確認會紅：把舊頭像與舊清單放回來，兩條都紅。

## 批次 6：連線雙貓劇情圖 24 張＋`storyslides.ts` 接線（`29e0f464`）

- **圖**：`tools/gen_coop_story_art.py`，每張照那一段台詞畫（台詞摘要寫在工具裡每張旁邊的註解）。每張附兩張參考圖：
  - 場景畫風：同一派的單人劇情圖。球球、菲菲、噹噹的是平塗描線，封封的是厚塗，所以封封三組的過關照封封的圖畫，跟同路線 09-20 那三張一致。
  - 角色表：出場的每隻貓用新版待機第 1 格，大俠貓用 `boss/idle1`。
  - 生出來的圖裁成 16:9、縮成 1280×720（跟單人劇情圖一樣大），每張 56～149 KB，共 2.5 MB。
  - 鍵名 `bg/<角色>_coop_<搭檔>_<段>`，跟封封那九張同一個樣子，照舊點到才載，不進首載。
- **張數**：
  - 生圖 25 次、採用 24 張：球球＋菲菲、球球＋噹噹、噹噹＋菲菲各 6 張（序章 2、兩次過關各 1、結局 2），封封三組各補兩次過關。
  - 只重生一張：球球＋噹噹結局第二張，第一版菲菲的藥箱上畫了紅十字，改成草葉記號。
- **接線**：
  - `storyslides.ts` 的 `FENGFENG_COOP_ART` 換成六組的 `COOP_ART`。
  - 序章與結局有兩張圖的，照台詞標的 `slideBreak` 分句；分不齊，或清單裡還沒有這批圖（舊快取），照舊整段退回純對白，絕不借單人的圖。
  - `dialogue.ts` 在球球＋菲菲的序章補一個切點：第二句「一路追到塔下」之後換圖。另外兩組原本就標了切點。
- **聯絡表／實機**：
  - 生圖時逐組排聯絡表看過，在 `art_inv\cs_ffnj.jpg`、`cs_ddnj.jpg`、`cs_ddff.jpg`、`cs_ffcoop.jpg`。每張的出場角色、動作都對得上台詞，沒有字，也沒有多出來的角色。
  - 實機截圖用 `pw\coopstory.js`（開發伺服器 5272，已關）。六組 × 四段實際播一遍，每張圖在它的第一句時截，排成 `docs/審查報告/連線劇情圖_實機_2026-09-23.jpg`，共 30 張，含封封原有的序章與結局。
  - 截圖結果：畫面上顯示的圖全部是該配的那張，每段都播完；台詞框壓在上緣，沒有擋到主角。
  - 主控台只有兩筆 `/favicon.ico` 的 404，是因為這條分支沒有 `<link>`，交給 qq-build。
- **測試**：
  - 新檔 `tests/ui/coop_story_art.test.ts`，共 29 條，守這幾件事：
    - 六組 × 兩個席位都配到自己的圖，一句不掉；
    - 兩台機器配到同一套圖；
    - 切點落在台詞寫的地方；
    - 困難難度多出來的那句旁白接在最後一張；
    - 24 張都在清單裡，大小是 1280×720；
    - 清單沒有這批圖時退回純對白。
  - `coop_slides.test.ts` 原本那條「過關與結局退回純對白」改成「只配連線圖、不借單人圖」。
  - 確認會紅：把 `storyslides.ts` 改回舊版，紅 14 條；把切點拿掉，紅 3 條。
  - 全套 283 個檔全綠，`tsc` 零錯誤，首載 97.9% 沒變。`分關載入.json` 多了這 24 張（第 0 關，點到才載），一起提交。
  - 補記：這一批提交當下跑的 `check_size.py` 讀的是舊的打包結果；之後重新打包再量一次，首載還是 97.9%，分關載入從 1103 個檔變 1127 個（多 2.5 MB），跟前面寫的一致。

## 批次 7：連線牌混搭牌面 159 張（`42c6b0ba`）

- **圖**：用 `tools/gen_coop_card_art.py` 生，程式照主控裁決不動。
  - 構圖：31 張牌各寫一段「兩位一起做這件事」的敘述，例如一起撐盾、互相遞東西、同時出手，不寫誰幫誰。牌面的鍵是排序過的兩位，兩個席位看到的是同一張圖，所以寫成左邊幫右邊的話，有一半的局方向會剛好相反。
  - 參考圖兩張：一張是這張牌現有的牌面，照它的顏色、特效和畫法，不照它的姿勢；另一張是兩位的新版待機角色表，照它的長相。
  - 背景透明。把主體裁出來，等比縮進 299×240（跟現有牌面一樣大），四邊各留 3 像素，存成品質 78 的 webp。
  - 每張 19～45 KB，159 張合計 4.3 MB。
- **張數**：
  - 四位都拿得到的 22 張 × 六組；只有球球拿得到的 3 張、只有菲菲的 2 張、只有封封的 4 張，各自跟另外三位組成 3 組。
  - 生圖 160 次，159 張全部採用。其中球球＋菲菲的「我幫你拍掉」第一次被內容審查誤擋（原因標的是「色情」，其實畫的是紫色黏液），改寫成「潑到衣服上的紫墨水」後重生就過了。
  - 先試了 4 張確認畫法，再整批生。
- **聯絡表**：
  - 生圖時用 `art_inv\cc_pilot.jpg`、`cc_sheet1～5.jpg` 分批看過全部 159 張。沒有多出來的貓，長相、衣服跟角色表一致，透明邊緣乾淨，每張的主色都對。
  - 成品縮小版放在 `docs/審查報告/連線牌混搭牌面_159張_2026-09-23.jpg`。
- **實機**：
  - 用 `pw\coopcards.js`，開發伺服器 5272，已關。直接叫遊戲自己畫牌的那支 `cardNode`，畫四組（噹噹＋封封、球球＋菲菲、封封＋球球、菲菲＋噹噹）各自拿得到的全部連線牌，分別是 24、27、28、26 張。
  - 結果：每張的圖都是這一組的混搭牌面，也都真的載入了。截圖在 `docs/審查報告/連線牌混搭牌面_實機_2026-09-23.jpg`，牌框裡看得到兩位的臉。
  - 主控台只有 favicon.ico 那一筆 404。
- **測試**：
  - 新檔 `tests/ui/coop_card_mixed_art.test.ts`，守四件事：
    - 33 張連線牌 × 每一組搭檔都有牌面，合計 171 張（原本 12 張＋這批 159 張）；
    - 兩個席位拿到同一張；
    - 雜湊跟紀錄一致，尺寸 299×240、有透明度；
    - 只在連線預載裡出現。
  - 確認會紅：把清單還原成舊版，紅 3 條。
  - 全套 284 個檔全綠，`tsc` 零錯誤。重新打包後首載 97.9% 沒變，分關載入多了這 159 張，一起提交。
- **要主控知道的一件事**：`preloadCoopArt`（`assets.ts` 的 `coopArtUrls`）一進連線大廳就把**所有**連線牌圖抓下來，四位各自的版本跟全部搭檔的混搭版都算在內。這批加完之後，從 119 張、4.28 MB 變成 278 張、8.62 MB。其實一局只會用到一組搭檔的混搭圖，看角色是 24～28 張。要省流量，可以改成知道搭檔之後只抓這一組的；這是改程式的事，照裁決我沒動。（主控之後交辦了，見批次 8。）

## 批次 8：連線牌面只抓這一組搭檔的（`68c954ce`，程式）

- **原本的三個問題**：
  - 一進連線大廳就把全部連線牌圖抓下來：278 張、8.62 MB，一局只用得到其中二十幾張。
  - 抓完不留參照，瀏覽器隨時可以把圖從記憶體丟掉。實測修前第一場戰鬥，手牌上的連線牌在畫出來那一刻還是空的，要在戰鬥裡重新下載。
  - 單人選菲菲、噹噹、封封時，選角後的補載（`heroArtUrls`）會順便抓她自己的連線牌，各 26、24、28 張（原本只擋了混搭的 `card/coop_` 開頭）。連線時這批還會跟連線預載同時各抓一次。
- **改了什麼**：
  - `assets.ts`：`coopArtUrls()` 換成 `coopArtUrlsFor(兩位)`。要抓哪幾張，直接問畫牌用的 `cardArtKey`，兩個席位各問一次，所以畫面畫哪張、這裡就抓哪張，兩邊不會對不上。不同角色抓混搭那組；同角色雙人抓那一位自己的版本；一個人玩不抓。`heroArtUrls` 改成連線牌一律不抓。
  - `preload.ts`：`preloadCoopArt(兩位)` 的圖自己一組留著。換關時會把共用那組整個放掉（`releaseHeldArt`），這一組不受影響。同一個分頁回標題、兩人換角色再開一局時，放掉上一組、補抓新組合；同一組再叫就沿用，不重抓。另外加了 `coopArtReady()`，讓開打前可以等。
  - `app.ts`：連線局 `startFight` 開打前等這一組抓完，另設 20 秒保險，網路整個卡死時不讓整局停在地圖上。這個保險是誠實交代的例外：真的超過 20 秒還沒抓完，圖會晚一點出現。
  - `lobby.ts`：開局時把兩位傳進去。
- **量測**：
  - 修前後各種組合要抓多少（照清單算）：
    - 同角色雙人：24～28 張、0.56～1.67 MB；
    - 不同角色：26～31 張、0.69～1.00 MB；
    - 修前一律 278 張、8.62 MB。
  - 實機做法：用 `pw\coopart.js`，開兩個獨立的瀏覽器，經中繼開房、加入，走到地圖；兩台做一樣的改動，把牌組換成連線牌；再一起走進第一場戰鬥。開發伺服器 5272，已關。

    | | 噹噹＋封封 | 球球＋菲菲 |
    |---|---|---|
    | 修前 大廳下載 | 330 次請求（278 張＋自己版本重抓 52 次）、實傳 10.85 MB | 304 次請求（278 張＋重抓 26 次）、8.62 MB 以上 |
    | 修後 大廳下載 | 28 張、0.91 MB，別組 0 張 | 29 張、0.81 MB，別組 0 張 |
    | 修前 手牌第一次畫出來 | 5 張全是空的，戰鬥中再下載 5 張 | 5～6 張全是空的，戰鬥中再下載 5～6 張 |
    | 修後 手牌第一次畫出來 | 兩台都是 5 張全部已在，戰鬥中下載 0 次 | 6 張／5 張全部已在，戰鬥中下載 0 次 |

  - 「第一次畫出來」怎麼量：手牌節點插進頁面的同一刻檢查圖是否已載入完成、寬度大於 0。
  - 截圖：`art_inv\coopload_combat.jpg`（兩組 × 開房／加入）。牌框裡都是這一組兩位的混搭牌面。
  - 主控台只有 favicon.ico 那一筆 404。
  - 修前 330 次和 304 次的位元組量法不同：前者用回應內容長度，把重抓的也算進去；後者用請求結束時的大小，重抓那幾次可能沒算到，所以寫「以上」。
- **測試**：
  - 新檔 `tests/ui/coop_art_preload.test.ts`，守這幾件事：
    - 十組搭檔：兩位拿得到的每一張連線牌、兩個席位看到的那張都在，別組不抓；張數 24～31；
    - 一個人玩不抓；換了搭檔抓的是另一組；
    - 單人補載不抓連線牌；
    - 抓完留著、同一組不重抓、換組會放掉舊的；
    - 連線局開打前會等。
  - `coop_card_art`、`coop_card_mixed_art`、`preload_hero` 三個舊測試改用 `coopArtUrlsFor`。
  - 反向驗證：四個修法各拿掉一個，各紅 1 條。
  - 全套 285 個檔全綠，`tsc` 零錯誤，首載 97.9% 沒變。
- **沒驗到的**：實機是在開發伺服器上量的，打包版沒跑實機，因為量測腳本要直接讀原始碼裡的牌表。

## 批次 9：驗收 M-1、M-3＋L-1（先合併 w0923-int；`edde244d`、`e14f968a`、`3ef88920`）

- **合併**：`git merge w0923-int` 只有 `lobby.ts` 開局那段衝突。取整合分支的 `adoptRun`，連線牌面預載保留「只抓這一組」的寫法。`coop_art_preload.test.ts` 的原始碼比對跟著改。合併後全套 301 個檔全綠。
- **M-1 鏡子走廊**（`e14f968a`，只改程式與字，不生圖）：
  - 插圖改照座位 0 挑：連線時鏡子走廊的主圖與結果圖照座位 0 那一位挑（`assets.ts` 的 `eventArtHero`）。
  - 本機立繪：插圖裡沒有本機這一位時，照 `data-art-cast` 的規矩在旁邊放它的立繪（`eventSidePortrait`）。
  - 立繪位置：站在插圖左邊、腳底對齊插圖底邊。第一版照紙箱的位置放，會踩在「鏡子走廊」名牌上，實機截圖看到後改了。
  - 十二組文字逐組對過座位 0 那張插圖：
    - 四張插圖的內容：菲菲那張鏡子裡是黑影；球球、噹噹、封封那三張是彩色倒影，其中一面擺出了架勢。
    - 照後三位那九組：開頭與結果的「黑影」改「倒影」。
    - 球球看到的三組：原本寫「無數個球球同時抬起頭」，改成「映出來的全是走在前面的同伴」。
    - 封封那三組：劍在腰上，倒影是握著拳，不再寫「背著劍」「按著劍柄」。
    - 改動只在 `MIRROR_EVENT_TEXT`，共 19 處。
  - 實機：用 `pw\coopmirror.js`，球球開房＋菲菲加入、封封開房＋噹噹加入，兩台都截了開頭與「走過去」的結果（`docs/審查報告/連線鏡子走廊_實機_2026-09-23.jpg`）。坐 1 號的兩台看到的都是同伴那張，旁邊有自己的立繪；坐 0 號照舊。主控台沒有錯誤。
  - 測試：新檔 `mirror_hall_coop_art.test.ts`。反向驗證：插圖改回照本機挑，紅 13 條；文字改回原本，紅 10 條。
- **M-3＋L-1 頭大小**（`3ef88920`）：
  - 逐格：新工具 `tools/motion_size_fix_0923.py`，量法跟 6pct 批次一樣（四種量法取中位數）。圖不動，只改資料檔的 scale。
    - 各套倍率：
      - 噹噹隱身 ×0.939：頭 1.04～1.09 → 0.97～1.025；
      - 噹噹掛彩 ×0.973：頭 1.00～1.055 → 0.97～1.025；
      - 封封集中 ×1.05：頭 0.93～0.97 → 0.975～1.02。倍率被整隻高度 +5% 的上限壓住；
      - 封封重劈 ×1.07：頭 0.895～0.975 → 0.955～1.04。
    - 重劈不在驗收清單上，是量逐格時發現第 3、6 格的頭只有 0.905、0.895，同一條規矩也超出，所以一起修。它的外框高包含舉過頭的劍，不套整隻高度的上限。
  - 靜態：`pack_static_from_motion.py` 加 `--only`，只重裁指定的幾張。腳底線照紀錄，頭要過 ±5% 的閘門才寫檔。重裁七張：
    - 噹噹隱身、掛彩，封封集中、卷軸、技能：從改過的逐格重裁。
    - 封封「拳」：改取重劈第 2 格，縮 0.942、頭 0.956。
    - 菲菲「翻肚」：改取第 5 格（剛翻過去、閉著眼）。躺姿照打包的縮放倍率算，是 0.971。第 6～8 格比畫布寬太多，至少要縮 0.92。
  - 聯絡表：`docs/審查報告/頭大小修正_M3L1_2026-09-23.jpg`。照遊戲大小、腳底對齊，每列最左邊是待機第 1 格，逐格修前修後各一列，靜態排「待機｜修前｜修後」。我逐列看過。
  - 實機：`pw\sizeshot.js`，`?motion=0` 下換立繪、逐格下真的掛狀態或出牌，都截了（`docs/審查報告/頭大小修正_實機_2026-09-23.jpg`）。
  - 測試：新檔 `size_fix_0923.test.ts`。反向驗證：資料檔改回，紅 4 條；靜態紀錄改回，紅 2 條。
  - 全套 2627 條全綠（6 條本來就略過），`tsc` 零錯誤，首載 98.0%。首載從 97.9% 變成 98.0%，時間點在合併整合分支之後；我這批改的七張靜態圖都是角色專屬的，不算首載。
