# 最終驗收報告（2026-09-23，決定推不推）

## 結論

**沒有高、沒有中的問題，四項門檻全數通過，建議可以推。**
- 上一輪的三條中都修好了，實機看得到：
  - M-1 連線鏡子走廊：四組真連線 24 格圖文都對得上。
  - M-2 換畫面閃米白：新版 22 段 0 段，舊版 12 段。
  - M-3＋L-1 頭大小：修過的逐格與靜態都回到 ±5% 內。
- 換畫面時墊在底下的舊畫面：狂點 3399 下，0 下點得到，沒有重複選路、拿牌、出牌、送兩次。
- 連線牌第一次畫出來就有圖，而且是這一組的混搭牌面。
- 效能比線上版好：中位數 40 對 48 毫秒。
- 剩下的都是低。只有一條低（噹噹掛彩第 7、8 格）是這次改動範圍內量到的邊界值，其餘都是舊版就有。

## 注意：驗收途中分支又多了一筆

- 我 14:20 打包驗的是 `49710a5b`（照派工單）。14:33 分支多了 `09e583ab`「第二輪推前審查修正」，改了三件事：
  - 連線牌面等待的保險從 20 秒改成 6 秒；
  - 離開連線時一起解開開打前的點不動；
  - 開場泡泡的樣式只看現在的畫面層。
- 改動只在 `app.ts` 9 行、`screens.css` 1 條規則，外加一支測試。
- 發現後我重新打包 `09e583ab`，重跑受影響的四件：
  - 換畫面膠卷＋狂點：20 段、7525 下。米白 0 段，落在退場層 0 下，每段只前進一步。
  - 關主門：跑一趟，正常。
  - 連線球球＋菲菲一整組：10 個時間點兩台一致；連線牌第一次畫出來 4 張都是這一組的；有擋住 3、塞牌光打對格。
  - 斷線回標題（貼碼直連）：通過。
- 事件→戰鬥那一段逐格看過，開場泡泡從第一格就在球球頭上，沒有先跳到右上。
- 這幾件都 0 錯誤。證據：`shots\swaps_ninja_int\`、`sheets\swaps_int_09e5.jpg`、`sheets\ev2combat_09e5.jpg`、`shots\coop_ninja_feifei_int\`、`shots\leave_int_paste\`。`49710a5b` 的結果改名加 `_49710` 留著。
- 其餘項目（單人四趟、噹噹＋封封、手機、`?motion=0`、效能、流暢度、四項門檻）是在 `49710a5b` 上量的。這筆改動碰不到那些路徑，但沒在 `09e583ab` 上重跑。

## 版本與做法

- 驗的版本：`F:\ClaudeWork\qq-int2`，分支 `w0923-int2`，HEAD `49710a5b`。正式打包 `SITE_NAME=qiuqiu-tower-coop npm run build`（型別檢查＋打包都過），`npx vite preview --port 5301`。`git status` 乾淨，只多了不進版控的 `dist\`。
- 對照組：`F:\ClaudeWork\qiuqiu-coop`（線上 `1902d32`）用開發伺服器 `npx vite --port 5302`，沒改那邊任何檔。上一輪（`accept\`）量的舊版數字也拿來比；舊版程式沒變，那些數字照樣有效。
- 瀏覽器：Playwright `channel:'chrome'`、無頭，每個 context 都是剛建的暫存設定檔（跑完刪掉），網址 `?debug`，只開 localhost。連線走遊戲本來的中繼（房號）；斷線測試與鏡子走廊四組另外走貼碼直連。
- 腳本、截圖、膠卷都在 `scratchpad\w0923\accept2\`（下面路徑都相對這裡）。總表 `_總表.jpg`（19 格），四項門檻 `_四項門檻.jpg`。
- 分工：門檻 1、2 與加項 3 由分身做（`gate12\`）；門檻 4 與加項 2 由另一個分身做（`gate4\`）。兩份聯絡表我都用 Read 看過。其餘我自己跑。
- 伺服器 5301、5302 都已照命令列確認是我開的之後關掉；瀏覽器與暫存設定檔都已清掉。兩個倉庫沒改檔、沒提交。

## 作弊（截圖裡的怪數字都是這些造成的）

- 單人：開局給三支忍具與 400 小魚乾；前兩場開戰換手牌（丟擲牌組合）、補飯糰、魔物血 ×3；第三場只換進一張丟擲牌；出滿 5 種牌、魔物出手過後把魔物血改 1；地圖跳到罐頭鋪前一格、13F；關主戰換手牌。
- 連線：兩台做一模一樣的改動（鎖步才不會分岔）：換手牌（每人加兩張連線牌）、指定魔物那一回合的招式與目標、魔物血改 1、兩台同時跳 13F、關主血 ×0.25。
- 換畫面膠卷：地圖跳到各種格子的前一格、魔物血改 1、一個事件格換成鏡子走廊；關主門那一段另開一局直接跳 14F。
- 流暢度：開戰指定手牌、魔物招式；手上沒防禦牌時直接把蜷縮設 5。
- 分身：事件圖把錢設 999、血補滿、用 `__app.show('event')` 開；貓窩改關數與樓層直接開。

---

## 一、全套重跑（每個情境）

| 情境 | 結果 | 證據 |
|---|---|---|
| 單人四隻：標題→選角→序章→三場以上戰鬥→事件→罐頭鋪→貓窩→第一關關主→過關幻燈片→第二關地圖（封封再打到第三關地圖） | 正常：四趟都走完，主控台 0 錯誤 | `shots\solo_*_int\`、`sheets\solo_*_A.jpg`、`_B.jpg` |
| 丟東西手上沒拿錯、飛出去的對 | 正常：四隻空手擲出，飛行物對得上（葉片、各自的毛球、毒丸、毒砂、飛爪、苦無／木桶、麻繩、貓薄荷球、定身釘） | `sheets\hands_*.jpg` |
| 狀態圖示等飛到才出現 | 正常：60 次丟擲與忍具，牌子都在落地後 2～6 毫秒才變；例外 2 次都是「最後一擊打死」那種（L-2，舊版就有） | `node analyze.js` 輸出 |
| 蜷縮擋下有「擋住 N」 | 正常：菲菲、噹噹、封封單人都有；球球那趟剛好魔物都在定身或打穿透，沒機會擋，改由流暢度場景與 `?motion=0` 那場證明（擋住 5） | `result.json` |
| 閃圖／露出舊立繪 | 正常：逐格模式下靜態立繪一格都沒露出；所有膠卷（單人、連線、流暢度、換畫面）掃亮度跳動，只剩兩種合理的（見第三節） | — |
| 空白畫面 | 正常：309 張截圖沒有單色的 | — |
| 台詞泡泡位置 | 正常：四隻 74 個泡泡都在舞台內。封封那趟有 3 次「陪練的黑貓」的泡泡撞到白貓那句、先收起來 1.2 秒後再冒（程式本來的避讓），不是出界 | `*_start_bubbles.png` |
| 牌面文字 | 正常：每回合量每張手牌，沒有擠出框 | — |
| 封封序章第 4 張、第二關過關幻燈片 | 正常：跟上一輪一樣對上 | `shots\solo_fengfeng_int\003_prologue_*`、`036_boss2_clear_01～03.png` |
| 連線 球球＋菲菲、噹噹＋封封（中繼）：打到第一關關主並過關 | 正常：兩組都過關、進第二關地圖；雙貓過關幻燈片；全程沒有紅色橫幅 | `shots\coop_*_int\` |
| 連線：自己擋下「擋住 N」、同伴被塞牌光在同伴那格 | 正常：兩組都是自己那台飄「擋住 3」＋盾牌光；塞給加入方時，開房方畫面紫光在同伴那格 | `host\film_coop_enemy_block_host`、`film_coop_curse_host` |
| 連線：兩台血量與蜷縮對帳 | 正常：兩組 18 個時間點全部一致 | `result.json` 的 `ledgers` |
| 連線：同伴出牌讓位 | 正常：我講話時同伴那張牌 0.1；泡泡淡出時交疊約 0.45 秒（L-4，跟上一輪一樣） | `bubbleSamples` |
| 連線序章中途斷線→回標題 | 正常：中繼（146 秒後才出紅色橫幅）與貼碼直連都通過：回到標題、狂點仍在標題、單機存檔一字不變、續玩讀回單人局 | `shots\leave_int\`、`leave_int_paste\` |
| 手機 844×390 | 正常：按住放大（說明 12 像素）、放開不出牌不選目標、輕點照舊；**新加的 0.38 秒慢點照常出牌、不放大**（推前審查低-3 的修正） | `sheets\mobile_int.jpg` |
| `?motion=0` 球球一場 | 正常：舊路線照常、擋住 5、0 錯誤 | `shots\solo_ninja_int_m0\` |
| 效能：點牌到畫面畫出下一格（瀏覽器事件計時，新舊交錯各 36 次） | 變好：新版中位數 40、平均 39.6、最大 104 毫秒；舊版中位數 48、平均 56.0、最大 128；兩邊都沒有 50 毫秒以上長任務。這一輪兩個分身同時在跑，兩邊都比上一輪（中位數 24）慢，但是交錯跑、同一時段，可以比 | `shots\perf_result.json` |

## 二、加項（這次新增的）

### 加項 1：換畫面膠卷＋狂點

- 做法：單人一趟走 22 段換畫面（開頁、標題→選角、選角→序章、序章→地圖、地圖→戰鬥／事件／紙箱／罐頭鋪／貓窩／關主門、打贏→戰利品 ×2、戰利品→地圖 ×2、事件→戰鬥、紙箱→地圖、罐頭鋪→地圖、貓窩→地圖、關主門→開場對白、開場對白→開打、收場對白→過關幻燈片、過關幻燈片→過關畫面、過關→走路→第二關地圖），每段錄影、每 25 毫秒取一格量米白比例、近黑比例、亮度跳動；每一段在換畫面那 0.26 秒內狂點「剛按的那顆」與舊畫面上其他點得到的東西（共 3399 下）。新舊各一趟。
- 聯絡表：`sheets\swaps_int.jpg`（新）、`sheets\swaps_old.jpg`（舊），我看過新版那張。
- **整片米白**：新版 22 段 **0 段**；舊版 **12 段**（開頁、標題→選角、地圖→戰鬥／事件／紙箱／罐頭鋪／貓窩／關主門、打贏→戰利品、事件→戰鬥、過關幻燈片→過關畫面、開場對白→開打）。另外打贏→戰利品單獨再跑三場：新版每一格米白最多 3～4%（舊版一格 100%）。
- **閃黑**：新版只剩兩處，舊版一模一樣：選角→序章那段是過場影片開播前的黑底（約 0.2 秒）；收場對白→過關幻燈片是幻燈片第一張圖還沒畫出來的 50 毫秒。舊版的「罐頭鋪／戰利品→地圖」閃黑新版沒有了。
- **舊畫面殘影卡住**：退場層每一段都在 223～284 毫秒內收掉，沒有一段卡住；同一時間沒有兩個 `#screen`。
- **墊在底下的舊畫面被點到**：3399 下裡 **0 下**落在退場層。每一段前後比對：選路只前進一格、戰利品只拿一張、罐頭鋪只多買我點到的、沒有重複出牌、沒有例外。
- 連線（`shots\coopswap_int\`）：地圖→戰鬥、戰利品挑牌、再選一格，兩台在換畫面時交錯狂點（42～78 下），每台每一輪只送出一次 `pick`，選路各前進一格、牌組各多一張、沒有紅色橫幅，最後兩台局面一模一樣。
- 看到但不算問題的：
  - 新畫面淡入那 0.22 秒裡**新畫面已經點得到**：狂點地圖格子時，點到罐頭鋪貨架的位置就買了、點到關主格子的位置就把「推開門」按下去了。舊版一樣（舊版連淡入都沒有，一樣立刻點得到）。玩家快速連點地圖格子才會碰到，列低（L-8）。
  - 事件→戰鬥那段有一格是「牌組」視窗：是我的狂點點到狀態列的「牌組」鈕，不是畫面問題。
  - 地圖→罐頭鋪約 300 毫秒沒有新的一格（罐頭鋪畫面重），之後直接出現幾乎淡完的罐頭鋪；舊版一樣，polish 報告也記過。

### 加項 2：M-1 鏡子走廊（分身做，我看過 `gate4\_鏡子走廊四組.jpg`、`gate4\_事件圖.jpg`）

- 四組真連線（貼碼直連）：球球開房＋菲菲加入、菲菲開房＋球球加入、封封開房＋噹噹加入、噹噹開房＋封封加入。每組兩台 × 開頭／打一場／走過去三段，共 24 格：
  - 兩台插圖都照 0 號那一位；
  - 1 號那台左邊放自己的立繪，腳底跟插圖底邊對齊（差 0 像素），沒踩到「鏡子走廊」名牌；0 號那台不放；
  - 三段文字都在講 0 號那位的倒影，跟插圖對得上；沒有紅色橫幅。
- 十二組混搭（除錯捷徑改成兩人局，自己坐 1 號）：插圖都照 0 號、立繪都在，三段文字逐組讀過都對得上。
- 上一輪的 M-1（球球開房＋菲菲加入，菲菲那台文字寫頭巾、圖是她自己）：**修好了**。

### 加項 3：M-3／L-1 頭大小修正（分身量，我看過 `gate12\_頭修正_1_1.jpg`、`_1_2.jpg`、`_2_2.jpg`）

- 逐格（照遊戲比例量頭；「本法」量整顆頭、吻合度不夠改量頭頂，「美術法」是美術修正用的四種量法取中位數）：

  | 動作 | 本法 上一輪→這次 | 美術法 上一輪→這次 | 實機逐格截圖 舊版→這次 |
  |---|---|---|---|
  | 噹噹隱身 | 1.027～1.113 → 0.968～1.037 | 1.040～1.090 → 0.970～1.025 | 1.112～1.122 → 1.045～1.048 |
  | 噹噹掛彩 | 0.998～1.089 → 0.968～1.057 | 1.000～1.055 → 0.970～1.025 | 1.055～1.065 → 1.025～1.035 |
  | 封封集中 | 0.931～0.971 → 0.968～1.018 | 0.930～0.970 → 0.975～1.020 | 0.928～1.005 → 0.985～1.005 |
  | 封封重劈（不含舉劍擋頭的第 3 格） | 0.893～0.978 → 0.951～1.047 | 0.895～0.975 → 0.955～1.040 | 0.893～1.015 → 0.955～1.025 |

  - 重劈第 3 格舉劍遮住頭、第 5 格本法鎖錯位置（美術法 1.005），人眼看大小跟其他格一樣。
  - 實機逐格是逐格模式下真的掛隱身、把血降到兩成、真的打吐納與重劈，每 90 毫秒量一格。
- 靜態七張（`?motion=0` 實機截圖，跟待機比頭）：噹噹隱身 1.113→1.047、噹噹掛彩 1.089→1.047、封封集中／卷軸／技能（同一張）0.931→0.988、封封拳 0.886→0.951、菲菲翻肚 0.823→0.876（躺姿量不準；打包紀錄 0.971、跟來源格比 0.965，人眼看頭跟待機差不多大）。七張腳底跟待機差都不超過 2.4 像素。
- 我看聯絡表的判斷：修過的幾套站在同一條地面線上，跟待機並排看不出忽大忽小；封封拳換成拿劍的那一格，大小對了。

### 加項 4：連線牌面

- 兩組連線第一場，每人手上各換進兩張連線牌（分你一半、幫你一把／你也抽一張、飯糰分你）。在手牌節點插進頁面的那一刻量圖：
  - 球球＋菲菲：兩台 4 張都是 `coop_feifei_ninja_*`、載好了、寬 299（開房方還多一張自然抽到的「換你多抽點」，也是這一組的）。
  - 噹噹＋封封：兩台 4 張都是 `coop_dangdang_fengfeng_*`、載好了、寬 299。
- 截圖看得到牌框裡是這兩位一起的圖。證據 `sheets\coop_cards.jpg`、總表第 9 格。

## 三、四項門檻（跟線上舊版比）

### 門檻 1：大小（分身量，我看過 `gate12\_大小_3.jpg`、`_大小_4.jpg`）

- 這一輪只換了加項 3 那七張靜態圖，其餘跟上一輪同一批檔，數字相同。
- 空手擲出 8 格：球球 0.97～0.99、菲菲 0.98～1.01、噹噹 0.99～1.05、封封 0.98～1.02。
- 對白頭像只有噹噹刻意換的那張（頭 1.10→1.14）；過關亮相、三關地圖頭像、選角新舊數字一樣。
- 量到超過 5%、但跟上一輪同一張檔、人眼看跟待機一樣大、是量法鎖錯位置的：球球踢 0.93、拳 0.94、吼 0.90；封封出招 0.76、翻肚 0.77、餓 0.89、吼 0.68、倒地 0.78（躺姿、張嘴、舉劍、低頭）。人眼並排見 `gate12\verify_*.jpg`。
- 封封腳底：24 種靜態圖跟待機差都不超過 2.4 像素，不浮不沉。舊版就有、新舊一樣：勝利浮 12.5、吃東西浮 14.5、倒地沉 68 像素。
- 跟舊版比：舊版靜態圖頭大小 0.52～1.06 亂跳、封封浮空 10～76 像素；新版一致。

### 門檻 2：貓窩（分身量，我看過 `gate12\_貓窩_3.jpg`）

- 四隻 × 三關 × 三張底圖 × 五種姿勢（進門蜷縮、打盹、磨爪、扶同伴、倒地）共 180 組，新舊外框差 0 像素、圖檔相同；真的按打盹、磨爪四隻 × 三關新舊各 24 次，也是差 0 像素。
- 每隻都在籃子裡或籃子前，沒滿出來、沒坐到地上、沒被切掉；封封沒浮也沒沉。單人四趟實際走到的 14F 貓窩截圖（`sheets\solo_*_B.jpg`）我也看過，位置正常。
- 扶同伴沒有真的連線觸發，是換圖片網址截的。

### 門檻 3：流暢度

- 四隻 × 新舊，每 50 毫秒一格：近戰、近戰後 350 毫秒接防禦、丟東西、挨打、擋一部分、最後一擊＋勝利。聯絡表 `sheets\fluid_ninja.jpg`、`fluid_feifei.jpg`、`fluid_dangdang.jpg`、`fluid_fengfeng.jpg`（新＝qq-int2、舊＝上一輪錄的線上版），我看過噹噹、封封那兩張，其餘兩張用程式掃亮度，沒有跳動。
- 單人四趟每張丟擲牌、忍具、第一次挨打、勝利、過關走路轉場；連線開房方看魔物打自己、看同伴被塞牌。
- 結果：沒有瞬間跳位、忽大忽小、露出舊畫風；逐格模式下靜態立繪 0 次露出；全部膠卷掃亮度，**上一輪抓到的勝利後閃白 6 次這次 0 次**。

### 門檻 4：事件圖片（分身做，我看過 `gate4\_事件圖.jpg`、`_鏡子走廊四組.jpg`）

- 四隻 529 格主圖＋結果圖，新舊圖檔名一張都沒變；內容不同的只有菲菲鏡子走廊三格（刻意）。每隻都是自己那份，沒有退回別隻的圖。
- 文字只有一處不同：封封「山賊再現」結果 2（劇情修正刻意改的）。
- 旁邊放立繪只在連線的鏡子走廊出現；5F 秘笈「插圖裡有這隻貓就不放頭像」單人、混搭、真連線都照規則。
- 新版 580 個圖網址全部 200，0 錯誤。

## 四、問題清單

### 高
- 無。

### 中
- 無。

### 低
- L-2｜舊版就有：丟東西打死魔物時，魔物身上「越戰越勇」「打倒回血」那類牌子、勝利台詞在東西飛到之前就先變（這次 60 次丟擲裡 2 次，都是最後一擊）。
- L-3｜舊版就有：「擋住 N」跟同一拍的「-N」疊在同一點。總表第 19 格。
- L-4｜同伴出牌讓位的淡出淡入交疊約 0.45 秒（跟上一輪一樣）。
- L-7｜中繼斷線要等約 146 秒才出「回標題」（接回寬限的設計，舊版一樣）。
- L-8｜舊版一樣：新畫面淡入那 0.22 秒已經點得到，快速連點地圖格子可能直接點到下一個畫面同一位置的東西（罐頭鋪買到貨、關主門被推開）。總表第 18 格。
- L-9｜這次修的範圍內、邊界值：噹噹掛彩逐格第 7、8 格，本法量到 1.057（超過 5%），美術法 1.025／1.020、實機截圖 1.025～1.035；這兩格有汗滴、頭轉開，本法只量得到頭頂（噹噹自己的待機用本法也量到 1.037）。判斷在量法誤差內，人眼看不出比待機大。重現：噹噹開戰、血降到兩成看掛彩逐格。證據 `gate12\_頭修正_1_1.jpg` 第 4 列。
- 貼著 5% 但在範圍內的：噹噹隱身靜態 1.047、掛彩靜態 1.047、封封拳 0.951、噹噹衝刺 0.95。

## 五、主控台錯誤與 404

- 新版：單人四趟、`?motion=0`、手機、連線兩組、連線狂點、換畫面、流暢度四隻、閃白、效能、事件掃描與鏡子走廊四組全部 **0 錯誤、0 警告、0 個 404、0 個頁面例外**。唯一的主控台錯誤是斷線測試裡遊戲自己記的 `[連線] 停下來了：…`（預期中）。
- 舊版：每次開頁 `/favicon.ico` 404 兩次（新版有網站小圖示，沒有了）。

## 六、跟線上版比

變好（都實機量過）：
- 換畫面：整片米白 12 段 → 0 段；罐頭鋪／戰利品→地圖的閃黑沒了；開頁從深色淡入。
- 丟東西：牌子等飛到才變（舊版出牌後 5～150 毫秒就先變）；四隻空手擲出。
- 連線：自己擋下飄「擋住 N」、塞牌的光打在中招的同伴、同伴牌讓位、雙貓過關幻燈片、斷線回標題不會以單機進兩人局。
- 連線鏡子走廊：插圖照 0 號、旁邊放自己的立繪、文字對得上（舊版十組是照自己，跟要打的對手不符）。
- 連線牌：混搭牌面、第一次畫出來就有圖。
- 大小：靜態立繪新畫風、大小一致、封封不浮空。
- 效能：點牌到畫出來中位數 40 對 48、平均 39.6 對 56.0 毫秒。
- 手機：0.38 秒慢點照常出牌。
- 網站小圖示 404 消失。

變差：
- 沒找到。

沒變：貓窩（差 0 像素）、事件圖（只有刻意換的菲菲鏡子走廊）、近戰與挨打逐格、「擋住 N」與「-N」疊字、最後一擊的勝利台詞先跳、新畫面淡入時就點得到。

## 七、沒測到的

- 真手機、真平板、iOS Safari、有畫面（有顯示卡）的瀏覽器、聲音。
- 手機只測 844×390；平板、740×360、667×375 沒重測。
- 同角色雙人連線、第三關與結局沒走到；事件後集沒走到。
- 連線狂點只測了地圖、戰利品、再一格三段；連線的罐頭鋪、事件、過關畫面沒有狂點。
- 效能比的是新版打包檔 vs 舊版開發伺服器（派工規定），兩邊條件不完全一樣。
- 大小：扶同伴沒真的連線觸發；過關走路轉場裡貓的大小沒量；只驗 1280×720；躺姿、背面、閉眼、張嘴、舉劍擋頭的量頭不可靠，改人眼判斷。
- 事件：三個「從 3 張牌挑 1 張」的選項本來就沒有插圖；十二組混搭只有四組是真連線，其餘八組用除錯捷徑。
