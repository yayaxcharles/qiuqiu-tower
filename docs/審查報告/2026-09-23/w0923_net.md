# net 代理報告（2026-09-23 連線稽核修正）

worktree：`F:\ClaudeWork\qq-net`（分支 `w0923-net`，從 coop `1902d32` 開出）。只改這個副本，沒推送、沒合併。

## 驗收總結
- `npx tsc --noEmit -p .`：零錯誤。
- `npx vitest run`：282 個檔案通過、5 個略過；2415 條通過、6 條略過、0 條失敗。
- 雷 5：跑完後 `docs/怪物工作檯.json` 只差日期，`docs/事件文案.md`、`docs/分關載入.json` 只差換行（numstat 是空的），都已 `git checkout --` 還原；沒有內容真的變動的重生檔。
- 每一條修正都做過「暫時改回壞寫法 → 對應測試變紅 → 還原」，共 13 處，全部變紅。用的腳本是 `scratchpad\w0923\net_revert_check.py`（逐位元組還原原檔，跑完 `git status` 確認乾淨）。
- 新測試檔案用 LF 換行存檔（git 會自己轉），比對多行原始碼的地方都先 `.replace(/\r\n/g, '\n')`；沒用到 `node:` 模組，也沒用假的瀏覽器環境。畫面層沒有新增「喵」字。

---

## 高-1　連線序章中途「回標題」→ 兩人局被當單機玩、單機存檔被蓋掉

### 改了什麼
- (a) 離開連線時把那一局一起丟掉：`src/ui/app.ts:317`、`:331`。`leaveCoop()` 先記下「這是不是連線局」（`coop` 還在，或局面人數大於一），是的話 `run`、`cs` 清成 null，並收掉所有還在演的劇情疊層。
- (b) 劇情疊層收掉、`onDone` 不會在離開後還接下去：
  - `src/ui/overlay.ts:76-87` 新增 `closeWithStory`／`closeStoryOverlays`（跟既有的 `closeWithScreen` 同一種寫法）。收掉時**不叫 onDone**，只拆節點、解鎖畫面。
  - 幻燈片 `src/ui/slides.ts:67`、對白 `src/ui/dialogue.ts:169`、過場影片 `src/ui/video.ts:32` 開場時登記，正常演完時自己除名。
  - 保險：序章 `app.ts:215`、只播一次的劇情 `app.ts:353`、關主開場後開打的 `go` `app.ts:413`，播完時都先確認「還是同一局」才接下去。
- (c) `save()` 多一道防線：`app.ts:303`，局面 `run.players.length > 1` 就絕不寫單機存檔，不管 `coop` 還在不在。判準選人數，是因為引擎本身（`engine/run.ts`、`actions.ts` 等十幾處）一律用 `players.length > 1` 判斷是不是兩人局，最可靠。
- 同一類洞一起堵（記取 result.ts 註解裡「同一件事有兩個呼叫點時，擋一個等於沒擋」）：打完整局記成績、清存檔 `app.ts:514`，結算畫面 `src/ui/screens/result.ts:36`，都加上看局面人數。

### 查過的其他路徑（連線中途回標題、斷線、關主戰中離開）
- 連線中能回標題的只有兩處：紅色橫幅（`lobby.ts:88`）和結算畫面的「回標題」（`result.ts`，本來就清掉了 run）。地圖、事件、貓窩、罐頭鋪、紙箱、戰利品、過關亮相、關主門都沒有自己的回標題鈕，全靠那條橫幅。
- 會在離開後接下去的，都是劇情疊層的 onDone：序章→地圖；塔頂段落→關主開場→開打；落敗對白→結算；結局影片→結局幻燈片→結算；關主收場對白→過關幻燈片→過關亮相；黑貓頭目那段→戰利品。這些現在全部被 (b) 收掉。就算真的漏接，(a) 已經把 run 清成 null，那幾個畫面開頭都有 `if (!run) show('title')`。
- 計時器：開打前暖機（`startFight` 的 `proceed`）看 `this.cs !== cs`、戰鬥收場看 `app.cs !== cs`，(a) 清掉 cs 之後會自己退場；貓窩、關主門的計時器本來就掛在 `disposers`，換畫面就拆了。
- 只斷線、沒按回標題：`coop` 還在，`save()` 原本就不寫，行為不變。

### 測試（每條都確認改回壞寫法會變紅）
- `tests/ui/coop_leave_0923.test.ts`（把 App 那幾支方法原樣切出來跑）：
  - 「連線已經離開、局面還是兩人局：不存」→ 拿掉 `app.ts:303` 那行就紅。
  - 「連線局按回標題：run／cs 清掉、劇情疊層收掉」→ 拿掉 `app.ts:331` 就紅。
  - 「連線序章播到一半離開連線：點完幻燈片不叫 after」→ `done` 改回直接叫 `after` 就紅；playOnce 那條同理。
  - 另外兩條守住「單機照存」「單機的新的一局、續玩不受影響」。
- `tests/ui/story_overlay_0923.test.ts`（用真的三支疊層、真的鎖畫面，只把建節點換成假節點）：三層同時開著一次收乾淨、onDone 一個都不叫、畫面解鎖、影片停下 → 拿掉幻燈片的登記、或讓對白收掉時叫 onDone，都會紅。
- `tests/ui/motion_result.test.ts` 新增「兩人局就算 coop 已經是 null，也不記成績、不清單機存檔」→ `result.ts:36` 改回只看 `app.coop` 就紅。

### 實機驗證（依派工單）
- 腳本：`scratchpad\w0923\net_pw\coop_leave.mjs`，結果與截圖在同一個資料夾（`fixed_*`、`before_*`）。
- 做法：`SITE_NAME=qiuqiu-tower-coop npx vite --port 5231` 起本機伺服器；網址加 `?debug`。兩位玩家各開一個獨立的瀏覽器（`channel:'chrome'`，各自用剛建的暫存設定檔，用完就刪；沒用使用者的 Chrome 設定檔），走「貼碼直連」，沒碰正式中繼、也沒碰正式網站。邀請碼、回應碼由腳本從一頁讀出來填進另一頁（兩個瀏覽器設定檔不共用儲存，沒法用 localStorage 傳）。
- 步驟：先在 A（本機）塞一個單機存檔當對照 → 兩台連上、各自進到連線序章 → 關掉 B 模擬斷線 → A 跳出紅色橫幅 → 按「回標題」→ 在畫面中央繼續點（把幻燈片點完）再等兩秒 → 再模擬「走完第一格」叫一次 `backToMap()`。
- 修正後（跑兩次都一樣）：按下去當場回到標題、疊層 0 個、run 是 null、畫面沒被鎖；點完、等完還在標題；單機存檔跟對照一模一樣（仍是 1 人局）。
- 對照組：把 `src` 的改動暫時收起來（`git stash`，跑完已經還原）跑同一支腳本，重現了稽核寫的情況：按回標題後幻燈片還蓋在標題上、畫面被鎖；點完就進了地圖、局面是兩人局；走一格之後單機存檔被換成 2 人局。所以腳本確實走到那條路，不是空測。
- 跑完已關掉瀏覽器與伺服器（Vite 的 node 子行程是停掉外殼後另外確認命令列才結束的，5231 埠已空出）。
- 旁注：兩個版本載入頁面時主控台都有一則「404」，頁面自己的回應事件抓不到它（可能來自背景工作），修正前後都有，跟這批改動無關；沒有再追。

---

## 中-1　塔頂段落長短不同，先進場的人 60 秒後能替還在看劇情的同伴收回合

### 改了什麼
- 新增一種網路訊息 `{ m: 'here', f }`＝「我進到第 f 場戰鬥了」：`src/net/transport.ts:90`。不進鎖步、不進對帳。
- 會話：新的一場 `attach` 時送一次（`src/net/session.ts:174`）；收到就記下同伴進場到第幾場（`:573`）；`mateHere`＝同伴也進了現在這一場（`:202`）；**同伴還沒進場時 `submit` 直接不收「替他收回合」**（`:373`）。
- 戰鬥畫面：閒置計時在同伴還沒進場時一直歸零，從他進場那一刻才起算（`src/ui/screens/combat.ts:944`）。
- 補充一個讀程式時發現的細節：原本的計時其實是從「我舉手後第一次檢查」才起算（`mateIdleMs` 第一次被叫時會因為回合數對不上而歸零），不是稽核寫的「從戰鬥畫面建立那一刻」；但結論一樣——我幾秒就舉手，60 秒後同伴還在看劇情，按鈕照樣會亮。
- 版本相容：房號中繼與貼碼直連兩條路都會比對打包編號，不同版本連不上，所以不會有「舊版收不到 here、按鈕永遠不亮」的情況。

### 測試
- `tests/net/mate_here_0923.test.ts`，用 `LoopbackPair.hold` 做出「一台先、一台後」：
  - 「先進場的那台：同伴 attach 之前 mateHere 是 false、替他收回合送不出去；他進場的那一則到了才可以」→ 拿掉 `session.ts:373` 就紅（修正前送得出去，後進場那台一進場第一回合就被收掉）。
  - 「我先進場 70 秒、同伴才進場：他進場後滿 60 秒才亮」（切出戰鬥畫面那三行照原樣跑，模擬每秒檢查從我舉手後開始）→ 拿掉 `combat.ts:944` 就紅。第一版這條測試沒有先模擬「舉手後第一次檢查」，改回壞寫法也不紅，已經修正測試再驗過一次。
  - 另一條守住「下一場重新算，上一場的 here 不算」。

---

## 低-1　分頁在背景時最後一刀的動作收不掉，停在打完的戰鬥畫面

### 改了什麼
- 新的純函式 `motionStillPlaying`：`src/ui/qiuqiu-combat-motion.ts:208-214`。背景分頁（`document.hidden`）一律當演完；前景時超過預定結束時間（`endsAt`）500 毫秒還沒收的才當演完。畫面刷新回呼正常時，結束那一刻十幾毫秒內就會收掉，所以前景的正常演出不會被提早切掉。
- 收場改用它：`src/ui/screens/combat.ts:3391`（原本只看 `active`）。

### 測試
- `tests/ui/background_finish_0923.test.ts`：六條純函式的邊界（前景未到、剛過一點、超過一段、背景、看 `document.hidden`、沒在演）；再把真的 `checkOver`／`finish` 切出來跑、畫面刷新回呼一次都不跑：
  - 「背景分頁：同伴那一刀收不掉也照樣收場」→ 改回只看 `active` 就紅（永遠不交棒）。
  - 「前景：還沒演完就等它，不提早切掉」→ 確認不會在預定結束之前離開。
- 既有的 `tests/ui/combat_motion_flow.test.ts` 那三組收場測試（有模擬畫面刷新）照樣通過，只補了 `motionStillPlaying` 的綁定。

---

## 低-2　舉手齊了 `hold()` 之後、`release()` 之前那段沒接例外

### 改了什麼
- 原本那支處理函式改成具名的 `onApplied`（`combat.ts:3645`），本體一行不動；外面再包一層註冊到會話上（`combat.ts:3946`）。照既有「連線演出例外防護」的寫法：記一行 `console.error('連線演出準備失敗，照常收尾', …)`、收掉演出，然後：
  - 收尾還沒開始 → 照常收尾（收牌→魔物回合→演完才 `release()`，跟平常同一條路）；
  - 收尾本身丟的例外、或還沒走到定義收尾那一行 → 直接 `release()`，並多記一行。寧可同伴下一手在這邊套不進去、兩邊跳紅色橫幅，也不要兩台無聲互等。
- `finishApplied` 改成只收一次（`combat.ts:3676`），並把自己交給外層（`:3702`），避免退路多跑一次魔物回合。
- 演出佇列那支算「這一項要等多久」原本在防護外（`combat.ts:2651`，它會查動作長度，資料不齊一樣會丟），丟了佇列就永遠停在「演出中」、收尾那一項輪不到 → 一起包進去。這一處稽核寫「有接住」，是我讀程式時發現同一類的漏洞，順手一起堵，特此說明。

### 測試
- `tests/ui/applied_guard_0923.test.ts`：
  - 整合測試（照 `combat_batch_hold` 的做法，把真的那一段接上兩台真的會話）：「批次重播算撞擊計畫時丟例外：照常收牌跑魔物回合、放開會話，同伴下一回合的牌套得進來」→ 外層改回沒有防護就紅。
  - 外層退路四種情況（收尾還沒開始、還沒走到定義收尾、收尾自己壞、退路再壞一次）。
  - 「等待時間算不出來的那一項跳過，後面的收尾照常」→ 把那一行移回防護外就紅。
- 既有兩支切這段原始碼的測試（`tests/ui/combat_batch_hold.test.ts`、`tools/coop_screen_flow.test.ts`）只補了 `turn: {}` 綁定，斷言不變。

### 沒做的
- 收尾交出去之後、在計時器裡跑的魔物回合（`runEnemyTurn` 的逐步演出）如果丟例外，一樣不會 `release()`。那是非同步的另一段，不在這一段準備程式裡，這次沒動。

---

## 低-3　心跳 3.5 秒被主執行緒卡 2.5～3 秒誤觸發

### 改了什麼
- `src/net/ws.ts:168`：這一拍比預定晚了半拍以上（兩拍間隔超過 1.5 秒），就是自己卡住，把晚掉的那一段從「多久沒收到」裡扣掉。計時器準時的拍子一毫秒都不扣，所以平常真的斷線照樣在 3.5 秒多一點就報；原本「超過 3 秒整段不算」那條保留。

### 測試
- `tests/net/ws.test.ts` 新增兩條：
  - 「主執行緒卡了快 3 秒：晚掉的那一拍扣掉，不先誤報一次 away」→ 拿掉那一行就紅（修正前會先報 away）。
  - 「卡過一次之後線真的斷了：跟沒卡過一樣在 3.5 秒多一點報」→ 用跟既有斷線測試同一組數字，證明不會變慢。
- 既有 27 條全部照過。

---

## 需要主控決定的事
1. 中-1 新增了一種網路訊息 `here`。兩條連線路都會擋不同版本，所以相容沒問題；但這是協定層的改動，合併時請留意有沒有別的分支也在改 `NetMessage`。
2. 低-2 的最後一道退路是「直接放開會話」：代表極端情況下會變成兩邊跳紅色橫幅，而不是無聲互等。我認為這樣比較好，如果主控想要別的處理方式請告知。
3. 低-1 的「前景超過預定結束 500 毫秒才當演完」是我選的數字（寧可多等半秒，也不要切到正在演的動作），要調可以改 `MOTION_OVERRUN_MS`。

## 主控裁定（2026-09-23）
三件都照原案：`here` 訊息保留（合併時主控會注意）、低-2 最後退路直接放開、500 毫秒不動。

---

## 追加：魔物回合交給計時器之後，演出丟例外也要放開會話（補上低-2「沒做的」）

### 改了什麼
- `src/ui/screens/combat.ts:2552`：`runEnemyTurn` 改成「包一層防護再跑」，原本的內容原封不動搬到 `startEnemyTurn`；`:2561` 每一步 `step` 也包同一層（只改頭尾兩行，本體沒動）。
- `combat.ts:2600-2618` 新增 `guardEnemyTurn`／`rescueEnemyTurn`，照低-2 的風格：
  - 丟例外 → 記一行 `魔物回合演出失敗，照常收尾` → 照常收尾：
    - 引擎還在魔物回合中（`cs.enemyActing`）就把剩下的魔物照常跑完、收掉這一回合，只是不演。走法跟引擎的 `endTurn` 一樣，兩台才會走到同一個地方、不會分岔。
    - 接著畫面重畫回真實狀態、照常判勝負、照常開選牌視窗。
  - 最後一定 `release()`；連收尾都丟例外，就多記一行、直接放開。
- 單機也吃得到：原本演出壞掉會停在魔物回合、按鈕全灰，現在一樣照常收尾。
- 附帶效果：收牌動畫很短、當場就開演魔物回合的那條路（在 `onApplied` 裡同步跑），現在由這一層先接住並把引擎那一半走完，不會落到低-2 外層「直接放開」那條比較粗的退路。

### 測試（`tests/ui/applied_guard_0923.test.ts` 新增一組，用真的引擎跑戰鬥畫面真正的 `runEnemyTurn`，出招預告打開，所以每一步都在計時器裡跑）
- 「沒出錯時照原本的路」：只放開一次，結果跟引擎一口氣收完一樣（守住防護沒有改到正常流程）。
- 「第一隻出完手、計時器裡那一步的演出丟例外」：只記一行、放開一次、魔物回合沒停在一半，整場指紋跟一口氣收完的對照組一樣，判勝負與選牌視窗都有跑。
  - 讓防護變成什麼都不包 → 紅（會話沒放開）。
  - 拿掉「引擎照常走完」那一行 → 紅（指紋對不上＝兩台會分岔）。
- 「連收尾都壞了」：還是放開一次，並多記一行。
- 既有 `tools/coop_screen_flow.test.ts` 切這一段的兩條測試不用改就照樣通過。

### 驗收
- `npx tsc --noEmit -p .` 零錯誤。`npx vitest run` 2418 條通過、6 條略過、0 條失敗。
- 雷 5 的重生檔照樣只差日期與換行，已還原。

### 還沒涵蓋的
- 出招預告那個計時器（只亮一下下一隻要出手的魔物）沒有包：它丟例外不會打斷整串，下一步照樣會跑。

---

## 推前審查修正（第二次推送用；`prepush_review.md` 低-4、低-2）

先照指示 `git merge w0923-int`：我的提交原本就都在整合分支裡，所以是直接快轉到 `3054127d`，沒有衝突。之後兩條各一筆提交。

### 低-4　兩個極少見的例外點，退路收不了尾、兩台分岔
- 新增 `turnWaiting()`（`src/ui/screens/combat.ts:2722`）。成立條件：都舉手了、沒人在選牌、魔物回合還沒開始、也不在收牌中。
  - 只看引擎狀態，所以兩台判斷一致。
  - 單機沒有舉手旗標，永遠不成立。
- 魔物回合的退路（`:2704`）：例外丟在魔物回合開始之前（例如開頭那一次快照）時，照 `endTurn` 補跑一次，兩台停在同一個地方。
- `onApplied` 外層退路（`:4162`）：收尾還沒接手就丟例外、而其實都舉手了，就對帳、暫停、照常開魔物回合（演完自己放開），不再只是放開。
  - 已經對帳暫停過的（`turn.sealed`，`:3878` 設定），不會再對帳一次。
- 做法跟審查建議有一處不同：我**沒有**把 `completedTurn` 那段挪到最前面，改成讓外層退路自己判斷 `turnWaiting()`。
  - 效果相同，而且不用搬動那一大段程式。
  - 另外三支切這段原始碼的既有測試也不用改。
- 測試（`tests/ui/applied_guard_0923.test.ts`）：
  - 兩台真的會話，最後舉手那一批在算出「這回合收完了」之前丟例外 → 兩台停在同一回合、下一張牌照常套得進去、沒有分岔。
  - 魔物回合開頭丟例外 → 結果跟引擎一口氣收完的一樣。
  - 外層退路的兩種新情況（沒對帳過、已對帳過）。
  - 兩處各自改回壞寫法都會紅。

### 低-2　同伴一直掛在劇情或關主門不點，這邊永遠不能替他收回合
- `src/net/session.ts:15` 新增 `MATE_ABSENT_MS = 180_000`。
- `:186` 自己進場時記下時間；`:225` 新增 `mayForce`：同伴進場了就可以；沒進場的話，我進場滿 180 秒也可以。`:399` 送出「替他收回合」改看它。
  - 只看這一台自己的時鐘。送出去的仍是一個明確的動作，兩台照編號套用，不會因為時鐘不同而分岔。
- 戰鬥畫面（`combat.ts:998`）：長上限一到，閒置計時就不再歸零，原本的一分鐘規矩照舊。
  - 所以同伴一直不進場時，按鈕是在我進場後約 **4 分鐘**亮（180 秒到了才開始數那一分鐘）。
  - 這是照「60 秒規則照舊、只是起算點補退路」的字面做的。想改成 3 分鐘整就亮，把這一行改成長上限到了直接回傳夠長的閒置就好。
- 測試（`tests/net/mate_here_0923.test.ts`）：
  - 用假時鐘：還差一秒送不出去，滿 180 秒送得出去。
  - 同伴那台的時鐘撥慢十分鐘再進場，排隊的動作照樣套下去、兩台指紋一致。
  - 畫面的閒置計時：長上限前不算閒置，之後再滿一分鐘才亮。
  - 會話那一行、畫面那一行各自改回舊寫法都會紅。

### 驗收
- 合併後型別檢查零錯誤；全套 2579 條通過、6 條略過、0 條失敗。
- 重生的 docs 檔只差日期與換行，已還原。
- 反向變紅腳本（`net_revert_check.py`）在合併後的程式上重跑全部 19 處，全紅。

## git log --oneline（第二次推送新增的兩筆）
```
c2ad9ec1 推前審查修正 低-2：同伴一直掛在劇情不進場，滿三分鐘也可以替他收回合
226f2a6e 推前審查修正 低-4：例外丟在魔物回合開始之前，退路也照常收這一回合，兩台不分岔
```
合併 `w0923-int` 之後 `coop..HEAD` 共 55 筆（含其他分支的），我自己的是上面兩筆加下面這幾筆。

## git log --oneline coop..HEAD（合併前，我的提交）
```
d50ec24 連線稽核 低-2 追加：魔物回合交給計時器之後演出丟例外，也照常收尾、放開會話
90a8305 連線稽核 中-1、低-1、低-2：同伴進場才起算閒置、背景分頁收得了場、收回合準備演出丟例外照常放開
3019672 連線稽核 低-3：主執行緒卡兩三秒不再誤報「連線不穩」
69a5004 連線稽核 高-1：序章中途斷線回標題，不再把兩人局當單機玩、蓋掉單機存檔
```
