# fix 代理報告（2026-09-23）

worktree：`F:\ClaudeWork\qq-fix`，分支 `w0923-fix`（從 coop `1902d32` 開出）。兩筆提交，沒推、沒合併。
驗收：`npx tsc --noEmit -p .` 零錯誤；`npx vitest run` 278 個檔、2403 條全過（5 檔／6 條原本就跳過）。
跑完照雷 5 收拾：`docs/怪物工作檯.json`（只差日期）、`docs/事件文案.md`、`docs/分關載入.json`（只差換行）還原；
`docs/牌池總表.json` 有四張牌的牌面真的變了，跟第一筆一起提交。

「確認會紅」的做法：每一條都把修正暫時改回舊寫法（或用 `false &&` 關掉），跑對應測試看到那一條紅，再改回來、看到全綠。

---

## 引擎與牌面（engine.md 低 1～6）——提交 0e436ed

### 1. 集中精神之後，飯糰類忍具白白用掉
- 改了什麼：新增 `potionBlockedReason(p, def)`（`src/engine/combat.ts:1090`），回「此刻為什麼用不出來」：
  原本的 `PotionDef.usable`（起死回生丹）收進來，再加一條「身上有 `energyGainBlockedThisPhase` 而且忍具效果裡有 `energy`」→
  回「集中精神之後，這回合不能再獲得飯糰」。`canUsePotion`（`combat.ts:1075`）改用它。
  畫面忍具列（`src/ui/screens/combat.ts:1781`）改讀同一支：格子變灰（`not-ready`）、說明框多一行原因，照起死回生丹那套。
  兩支機器人也改讀同一支（`src/engine/bot.ts:29`、`src/engine/smartbot.ts:856`）——不改的話 `bot.ts` 在模擬裡會因為 `usePotion` 回 false 直接丟例外。
- 測試：`tests/fengfeng_0923.test.ts`「低-1」三條（擋下、留在袋子、不給飯糰的照常用、下一回合恢復；起死回生丹原因字串；畫面與兩支機器人都讀同一支）。
  關掉 energy 那一行 → 第一條紅。
- 實機：封封開戰、打集中精神後三格變灰（`fix_shots/01c_potion_before.png` 對照 `03b_potion_zoom.png`），
  說明框「本回合多 2 顆飯糰。（集中精神之後，這回合不能再獲得飯糰）」（`03_potion_blocked_after_jizhong.png`）；點下去忍具仍在袋子裡。
- 範圍說明：派工單點名兩顆飯糰、半卷殘頁；**「飯糰」（onigiri，+1 顆）也是同一個毛病，一起擋了**。
  另外**半卷殘頁的「抽 2 張」其實還拿得到**，照派工單一律擋；若主控想讓它照樣能喝（只丟掉飯糰那一半），把 `some` 改成 `every` 一行就好，但那樣又回到「一半效果靜靜消失」。

### 2. 絕學·藏鋒「同名取高」
- 牌面：`src/ui/cardtext.ts:357`，每回合開始時那一支也補「（同名取高）」→「每回合開始時獲得 2 點蓄氣（同名取高）。」
- 引擎：選「**不能打並說明**」。`canPlay`（`combat.ts:390`）的取高判斷原本只看有沒有磨過；改成升級版的能力內容
 （效果、牌型、蓄氣門檻、每回合一次）跟已掛的一模一樣時，也當成「同名同級」擋下，原因沿用「同名或更高版本的能力已經生效」。
  理由：「取高」的本意是只留比較強的那份。循息、收勢、連息的升級版數字變大，照舊取代基礎版（FG-T11 與新測試都還綠）；
  藏鋒的升級只降費用，掛上去的能力分不出差別，取代它等於白花一張牌和飯糰——跟同級一樣變灰才一致。
- 測試：「低-2」三條（牌面兩版都有那四個字；已掛基礎版時升級版 `canPlay` 回那句原因、飯糰手牌能力不動；循息升級版照舊取代）。牌面、引擎各自改回舊寫法都會紅。
- 實機：`02_cunfeng_upgraded_disabled.png`（打完基礎版後升級版變灰，說明框「這張打不出來／同名或更高版本的能力已經生效」）。

### 3. 退步守勢、看準劍路補「至少」「再」
- `cardtext.ts:166`：「出牌前有**至少** N 點蓄氣的話，**再**…」，接「再」用噹噹 `ifBlock` 同一支 `again()`。
  **借我擋一下**（`ifAllyBlockAtPlay`）是同一型句子（「獲得 3 點蓄氣，…的話，獲得 2 點蓄氣」），一起補了「再」——派工單第 3 條沒點名，屬順手同型，請主控知悉。
  `ifSpentQiAtLeast` 沒動（前面是傷害，後面不是重複的東西，不需要「再」）。
- 測試：「低-3」（兩張三個版本逐字比對）。改回舊句 → 紅。
- 實機：`01b_hand_zoom.png`、`fix_shots_out.json` 的 `handText`：「獲得 8 點蜷縮，出牌前有至少 3 點蓄氣的話，再獲得 3 點蜷縮。」「抽 2 張牌，出牌前有至少 4 點蓄氣的話，再抽 1 張牌。」

### 4. 借我擋一下補「一個人玩時同伴＝你自己」
- `cardtext.ts:28`：`hasAlly` 對 `ifAllyBlockAtPlay` 一律回 true（條件本身就在看同伴），其餘兩種條件句照舊只看 `then`。
- 測試：「低-4」逐字比對。改回舊判斷 → 紅。
- 實機：同上 `handText`：「…再獲得 2 點蓄氣。一個人玩時「同伴」＝你自己。」（引擎同伴倒下也退回自己，跟其他同伴牌同一套，這句涵蓋得到。）

### 5. 戰鬥雜牌不觸發循息
- `combat.ts:626`：「打出牌後」能力那一段加 `!st.def.combatOnly`，跟同一支函式上面詛咒、憤怒同一個排除。
- 查過：`afterCard` 能力全牌池只有循息、收勢、連息三張（都是封封的），牌面都沒寫雜牌也算，沒有刻意吃雜牌的。
- 測試：「低-5」（掛循息、打眼冒金星＋黏液不給蓄氣，接著打護身才拿到 1 點）。拿掉排除 → 紅。
- **沒改、請主控決定**：連線支援牌的監聽（`combat.ts` 的 `coopWatchers`：「同伴第一次打出技能牌／牌時抽 1 張」「自己第一次打出牌時同伴 +6 蜷縮」）
  一樣會吃雜牌，牌面也沒寫。那是另一支函式、不在這次稽核條目裡，我沒動。

### 6. 封封手上的共用牌拿掉「忍術·」
- `src/content/cards.ts:1100`：`cardNameFor` 對封封一律拿掉前綴（沒有專屬改名表，所以不做空表）。
- `src/ui/compendium.ts:38`：圖鑑分區標題對封封顯示「劍術」（跟噹噹「拳腳」同一套，只換顯示字、`pool` 鍵不動）。
- 撞名：把 `tests/content/name_unique.test.ts` 擴成菲菲、噹噹、封封三位看到的名字都跟秘寶／忍具／狀態／別張牌比，沒有撞。
- 存檔：存的是牌號（`save.ts` 驗 `cardById[cardId]`），名字不進存檔；連線指紋也不含牌名。不影響。
- 測試：「低-6」兩條（他拿得到的每張都沒有「忍術·」、絕學留著、球球不動；分區標題「劍術」、沒有叫「劍術」的池子）。兩處各自改回 → 紅。
- 實機：`04_compendium_fengfeng.png`，封封圖鑑分區「起手（3）／劍術（70）／絕學（37）／壞毛病（8）／雙人（28）」，卡名裡沒有任何「忍術」；手牌「瞬間移動」。
- **需要主控決定**：「劍術」是我挑的字（契約寫「不新增劍術**牌池**」，指規則用的池子；這裡只是顯示的四個字）。若不要，改回 `pool` 就是「忍術」標題配無前綴牌名——菲菲現在就是這樣。

---

## 效能（ui.md 低-3、低-4）——提交 b7c19bf

量測方法（`scratchpad\w0923\fix_pw\perf.js`）：同一台、同一個種子，全新暫存設定檔，把 `navigator.deviceMemory` 假裝成 2（上限 128 MB，等同手機），
包住 `createImageBitmap`、`ImageBitmap.close`、`HTMLImageElement.decode` 計數；流程＝開局走完序章到地圖 → 開一場戰鬥 → 等背景解碼停下來。
修前是在我的 worktree 還沒動任何檔時量的（＝coop `1902d32` 原版），修後是兩筆提交之後。原始數字：`fix_perf_before_ninja.json`、`fix_perf_before.json`、`fix_perf_after.json`。

### 7. 快取滿了之後背景還在下載、解開、馬上丟
- `src/ui/decoded-atlas.ts:103`：背景解碼佇列取出一張「預先解、還沒人要畫」的，先用圖的長寬算解開後多大、用它的分數算
  「存進去之後留不留得住」；留不住就跳過（不 fetch、不 createImageBitmap），不記成失敗。正要畫的（`inUse`）照舊一定解、插隊。
  算法抽成 `survivesBudget`（`decoded-atlas.ts:63`），跟 `store` 的放法同一套（放掉分數不比它高的之後不超過上限；同分先放舊的）。
  既有設計照守：仍是 `fetch(force-cache)` → `createImageBitmap(blob)`，沒有從 `<img>` 直接解。
- 測試：`tests/ui/decoded_atlas.test.ts` 新增兩條（裝不下的第三張沒被下載、沒被解，正要畫時照樣解；`survivesBudget` 五個邊界）。關掉跳過 → 第一條紅。
- 實機（開局＋一場戰鬥，全程）：

| 角色 | createImageBitmap 次數 修前→修後 | 解出來總量 | 解完就被 close() |
|---|---|---|---|
| 球球 | 47 → 25 | 276.6 → 144.6 MB | 25 張（150 MB）→ 3 張（18 MB） |
| 菲菲 | 33 → 25 | 192.3 → 144.3 MB | 11 → 3 |
| 噹噹 | 28 → 25 | 159.1 → 141.1 MB | 6 → 3 |
| 封封 | 31 → 25 | 176.6 → 140.6 MB | 9 → 3 |

  剩下那 3 張是戰鬥中「正要畫」的（魔物圖集等）插隊解、擠掉預先解的，這是設計上該發生的。桌機（上限 224／384 MB）影響小。

### 8. 逐格模式下仍把全套靜態立繪解好留到整場結束
- `src/ui/rest-state-motion.ts:48` 新增純函式 `combatWarmPoses`；`src/ui/screens/combat.ts:1150～1162`：
  這一位的逐格動作**已經載好**，只暖「待機狀態」那批（延後下載的逐格圖還沒到時就是它們露出來）＋挨打、閃、擋、勝、敗、倒下六張保險；
  **還沒載好（冷快取第一場）或 `?motion=0`** 照舊全套。出牌、忍具那批招式圖不暖——`combat_no_old_pose.test.ts` 已經釘住每張牌、每種忍具都選得到畫得出來的動作。
- 為什麼留那六張保險：那幾個反應的逐格動作不是延後下載的，照理不會露出靜態圖，實機也沒看到；但那幾條交還路線沒有測試釘著，一位才六張，空白的代價比較大。主控若要再省可以拿掉（每位再少約 7 MB）。
- 測試：`tests/ui/combat_warm_once.test.ts` 新增三條（已載好的那位只暖退路、還沒載好的那位全套；`?motion=0` 全套；純函式兩條）。
  把 `motionReady` 固定成 false → 第一條紅。原本四條照綠。
- 實機：戰鬥畫面開場熱身的角色立繪，四位都是 **31 張（約 36 MB）→ 18 張（約 21 MB）**；`?motion=0` 仍是 31 張（`fix_perf_after_motion0.json`）。
- 空白檢查：
  - 用探針每一幀看角色那格（`probe_static.js`）：四位各出 8 張牌、挨打兩輪，逐格模式下靜態立繪一次都沒露出來（修前修後都一樣）；同一支探針在 `?motion=0` 下看得到挨打、蜷縮、格擋等靜態圖，證明探針本身有效。
  - 把封封「氣勢」逐格圖集卡住不給（模擬慢網路延後下載沒到），爪力 6 開戰：畫面退回靜態立繪 `fengfeng_power`，`complete`、寬 560、可見，有圖不空白，而且那張在戰鬥畫面熱身名單裡（`fix_shots/fallback_fengfeng_power.png`）。
  - 另外兩張 `fallback_fengfeng_hungry.png`、`fallback_fengfeng_curl.png` 是沒觸發到退路的試跑（出牌動作蓋過去、收招後回待機），不算證據。
- 四位各開一場戰鬥出一張牌：逐格畫布都在、角色有畫出來、主控台沒有錯（`05_fight_{ninja,feifei,dangdang,fengfeng}.png`）。

---

## 其他
- 截圖全在 `scratchpad\w0923\fix_shots\`，我都用 Read 看過。實機工具在 `scratchpad\w0923\fix_pw\`（Playwright 用的是另一個會話暫存區裡既有的 playwright-core 1.63，`channel:'chrome'`、全新暫存設定檔、`?debug`，沒碰正式網站）。
- 量測時主控台每場都有一行「Failed to load resource: 404」，修前修後都有，單獨跑一次聽回應沒抓到是哪個網址（多半是瀏覽器自己要的圖示），不是這次改動造成的。
- 本機伺服器只開過我自己的 5251，已經關掉（先看過命令列是 `qq-fix` 的 vite）。

## 需要主控決定的事
1. 半卷殘頁要不要保留「只抽牌」的用法（見第 1 條）。
2. 封封圖鑑分區標題用「劍術」可以嗎（見第 6 條）。
3. 連線支援牌的監聽也會吃雜牌，要不要一起排除（見第 5 條）。
4. 第 8 條那六張保險要不要拿掉。

## git log --oneline coop..HEAD（第一輪）
```
b7c19bf 稽核修正（畫面 低-3、低-4）：快取裝不下的圖集不再背景下載解開、逐格動作開著時只暖退路用得到的靜態立繪
0e436ed 稽核修正（引擎 低 1～6）：集中精神後飯糰類忍具變灰、藏鋒同名取高、封封牌面三句、雜牌不觸發循息、封封牌名拿掉忍術
```

---

## 追加：主控裁決補做（提交 7dbcf6f）

裁決 2（「劍術」照用）、4（六張保險保留）不用改程式。

### 裁決 1：半卷殘頁照樣能喝，被擋的飯糰要說出來
- `src/engine/combat.ts:1098`：`potionBlockedReason` 從 `some` 改成 `every`，只擋整支都是給飯糰的（飯糰、兩顆飯糰）。
- `src/engine/actions.ts:156`：`gainEnergy` 被集中精神擋下時，呼叫新的 `logEnergyBlocked`（`actions.ts:144`）印一行
  「集中精神：這回合拿不到飯糰」，連線時帶名字（「集中精神：封封這回合拿不到飯糰」）。牌、忍具、秘寶（第 N 張給飯糰那類）、同伴送的都走這一條。
- `src/engine/effects.ts:366`：轉移型（這些你先吃）是在呼叫 `gainEnergy` 之前就擋掉，也補印同一行，不然那張牌打出去像什麼都沒做。
- 測試（`tests/fengfeng_0923.test.ts`）：
  - 「半卷殘頁照樣能喝、抽得到兩張；飯糰那一半被擋，戰報寫出來」：`every` 改回 `some` → 紅；拿掉 `gainEnergy` 那行 → 紅。
  - 「同伴送的飯糰被擋也寫出來，連線時帶名字（給一顆、轉兩顆兩條路都算）」：拿掉 `gainEnergy` 那行 → 紅；只拿掉轉移型那行 → 紅（實際訊息：戰報只有「噹噹打出「這些你先吃」」）。
  - 原本「兩顆飯糰…擋下來」那條改成只列飯糰、兩顆飯糰（半卷殘頁移到新測試）。

### 裁決 3：連線支援牌的監聽不吃雜牌
- 查牌面：你忙我補位（`watchAllyPlay`：「同伴第一次打出技能牌／牌時，自己抽 1 張」）、有我在前面（`watchSelfPlay`：「自己第一次打出攻擊牌／牌時，同伴獲得 6 點蜷縮」）都沒寫雜牌也算 → 排除。
  別碰針尖喔的附毒、我有先備好看的是「真的扣到血」，雜牌不打人、本來就碰不到 → 不動。
- `src/engine/combat.ts:580`：`coopWatchers` 多傳一個「這張是不是戰鬥雜牌」；`combat.ts:206、212、220` 三個監聽條件加 `!junk`。
- 測試：「你忙我補位、有我在前面都不吃雜牌」（同伴打眼冒金星＋黏液：我不抽、不拿蜷縮；接著打淡定才抽 1 張、拿 6 點）。把旗標固定成 false → 紅。

### 驗收
- `npx tsc --noEmit -p .` 零錯誤；`npx vitest run` 278 檔、2406 條全過（6 條原本就跳過）。只差日期／換行的三個匯出檔還原；牌池總表這次沒有變。
- 這一輪是引擎規則與戰報文字，沒另外開瀏覽器實機（戰報那一行的內容由測試逐字釘住）。

### 觀察（沒改，給主控參考）
- 影子分身（「之後每回合第一張不是能力牌的牌，會再打一次」）：這回合第一張打的若是眼冒金星，那一次重播會被雜牌用掉（重播空效果）。照裁決 3 的標準應該也要排除，但不在這次交辦裡。
- 金爪套一類「第 N 張牌」的秘寶用連抓數，雜牌也算一張；秘寶說明怎麼寫我沒逐一查。

## git log --oneline coop..HEAD（最終）
```
7dbcf6f 主控裁決補做：半卷殘頁照樣能喝、被擋的飯糰寫進戰報、連線支援牌的監聽不吃雜牌
b7c19bf 稽核修正（畫面 低-3、低-4）：快取裝不下的圖集不再背景下載解開、逐格動作開著時只暖退路用得到的靜態立繪
0e436ed 稽核修正（引擎 低 1～6）：集中精神後飯糰類忍具變灰、藏鋒同名取高、封封牌面三句、雜牌不觸發循息、封封牌名拿掉忍術
```
