# 2026-09-25 事件圖重審：驗收總結

## 流程
1. 審圖：四隻主角的事件圖（主圖＋結果圖，連文字一起截圖）共 971 張，十二個審圖代理逐張看；主控親自複查所有高／中問題（噹噹封封 44 條、球球菲菲 77 條），確認後開重生工單。
2. 重生：`tools/regen_event_art.py`（`REGEN_ROUND=0925`，新增 READING 規則：看書信畫時有內容那面朝角色自己），十一組工人（d1–d3、f1–f2、n1–n2、p1–p2、c1–c2），每張至少生 2 次、親眼看過再挑；3 張只清去背殘留沒重畫（記在 `regen0925/picks_clean.json`）。各組報告 `report_*.md`。
3. 獨立驗收：四個新的驗收代理（沒參與生圖）逐張判 118 張：通過 114、退回 2、存疑 2。
   - 退回 `event_feifei_lost_scroll_r0`：手勢在實際尺寸下像比中指 → c2 組重畫。
   - 退回 `event_sleeping_guard`：球球整組是舊的冷灰色（不是本輪造成，屬於待使用者裁決的「球球灰色舊圖」那批）。
   - 存疑 `event_medicine_cat_r3`：櫃台少了忍具木箱 → c2 組補。
   - 存疑 `event_fengfeng_shortcut_scroll_r0`：結果文字寫「依照圖解」但卷軸沒有圖解 → 改文字為「照卷軸上的訣竅」（同球球版）。
   - 另外四位驗收員都提到的 `event_coop_seesaw_r2`（四隻共用，畫成另一座機關）→ c2 組重畫。

## 沒處理、留給使用者裁決的
- **球球冷灰色舊圖**約 30 張（`event_toll`、`event_blocked_r0`、`event_hidden_box`、`event_rat_stall`、`event_lost_kitten`、`event_old_well`、`event_broken_shrine`、`event_lost_scroll_r0`、`event_mirror_hall`、`event_sleeping_guard` 系列等），跟參考圖的淡奶油色不同。
- 三隻的 `rare_hot_spring_r0`（整隻跳進去，泡澡還穿著／戴著配件）：文字說得通，沒改。
- 老貓讀信的「掌印那面朝外」（`pigeon_grandpa` 主圖與 r2，四隻都有）：像信封封蠟，沒改。
- 舊書攤攤主把卷軸圖解朝外舉（`lost_scroll_r1`）：可解讀成展示給主角看，沒改。
- 只在淺色底看得到的半透明灰斑（`noisy_kitchen_r1`、`dangdang_moving_rat_r0`）：遊戲底色是深色看不到，沒改。
