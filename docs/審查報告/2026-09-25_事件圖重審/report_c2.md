# 事件圖重審 c2 組報告（驗收退回與小修，3 張）

（子代理不能寫報告檔，由主控照回報代存。）工單 `jobs_c2.json`；提示詞紀錄 `regen0925/prompts_c2.json`；挑定紀錄 `picks_c2.json`；聯絡表 `regen0925/_sheet_c2.png`。嘗試編號：`event_feifei_lost_scroll_r0` 的 try1、try2 是 p1 組生的，`event_medicine_cat_r3` 的 try1、try2 是 c1 組生的，本組生 try3、try4；`event_coop_seesaw_r2` 本組生 try1、try2。

## 每張結果
- `event_feifei_lost_scroll_r0`｜驗收退回（嚴重）：右爪舉在臉前只豎一根指頭，遊戲尺寸下像比中指；卷軸多畫 V 字手勢｜本組生 2 次，挑 try3｜構圖照舊（卷軸平攤階梯下、菲菲跪在靠觀眾這側背影）；右爪整隻平按在第一段圖旁邊，五指併攏，沒有任何一指豎起；卷軸三段只有出拳、飛踢、低馬步三隻小貓，跟主圖一致；原尺寸、放大兩倍、縮到 240 寬都檢查過沒有豎指輪廓｜左爪被身體擋住（背影角度）。try4 多出一隻看不到手臂的爪子，沒挑
- `event_medicine_cat_r3`｜櫃台少了忍具木箱和價目牌｜本組生 2 次，挑 try4｜人物構圖照舊（三花貓餵解藥拿筆記本、球球淡奶油色臉發青冒綠氣）；櫃台照主圖：左邊開口忍具木箱、中間三條魚價目牌、右邊四瓶藥；石板地｜「塞一瓶補身藥當謝禮」沒畫（舊圖本來就沒畫）
- `event_coop_seesaw_r2`｜畫成水平兩頭有座位的翹翹板，跟主圖、新 r0 不是同一座｜本組生 2 次，挑 try1｜照主圖與新 r0 的機關（斜梁、齒輪轉軸、柱頂木架、欄杆、貓掌印）；布包木盒照主圖留在架上；地台左前方兩個繡貓掌的坐墊和一條條紋毛巾，表示玩完坐下休息；不畫角色｜翹板只畫停住的一種角度

## 5 次還不行的
無。

## 要讓主控知道的事
1. 第一輪用同一個指令生三張，給菲菲的 `--note`（不要豎單指、爪子按在卷軸旁）也加到了另外兩張的提示詞（`prompts_c2.json` 有紀錄）；成品都親眼看過，沒有多畫出角色。
2. `_old/` 裡 `event_feifei_lost_scroll_r0`、`event_medicine_cat_r3` 是最早的線上原版（工具只備份第一次）；p1、c1 組挑的那版原始生圖檔還在 `…lost_scroll_r0.try2.png`、`…medicine_cat_r3.try1.png`。
3. 過程中工具沒報錯、沒被權限擋。
