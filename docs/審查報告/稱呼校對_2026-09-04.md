# 大俠貓／師父／塔主稱呼＋魔塔用語 校對清單

專案：`F:\ClaudeWork\qiuqiu-tower`　校對日期：2026-09-04　唯讀校對，未修改任何檔案。

裁決規則（使用者 2026-09-04 訂）：
- 球球（主角）叫他「師父」（不是「師傅」）。
- 第三方與魔物叫他「塔主」或「上面那位」「紫眼睛的大貓」，不叫「師父」（除非是轉述「你師父」）。
- 他的形象是「大俠貓」，旁白可用「大俠貓」；「走火入魔的大俠貓」是他當關主時的名字。
- 依說話者與劇情狀態決定，不是全文統一換字。

---

## 掃描範圍與方法（誠實揭露）

全部 11 個指定檔案都讀過（`dialogue.ts`／`events.ts`／`enemies.ts` 全文逐行看過；`cards.ts`／`relics.ts`／`potions.ts`／`glossary.ts`／`title.ts`／`result.ts`／`actclear.ts`／`docs/設計總覽.md` 全文讀過），另外用兩輪 `grep` 系統性掃描「師父／師傅／大俠貓／塔主／走火入魔／上面那位／魔氣／紫氣／邪氣／小魚乾／魚乾／塔中塔頂塔下」等關鍵詞交叉核對，並寫了一支小腳本，直接套用 `dialogue.ts` 裡的 `qiuqiuLineOk()` 判斷式，逐句驗證球球台詞的喵結尾規則（不是用肉眼數，是真的跑程式判斷）。

需要特別說明的地方：
- **`cards.ts` 沒有 `text` 欄位**——卡牌說明文字是由 `effects` 結構化資料在別的檔案（不在本次指定清單內）動態組成句子顯示出來的，`cards.ts` 本身只有牌名。本次把 159 個牌名全部抽出來逐一看過，沒發現問題；但「牌面說明文字」本身沒有掃到，因為它不在這個檔案裡、也不在使用者指定的清單內。
- `src/ui/screens/reward.ts` 有一句跟 `actclear.ts` 幾乎一模一樣的「這就是塔主的信物喵！」（grep 時順帶看到），但這個檔案不在指定清單內，沒有正式審查，只列出來讓你知道有這回事。
- `docs/設計總覽.md` 依指示只檢查「劇情段」有沒有跟程式文案矛盾，沒有逐句核對平衡數據、技術備忘等其他段落。
- 沒有發現任何一處「師父／師傅」被打成簡體字（無 `师`），也沒有發現「気」「気」之類的日文漢字誤用。

---

## 一、「師傅」錯字（規則：師父）—— 1 條，待確認

| 檔案:行號 | 原句 | 說明 |
|---|---|---|
| `src/content/enemies.ts:274` | `{ id: 'hedgehog', name: '刺蝟師傅', ...}` | 這不是稱呼大俠貓，是另一隻魔物（刺蝟）自己的名字叫「刺蝟師傅」。嚴格講「師傅」（工匠、老手的尊稱，像「老師傅」）跟「師父」（武術/ 師徒關係專用字）本來就是兩個不同的詞，用在一隻刺蝟身上比較像雙關命名，不像打錯字。**待確認**：只是想確認這隻怪取名「師傅」是有意的雙關，還是想寫別的字；如果擔心玩家搜尋或校對時跟大俠貓的「師父」搞混，可以考慮改名（例如「刺蝟老大」）。 |

---

## 二、第三方／魔物稱「師父」——查核 4 處，0 條違規

以下是全篇「非球球、非旁白」的說話者提到「師父」二字的**全部**出現位置，逐一核對後，4 處都屬於「轉述『你師父』」的允許例外（婉物在跟球球說話時提到「你師父」，等於在用球球自己的稱呼跟他對話，不是自己主動稱呼大俠貓為師父）：

| 檔案:行號 | 說話者 | 原句 |
|---|---|---|
| `dialogue.ts:170` | 塔主（貓又婆婆） | 「你師父……婆婆見過他上去。那雙眼睛已經是紫的了。」 |
| `dialogue.ts:193` | 塔主（狸大人） | 「你師父？呵，上面那位灌的可不是酒，是魔氣。」 |
| `dialogue.ts:218` | 塔主（詛咒老住持） | 「你師父？呵……貧僧也曾想勸他。」 |
| `dialogue.ts:234` | 塔主（貓又婆婆，戰敗台詞） | 「記住，他還沒被魔氣吞光。你要快，再晚，剩下的就不是你師父了。」 |

這一類**沒有問題**，列出來是為了讓你確認我真的查過、不是漏掉。另外也確認過黑貓忍者頭目（`dialogue.ts:130`）、稻草人守衛（`enemies.ts:91`）、橘皮大王等第三方講到「上面那位」「紫眼睛的大貓」「塔頂那位」時，用詞都符合規則。

---

## 三、旁白稱「師父」還是「大俠貓」——9 條，全部待裁決

規則允許「旁白站在球球視角時用師父也許可以」，所以下面全部標「待裁決」，不是「錯誤」。我按劇情階段分三群，方便你一次決定一群：

### （A）序幕，大俠貓還沒黑化／剛黑化那三句
| 檔案:行號 | 原句 |
|---|---|
| `dialogue.ts:11` | 「球球是大俠貓的徒弟。每次練習蜷縮，他總會歪成一團；**師父**看了不但不責備，反而笑著再示範一次。」（同一句話前面用「大俠貓」、後面接著用「師父」） |
| `dialogue.ts:12` | 「那天夜裡，一座魔塔突然在村外拔地而起，塔頂亮起詭異的紫光。一縷魔氣鑽進**師父**體內；他痛苦地跪倒在地……」 |
| `dialogue.ts:13` | 「受到魔氣操控的**師父**一語不發，直奔魔塔，把自己關在塔頂。」 |

這三句是他還沒變成「走火入魔的大俠貓」之前／變身當下的敘述，此時故事還沒進入「大俠貓＝反派關主」的階段，用「師父」問題比較小；但如果你想讓「大俠貓」這個詞在旁白裡先立起來（跟後面塔頂大魔物的身分做呼應），這三句是候選修改點。

### （B）結局重逢，師父剛清醒那三句
| 檔案:行號 | 原句 |
|---|---|
| `dialogue.ts:338` | 「最後一縷魔氣從**師父**身上散去，他眼中的紫光終於熄滅。」 |
| `dialogue.ts:341` | 「球球再也忍不住，撲進**師父**懷裡。**師父**緊緊抱住他，師徒倆又哭又笑，誰也捨不得先放手。」（一句裡出現兩次） |
| `dialogue.ts:342` | 「夕陽把一大一小的影子拉得長長的。球球和**師父**並肩走回村子，今晚終於可以安心地一起吃小魚乾了。」 |

這三句是打贏之後、魔氣已經散去，他已經不是「走火入魔的大俠貓」了，用「師父」在劇情邏輯上站得住腳（人已經清醒，稱呼跟著轉回來）。個人判斷這三句改動必要性最低，但仍照規矩列出待你確認。

### （C）其他
| 檔案:行號 | 原句 |
|---|---|
| `dialogue.ts:354` | `hardModeEpilogue`（難度 4 以上結局多播的一句旁白）：「這一路，球球走的是最陡的那條樓梯。**師父**後來每次講起，都要多說一遍。」 |
| `events.ts:7` | `daxia_teach` 事件（5F 固定事件「大俠傳功」）開場敘述：「樓梯間掉著一本秘笈，封面被貓爪抓得毛毛的。翻開第一頁，是**師父**的字。」——這個事件標題用的是「大俠」（大俠傳功），但內文敘述卻用「師父」，同一個事件裡兩種叫法並存。 |
| `events.ts:173` | `old_master_ghost` 事件標題：「**師父**的影子」——對照上面 `daxia_teach` 事件標題用「大俠」，兩個性質類似的「回憶恩師」事件，標題用詞不一致（一個用大俠、一個用師父）。 |

---

## 四、球球句尾缺喵——0 條違規

用程式（不是肉眼）跑了一次 `dialogue.ts` 裡定義的 `qiuqiuLineOk()` 判斷式，把下面全部球球台詞都驗證過一遍：

- `dialogue.ts` 裡 `speaker: '球球'` 的 `DialogueLine`（序幕、戰前獨白、關主開場／戰敗／二階段／三階段、勝利、過關、陣亡……全部區塊）
- `firstMeet`（95 隻魔物初遇吐槽）、`battleStart`（8 句）、`battleWin`（10 句）、`hungry`（3 句）、`lowHp`（3 句）、`restNapLines`（4 句）、`restSharpenLines`（4 句）、`chestLines`（5 句）
- `events.ts` 裡所有「球球：「……」」引號句（36 句）

總共驗證超過 260 句，**沒有一句違規**（全部結尾正確帶喵）。同時也反向驗證了旁白／事件敘述文字**沒有**誤帶喵尾（20 句旁白 DialogueLine ＋ 77 句 events 的 `result` 敘述 ＋ 35 句 events 的 `text` 開場敘述，全部通過）。這條規則目前執行得很徹底，不需要改動。

---

## 五、同一件事不同叫法——查了 4 組，0 條不一致

| 詞組 | 結果 |
|---|---|
| 魔塔／魔物塔／塔 | 全篇只用「魔塔」（含遊戲全名《爪破魔塔：球球參上》），沒有出現「魔物塔」。單獨的「塔」只在上下文夠清楚時當簡稱用（例如「是塔在操縱它喵」），不算另一種叫法。**一致**。 |
| 魔氣／紫氣／邪氣 | 全篇只用「魔氣」。「紫光」「紫眼睛」是視覺描述（魔氣造成的外觀變化），不是拿來取代「魔氣」這個詞本身。**一致**。 |
| 小魚乾／魚乾 | 全篇一律「小魚乾」，逐檔 grep 過沒有找到單獨的「魚乾」。**一致**。 |
| 塔中／塔頂／塔下 | 三關地名（第一關塔下、第二關塔中、第三關塔頂）在對白、程式註解、設計文件裡用法一致，沒有混用或錯置。**一致**。 |

---

## 六、明顯錯字或簡體字、大陸用語——2 條，皆待確認（非簡體字）

系統性掃過近 90 個常見簡繁混淆字（说/后/这/会/还/没/时/对/觉/现/头/发/车/长/门/问/间/卫/团/图/处/变/让/应/环/归/华/认/识/从/众/电/阳/阴/实/买/卖/东/气/风/儿/么/尔/见/闻/龙/凤/剑/伤/击/尽/网/绝/终/继……等），**沒有找到任何簡體字或大陸用語殘留**。唯一一個近似命中（`enemies.ts:791` 的「月蛾后」）查證後確認「后」在這裡是「皇后」的「后」（正確的繁體字，意思是女王），不是「後」被簡化，不算錯誤。

以下兩條是詞義層面「疑似」打錯別字，跟簡體字無關，把握度中等，列出來給你確認：

| 檔案:行號 | 原句 | 疑點 |
|---|---|---|
| `dialogue.ts:67` | `tofu_boy: '請我吃豆腐？……有股霉味喵。'` | 「霉味」是否該寫「黴味」——教育部字典裡「黴」是黴菌／發黴的正字，「霉」是異體字，台灣兩種寫法其實都常見（例如「倒楣」也常寫成「倒霉」），**不是簡體字**，優先度低，可以不用改。 |
| `dialogue.ts:76` | `persian_lady: '毛好澎的大小姐……'` | 「毛好澎」疑似該寫「毛好**蓬**」（蓬鬆）——「澎」（澎湃、波濤義）跟「蓬」（蓬鬆義）是同音字（都唸 péng），容易打錯，「毛好蓬」語意比較說得通。把握度較高，建議確認是否要修成「毛好蓬的大小姐」。 |

---

## 補充發現：球球本人在「師父」跟其他叫法之間切換——2 條，待裁決

這兩條不算在原本六類裡，但直接跟「球球該怎麼稱呼他」有關，一併附上：

| 檔案:行號 | 原句 | 說明 |
|---|---|---|
| `dialogue.ts:180` | 球球（對橘皮大王說）：「村裡的小魚乾就是你搶的？你跟**上面那隻大貓**是一夥的喵？」 | 球球本人這句沒有用「師父」，改用了魔物慣用的「上面那隻大貓」。可能是刻意順著對話對象（橘皮大王自己講「上面那位」）的用語，讓對話更順；也可能該統一改回「師父」。 |
| `events.ts:28` | 球球（rescue 事件，選「拿小魚乾」）：「我會把**塔主**打下來還你喵。」 | 球球本人用「塔主」而不是「師父」。這個事件沒有限定 `acts`，三關都可能抽到；若在第三關（塔頂）抽到，這裡的「塔主」就是特指大俠貓本人。對照 `actclear.ts:124`「這就是塔主的信物喵」——那句經查證只會在打贏第一、二關（非大俠貓本人）的關主後出現，「塔主」用來泛指「這一關的守關者」是合理的；但這句 `events.ts:28` 沒有這層限制，語意比較模糊，可能也是泛指「魔塔頭目」這個角色功能，不一定違規。 |

---

## 統計總表

| 類別 | 條數 | 性質 |
|---|---|---|
| 一、「師傅」錯字 | 1 | 待確認（可能是雙關，非典型違規） |
| 二、第三方稱「師父」 | 0 | 查核 4 處，全部合規 |
| 三、旁白稱「師父」vs「大俠貓」 | 9 | 待裁決 |
| 四、球球句尾缺喵 | 0 | 程式驗證 260+ 句，全數通過 |
| 五、同件事不同叫法 | 0 | 查了 4 組，全部一致 |
| 六、錯字／簡體字／大陸用語 | 2 | 待確認（皆非簡體字，詞義層面疑似錯別字） |
| 補充：球球本人稱呼不一致 | 2 | 待裁決 |
| **合計需要你裁決／確認的條目** | **14** | |
