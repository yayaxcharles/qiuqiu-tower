# E1 球球圖片總稽核報告

稽核範圍：`F:\ClaudeWork\qiuqiu-coop`，球球那一半的圖（manifest 鍵不含 `feifei`）——
`bg/event_*`（開場41＋結果60）、`cards/card/*`（137）、`sprites/hero/*`（34）、
`sprites/boss|master|shop/*`（29）、`monsters/*`（434，102隻怪）、
`bg/screen_*|low|mid|top|still_*|door_*|card_paper_*|map_*|boss1-3`（66）。
共 **801 張**，全數看完（見第3節）。倉庫全程唯讀，未做任何修改。

---

## 一、程式檢查結果

| 檢查 | 結果 |
|---|---|
| `npx vitest run tools/manifest_hygiene.test.ts` | **3/3 通過**（清單無中途檔、事件插圖無孤兒、清單檔案都存在） |
| `python tools/check_haze.py`（全掃，不分角色） | **578 張全過，0 張灰膜** |
| `python tools/check_size.py` | **超標**（程式493KB/200KB、樣式101KB/55KB、圖片17.8MB/9.3MB），但 **dist/ 是舊建置**（12:44），`manifest.json`（16:56）與 `docs/分關載入.json`（17:13）之後又改過，有 23 個素材檔比這次 dist 還新。這份規則只准跑唯讀 python／vitest，沒有重新 `npm run build`，所以這個超標數字對不上目前素材的真實狀態，**不列入圖片正確性問題**，如需要準確的大小數字要請人重新建置後再跑一次。 |

**額外用 python 做的 (a)(b)(c) 檢查**（範圍是整份 manifest，不分角色）：

- **(a) manifest 指到但檔案不存在**：0 筆。
- **(b) `public/assets` 下存在但 manifest 沒引用的檔**：26 筆，全部是 `assets/sfx/*.mp3`，共 0.19MB。查證：這些不是真孤兒——manifest.json 本來就只登錄圖片，音效是 `src/ui/audio.ts` 的 `Sfx` 型別＋固定路徑直接讀取，26 個型別名稱跟 26 個檔案一一對應，程式裡有在用。
- **(c) 程式碼引用但 manifest 沒有的鍵**：原始比對抓到 1 筆 `codex/curl`（`src/engine/types.ts`），查證後是寫在**程式註解**裡的舉例文字（說明 `art` 欄位格式用的例子），不是真的查找鍵。**實際 0 筆**。

---

## 二、問題清單

### 高（畫錯角色／缺圖）
**無。** 801 張裡沒有一張把球球畫錯（黃色大眼、沒頭巾、變菲菲）、沒有混入菲菲的圖、沒有讀取失敗的圖、沒有真的浮水印。

### 中（12 個問題，共 13 個檔案）

**系統性規律：8 隻魔物的「攻擊動作」跟自己的 idle／hurt／block 判若兩物**（三組子代理各自獨立抓到、且逐一開原圖複查過，其中 `catgrass_bug_attack`／`hibernating_bear_attack`／`roomba_king_attack`／`vacuum_attack` 這 4 張我本人也直接讀原圖覆核確認）：

| 鍵 | 檔案 | 問題 |
|---|---|---|
| `monsters/…/catgrass_bug/attack` | `public/assets/monsters/catgrass_bug_attack.webp` | idle/hurt/block 是純綠色葉叢、無臉無肢；attack 變成灰白虎斑貓臉＋爪子吐綠色酸液 |
| `monsters/…/hibernating_bear/attack` | `public/assets/monsters/hibernating_bear_attack.webp` | idle 是紅金條紋帽＋紅金條紋圍巾；attack 變成深藍毛帽配白毛球＋紅白條紋圍巾 |
| `monsters/…/rat/attack` | `public/assets/monsters/rat_attack.webp` | idle/hurt/block 戴尖尖紙帽；attack 戴綠色軍用鋼盔 |
| `monsters/…/roomba_king/attack` | `public/assets/monsters/roomba_king_attack.webp` | idle 是紫藍機身＋紫框橢圓大眼；attack 變成米白機身＋藍色圓眼，只有皇冠沒變 |
| `monsters/…/stone_lion/attack` | `public/assets/monsters/stone_lion_attack.webp` | idle/hurt/block 是素灰石獅；attack 全身佈滿發光岩漿裂紋 |
| `monsters/…/tadpole/attack` | `public/assets/monsters/tadpole_attack.webp` | idle/hurt/block 戴貼頭小尖帽；attack 戴誇張大斗笠草帽 |
| `monsters/…/vacuum/attack` | `public/assets/monsters/vacuum_attack.webp` | idle 是黑灰機身＋橘色瞇眼；attack 變成白色機身＋藍色圓眼，跟 roomba_king 錯法幾乎一樣 |
| `monsters/…/puppeteer/attack` | `public/assets/monsters/puppeteer_attack.webp` | idle/hurt/block 是近黑素色長袍；attack 變亮紫色＋貓臉花紋（程度較輕，但同一種規律） |

建議：這 8 張＋待確認的 `mirror_shard_attack`／`shamisen_cat_attack`（見第四節）都指向「attack 這個狀態當初生成時系統性跑掉」，值得整批 `_attack` 圖重新核對一次，而不是逐張單獨修。

**事件圖（4 個問題，5 個檔案，都已用原圖覆核，非拼圖縮圖誤判）：**

| 鍵 | 檔案 | 問題 | 對應文字 |
|---|---|---|---|
| `bg/event_rescue_return_herb` ＋ `_r0` | `public/assets/bg/event_rescue_return_herb.webp`、`_r0.webp` | 兩張都畫成村貓遞一條綁紅緞帶的**魚**給球球；魚頭魚鰭魚眼都清楚。但文字明講謝禮是「**一個布包**」，選項0結果是「**秘寶**」，完全沒提魚，而且這事件的前提是「球球上次把魚留給村貓」，這次村貓還魚邏輯也講不通 | desc「牠捧出一個布包」／choice[0] flavor「村貓解開布包，把珍藏的秘寶交給球球」 |
| `bg/event_grindstone_r0` | `public/assets/bg/event_grindstone_r0.webp` | 球球臉頰旁一塊實心亮綠色不規則色塊，不是任何道具，像去背或算圖殘留瑕疵 | （此選項效果是移除牌＋扣生命上限，無相關道具） |
| `bg/event_noisy_kitchen`（開場） | `public/assets/bg/event_noisy_kitchen.webp` | 頭巾旁蒸氣雲裡混了一塊獨立亮綠色塊，色調跟白色蒸氣不同，也沒接到鍋裡的湯料 | desc 只提鍋子、蒸氣、備用忍具，無綠色物件 |
| `bg/event_stuck_kitten`（開場） | `public/assets/bg/event_stuck_kitten.webp` | 文字明講「牠媽媽正扶著牠的肩膀」，畫面只有球球在拉小貓，**沒有母貓**（兩張結果圖 r0/r1 都正確畫出母貓，只有開場圖漏畫）。此條是我自己先直接開圖發現，EVT子代理獨立複查也抓到同一張 | desc「一隻小貓把頭伸進欄杆後縮不回來……牠媽媽正扶著牠的肩膀，小聲提醒牠先放鬆」 |

### 低（3 個，小瑕疵）

| 鍵 | 檔案 | 問題 |
|---|---|---|
| `card/taxue`（絕學·踏雪無痕） | `public/assets/cards/card/taxue.webp` | 畫面留下清楚白色腳印，但牌名是「無痕」，字面小矛盾；用腳印表現快速移動是常見畫法，意象仍算合理 |
| `bg/event_fish_pond`（開場） | `public/assets/bg/event_fish_pond.webp` | 文字說「胖魚」，圖裡魚偏瘦長 |
| `bg/event_weapon_rack`（開場） | `public/assets/bg/event_weapon_rack.webp` | 文字說兵器「積滿灰、大多已生鏽」，圖裡兵器乾淨鋥亮 |

### 需要撤銷的一條（我方指示錯誤，非圖片問題）

負責背景圖的子代理原本回報 `bg/map_tall`／`map_tall_mid`／`map_tall_top` 三張缺了代表玩家位置的小球球，判為中度問題——**這是我自己給錯線索**。事後查證：manifest 裡真正的「地圖上的球球」是另一組獨立鍵 `icon/map_hero_low`／`_mid`／`_top`（在 `icons` 類別下，不在這次分派的 bg 範圍內），我讀取其中一張確認畫面正是正確的球球忍者小圖示。所以 `bg/map_tall*` 這三張地圖背景本身不需要畫出球球，**撤銷此條，不算問題**。`icon/map_hero_*` 系列不在本次審查鍵清單內，我沒有逐張看過，如需涵蓋 icons 類別要另外派工。

---

## 三、統計

- **分派審查 801／801 張**，六個子代理各自產出的拼圖張數與看圖數，跟我事先用 python 算好的分類清單（`categories.json`）逐一核對一致，沒有漏派：

| 子代理 | 範圍 | 張數 | 拼圖張數與路徑 |
|---|---|---|---|
| 審查事件圖 | `bg/event_*` 開場＋結果 | 101/101 | `…\audit\E1_sheets\event_open\`（3張）、`…\event_result\`（4張） |
| 審查牌面圖 | `cards/card/*`（非feifei） | 137/137 | `…\audit\E1_sheets\cards\`（9張） |
| 審查立繪與背景 | 球球立繪＋塔主/店家立繪＋各類底圖 | 129/129 | `…\audit\E1_sheets\hero_sprites\`（3張）、`…\other_sprites\`（2張）、`…\misc_bg\`（5張） |
| 審查魔物 第1組 | `monsters/*` A段 | 146/146 | `…\audit\E1_sheets\monsters_1\`（10張） |
| 審查魔物 第2組 | `monsters/*` B段 | 146/146 | `…\audit\E1_sheets\monsters_2\`（10張） |
| 審查魔物 第3組 | `monsters/*` C段 | 142/142 | `…\audit\E1_sheets\monsters_3\`（9張） |

合計 55 張聯絡表（4欄、每格500px不縮小），801 張圖全數看過；每張子代理報告中標記為「存疑」的格子，各自都另外開了 `public/assets/...` 原圖（部分再用 PIL 裁切放大）覆核，不是只看拼圖縮圖。

- **我本人額外直接開原圖覆核的（不透過拼圖）**：今晚剛重生的 7 張事件圖（`event_gambling_rats`＋`_r0`、`event_heavy_door`＋`_r0`＋`_r1`、`event_rescue`＋`_r1`，見下方說明）、中度問題中的 4 張事件圖、2 張魔物攻擊格（roomba_king、vacuum）、`icon/map_hero_low`、`hero/cover`，共 15 張。

**額外驗證**：`tools/make_qiuqiu_fix_0914.py` 顯示今晚 16:50 剛重生過 7 張球球事件圖（原本的已知問題：賭博的老鼠與很重的門球球畫成黃色大眼、江湖救急村貓三隻手兩隻腳、村貓手臂下有殘留綠色塊）。我逐張開原圖覆核，**這 7 張現在看起來都已修好**：眼睛都是黑色圓點、四肢數量正常、沒看到殘留色塊。

---

## 四、待人眼確認（8 項，都無法肯定是不是刻意設計）

| 鍵 | 檔案 | 猶豫原因 |
|---|---|---|
| `card/mabu`（絕學·貓步） | `public/assets/cards/card/mabu.webp` | 效果「獲得2點貓步」給人輕盈印象，畫面卻是雙腳穩踩裂地、比讚的穩重構圖；也可能是刻意表現氣勢 |
| `bg/event_rescue_return_fish`（開場） | `public/assets/bg/event_rescue_return_fish.webp` | 文字說「只剩這罐藥」該是罐子，圖上是印小魚圖案的紙袋；也可能是刻意畫「魚乾吃完的空袋」 |
| `bg/event_greedy_merchant`＋`_r0`＋`_r1`（3張） | `public/assets/bg/event_greedy_merchant*.webp` | 文字說「戴眼鏡的灰貓攤開布包」，三張圖一致畫成西裝領結＋銀盤的管家造型；三張一致，較像刻意的角色設計而非畫錯 |
| `sprites/boss` 第二型態 15 張（defeat3/drunk2-3/guard2-3/headbutt2-3/hurt2-3/idle2-3/palm2-3/roar2-3） | `public/assets/sprites/boss/*.webp` | 斗笠不見、袍子破爛、體型變壯碩，偏離塔主參考圖標準造型；但劇本裡塔主確實有「走火入魔」變身橋段，很可能是刻意設計的黑化第二階段 |
| `sprites/hero/cover` | `public/assets/sprites/hero/cover.webp` | 圖上燙了「參上」兩字加爆炸特效。我已親自複查：跟遊戲本身標題「爪破魔塔：球球參上」完全吻合，像是刻意設計的登場卡面而非浮水印，但規則寫「烙字＝高」，保險起見列出來請你拍板 |
| `bg/still_teach` | `public/assets/bg/still_teach.webp` | 對應「師父示範蜷縮動作教學」，畫面是師父抱胸微笑坐著、球球在旁發抖，屬情境示意而非逐字演出蜷縮動作；其他 still 圖也偏向情境示意，可能沒問題 |
| `monsters/…/mirror_shard/attack` | `public/assets/monsters/mirror_shard_attack.webp` | idle/hurt/block 是站立水晶人形，attack 變成橫飛水晶劍刃只剩一顆眼睛；可能是刻意設計的「衝刺化為刀刃」動態效果 |
| `monsters/…/shamisen_cat/attack` | `public/assets/monsters/shamisen_cat_attack.webp` | 和服配色從藍色碎花變紅邊金漩渦花紋，貓本身外型一致；可能只是布料花色差異 |

---

## 五、誠實條款

801 張全部由六個子代理逐格看過拼圖（4欄、500px、不縮小），存疑格子都另外開原圖（部分放大裁切）複查，不是只憑檔名猜。我本人針對高風險項目（今晚剛修的7張、4張中度事件圖問題、2張魔物攻擊格、地圖圖示的判讀爭議）額外直接開原圖覆核，結論與子代理一致或做了必要更正（撤銷 `map_tall` 那條）。沒有任何一張是「大概沒問題」就寫成「沒問題」——801 張都有確實的觀看紀錄。
